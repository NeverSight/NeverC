# NeverC `std` Audit Coverage Ledger

Baseline: `c559a30f646021d8757d288a71b5e1414d1ec761` (`dev`)

This ledger owns every file returned by `rg --files std tests/neverc/std`, plus the external std fuzz/interop files listed below. Ownership is rule-based: the first matching explicit domain row is primary; cross-cutting audit 23 owns every residual or shared artifact. Audit agents are read-only and inherit the root model.

| ID | Canonical agent task | Primary path ownership | Status | Report summary |
|---:|---|---|---|---|
| 01 | `/root/audit_01_foundation` | `std/src/{arena,bufio,bytes,cmp,context,cstring,errors,flag,fmt}/**`; corresponding public headers and `tests/neverc/std/{test_,}*` files named for those modules | complete | P2 candidates: seekable `fmt.Fscanf` embedded-NUL truncation; exact-64-KiB `bufio.Scanner` token misclassified after a legal zero-length read. |
| 02 | `/root/audit_02_containers` | `std/src/{container,maps,slices,sort}/**`; corresponding headers/tests/benchmarks | complete | P1 candidate: 32 public sort/slices/vector APIs missing dot dispatch. P2 candidate: vector `size_t` capacity exceeds `long` index ABI on LLP64. |
| 03 | `/root/audit_03_lifetime` | `std/src/{sync,thread,weak,unique}/**`; corresponding headers/tests | complete; P1 fixed | Executor native-join failure could discard retry state and release storage still reachable by a worker. RED runs `33433637531`/`33438887063` proved retry and teardown failures; GREEN runs `33435850083`/`33440089268` and final-merge run `33441265315` passed `thread_failures` under ASan/UBSan. The fix preserves joined-prefix retry state and waits for explicit worker-exit proof before public or partial-create teardown. |
| 04 | `/root/audit_04_strconv_math` | `std/src/strconv/**`; scalar files and `_math_internal.h`, `_trig_reduce.h` directly under `std/src/math`; corresponding headers/tests/benchmarks | complete | P2 candidates: negative-imaginary exact buffer, quadratic complex prefix parsing, `atan2` quotient-underflow quadrant, and hex exponent/significand cancellation. |
| 05 | `/root/audit_05_advanced_math` | `std/src/math/{big,bits,cmplx,rand}/**`; corresponding headers/tests/benchmarks | complete | P1/P2 candidates: bigint exponentiation and negative bitwise operations violate unchanged-destination behavior on OOM. |
| 06 | `/root/audit_06_hashes` | `std/src/hash/**`; `std/include/neverc/std/hash/**`, `hash.h`; corresponding tests | complete | No confirmed defect; only bounded-entropy and dot-syntax coverage gaps. |
| 07 | `/root/audit_07_simple_encoding` | `std/src/encoding/{ascii85,base32,base64,binary,hex}/**`; corresponding headers/tests/benchmarks | complete | P2 candidate: Uvarint truncation/overflow rejection point. Base32 lowercase is a contract decision, not yet accepted as a bug. |
| 08 | `/root/audit_08_struct_encoding` | `std/src/encoding/{asn1,csv,json,pem,protobuf,xml}/**`; corresponding headers/tests/benchmarks | complete | P1/P2 candidates: singular protobuf packed-wire acceptance, NULL+positive-length EOF normalization, and reserved field-number encoding. |
| 09 | `/root/audit_09_archive_compress` | `std/src/{archive,compress}/**`; corresponding headers/tests/benchmarks | complete | Candidates: TAR quadratic iteration and aliased-payload UAF; ZIP 65,535 writer boundary; concatenated bzip2; zlib/ZIP manifest dependency closure. |
| 10 | `/root/audit_10_symmetric_crypto` | `std/src/crypto/{aes,chacha20,chacha20poly1305,cipher,des,gcm,poly1305,rc4,subtle}/**`; corresponding headers/tests | complete | P2 candidates: ChaCha20-Poly1305 whole-transcript allocation before authentication; GCM nonce/tag/output aliasing can return non-self-consistent ciphertext. |
| 11 | `/root/audit_11_crypto_hash_kdf` | `std/src/crypto/{md5,sha1,sha224,sha256,sha3,sha384,sha512,sha512_224,sha512_256,hmac,hkdf,pbkdf2,rand}/**`; corresponding headers/tests/benchmarks | complete | P2 candidate: SHA-512 HMAC key and HKDF salt lengths incorrectly share the stricter message/IKM overflow bound. |
| 12 | `/root/audit_12_public_key` | `std/src/crypto/{dsa,ecdh,ecdsa,ed25519,elliptic,hpke,rsa}/**`; corresponding headers/tests | complete | Candidates include Ed25519/RSA unchecked bigint OOM, nullable curve initialization, secret-dependent scalar/exponent traces, copyable HPKE nonce state, and unsafe legacy RSA surfaces; cryptographic-policy findings require separate API triage. |
| 13 | `/root/audit_13_post_quantum` | `std/src/crypto/{mldsa,mlkem}/**`; corresponding headers/tests | complete | High-confidence API defects: ML-DSA verify and ML-KEM decaps cannot validate external input lengths; ML-KEM encapsulation fails to clear the surviving output when the other output pointer is NULL. |
| 14 | `/root/audit_14_tls_x509` | `std/src/crypto/{tls,x509}/**`; corresponding headers/tests, `run_tls_*_interop.sh`, `tests/neverc/TlsProtocolFuzzer.cpp`, TLS workflows/scripts | complete | High-impact candidates: Windows ROOT flattening ignores explicit distrust/policy; X.509 accepts weak RSA; TLS AES-GCM traffic-secret record limits are absent. |
| 15 | `/root/audit_15_io_os_path` | `std/src/{io,os,path}/**`; corresponding headers/tests | complete | Candidates: POSIX exec loses child `errno`; Windows RemoveAll loses Win32 errors; sticky stdio EINTR; missing IO/OS dot methods. |
| 16 | `/root/audit_16_text_regexp_html` | `std/src/{regexp,text,html}/**`; corresponding headers/tests/benchmarks | complete | High-impact HTML template URL-scheme escape across control nodes; regexp quadratic UTF-8/replacement scans, capture-matrix amplification, and missing html/template data dot methods. |
| 17 | `/root/audit_17_image` | `std/src/image/**`; corresponding headers/tests/benchmarks | complete | Image decoders lack peak decoded/compressed-byte budgets, allowing multi-GiB allocation amplification; four public image dot methods are missing. |
| 18 | `/root/audit_18_network_core` | `std/src/net/{tcp,udp,netip,resolve,interface,mail,textproto,smtp,url}/**`, `std/src/net/_net_*`, `std/src/net/idna_inc.h`; corresponding headers/tests/benchmarks | complete | Candidates: EINTR can extend deadlines indefinitely; network init overwrites process SIGPIPE handlers; IDNA lacks NFC and IDNA2008 validity processing. |
| 19 | `/root/audit_19_http_rpc_ws` | `std/src/net/{http,grpc,rpc,websocket}/**`; corresponding headers/tests/benchmarks, `h2_grpc_interop_server.c`, `network_interop_server.c`, HTTP/2/gRPC/WebSocket workflows/scripts | complete | P1: HTTP/1 gRPC requests can pass an `http1_request_stream_t` to HTTP/2 stream code, causing a remotely triggerable out-of-bounds read/type confusion. HPACK failure rollback and binary-metadata length are design risks. |
| 20 | `/root/audit_20_quic_http3` | `std/src/net/{quic,http3}/**`; corresponding headers/tests, `http3_interop_server.c`, `network_test_{certs,support}.h`, HTTP/3 workflow/scripts | complete | High-impact candidates: overlapping STREAM frames bypass flow-control accounting and amplify heap use; AEAD use limits are absent. Key-phase handling drops valid reordered old-phase packets. HTTP/3's documented concurrent-stream limit is implemented as a cumulative connection limit. |
| 21 | `/root/audit_21_debug_formats` | `std/src/debug/**`; corresponding headers/tests/benchmarks | complete | Confirmed resource-exhaustion candidates: quadratic Mach-O fat-architecture de-duplication, quadratic DWARF abbreviation lookup, and unbounded cumulative Plan 9 expanded-path allocation. No confirmed memory-corruption finding. |
| 22 | `/root/audit_22_remaining` | `std/src/{time,unicode,uuid,log,mime,index}/**`; corresponding headers/tests/benchmarks | complete | Confirmed: built-in tzdata returns wrong 2024 offsets for Cairo and Casablanca; stored tzdata ZIP parsing rejects legal EOCD comments. Twenty-one exported unicode/log/time APIs lack dot-method mappings. |
| 23 | `/root/audit_23_cross_cutting` | `std/CMakeLists.txt`, `std/manifest.json`, all residual/umbrella/platform headers, every residual test/benchmark/support/script under `tests/neverc/std`, `tests/neverc/StdLibTests.cpp`, `tests/neverc/{NetworkProtocolFuzzer.cpp,NetworkProtocolFuzzAdapters.c}`, relevant `tests/neverc/CMakeLists.txt`, workflows and CI scripts | complete | Critical CI/artifact bug: green Linux/Windows runs silently produce only 19/246 Darwin-x64 std TUs because the universal macOS sysroot is not supplied and per-TU failures are non-fatal. Also confirmed `std.time` root-marker mismatch, nine non-self-contained leaf headers, dependency-linter false greens, and incomplete fuzz/interop path triggers. |

## Completeness rules

- All 441 files currently under `std/` are owned by their module row; `std/CMakeLists.txt`, `std/manifest.json`, root-level shared headers, and any residual file are owned by audit 23.
- All 334 files currently under `tests/neverc/std/` are owned by their module row based on the tested module name; cross-module tests (`test_dot_syntax.c`, `test_crypto_ctx_abi.c`, `test_network_builtin.c`, and similar), shared support files, interop servers/scripts, and any residual file are owned by audit 23 unless rows 14, 19, or 20 explicitly claim them. The four named harness/fuzzer files outside that directory are counted separately in the ledger scope.
- Generated/static tables (`std/src/html/html_entities.h`, `std/src/unicode/unicode_tables.h`, `std/src/strconv/{pow10_eisel.h,ryu_table.h}`, `std/src/hash/_wyhash_final3.h`, `std/src/crypto/aes/aes_ct_sbox.h`, and `std/src/net/idna_inc.h`) stay with their functional domain; their generator/provenance is part of the review.
- Every manifest entry and `STD_TEST` registration is checked both by its functional owner and audit 23 for global completeness/duplication.
- No agent may edit files, compile, or run tests. Findings require exact evidence, consequence, and a focused GitHub-only regression-test proposal.
