**語言**: [English](../translate.md) | [简体中文](../zh-CN/translate.md) | [繁體中文](translate.md) | [日本語](../ja/translate.md) | [한국어](../ko/translate.md) | [Français](../fr/translate.md) | [Deutsch](../de/translate.md) | [Español](../es/translate.md) | [Italiano](../it/translate.md) | [Русский](../ru/translate.md) | [العربية](../ar/translate.md)

[← 文件](README.md)

# 將 C++ 轉譯為 NeverC

實驗性命令 `neverc translate` 透過 `cpp-core-v1`、`cpp-core-v2`、`cpp-project-v1` 和 `cpp-math-v1` 產生可審查的 `.nc` 原始碼。

**目前僅實作了 C++ 輸入轉譯。** 易語言（E Language，`.e`）、Python、Go、Rust、TypeScript 和 JavaScript 均為未來計畫，目前尚無可用的轉譯器。

部分特化宣告保留主要範本引數檢查的原始結果，包括內部引數仍相依的成員變數部分特化副本。即使從未選用該特化，也會檢查已解析的參數型別原始碼。長度未定的參數包展開保留已檢查的前綴與實際原始碼，不推測引數槽位；仍待解析的類別內成員變數完整特化宣告也保留相同檢查。完整 C++／STL 尚未完成。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#partial-declaration-source-checks).

保留參數檢查結果不等於偏特化宣告有效：它仍必須比主模板更特化，且參數能夠推導。非法參數包模式，以及明確實例化定義之後的 extern 實例化宣告，仍回報 C++ 診斷（`TR0202`）。

Core v2 支援已接納泛型類別中的具名一般巢狀類別、具體副本及自行撰寫的成員類別明確特化。一般類別本體不增加範本參數層級；未使用的本體保持延遲實例化，已實例化欄位、選定方法、成員範本、物件生命週期和純量靜態儲存保留正確來源及所屬實例。明確實例化指令逐條檢查書寫的限定名稱，包括 extern 及沒有額外作用的重複指令。繼承、動態配置與完整 C++／STL 仍未完成。目前僅實作 C++ 輸入，易語言和 Python 等仍在規劃中。翻譯使用內建 Clang 程式庫，不呼叫外部 Clang 程式；原生結果以實作提交的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-nested-classes-in-generic-owners).

Core v2 支援已接納記錄及具體類別範本實例中的純量靜態成員變數範本，包括預設引數、有界參數包、部分／完整特化與類別外定義。等價實例共用儲存，外層或內層引數不同的實例保留獨立物件。已檢查的非 inline const 值可以不配置儲存直接讀取，實際求值的位址與參考存取則需要定義。宣告與定義都保留原始碼檢查，物件存取保留接收物件的副作用與暫存物件清理。相依外層類別中的類別內完整特化保留原始宣告與具體實例的原始碼資訊：已確定的引數、預設值與參數型別立即檢查，相依外層的部分在實例化後檢查。其他結果型別、動態初始化及完整 C++／STL 尚未完成。翻譯使用內建 Clang 程式庫，無須外部 Clang 程式。目前僅實作 C++ 輸入，易語言、Python 等前端仍在規劃中；原生驗證以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-variable-templates).

Core v2 支援已接納的一般記錄（包括一般巢狀記錄）與具體類別範本實例的成員別名範本。預設引數與有界參數包可以引用外層類別參數及內層別名參數，各自的引數與替換後的型別原始碼分別檢查。別名保留既有值、參考、陣列與記錄語義，不產生執行期範本物件。非相依宣告即使未使用也會檢查，相依型別則延後至替換時處理。翻譯使用內建 Clang 程式庫，無須外部 Clang 程式；原生驗證以實作版本的 CI 為準。完整 C++／STL 尚未完成。目前僅實作 C++ 輸入，易語言、Python 等前端仍在規劃中。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-alias-templates).

Core v2 支援已接納記錄與類別範本實例中的具體成員函式範本：一般／靜態方法、運算子、建構與轉換函式，以及有界參數包、預設引數、類別外定義與特化。內外層引數分別保留身分，等價呼叫共用程式碼與靜態儲存；選中的呼叫、預設值及已具現化函式本體繼續接受原始碼與生命週期檢查。翻譯使用內建 Clang 程式庫，無須安裝或呼叫外部 Clang 程式。原生結果以實作版本的 CI 為準。繼承、動態配置及完整 C++／STL 尚未完成。目前僅實作 C++ 輸入，易語言、Python 等前端仍在規劃中。 成員範本宣告中具體外層類別的限定符，即使尚未具現化函式本體，也會接受原始碼檢查。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-function-templates).

Core v2 支援命名空間中的整數、布林和列舉變數模板，包括推導的 auto、選中的部分特化、完整特化、預設引數和有界型別／純量參數包。C++17 型別特徵可用於 static_assert、if constexpr 和其他模板引數。等價實例共用帶型別的全域儲存，不同實例保留獨立值和位址。查詢固定型別不會強制具現化未使用的初始化器，也不會憑空配置儲存。實際引數、選中的預設值、宣告／定義型別和已具現化的初始化器仍會檢查。動態初始化、其他結果型別、完整 C++／STL 尚未完成；原生驗證以實作版本的 CI 為準。

非型別參數的書寫型別中，直接的 sizeof(T)／alignof(T) 可作為宣告中繼資料保留，但 T 必須是同一參數表中的該型別參數。選中的替換仍須接受具體型別檢查；其他相依查詢形式不屬於此例外。單獨的 extern template 宣告不提供目前封閉來源單元翻譯契約所要求的具體定義。

Core v2 在所有支援的目標上使用標準 C++17 模板解析規則，包括 Windows x64 和 ARM64。模板定義不再使用 MSVC 延遲解析；重複的明確具現化定義和不相容的例外規格仍回報語言錯誤。未使用的相依函式本體仍按需具現化。完整 C++／STL 支援尚未完成。 函式的明確特化仍須在本原始碼單元內提供自身定義；重複宣告不能代替定義。

Core v2 支援命名空間作用域中可變的整數、布林和列舉全域變數，可使用零初始化或經完整檢查的常數初始化。變數及其位址在多次呼叫間保持一致；參考、指標和參數預設值存取同一儲存空間。`extern` 宣告必須在目前來源檔案中找到定義。常數全域變數仍為唯讀。動態初始化、全域記錄/陣列/指標/參考、執行緒區域儲存和靜態區域變數仍受各自的限制。

Core v2 支援對已支援的固定陣列和來源檔案中定義的範圍執行 C++17 範圍 `for`，透過已解析的成員函式或 ADL 查找呼叫 `begin/end`。值或參考迴圈變數、記錄型別迭代器及不同型別的結束迭代器遵循一般呼叫和生命週期規則。範圍初始化及 `begin/end` 只執行一次；每次迭代的物件在遞增或離開前解構，涵蓋 `continue`、`break` 和 `return`。其他類別範本形式、標準標頭檔、STL 容器、結構化繫結和 C++20 範圍初始化陳述式尚不在本次支援範圍內。原生驗證以實作版本的 CI 為準。

Core v2 支援在其他條件均受支援的標準配置類別中使用 `private/protected` 資料成員。內建 Clang 在翻譯前檢查存取權限；合法的成員函式、建構函式、工廠函式、預設引數和產生的複製/移動操作使用同一份具型別的成員儲存空間。類別外的非法存取仍回報 C++ 診斷。存取標籤屬於來源語言規則，不提供執行期保密能力。混合存取層級的非標準配置類別、範本友元、繼承和不支援的欄位型別仍受限制。原生結果以實作版本的 CI 為準。

Core v2 支援從已接納類別範本、偏特化、成員類別範本、複製的全特化及一般巢狀類別實例化非範本友元函式。隱藏友元 ADL、私有存取、自由運算子、命名空間重複宣告、區域類別及純量靜態區域變數保留實際函式身分，產生一般具型別呼叫，不新增隱式接收者。未使用的範本本體維持延遲處理；選中的定義與原始簽章會檢查。若選用另一份宣告的簽章，原始簽章須有完整的非相依來源證據，包括自身預設引數及已解析的 noexcept。未支援的相依友元類別範本形式、友元型別參數包展開、完整 C++／STL 尚未完成；既有非相依友元型別與成員友元規則不變。目前僅實作 C++ 輸入，易語言與 Python 待實作。翻譯使用內建 Clang 程式庫，不呼叫外部 Clang 程式；原生驗證以實作提交的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-functions).

Core v2 支援已接納泛型類別本體中替換完成的非範本友元型別，包括 `friend T;`、別名、相依巢狀型別及類別特化。內建 Sema 檢查私有存取及合法但不授權的非類別友元。原始與替換後的型別來源皆會檢查；來源未證明的副本、未支援的相依友元類別範本形式及型別參數包展開仍不支援。授權不新增執行期函式、接收者或儲存空間。原生行為須由實作提交的 CI 驗證，完整 C++／STL 尚未完成。目前僅實作 C++ 輸入，易語言與 Python 待實作；使用內建 Clang 程式庫，不呼叫外部 Clang 程式。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-types).

Core v2 支援已接納普通類別與泛型類別中的自由友元函式範本，包括已支援的運算子、ADL、私有存取、內外層參數、預設值及參數包。具體呼叫保留參考、物件生命週期與獨立的實例／靜態儲存身分；原始宣告及實際函式本體的上下文皆會檢查。未支援的相依友元類別範本形式、無法證明來源的複製鏈及完整 C++／STL 尚未完成，原生行為須由實作提交的 CI 驗證。目前僅實作 C++ 輸入，易語言與 Python 待實作。使用內建 Clang 程式庫，不呼叫外部 Clang 程式。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-function-templates).

Core v2 支援已接納普通類別與泛型類別中的一般友元類別範本宣告，包括命名空間的新宣告、既有目標及已支援的限定目標，並保留各特化的存取權限。重複授權共用目標類別及靜態儲存；原始宣告自行寫入預設參數會遭拒絕，繼承的預設參數仍檢查來源。未支援的相依友元形式及完整 C++／STL 尚未完成，原生行為須由實作提交的 CI 驗證。目前僅實作 C++ 輸入，易語言與 Python 待實作。使用內建 Clang 程式庫，不呼叫外部 Clang 程式。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-class-templates).

Core v2 支援指向已有定義的一般自由函式與靜態成員函式的具型別指標，包括間接呼叫、回呼參數與回傳值、參考傳遞、陣列和欄位，以及常數初始化的全域、靜態區域與一般靜態成員回呼儲存。呼叫目標在引數求值前保存。僅接納預設函式 ABI 及已支援的純量／參考簽章；物件值傳遞、非靜態成員指標、可變引數、lambda 與跨編譯單元回呼仍待實作。完整 C++／STL 尚未完成，原生驗證須由 CI 執行。目前僅實作 C++ 輸入，易語言與 Python 待實作。使用內建 Clang 程式庫，不呼叫外部 Clang 程式。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-function-pointers).

Core v2 也支援對已接納的具體自由函式範本與靜態成員函式範本實例取址，可明確指定引數，或由預期指標型別推導。仍檢查原始碼、定義與 ABI；直接呼叫與回呼共享同一實例的函式及靜態區域儲存，不同實例保持獨立。完整 C++／STL 尚未完成，原生驗證須由 CI 執行。目前僅實作 C++ 輸入，易語言與 Python 待實作。使用內建 Clang 程式庫，不呼叫外部 Clang 程式。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-function-template-pointers).

Core v2 也支援具體命名空間變數範本與靜態成員變數範本中的常數／零初始化回呼。同一實例共享儲存，不同實例即使指向同一函式也保持獨立；直接書寫的 auto 仍接受完整原始碼檢查，動態初始化與指標值範本引數尚不支援。完整 C++／STL 尚未完成，原生驗證須由 CI 執行。目前僅支援 C++ 輸入，易語言與 Python 待實作；Clang 程式庫內建，不呼叫外部 Clang 程式。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#callback-variable-templates).

Core v2 支援具名非範本巢狀記錄，包括透過合法別名或工廠函式公開的私有/受保護型別。每個巢狀物件保留自己的儲存空間和接收者；明確的外層參考、作用域型別身分、成員/陣列複製與移動、解構及巢狀迭代器沿用一般語意。按值相依型別先輸出。匿名巢狀記錄、其他類別範本形式、繼承及完整 STL 仍待實作；原生驗證以實作版本的 CI 為準。

Core v2 支援具有定義的整數、布林及列舉靜態資料成員，包括 inline/constexpr 與類別外定義，可採常數或零初始化。所有實例共用同一份具型別儲存空間。接收物件的副作用與暫時物件清理均保留；靜態成員參考不會因暫時接收物件解構而失效。對於類別內常數初始化經過檢查的非 inline const 整數、布林及列舉成員，讀取值或捨棄運算式結果無須另行定義，也不會建立全域物件；對成員取址或繫結參考仍要求目前來源檔中有定義。動態初始化、其他靜態型別及完整 STL 仍待實作；原生驗證以實作版本的 CI 為準。

Core v2 支援已接納的靜態區域純量、指標、回呼、記錄及固定長度陣列的常數初始化與同步首次初始化，包括具有平凡靜態解構的 const 物件。初始化器可使用參數、自動區域變數及 this；後續呼叫跳過初始化器，並行呼叫會等待初始化與暫存物件清理完成。正規宣告和範本實例保持物件身分，常數初始化仍不需要執行期守衛。原生目標必須支援無鎖的 32 位元整數原子操作。constexpr 函式內的靜態區域宣告、非平凡靜態解構、TLS、例外傳播與重試及非區域動態初始化仍未支援。完整 C++／STL 尚未完成；原生驗收以實作版本的 CI 為準。 靜態區域參考可透過參數、指標或回傳參考的呼叫繫結既有物件；後續呼叫重用該繫結，但不會延長被參考物件的生命週期。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-initialization). 靜態區域參考亦可透過同步首次初始化延長已接納暫存物件的生命週期，並在永久儲存中完成建構。完整物件、子物件別名、自指標及選取的條件分支維持身分；一般暫存物件清理完成後才發布。靜態暫存物件仍須具有平凡解構。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-temporaries).

Core v2 支援行內命名空間、命名空間別名，以及命名空間與區塊內的 using 指令和已解析的 using 宣告。匯入的函式、變數、型別和無作用域列舉值保留原有身分、名稱查找規則、存取檢查及物件生命週期。行內命名空間保留與父命名空間合併的查找及雙向引數相依查找，並支援巢狀結構和重新開啟。類別成員匯入、繼承建構函式、其他類別範本形式、C++20 列舉匯入及顯式巢狀 inline 語法尚未在此階段支援；完整 C++／STL 仍未完成。

Core v2 支援一般函式及已支援的函式範本具體實例中的 C++17 if constexpr，僅為選中分支產生執行期程式碼，並保留初始化儲存與作用域清理。一般函式仍檢查兩個來源分支；Clang 不會實例化遭捨棄的相依分支。一般函式與全域變數的完整定義限制仍保留，C++23 if consteval 不受支援。

Core v2 支援最多 64 個參數的自由函式範本，可混合受支援的型別參數與整數、布林及列舉值參數。型別推導、顯式實例化與特化、預設型別、已解析的函式預設引數、遞迴與命名空間匯入皆轉為一般有型別函式。不同實例保留各自的區域型別與純量靜態儲存，即使主範本不同而簽章相同也不會合併。泛型函式本體與未使用的延遲預設引數於實例化後接受檢查，每個已產生實例的本體皆會檢查。sizeof/noexcept 內只有簽章的範本呼叫仍要求定義。未支援的相依友元類別範本形式、標準標頭與完整 STL 尚未完成；原生驗證以實作版本的 CI 為準。

支援 C++17 純量 auto 引數、相依型別的純量參數、陣列長度推導及有限的 constexpr 範本遞迴。值引數轉為有型別常數，不增加執行期參數；等價引數共用實例，不同值或推導型別保留各自的靜態儲存。書寫的引數運算式與參數型別會在摺疊消除前接受檢查。指標／參考／類別值引數仍不在本次增量範圍內。

Core v2 支援命名空間中標準配置類別範本的具體實例，最多含 64 個型別或純量值參數。預設型別、明確實例化／特化、型別別名、固定陣列及已選取的欄位初始化均產生一般的有型別記錄。等價引數共用型別，不同值、型別與主範本保留獨立身分；複製、參考、回傳與解構沿用既有成員語意。書寫的型別、預設型別及引數運算式會在消除前檢查。完整 C++／STL 尚未完成。原生結果以實作版本的 CI 為準。

這些標準配置類別範本也支援普通具名成員函式，包括 const 與參考限定多載、靜態工廠函式、成員範圍迴圈，以及每個實例獨立的純量靜態區域變數。未使用的泛型函式本體與預設引數保持按需實例化；選中的呼叫與所有已實例化本體均接受現有的型別、定義及生命週期檢查。類別外定義處的外層範本參數型別寫法也會檢查。成員函式沿用一般接收物件與回傳值約定，不產生執行期範本參數。

這類範本支援使用者定義的預設／參數建構、明確／轉換建構及複製、移動建構函式，也支援類別外定義與明確實例化／特化。未使用的本體、初始設定式及預設引數保持按需實例化；實際建構必須具備已檢查的定義。複製建構的預設引數只在省略引數時求值。初始化直接寫入目標儲存，遵循成員宣告順序，並沿用暫存物件、欄位與陣列的生命週期規則。繼承建構仍未支援；原生結果須由實作版本 CI 確認。

這些類別範本也支援使用者定義的非虛擬解構函式，包括類別外定義與明確實例化／特化。內建前端判定需要解構定義時必須提供定義，直接回傳物件也會檢查。僅查詢型別或不求值的運算式仍維持未使用解構本體的惰性，同時檢查已選取的例外規格。解構函式使用明確的接收物件儲存，先執行使用者本體，再反向清理成員與陣列元素，並沿用完整運算式、參考延長與傳值參數的生命週期規則。明確解構呼叫與完整 C++／STL 尚未完成；原生驗證以實作版本的 CI 為準。

這類範本支援以 `= default` 定義預設、複製、移動建構函式，複製、移動指派及非虛擬解構函式，包括類別內外定義。已選取的操作保留成員順序、來源與目標儲存身分、指派的參考結果和反向清理；值初始化區分首次宣告預設化與類別外預設化。未使用的本體維持惰性，查詢仍檢查已解析的書寫例外規格，實際使用的非平凡操作必須有屬於輸入原始碼的產生定義。明確特化不能繼承主範本的預設化標記。完整 C++17／STL 尚未完成，原生驗證以實作版本的 CI 為準。

這類範本也支援使用者定義的運算子和轉換函式，包括複製／移動指派、迭代器式解參考／遞增／下標／箭頭、函式物件及明確 bool 轉換。選取的多載保留 C++17 求值順序、參考別名、最終結果儲存和既有生命週期。未使用的本體與預設引數維持惰性；選取的宣告和所有具現化本體仍接受原始碼與定義檢查。類別外定義、明確具現化／特化及各實例的純量靜態區域變數使用一般具型別函式，不增加執行期範本參數。配置運算子、標準標頭及完整 C++17／STL 尚未完成；原生結果以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-operators-and-conversions).

在支援的自由函式範本或類別範本成員中具現化的區域類別，可使用一般方法、建構／解構、運算子、轉換和已選 defaulted 操作。具體成員來源、原始碼型別、接收物件儲存和各實例的純量靜態區域變數仍受檢查，所有已具現化的區域函式本體都要檢查。明確函式具現化的每次宣告都保留書寫的引數、型別、轉換函式名稱、限定名稱和屬性檢查，包括 extern 及不產生新實例的重複宣告，避免重用標準宣告時漏掉未支援的原始碼。原生結果以實作版本的 CI 為準，完整 C++17／STL 尚未完成。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-local-classes-and-written-directives).

Core v2 也支援命名空間作用域的一般運算子函式範本。算術、比較、回傳參考的操作、迭代器運算子與記錄回傳值保留所選多載、C++17 求值順序、實際儲存及暫存物件清理。ADL、顯式呼叫、特化／實例化和各實例的區域型別與靜態變數沿用現有有型別函式，不產生執行期範本參數。未使用的本體維持按需實例化；選中原始碼、已實例化本體及顯式實例化語句的原始寫法都會檢查。未支援的相依友元類別範本形式、配置、標準標頭與完整 C++17／STL 仍待實作；原生驗證以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-operator-function-templates).

這些類別範本也支援零初始化或常數初始化的整數、布林和列舉靜態資料成員，包括 inline／constexpr 與類別外定義。等價範本引數共用儲存，不同實例保留獨立物件。已檢查的非 inline const 值無須建立虛構儲存；實際求值的位址或參考存取需要定義。未使用的初始化器維持按需實例化；已實例化的初始化器與每條明確實例化語句都保留原始碼檢查。接收物件的副作用與暫存物件清理沿用一般有型別操作。動態初始化、其他靜態型別、完整 C++17／STL 仍未完成；原生驗證以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-scalar-static-data).

Core v2 支援已准許的函式、運算子及類別範本的純量非型別參數預設值。相依於前面參數的運算式、純量 auto、繼承的預設值與已檢查的 constexpr 轉換保留 Clang 的選擇與推導規則。省略參數與明確指定等價參數時，共用函式、記錄和靜態物件。非相依預設值的原始碼會檢查；未使用的相依預設值維持按需實例化。轉換成功後保留原運算式與轉換後原始碼，替換或轉換失敗仍依一般多載回退處理。完整 C++17／STL 尚未完成；原生驗證以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#scalar-template-parameter-defaults).

推導回傳型別的參考轉換保留別名及 const 屬性，包括 decltype(auto) 與 auto&& 的參考折疊。內建前端在篩選候選前推導必要的回傳型別，不實例化無關的按值回傳範本函式本體。完整 C++17／STL 尚未完成，原生驗證以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deduced-reference-conversions). 首次將物件傳給參考參數時，會在相容性篩選前推導必要的轉換結果。直接繫結階段的篩選不免除後續間接初始化所需的回傳型別推導；競爭候選 auto& 的函式本體若無法推導回傳型別，仍會產生一般 C++ 診斷。

Core v2 支援已准許的命名空間函式、運算子及類別範本中的具體型別與純量參數包。主範本最多 64 個參數，每個包最多 64 個元素。展開參數保留獨立儲存，sizeof... 轉為有型別的數量，已解析的摺疊運算式保留求值順序、短路及解構。書寫的參數型別可保留同一範本參數列表中相依包數量的中繼資料。空摺疊的模式不實例化；初始值及實際產生的元素仍會檢查。完整 C++17／STL 尚未完成，原生結果以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-parameter-packs).

Core v2 支援命名空間別名範本，可組合已支援的純量、void、指標、參考、固定陣列與類別型別，以及型別／純量預設參數和具體參數包。別名共用原有型別身分與靜態儲存空間。實際使用時檢查書寫的引數、選中的預設值與替換後的底層原始碼；未選中的候選與未實例化的相依函式主體仍延後處理。顯式引數保留呼叫端脈絡，包括同一範本的遞迴呼叫及 this 運算式。參數數量、各參數包元素數與原始碼展開深度上限均為 64，並受共用原始碼預算限制。完整 C++17／STL 尚未完成；原生驗證以實作版本的 CI 為準。目前僅實作 C++ 輸入，易語言、Python 等前端仍屬後續範圍。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-alias-templates).
選中的非型別樣板引數也會保留參數型別的原始碼，包括明確指定的函式樣板引數和推導出的空參數包；別名折疊掉的不支援原始碼仍會檢查。

非相依的型別預設參數即使位於未使用的範本中也會檢查。帶限定名稱的函式範本呼叫及具體轉換函式定義保留實際書寫的原始碼，包括類別外定義中的別名寫法。傳回左值參考的轉換可為 const 參考引數產生擴寬型別的暫時值；右值參考引數仍遵循 C++ 的多載限制。
Core v2 也支援專案自有命名空間中的類別範本偏特化。選中的偏特化及其推導參數與主範本引數分別檢查。別名共用同一具體型別及靜態儲存，已支援的成員保留呼叫、參考及生命週期語意。參數數量及每個參數包的元素數量上限仍為 64。完整 C++17／STL 尚未完成，原生驗證以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-partial-specializations).

Core v2 支援獨立的 `decltype(nullptr)` 值型別，包括別名、參考、欄位／陣列、回呼簽章，以及零初始化或常數初始化的純量靜態儲存與變數範本儲存。轉換為指標時保留來源運算式副作用與暫時物件解構；不同空值物件保留各自位址。動態靜態初始化、空值範本引數及標準標頭仍未完成，原生驗證須以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#null-pointer-values).

Core v2 支援一般類別與泛型類別的委派建構，包括建構鏈與選定的建構函式範本。目標建構與委派方本體使用同一最終物件，成員只初始化一次，引數暫存物件在委派方本體執行前解構。來源選擇與定義檢查仍有效；原生驗證須以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#delegating-constructors).

Core v2 支援一般、泛型及巢狀類別中的 const 資料成員，型別可為受支援的純量、紀錄、指標、回呼或陣列。初始化寫入最終儲存，保留來源選定的複製／移動、獨立成員位址及 const 參考／指標型別；非法寫入與已刪除的指派由 Clang 拒絕。這可用於自訂的常數鍵值類別，標準標頭與完整 STL 仍未完成。原生驗證須以實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#const-data-members).

Core v2 接受受支援的刪除函式，以及預設產生後被刪除的特殊成員，包括相應的樣板和友元形式。Clang 保留多載選擇與存取診斷；刪除宣告不產生可執行定義。僅可移動物件、選定的複製後備及 C++17 保證的複製省略沿用現有生命週期處理。須由實作版本的 CI 驗證，完整 C++／STL 仍未完成。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deleted-function-declarations).

Core v2 接受 IEEE `float` 和 `double`、算術與轉換、參考、欄位與陣列、回呼，以及零初始化或常數初始化的純量靜態物件。精確的字面值位元表示及獨立目標配置檢查保留資料表示；執行要求預設浮點環境，並停用快速數學、額外精度和隱含乘加收縮。須由實作版本的 CI 驗證；`long double`、標準標頭及完整 C++／STL 仍未完成。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#binary-floating-point-values).

Core v2 接受窄字元、UTF-8、UTF-16、UTF-32 和寬字元字串字面值，保留精確字元單元與靜態唯讀儲存，並支援字元陣列初始化及補零、完全常數初始化的命名空間陣列。別名、欄位複製與原始碼生命週期沿用現有型別操作。須由實作版本的 CI 驗證；標準標頭以及 `std::string` 的配置與操作仍未完成。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#string-literals-and-constant-arrays).

Core v2 支援命名空間陣列、函式內靜態陣列及類別靜態陣列成員的零初始化或常數初始化，包含已支援的範本實體，並保留可變與 const 規則。同一陣列跨呼叫保留狀態及位址，不同範本實體使用獨立儲存空間。動態初始化、靜態解構及完整 STL 仍待完成。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#fixed-array-static-storage).

Core v2 支援靜態物件指標的零初始化與常數初始化，保留全域物件、字串、陣列元素及欄位的位址，以及有效的末尾後一位指標。命名空間、函式、類別及範本儲存空間皆保留指標身分、可變性與 const 存取規則。前向位址直接輸出為 C23 常數，無須執行期初始化。靜態參考、動態配置及完整 STL 仍待完成。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-object-pointer-storage).

靜態參考現在可繫結既有靜態物件、陣列／記錄子物件及字串，涵蓋區域／類別靜態參考及已支援的範本實例。讀寫保留原物件身分與 const 權限。動態初始化及靜態解構尚未完成，原生驗證仍需對應實作版本的 CI。請參閱[靜態參考契約](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-bindings)。

Core v2 支援常數初始化的靜態參考暫存物件，包括純量、陣列及記錄物件。完整物件、子物件別名、自身指標、const 權限及範本實例身分均保留持久儲存空間。目前要求平凡解構；動態初始化、靜態解構、TLS 和完整 C++／STL 仍待完成，原生驗證以此實作版本的 CI 為準。 [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-temporary-lifetime-extension).

靜態記錄物件現在支援常量初始化與共用可寫狀態，涵蓋區域／類別靜態物件及已支援的範本實例。物件自身位址、constexpr 建構與靜態零初始化保留原物件身分。動態初始化及靜態解構尚未完成，原生驗證仍需對應實作版本的 CI。請參閱[靜態記錄契約](../../utils/translate-frontends/docs/cpp-core-v2.md#static-record-objects)。

## 安裝與純量轉譯

使用正常安裝的 NeverC 及其標準資源即可。C++ 前端與核准的 SDK 標頭均已內建，無需另行安裝 Clang。建置細節見[前端說明](../../utils/translate-frontends/cpp/README.md)。

```sh
neverc translate --from cpp input.cpp -o output.nc
neverc output.nc -c -o output.o
```

`cpp-core-v1` 接受一個不含 include 的獨立 C++17 原始檔，支援 `int`、`unsigned int`、`bool`、`void`、簡單聚合型別、自由函式、命名空間、多載及文件列出的控制流程。所有輸入宣告都會檢查，包括未使用的程式碼。

使用 `--profile cpp-core-v2` 可增加經過檢查的 `typedef`／`using` 型別別名、底層型別為受支援整數型別的列舉、`static_assert`，以及限定範圍的物件指標與左值參考。參考保留別名關係，包括參數和回傳參考；支援多層指標的 `const` 與空指標。仍限單一原始檔，且不允許 include。詳見 [core v2 支援範圍](../../utils/translate-frontends/docs/cpp-core-v2.md)。

core v2 也增加了固定長度的區域陣列與陣列欄位、多維下標存取，以及陣列指標和參考。部分初始化將省略的純量元素設為零，紀錄元素依所選的初始化方式處理；元素初始化保留原始碼順序與別名關係。陣列長度與初始化展開量有上限。 全域陣列與變長陣列仍未支援。

core v2 支援 `switch`／`case`／`default`，包括 C++17 初始化陳述式、分支貫穿及經過驗證的 `[[fallthrough]]` 標註。選擇器只求值一次；巢狀 switch 與迴圈保留各自的 `break`／`continue` 目標。GNU case 範圍及其他陳述式屬性仍被拒絕。

Core v2 現支援有號與無號的 8／16／32／64 位整數、字元型別與常值，以及常數 `sizeof`／`alignof` 查詢。先依來源 C++ 規則完成整數提升與多載解析，再將位元寬度標準化；`long`、`wchar_t` 與大小型別遵循目標平台。列舉也支援窄整數與寬整數作為底層型別。標準函式庫仍在開發中。

Core v2 另支援物件指標偏移、差值、遞增／遞減及複合賦值，可用於走訪陣列並保留多維陣列的步長。產生的輔助函式保留 C++17 空指標加減零及空指標相減的行為。物件指標大小比較也已接入，在原生 x86／AArch64 目標上使用獨立驗證的無符號位址表示；原生結果仍需對應提交的 CI 驗證。原始碼指標／整數轉換與完整 STL 仍未完成。

Core v2 會用 NeverC 自身的目標模型核對來源型別的大小與 ABI 對齊，包括結構體大小和欄位偏移。產生的程式碼透過編譯期斷言再次檢查配置，清單也會記錄這些驗證依據。

Core v2 支援已接納的紀錄型別中的具名非虛擬成員函式，包括 const 多載、左值限定方法、靜態方法和 `this`。呼叫保留原物件身分，並在引數之前求值接收物件。

Core v2 也支援具有標準布局及受支援複製操作的紀錄型別的一般使用者建構函式。區域物件、紀錄欄位與陣列元素直接在最終儲存位置建構，欄位依宣告順序初始化。例外展開仍未支援。

Core v2 為每個以值傳遞的紀錄參數建立獨立物件，並將紀錄回傳值直接寫入呼叫者的目標位置。建構函式與成員函式採用相同規則；原始碼要求的複製與參考別名仍會保留。對於符合條件的平凡紀錄型別，其他 C++17 實作可能增加參數或回傳值的複製。

Core v2 支援一般使用者解構函式，以及正常離開時的隱含成員解構。區域物件、紀錄欄位與陣列元素以反向順序解構；暫存物件在完整運算式結束時清理，先保存需要使用的值。回傳、分支、迴圈、break 與 continue 均執行對應清理。以值傳遞的參數在被呼叫函式結束時解構，回傳物件由呼叫者管理。明確解構呼叫、靜態物件解構與例外展開仍未支援。

Core v2 支援來源參數為 `R&` 或 `const R&` 的一般使用者複製建構與複製指定函式。複製直接作用於實際目標，保留函式的副作用與回傳參考。指定運算子語法先求值右運算元，明確呼叫 `operator=` 時先求值接收物件。

Core v2 也支援編譯器產生或明確 default 的預設建構函式，以及 default 解構函式，包括巢狀紀錄與陣列成員。預設建構使用實際目標位置，並依宣告順序初始化成員；值初始化只執行 C++ 規則要求的清零。類別外 default 保留其不同的初始化規則。未使用或僅出現在未求值運算式中的 default 建構不要求額外產生函式本體。

Core v2 支援隱含及明確 default 的複製建構，包括巢狀紀錄與多維陣列。選定的成員複製建構依宣告及元素順序在實際目標位置執行，來源陣列位址只計算一次。平凡複製保留已儲存的指標欄位原值。按值傳參與從來源物件回傳保留複製副作用，直接轉送純右值不會額外複製。

Core v2 也支援隱含及明確 default 的複製指定。成員與陣列元素保留選定的指定操作及其順序，產生的平凡陣列複製轉為具型別的元素指定，無需外部記憶體複製呼叫。平凡指定保留指標欄位原值並回傳實際接收物件。運算子語法先擷取來源參考，明確成員呼叫先擷取接收物件；來源值在這些副作用完成後讀取，指定不會額外建構或解構物件。

Core v2 支援已准入欄位的預設成員初始化器，包括存取先前成員、一般呼叫、巢狀記錄與陣列。選中的預設值使用實際所屬物件作為 `this`，明確的聚合初始化項保留呼叫者的 `this`；明確初始化會覆蓋對應成員的預設值。隱含或 default 複製及指定不重跑預設值，使用者複製建構函式可為省略的成員選用預設值。建構成員初始化與聚合初始化各自保留暫存物件的清理邊界。樣板與完整 STL 仍在開發中。

Core v2 支援指向既有存活物件的右值參考，包括純量、指標、記錄與陣列別名、參考參數／回傳值、條件亡值及一般 `&&` 限定方法。`static_cast<R&&>(live)` 等轉換保留同一個物件，具名右值參考變數仍是左值。多載與既有複製行為遵循 Clang 的選擇，參考不會增加清理擁有者。

Core v2 支援以存活的 `R&&` 或 `const R&&` 為來源的一般使用者移動建構與移動指定。建構使用實際目標；指定保留來源修改、運算元求值順序與回傳的 `R&` 別名，並支援 `&`／`&&` 限定的接收物件。具名右值參考仍選擇左值多載，明確建構函式保留其初始化規則。移動不會結束來源物件的生命週期，來源與目標仍各自正常解構。

Core v2 也支援隱含及明確 default 移動建構與移動指定，包括巢狀記錄與多維陣列。成員依宣告與元素順序執行 C++ 選中的複製或移動。產生的移動不重跑預設成員初始化器，平凡操作保留已儲存的指標值。來源物件仍正常解構，未使用或平凡的產生操作不需要虛構函式主體。

Core v2 支援已解析的標準 `noexcept`、`noexcept(true/false)`、C++17 `throw()` 宣告，以及常數 `noexcept(expression)` 查詢。查詢保留選定函式與解構函式的例外規格，不執行運算元。所有運算元與手寫規格仍接受原始碼檢查，包括未使用的程式碼。擲出、捕捉、堆疊展開、樣板與完整 STL 仍在開發中。

Core v2 支援一般成員及自由多載運算子，包括算術、比較、下標、解參考、遞增、函式物件與一般指定簽章。呼叫保留選定函式、參考別名及物件結果。運算子語法遵循 C++17 求值順序，多載邏輯運算子會求值兩個運算元。自由指定運算子統一由右向左初始化參數，再依反向順序解構。樣板、記憶體配置與完整 STL 仍在開發中。

Core v2 也支援現存物件上的一般轉換函式：隱式與顯式整數、列舉及指標轉換，條件中的顯式 `bool`，以及參考或物件結果。每次轉換執行一次選定的成員函式。參考保留別名；物件純右值直接初始化實際目標，從參考初始化值則保留選定的複製或移動。const／參考限定、constexpr 與 noexcept 遵循 C++17 選擇規則。

Core v2 現支援暫存物件接收者，以及存活到完整運算式結束的純量／記錄暫存參考引數，涵蓋建構函式、方法、運算子與轉換函式。每次求值都有實際儲存，物件結果保留最終目標，參考保留別名。呼叫及被呼叫函式的參數解構完成後，暫存物件依建構反向順序清理，並正確處理條件分支與迴圈中的重複求值。暫存記錄物件的陣列子物件也受支援。

Core v2 現支援普通自動區域參考延長暫存物件的生命週期，包括 `const T& r{T{...}}` 與 `T&& r = T{...}`。純量、列舉、指標及記錄暫存值使用實際儲存；C++ 規定延長生命週期時，指向成員或陣列元素的參考會保留完整記錄物件。大括號及等號加大括號初始化保留同一個物件。物件在參考所屬範圍結束時解構，涵蓋條件、迴圈與提前離開；初始化器內其他暫存物件仍在各自完整運算式結束時清理。別名不增加擁有者，後續複製或移動保留選定操作。靜態／全域／執行緒區域參考、參考欄位、樣板與完整 STL 仍在開發中。

Core v2 也支援獨立固定長度陣列暫存值，可用於呼叫、退化／下標存取、捨棄運算式及自動區域參考。每個陣列使用一個實際目標；純量寫入、記錄建構與共用預設填補依序直接初始化各元素，不增加元素擁有者。C++ 規定延長生命週期時，多維陣列的列／元素參考會保留完整陣列。元素在完整運算式或參考範圍結束時依反向順序解構。陣列參考保留有型別的位址，仍遵守既有維度、儲存與展開限制。完整 C++／STL 仍在開發中。

Core v2 支援空的標準配置類別及無狀態的函式物件／轉換物件。空物件保留 C++ 的一位元組大小與對齊、語言要求的獨立儲存、選定建構／運算子呼叫及正常解構。平凡複製仍會求值運算元；陣列及含空成員的記錄會計入儲存限制。產生的 NC 使用一個內部儲存位元組，來源欄位仍為空。繼承、範本與完整 STL 仍在開發中。

Core v2 支援轉為 void、`void()` 和 `void{}`，可結合受支援的 void 別名、呼叫、回傳、逗號運算式及條件分支。捨棄非 volatile 左值時保留接收者／下標的副作用而不讀取其儲存值；捨棄暫存物件仍會建構物件，並在原有完整運算式邊界解構。不會產生 void 變數或值載體。常量與 noexcept 的運算元仍完整檢查。volatile 物件、不支援的運算元型別、範本及完整 STL 不在本次增量範圍內。

Core v2 支援允許的函式、方法、呼叫運算子及使用者建構函式的預設引數，包括使用者複製／移動建構函式的尾隨預設引數。名稱依宣告位置解析，預設運算式在每次省略引數的呼叫中求值。參考和值參數保留原有身分與清理規則。陣列遺漏元素及產生的陣列複製會在下一個元素前清理預設引數暫存物件；明確的陣列初始化項保留外層完整運算式壽命。未使用或被明確引數覆蓋的預設值仍會檢查。範本與完整 STL 尚未完成。

## 多檔案專案

從編譯資料庫明確選擇翻譯單元，並指定專案根目錄。內建前端逐一分析翻譯單元，合併器檢查定義完整性、連結屬性及共用型別，並保守驗證單一定義規則（ODR）。

```sh
neverc translate --from cpp --profile cpp-project-v1 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/a.cpp src/b.cpp --out-dir generated
```

專案輸出為 `translated.nc` 與 `translated.h`，僅允許該根目錄內的專案標頭檔。檔案有多種組態時，以 `--compdb-entry src/a.cpp=4` 選擇編譯資料庫的零起始索引。儲存的編譯命令僅作為資料解析，絕不執行。

## 有限的雙精度數學支援

`cpp-math-v1` 在專案支援上增加 `double`、文件列出的轉換／比較，以及精確簽章 `std::fabs(double)` 和 `std::floor(double)`。它使用內建的 Clang 20.1.8／libc++ 200100／macOS 15.5 標頭集合，要求明確的 macOS 15.0 目標（arm64 或 x86_64）。暫不支援通用浮點算術。浮點環境要求遮罩例外陷阱且停用非正規數歸零模式；四種標準捨入模式均已測試。

```sh
neverc translate --from cpp --profile cpp-math-v1 \
  --target arm64-apple-macosx15.0.0 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/math.cpp --out-dir generated-math
```

數學轉譯還會驗證已安裝的 NeverC 數學標頭、嵌入實作的身分，並實際執行連結探測。產生的數學模組使用 NeverC 執行階段函式庫。使用 `-fno-builtin-std` 時，僅當最終輸出需要 `fabs`／`floor` 映射才會在寫入前失敗；無需這些映射的數學程式碼仍可轉譯。

## 驗證與輸出

以 `--check` 取代輸出選項，可執行相同的分析、產生、語法與目的碼驗證，但不保留產生的檔案。`--report PATH` 寫入結構化診斷。轉譯過程不會執行原始程式。

輸出及附屬檔案不會覆寫既有檔案。`--out-dir` 要求父目錄存在且目標目錄不存在；`-o` 要求新的 `.nc` 路徑。清單記錄目標需求、輸入／輸出雜湊及編譯方式，原始碼映射將產生行對應至原始位置。

CI 結果、執行與安裝驗證以及測試跳過項目依平台分別記錄。原生 macOS arm64 與透過 Rosetta 執行的 macOS x86_64 仍是不同的驗證環境。完整 C++／STL、例外、範本、字串及 `std::vector` 不在已公布的支援範圍內。請參閱[支援矩陣](../../utils/translate-frontends/docs/support-matrix.md)、[協定與復原規則](../../utils/translate-frontends/docs/protocol.md)及[專案範例](../../tests/neverc/Inputs/translate/cpp/project)。
