**Idiomas**: [English](../translate.md) | [简体中文](../zh-CN/translate.md) | [繁體中文](../zh-TW/translate.md) | [日本語](../ja/translate.md) | [한국어](../ko/translate.md) | [Français](../fr/translate.md) | [Deutsch](../de/translate.md) | [Español](translate.md) | [Italiano](../it/translate.md) | [Русский](../ru/translate.md) | [العربية](../ar/translate.md)

[← Documentación](README.md)

# Traducir C++ a NeverC

Core v2 admite el reenvío predeterminado de sized delete: un sized delete/delete[] global implícito sin redeclaraciones en el código puede llamar al operador unsized correspondiente definido en el código. Las definiciones sized explícitas tienen prioridad y se conserva la disposición original del cookie del array. No se añade un asignador predeterminado ni un runtime de excepciones; la verificación nativa requiere la CI de esta revisión. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

Core v2 también admite `__array_rank` y `__array_extent` verificados con índices enteros constantes no negativos, incluidos índices de plantillas para tipos de array fijos. Ambos operandos se revisan antes del plegado. Los tipos no admitidos y los índices con conversión implícita desde una clase siguen excluidos. Los resultados nativos requieren la CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#array-type-queries).

Core v2 añade clasificación integrada y verificada de tipos, como `__is_integral`, `__is_pointer` y `__is_same`. Las consultas conservan la identidad de los tipos C++ y revisan los operandos escritos antes de producir un booleano. Los tipos no admitidos y otros rasgos siguen restringidos. Las pruebas O0/O2 y de reubicación requieren la CI de la revisión que implementa el cambio; las cabeceras estándar y C++/STL completo siguen pendientes. Las consultas también cubren agregados, clases vacías, disposición estándar, propiedades trivial, trivially-copyable y POD, polimorfismo, abstracción y relaciones de base con `__is_base_of` para los tipos admitidos. También se admiten las clases estándar `final` y las consultas `__is_final`, `__is_literal` y `__has_unique_object_representations`. Se conservan el relleno del tipo fuente, la representación de clases vacías y las reglas de tipos literales C++17; los demás atributos y métodos virtuales mantienen sus restricciones. La destructibilidad y la destrucción trivial también cubren las clases admitidas, destructores privados o eliminados y referencias, manteniendo diferidas las instanciaciones de cuerpos y especificaciones de excepción de plantillas. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#builtin-type-classification).

Core v2 admite consultas de construcción, asignación, conversión y destrucción para los tipos admitidos que no son clases, punteros, referencias y matrices de tamaño fijo. Las expresiones conservadas de las operaciones de clase permiten operaciones triviales implícitas y operaciones ordinarias con definiciones, conversiones de argumentos y dependencias de destrucción verificadas. Las consultas de construcción también aceptan argumentos predeterminados ordinarios sin reescritura tras comprobar el parámetro exacto, su expresión y la limpieza de temporales, sin ejecutar efectos secundarios. Los argumentos predeterminados nothrow también requieren especificaciones de excepción ya resueltas para los tipos de llamada efectivos. La destrucción nothrow de valores de clase y matrices fijas conserva el destructor elegido y el prototipo resuelto y verifica la destrucción implícita, los destructores ordinarios explícitamente predeterminados o la destrucción ordinaria de usuario con definición comprobada. Conserva los resultados anticipados por eliminación, acceso y referencias. Siguen pendientes las operaciones de plantillas, los destructores escritos de plantillas y las dependencias de excepción sin resolver. Las pruebas O0/O2, de reubicación y de ocho ABI requieren la CI de esta revisión; las cabeceras estándar y C++/STL completo aún no están terminados. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#non-record-operation-traits).

Core v2 añade llamadas directas a `malloc/calloc/free` mediante declaraciones C globales exactas en el código fuente. Los asignadores C++ definidos en el código pueden usar el montón nativo e intercambiar y liberar memoria con un cliente C independiente. Se mantienen las comprobaciones de anchura, convención de llamada, código e IR. La validación O0/O2 requiere la CI de esta implementación. `realloc`, la asignación C++ predeterminada con excepciones, las cabeceras estándar y C++/STL completo siguen pendientes. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#native-c-heap-calls).

Core v2 registra la destrucción de objetos estáticos, arrays y temporales admitidos con vida extendida cuando termina cada construcción. Conserva los valores constantes; registra los locales en el primer paso y los no locales durante el inicio nativo. El CRT conserva el orden de salida y descarga de módulos. Las pruebas O0/O2, de descarga y primer uso concurrente requieren la CI de esta implementación. TLS, desenrollado de excepciones, heap predeterminado, cabeceras estándar y C++/STL completo siguen pendientes; la carga manual/DynCode no está admitida. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-destruction).

Core v2 admite ahora la inicialización dinámica no local de objetos y referencias compatibles, incluidas instancias de plantillas materializadas. Un constructor nativo interno verificado se ejecuta antes de main en orden de definición, después de la inicialización cero o constante, y destruye los temporales ordinarios tras cada inicializador. Los estáticos locales conservan la inicialización al primer uso. Las pruebas O0/O2 del programa y de un cliente C separado requieren CI de esta revisión. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#nonlocal-dynamic-initialization).

Las longitudes de new[] en ejecución también admiten asignadores de clase noexcept definidos en el código fuente, sin argumentos placement. Una longitud no válida devuelve null antes de llamar al asignador; los elementos válidos se construyen en un bucle que destruye los temporales de los argumentos predeterminados tras cada elemento. Los temporales del prefijo explícito y de la conversión de longitud viven hasta el final de la expresión completa. Los agregados repetidos pueden inicializar directamente cada elemento sin objetos temporales adicionales, incluidas listas anidadas y referencias a objetos existentes. Las inicializaciones que requieren dichos temporales, las asignaciones con excepciones o placement en ejecución, el heap predeterminado y C++/STL completo siguen pendientes. La verificación nativa espera la CI de esta versión. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#runtime-array-allocation).

Core v2 admite new/delete de un solo objeto mediante funciones de asignación definidas y verificadas en el código fuente, con sobrecargas placement de clases y plantillas, identidad del almacenamiento y limpieza de argumentos. La destrucción explícita y la reconstrucción placement conservan las obligaciones de limpieza automática. La verificación nativa requiere la CI de esta implementación. El runtime del heap predeterminado, las longitudes new[] en ejecución, las excepciones, las cabeceras estándar y C++/STL completo siguen pendientes. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

El comando experimental `neverc translate` genera código `.nc` revisable con `cpp-core-v1`, `cpp-core-v2`, `cpp-project-v1` y `cpp-math-v1`.

**Actualmente solo está implementada la traducción de código C++.** Está previsto añadir E Language (易语言, `.e`), Python, Go, Rust, TypeScript y JavaScript; sus traductores aún no están disponibles.

Las declaraciones de especialización parcial conservan la comprobación de argumentos primarios, incluidas las copias de variables miembro con argumentos internos dependientes. Se comprueban los tipos de parámetros ya resueltos aunque la especialización nunca se seleccione. Las expansiones de longitud desconocida conservan el prefijo comprobado y la fuente real sin inventar posiciones. Las especializaciones completas de variables miembro aún dependientes conservan las mismas comprobaciones. C++/STL completo sigue pendiente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#partial-declaration-source-checks).

Conservar la comprobación de argumentos no convierte una especialización parcial en válida: debe seguir siendo más especializada y sus parámetros deben poder deducirse. Los patrones de paquetes inválidos y una instanciación extern posterior a una definición de instanciación siguen produciendo diagnósticos de C++ (`TR0202`).

Core v2 admite clases anidadas ordinarias con nombre dentro de clases genéricas admitidas, sus copias concretas y especializaciones explícitas propias. Sus cuerpos no añaden un nivel de parámetros de plantilla. Los cuerpos sin uso se aplazan; campos instanciados, métodos seleccionados, plantillas miembro, vidas de objetos y almacenamiento estático escalar conservan su fuente y propietario exactos. Cada instanciación explícita comprueba su calificador escrito, incluso extern y repeticiones sin efecto. Siguen pendientes la herencia, la asignación dinámica y C++/STL completo. Solo se implementa entrada C++; E Language y Python están previstos. Se usan bibliotecas Clang integradas sin un ejecutable Clang externo. Los resultados nativos requieren CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-nested-classes-in-generic-owners).

Core v2 admite plantillas escalares de variables miembro estáticas en registros e instancias de clases compatibles, con argumentos predeterminados, paquetes limitados, especializaciones parciales/completas y definiciones externas. Las instancias equivalentes comparten almacenamiento; distintos argumentos internos o externos conservan objetos separados. Los valores const no inline comprobados se leen sin almacenamiento; las direcciones y referencias evaluadas requieren definición. Se conservan las comprobaciones de declaraciones y definiciones, los efectos del receptor y la destrucción de temporales. Las especializaciones completas dentro de clases externas dependientes conservan las fuentes de las declaraciones originales y concretas. Los argumentos, valores predeterminados y tipos de parámetros resueltos se comprueban de inmediato; las fuentes dependientes se comprueban tras la instanciación. Siguen pendientes otros resultados, C++/STL completo. Se usan bibliotecas Clang integradas sin Clang externo. Solo C++ está implementado como entrada; E Language, Python y otros frontends están previstos. La validación nativa requiere el CI de la implementación. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-variable-templates).

Core v2 admite plantillas de alias miembro en registros ordinarios, incluidos los anidados, e instancias de clases compatibles. Los argumentos predeterminados y paquetes limitados pueden usar parámetros externos de la clase e internos del alias; sus argumentos y tipos fuente sustituidos se verifican por separado. Se conservan las semánticas de valores, referencias, matrices y registros sin crear objetos de plantilla en ejecución. Las declaraciones no dependientes se comprueban aunque no se usen; los tipos dependientes se aplazan hasta la sustitución. Se usan bibliotecas Clang integradas sin ejecutable externo. La validación nativa requiere el CI de la implementación. C++/STL completo sigue pendiente. Solo C++ está implementado como entrada; E Language, Python y otros frontends están previstos. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-alias-templates).

Core v2 admite plantillas concretas de funciones miembro en registros e instancias de clases compatibles: métodos normales y estáticos, operadores, constructores y conversiones, con paquetes limitados, valores predeterminados, definiciones externas y especializaciones. Los argumentos internos y externos conservan identidades propias; las llamadas equivalentes comparten código y almacenamiento estático. Se verifican el código fuente y las vidas útiles de llamadas, valores predeterminados y cuerpos seleccionados. La traducción usa bibliotecas Clang integradas sin ejecutar un Clang externo. Los resultados nativos requieren el CI de la implementación. La herencia, la asignación dinámica y C++/STL completo siguen pendientes. Solo C++ está implementado como entrada; E Language, Python y otros lenguajes están previstos. Los calificadores externos concretos de las declaraciones de plantillas miembro se verifican aunque no se instancie el cuerpo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-function-templates).

Core v2 admite plantillas de variables en espacios de nombres con resultados enteros, booleanos o enum, incluidos auto deducido, especializaciones parciales seleccionadas, especializaciones completas, valores predeterminados y paquetes limitados de tipos o escalares. Los rasgos de C++17 pueden usarse en static_assert, if constexpr y otros argumentos de plantilla. Las instancias equivalentes comparten almacenamiento global tipado; las distintas mantienen valores y direcciones propios. Consultar un tipo fijo no fuerza un inicializador sin usar ni crea almacenamiento. Se comprueban los argumentos reales, los valores predeterminados seleccionados, los tipos de declaración y definición y los inicializadores materializados. otros resultados, C++/STL completo siguen pendientes; la validación nativa requiere el CI de la revisión que implementa el cambio.

Un sizeof(T) o alignof(T) directo en el tipo escrito de un parámetro no tipo puede conservarse como metadato de declaración si T es el parámetro de tipo exacto de esa misma lista. Las sustituciones seleccionadas siguen requiriendo tipos concretos verificados; la excepción no incluye otras consultas dependientes. Una declaración extern template por sí sola no aporta la definición concreta exigida por el contrato actual de traducción de unidad cerrada.

Core v2 analiza las plantillas según C++17 en todos los destinos admitidos, incluidos Windows x64 y ARM64. Las definiciones se analizan sin el procesamiento diferido de MSVC; las definiciones duplicadas de instanciación explícita y las especificaciones de excepciones incompatibles siguen siendo errores del lenguaje. Los cuerpos dependientes no utilizados se instancian solo cuando se necesitan. El soporte completo de C++/STL sigue pendiente. Las especializaciones explícitas de funciones siguen necesitando su propia definición en la unidad de código fuente; repetir declaraciones no la proporciona.

Core v2 admite variables globales modificables de tipo entero, booleano y enumeración en el ámbito de un espacio de nombres, con inicialización a cero o mediante una constante totalmente comprobada. Su almacenamiento y dirección se conservan entre llamadas; las referencias, los punteros y los argumentos predeterminados acceden a la misma variable. Una declaración `extern` debe tener una definición en la misma unidad fuente. Las globales const siguen siendo de solo lectura. Se mantienen las restricciones específicas para los registros/arreglos/punteros/referencias globales, el almacenamiento local de hilos y las variables locales estáticas.

Core v2 admite bucles `for` por rango de C++17 sobre arreglos fijos compatibles y rangos definidos en el código fuente, con llamadas resueltas a `begin/end` mediante miembros o ADL. Las variables por valor o referencia, los iteradores de tipo registro y los centinelas de tipos distintos conservan las reglas normales de llamadas y duración de vida. El rango y `begin/end` se inicializan una sola vez; los objetos de cada iteración se destruyen antes del incremento o la salida, incluidos `continue`, `break` y `return`. Las otras formas de plantillas de clase, cabeceras estándar, contenedores STL, enlaces estructurados e instrucciones de inicialización de C++20 quedan fuera de esta ampliación. La validación nativa requiere el CI de la revisión que la implementa.

Core v2 admite miembros de datos privados y protegidos en clases de disposición estándar que cumplan las demás restricciones. Clang integrado comprueba los accesos antes de traducir; los métodos, constructores, fábricas, argumentos predeterminados y operaciones de copia/movimiento permitidos usan el mismo almacenamiento tipado. Los accesos externos ilegales siguen generando diagnósticos de C++. Los especificadores de acceso son reglas del lenguaje fuente y no garantizan secreto en ejecución. Se mantienen las restricciones para clases sin disposición estándar con accesos mixtos, formas dependientes no admitidas de plantillas de clases amigas, herencia y tipos de campo no admitidos. Los resultados nativos requieren el CI de la revisión que lo implementa.

Core v2 instancia funciones amigas no plantilla desde plantillas de clases admitidas, especializaciones parciales, plantillas miembro y copias de clases completas o anidadas ordinarias. ADL, acceso privado, operadores libres, redeclaraciones, clases locales y variables estáticas escalares conservan su identidad sin receptor implícito. Los cuerpos sin uso siguen diferidos; se comprueban las definiciones seleccionadas y firmas originales. Normalizar a otra declaración exige evidencia completa de fuente no dependiente, incluidos sus propios argumentos predeterminados y noexcept resuelto. Formas dependientes no admitidas de plantillas de clases amigas, expansiones de tipos amigos, C++/STL completo siguen pendientes. Se conservan las reglas de tipos amigos no dependientes y miembros amigos. Solo C++ está implementado; E Language y Python están previstos. Se integra Clang sin invocar un ejecutable externo. La validación nativa requiere CI del commit de implementación. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-functions).

Core v2 admite tipos amigos no plantilla resueltos en clases genéricas compatibles: `friend T;`, alias, tipos anidados dependientes y especializaciones de clases. Sema integrado comprueba el acceso privado y los tipos no clase ignorados legalmente. Se verifican las fuentes originales y sustituidas; copias sin origen probado, formas dependientes no admitidas de plantillas de clases amigas y expansiones de paquetes siguen excluidas. No se crean receptores ni almacenamiento adicional. El comportamiento nativo requiere CI del commit; C++/STL completo sigue pendiente. Solo C++ está implementado; E Language y Python están previstos. Clang está integrado sin ejecutable externo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-types).

Core v2 admite plantillas de funciones amigas en clases ordinarias y genéricas compatibles: operadores admitidos, ADL, acceso privado, parámetros internos y externos, valores predeterminados y paquetes. Conserva referencias, vidas de objetos e identidades separadas de instancias y almacenamiento estático. Comprueba las declaraciones originales y el contexto del cuerpo seleccionado. Las formas dependientes no admitidas de plantillas de clases amigas, copias sin origen probado y C++/STL completo siguen pendientes; se requiere CI del commit. Solo C++ está implementado; E Language y Python están previstos. Clang está integrado sin ejecutable externo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-function-templates).

Core v2 admite declaraciones ordinarias de plantillas de clases amigas en clases ordinarias y genéricas compatibles: introducciones en espacios de nombres, destinos existentes o cualificados admitidos y acceso por sus especializaciones. Los permisos repetidos comparten la clase destino y su almacenamiento estático. Se rechazan los argumentos predeterminados escritos en la declaración amiga y se comprueba el origen de los heredados. Las formas dependientes no admitidas y C++/STL completo siguen pendientes; se requiere CI del commit para validar el comportamiento nativo. Solo C++ está implementado; E Language y Python están previstos. Clang está integrado sin ejecutable externo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-class-templates).

Core v2 admite punteros tipados a funciones libres ordinarias y métodos estáticos definidos: llamadas indirectas, parámetros y resultados de callback, referencias, arrays, campos y almacenamiento global, local estático o miembro estático ordinario con inicialización constante. Captura el destino antes de evaluar argumentos. Solo admite la ABI predeterminada y firmas escalares o por referencia compatibles. Objetos por valor, punteros a miembros no estáticos, variádicas, lambdas y callbacks entre unidades siguen pendientes. C++/STL completo no está terminado; la validación nativa requiere CI. Solo C++ está implementado; E Language y Python están previstos. Clang está integrado sin ejecutable externo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-function-pointers).

Core v2 también admite punteros a instancias concretas compatibles de plantillas de funciones libres y miembros estáticos, con argumentos explícitos o deducción desde el tipo de puntero esperado. Conserva las comprobaciones del código fuente, la definición y la ABI. Las llamadas directas e indirectas comparten la función y su almacenamiento local estático; las instancias diferentes permanecen separadas. C++/STL completo sigue pendiente y la validación nativa requiere CI. Solo C++ está implementado; E Language y Python están previstos. Las bibliotecas Clang están integradas sin ejecutable externo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-function-template-pointers).

Core v2 también admite callbacks con inicialización constante o nula en plantillas de variables de espacio de nombres y miembros estáticos. Las instancias equivalentes comparten almacenamiento; las distintas son independientes aunque apunten a la misma función. Conserva las comprobaciones de auto escrito directamente; los argumentos plantilla de valor puntero siguen excluidos. C++/STL completo sigue pendiente y la validación nativa requiere CI. Entrada C++ únicamente; E Language y Python están previstos. Clang está integrado sin ejecutable externo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#callback-variable-templates).

Core v2 admite registros anidados con nombre y sin otras formas de plantillas de clase, incluidos tipos privados o protegidos expuestos mediante alias o fábricas permitidos. Cada objeto conserva su almacenamiento y receptor; las referencias explícitas al objeto exterior, la identidad de tipo por ámbito, copias y movimientos de miembros y arrays, destrucción e iteradores anidados mantienen su semántica habitual. Las dependencias por valor se emiten primero. Los registros anidados anónimos, las otras formas de plantillas de clase, la herencia y la STL completa siguen pendientes; la validación nativa requiere el CI de la revisión que lo implementa.

Core v2 admite miembros estáticos definidos de tipo entero, booleano o enumeración, con definiciones inline/constexpr o fuera de la clase e inicialización constante o a cero. Todas las instancias comparten el mismo almacenamiento tipado. Se conservan los efectos del receptor y la destrucción de temporales; las referencias a miembros estáticos sobreviven al receptor temporal. Los miembros const no inline de tipo entero, booleano o enumeración, con inicialización constante verificada dentro de la clase, también permiten leer su valor o descartar el resultado sin una definición separada. No se crea ningún objeto global; obtener su dirección o vincular una referencia al miembro sigue exigiendo una definición en la misma unidad fuente. otros tipos estáticos y la STL completa siguen pendientes; la validación nativa requiere el CI de la revisión correspondiente.

Core v2 conserva la inicialización constante y la primera inicialización sincronizada de objetos y referencias locales estáticos. Los inicializadores pueden usar parámetros, variables automáticas y this. Una guarda de 32 bits sin bloqueo publica la construcción tras limpiar los temporales; los objetos constantes con destrucción la usan solo para registrar. Las referencias mantienen alias y temporales admitidos en almacenamiento permanente. La destrucción sigue el contrato anterior; TLS y declaraciones estáticas en constexpr siguen excluidos. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-initialization) [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-temporaries)

Core v2 admite miembros de referencia a objetos, incluidos arrays, punteros y punteros a función. La construcción guarda el enlace; el acceso y la copia o movimiento conservan los alias. Los contenedores const mantienen referentes modificables. La inicialización de agregados conserva las vidas extendidas y el orden de destrucción, incluidos grupos estáticos locales sincronizados y temporales distintos para rellenos de arrays en ejecución. Los elementos implícitos que materializan temporales tienen identidades semánticas distintas dentro de los límites de recursos y conservan los límites de limpieza de los argumentos temporales del constructor predeterminado que inicializa directamente el elemento. C++/STL completo sigue pendiente; la validación nativa requiere la CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 admite espacios de nombres inline, alias, directivas using y declaraciones using resueltas en espacios de nombres y bloques. Las funciones, variables, tipos y enumeradores sin ámbito importados conservan su identidad, las reglas de búsqueda, los controles de acceso y la vida de los objetos. Los espacios inline conservan la búsqueda combinada con el padre y la búsqueda dependiente de argumentos en ambas direcciones, incluso al anidarlos o reabrirlos. La importación de miembros de clase, los constructores heredados, las otras formas de plantillas de clase y las importaciones de enumeradores o sintaxis inline anidada explícita de C++20 quedan fuera de esta etapa; C++/STL completo sigue pendiente.

Core v2 admite if constexpr C++17 en funciones ordinarias e instancias concretas de plantillas de función admitidas. Solo se emite la rama elegida, conservando almacenamiento de inicialización y destrucción por ámbito. En funciones ordinarias se revisan ambas ramas; Clang no instancia la rama dependiente descartada. Se mantienen los requisitos de definición de funciones y globales ordinarias. C++23 if consteval sigue excluido.

Core v2 admite plantillas de funciones libres con hasta 64 parámetros, combinando tipos compatibles con valores enteros, booleanos y enumerados. Deducción, instanciación y especialización explícitas, tipos predeterminados, argumentos predeterminados resueltos, recursión e importaciones producen funciones tipadas ordinarias. Cada instancia conserva tipos locales y almacenamiento estático escalar propios, incluso entre plantillas primarias con firmas idénticas. Los cuerpos genéricos y valores predeterminados diferidos se revisan al instanciarse; se revisa todo cuerpo materializado. Las llamadas con solo firma dentro de sizeof/noexcept aún exigen definición. Formas dependientes no admitidas de plantillas de clases amigas, cabeceras estándar y STL completo siguen pendientes. La validación nativa requiere el CI de la revisión correspondiente.

Se admiten argumentos auto escalares de C++17, parámetros escalares de tipo dependiente, deducción del tamaño de arrays y recursión constexpr finita. Los valores se convierten en constantes tipadas sin añadir parámetros de ejecución. Los argumentos equivalentes comparten instancia; los valores o tipos deducidos distintos conservan almacenamiento estático separado. Las expresiones escritas y los tipos de parámetros se comprueban antes de reducirlos a constantes. Los argumentos de puntero, referencia o valor de clase siguen fuera de este incremento.

Core v2 admite instancias concretas de plantillas de clases de disposición estándar en espacios de nombres, con hasta 64 parámetros de tipo o valor escalar. Los tipos predeterminados, instanciaciones/especializaciones explícitas, alias, arrays fijos e inicializadores de campos seleccionados generan registros tipados ordinarios. Los argumentos equivalentes comparten tipo; valores, tipos y plantillas primarias distintos conservan identidades separadas. Se aplican las reglas existentes de copia, referencia, retorno y destrucción de miembros. Los tipos, tipos predeterminados y expresiones escritos se comprueban antes de eliminarlos. C++/STL completo continúa pendiente; los resultados nativos requieren CI de la revisión implementada.

Estas plantillas de clases de disposición estándar también admiten funciones miembro ordinarias con nombre: sobrecargas const y calificadas por referencia, fábricas estáticas, recorridos mediante miembros y variables locales estáticas escalares por instancia. Los cuerpos genéricos y argumentos predeterminados sin usar se instancian cuando se necesitan; las llamadas seleccionadas y todos los cuerpos instanciados pasan los controles existentes de tipos, definiciones y duración de vida. También se comprueban los tipos de parámetros de plantilla externos escritos en las definiciones fuera de la clase. Los métodos usan la ABI ordinaria del receptor y del resultado, sin parámetros de plantilla en tiempo de ejecución.

Estas plantillas admiten constructores definidos por el usuario: predeterminados, con parámetros, explícitos, de conversión, copia y movimiento, además de definiciones fuera de clase e instanciaciones/especializaciones explícitas. Los cuerpos, inicializadores y argumentos predeterminados sin usar se mantienen diferidos; toda construcción seleccionada exige una definición comprobada. Los argumentos predeterminados de copia solo se evalúan si se omiten. La inicialización escribe en el destino según el orden de declaración de los miembros y conserva la duración de temporales, campos y arrays. Los constructores heredados siguen excluidos. La validación nativa requiere CI de la revisión implementada.

Estas plantillas también admiten destructores no virtuales definidos por el usuario, definiciones fuera de clase e instanciaciones/especializaciones explícitas. Se exige una definición cuando el frontend integrado determina su uso, incluso al devolver un objeto directamente. Las consultas de tipo y expresiones no evaluadas mantienen diferidos los cuerpos sin usar y comprueban las especificaciones de excepción seleccionadas. El cuerpo se ejecuta antes de destruir miembros y elementos de arrays en orden inverso, con almacenamiento receptor explícito. Se conservan las reglas de temporales, referencias que prolongan la vida y parámetros por valor. Las llamadas explícitas al destructor y C++/STL completo siguen pendientes; la validación nativa requiere CI de la revisión implementada.

Estas plantillas admiten constructores predeterminados, de copia y movimiento, asignaciones de copia y movimiento y destructores no virtuales definidos con `= default`, dentro o fuera de la clase. Las operaciones seleccionadas conservan el orden de miembros, los almacenamientos de origen y destino, el resultado por referencia de la asignación y la destrucción inversa. La inicialización por valor distingue la primera declaración de la definición predeterminada fuera de clase. Los cuerpos sin usar se mantienen diferidos; las consultas comprueban las especificaciones de excepción escritas y resueltas. Las operaciones no triviales usadas requieren definiciones generadas pertenecientes al código de entrada. Una especialización explícita no hereda el defaulting de la plantilla primaria. C++17/STL completo sigue pendiente; la validación nativa requiere CI de la revisión implementada.

Estas plantillas también admiten operadores y funciones de conversión definidos por el usuario: asignación de copia/movimiento, desreferencia, incremento, subíndice, flecha, objetos invocables y conversión bool explícita. Las sobrecargas seleccionadas conservan el orden C++17, los alias, el almacenamiento final y los tiempos de vida. Los cuerpos y argumentos predeterminados sin usar siguen diferidos; las declaraciones seleccionadas y todos los cuerpos instanciados mantienen las comprobaciones de código y definición. Las definiciones externas, instanciaciones/especializaciones explícitas y variables locales estáticas por instancia usan funciones tipadas sin parámetros de plantilla en ejecución. Asignación de memoria, cabeceras estándar y C++17/STL completo siguen pendientes; los resultados nativos requieren CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-operators-and-conversions).

Las clases locales instanciadas en plantillas admitidas de funciones libres o miembros conservan métodos, construcción/destrucción, operadores, conversiones y operaciones defaulted seleccionadas. Se comprueban origen, tipos, almacenamiento del receptor y variables estáticas por instancia; se inspeccionan todos los cuerpos instanciados. Cada instanciación explícita conserva argumentos, tipos, nombres de conversión, calificadores y comprobaciones de atributos, incluso con extern o repeticiones sin efecto. Reutilizar una declaración canónica no oculta código no admitido. Los resultados nativos requieren el CI correspondiente; C++17/STL completo sigue pendiente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-local-classes-and-written-directives).

Core v2 también admite plantillas de funciones operadoras ordinarias en espacios de nombres. Aritmética, comparaciones, referencias, operadores de iteradores y retornos de registros conservan la sobrecarga elegida, el orden C++17, el almacenamiento real y la destrucción de temporales. ADL, llamadas explícitas, especializaciones e instanciaciones usan las funciones tipadas existentes, sin parámetros de plantilla en ejecución. Los cuerpos no usados siguen diferidos; se comprueban el código seleccionado, todos los cuerpos instanciados y cada directiva escrita. Formas dependientes no admitidas de plantillas de clases amigas, asignación de memoria, cabeceras estándar y C++17/STL completo siguen pendientes; la validación nativa requiere el CI correspondiente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-operator-function-templates).

Estas plantillas de clases también admiten miembros estáticos enteros, booleanos y enumerados con inicialización cero o constante, incluidos inline/constexpr y definiciones fuera de la clase. Argumentos equivalentes comparten almacenamiento; instancias distintas conservan objetos separados. Los valores const no inline comprobados no crean almacenamiento, pero las direcciones y referencias evaluadas requieren una definición. Los inicializadores no usados siguen diferidos; se comprueban los instanciados y cada directiva explícita escrita. Se conservan los efectos del receptor y la destrucción de temporales. otros tipos estáticos, C++17/STL completo siguen pendientes; la validación nativa requiere el CI correspondiente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-scalar-static-data).

Core v2 admite valores predeterminados de parámetros escalares no tipo en las plantillas de funciones, operadores y clases compatibles. Parámetros anteriores, auto escalar, valores heredados y conversiones constexpr comprobadas siguen a Clang. Argumentos omitidos y explícitos equivalentes comparten funciones, registros y objetos estáticos. Se comprueba el código no dependiente; los valores dependientes no usados siguen diferidos. Las conversiones correctas conservan el código original y convertido; los fallos permiten elegir otra sobrecarga normalmente. C++17/STL completo sigue pendiente; la validación nativa requiere el CI correspondiente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#scalar-template-parameter-defaults).

Las conversiones con retorno deducido conservan las referencias y const, incluidos decltype(auto) y el colapso de referencias auto&&. El frontend integrado deduce los tipos necesarios para seleccionar candidatos sin instanciar cuerpos de plantillas ajenos que devuelven valores. El soporte completo de C++17/STL sigue pendiente; la validación nativa requiere la CI de esta implementación. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deduced-reference-conversions). Los argumentos de referencia nuevos deducen los resultados de conversión necesarios antes de comprobar la compatibilidad. El filtro de enlace directo no exime a un candidato de deducir el tipo de retorno necesario para una inicialización indirecta posterior; un cuerpo auto& competidor no válido conserva el diagnóstico habitual de C++.

Core v2 admite paquetes concretos de tipos y valores escalares en plantillas permitidas de funciones, operadores y clases de espacios de nombres. El límite es 64 parámetros por plantilla primaria y 64 elementos por paquete. Los parámetros expandidos conservan almacenamiento separado; sizeof... genera un contador tipado. Las expresiones de plegado resueltas conservan orden, cortocircuito y limpieza. Un contador dependiente de la misma lista puede seguir como metadato en el tipo escrito de un parámetro. Los patrones de plegados vacíos no se instancian; se comprueban semillas y elementos materializados. C++17/STL completo sigue pendiente; los resultados nativos requieren CI de la implementación. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-parameter-packs).

Core v2 admite plantillas de alias en espacios de nombres propios para tipos escalares, void, punteros, referencias, arreglos fijos y clases permitidas, con argumentos predeterminados de tipo o escalares y paquetes concretos. Los alias comparten tipos canónicos y almacenamiento estático. Los argumentos escritos, valores predeterminados seleccionados y tipos fuente sustituidos se revisan al usarse; los candidatos no elegidos y cuerpos dependientes sin instanciar siguen diferidos. Los argumentos explícitos conservan el contexto del llamador, incluso en recursión y expresiones this. Se mantienen límites de 64 parámetros, elementos por paquete y niveles, además del presupuesto común. C++17/STL completo sigue pendiente; la validación nativa depende del CI de la revisión. Solo está implementada la entrada C++; E Language, Python y otros frontends están previstos. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-alias-templates).
Los argumentos de plantilla no tipo seleccionados también conservan el código fuente de su tipo de parámetro, incluidos los argumentos explícitos de plantillas de función y los paquetes vacíos deducidos; se sigue comprobando el código no admitido que un alias elimina al simplificarse.

Los tipos predeterminados no dependientes se comprueban incluso en plantillas sin usar. Las llamadas cualificadas a plantillas de función y las definiciones concretas de funciones de conversión conservan su código escrito exacto, incluidos los alias en definiciones fuera de la clase. Una conversión lvalue puede crear un temporal ampliado para un parámetro de referencia const; los parámetros de referencia rvalue mantienen la restricción de sobrecarga de C++.
Core v2 también admite especializaciones parciales de plantillas de clase en espacios de nombres del proyecto. El patrón seleccionado y sus parámetros deducidos se verifican por separado de los argumentos de la plantilla primaria. Los alias comparten el mismo tipo concreto y almacenamiento estático; los miembros admitidos conservan llamadas, referencias y duración de vida. El límite sigue siendo de 64 parámetros y 64 elementos por paquete. C++17/STL completo sigue pendiente; la validación nativa corresponde a la CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-partial-specializations).

Core v2 admite el tipo de valor independiente `decltype(nullptr)` en alias, referencias, campos/arrays, firmas de callbacks y almacenamiento escalar estático o de plantillas de variables inicializado a cero o con constantes. Las conversiones a punteros conservan los efectos de las expresiones y la destrucción de temporales; cada objeto null mantiene su propia dirección. Los argumentos de plantilla `nullptr_t` admiten valores predeterminados, paquetes y contextos fuente ya aceptados, con sus comprobaciones e identidad distinta del cero entero y false booleano. Los argumentos null de tipo puntero y las cabeceras estándar siguen sin admitirse. La validación nativa requiere la CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#null-pointer-values).

Core v2 admite constructores delegados en clases ordinarias y genéricas compatibles, incluidas cadenas y plantillas de constructores seleccionadas. El destino y el cuerpo que delega usan el mismo objeto final; los miembros se inicializan una vez y los argumentos temporales se destruyen antes del cuerpo que delega. Se mantienen las comprobaciones de selección y definición. La validación nativa requiere la CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#delegating-constructors).

Core v2 admite miembros const de tipos escalares, registros, punteros, callbacks y arrays compatibles, también en clases genéricas y anidadas. Conserva el almacenamiento final, las copias/movimientos seleccionados, las direcciones distintas y las referencias/punteros const. Clang rechaza escrituras inválidas y asignaciones eliminadas. Permite clases con claves constantes definidas en el código; las cabeceras estándar y el STL completo siguen pendientes. La validación nativa requiere la CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#const-data-members).

Core v2 admite funciones eliminadas y miembros especiales predeterminados definidos como eliminados, incluidas las formas compatibles de plantillas y amigos. Clang conserva la resolución de sobrecargas y los diagnósticos de acceso; estas declaraciones no generan definiciones ejecutables. Los objetos solo movibles, las copias alternativas seleccionadas y la elisión garantizada de C++17 mantienen la gestión de duración. Se requiere CI de la revisión; C++/STL completo sigue pendiente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deleted-function-declarations).

Core v2 admite IEEE `float` y `double`, aritmética y conversiones, referencias, campos y matrices, callbacks y objetos escalares estáticos inicializados a cero o con constantes. Los bits exactos de los literales y la comprobación independiente de la disposición conservan la representación. La ejecución requiere el entorno flotante predeterminado, sin matemática rápida, precisión adicional ni contracción implícita. Se requiere CI de la revisión; `long double`, cabeceras estándar y C++/STL completo siguen pendientes. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#binary-floating-point-values).

Core v2 admite literales estrechos, UTF-8, UTF-16, UTF-32 y anchos con unidades exactas y almacenamiento estático de solo lectura, inicialización de matrices de caracteres con ceros y matrices constantes de espacio de nombres. Alias, copias y duración usan las operaciones tipadas existentes. Se requiere CI de la revisión; las cabeceras estándar y las asignaciones y operaciones de `std::string` siguen pendientes. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#string-literals-and-constant-arrays).

Core v2 admite inicialización a cero o constante de arrays mutables y constantes en espacios de nombres, variables estáticas locales y miembros estáticos de clase, incluidas las instancias de plantilla admitidas. Cada array conserva su estado y dirección entre llamadas; las instancias distintas tienen almacenamiento separado. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#fixed-array-static-storage).

Core v2 admite inicialización a cero o constante de punteros estáticos a objetos globales, literales, elementos de array y campos, incluidos los punteros válidos justo después del final. El almacenamiento de espacios de nombres, funciones, clases y plantillas conserva identidad, mutabilidad y acceso const. Las referencias a direcciones posteriores se emiten como constantes C23 sin inicialización en ejecución. Las referencias estáticas, la asignación de memoria y la STL completa siguen en desarrollo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-object-pointer-storage).

Las referencias estáticas pueden enlazar objetos estáticos existentes, subobjetos de arreglos o registros y cadenas, incluidas referencias estáticas locales, de clase e instancias de plantillas admitidas. Las lecturas y escrituras conservan el objeto original y los permisos const. Consulte el [contrato de referencias estáticas](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-bindings).

Core v2 admite temporales escalares, arrays y registros con inicialización constante cuya vida se extiende mediante referencias estáticas. Conserva objetos completos, alias de subobjetos, punteros propios, const e identidad de instancias. La validación nativa requiere CI de esta implementación. En C++17, la inicialización constante de estos temporales sigue exigiendo destrucción trivial; los temporales estáticos no triviales usan inicialización dinámica y registro. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-temporary-lifetime-extension).

Los registros estáticos admiten inicialización constante y estado mutable compartido, también en variables estáticas locales, clases e instancias de plantillas admitidas. Las direcciones internas, la construcción constexpr y la inicialización estática a cero conservan la identidad del objeto. Consulte el [contrato de registros estáticos](../../utils/translate-frontends/docs/cpp-core-v2.md#static-record-objects).


Core v2 admite cadenas verificadas de bases vacías triviales, incluidas las plantillas compatibles, métodos heredados, conversiones y valores estáticos. Cada base ocupa un primer miembro C real con disposición comprobada de forma independiente. Se conservan punteros nulos, identidad de referencias, rutas constantes y efectos. Los ciclos de vida no triviales y las cabeceras estándar siguen pendientes; la validación nativa requiere CI de la revisión implementada. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#trivial-empty-base-chains).
## Instalación y traducción escalar

Use una instalación normal de NeverC con sus recursos estándar. El frontend C++ y las cabeceras SDK aprobadas están integrados; no hace falta instalar Clang por separado. Consulte las [notas de compilación del frontend](../../utils/translate-frontends/cpp/README.md).

```sh
neverc translate --from cpp input.cpp -o output.nc
neverc output.nc -c -o output.o
```

El perfil `cpp-core-v1` acepta un único archivo C++17 autónomo sin includes. Admite `int`, `unsigned int`, `bool`, `void`, tipos agregados triviales, funciones no miembro, espacios de nombres, sobrecargas y el flujo de control documentado. Se comprueban todas las declaraciones de entrada, incluido el código no utilizado.

Seleccione `--profile cpp-core-v2` para añadir alias `typedef`/`using` comprobados, enumeraciones con tipos subyacentes enteros admitidos, `static_assert`, punteros a objetos y referencias a lvalues dentro de un alcance limitado. Los parámetros y resultados por referencia conservan los alias; se admiten punteros nulos y calificaciones `const` anidadas. Se mantienen las restricciones de un solo archivo fuente y sin includes. Consulte el [contrato de core v2](../../utils/translate-frontends/docs/cpp-core-v2.md).

Core v2 añade arrays locales de tamaño fijo y campos de array, indexación multidimensional y punteros o referencias a arrays. La inicialización parcial inicializa a cero los elementos escalares omitidos; los registros siguen su inicialización seleccionada y conserva el orden y los alias. El tamaño y la expansión de la inicialización tienen límites. Los arrays globales y los de longitud variable siguen sin admitirse.

Core v2 admite `switch`/`case`/`default`, con sentencias de inicialización de C++17, continuación entre casos y anotaciones `[[fallthrough]]` validadas. El selector se evalúa una vez y los switches y bucles anidados conservan los destinos de `break`/`continue`. Se rechazan los rangos case de GNU y otros atributos de sentencia.

Core v2 admite enteros con y sin signo de 8, 16, 32 y 64 bits, tipos y literales de caracteres, y consultas constantes `sizeof`/`alignof`. Las promociones y la resolución de sobrecargas preceden a la normalización; `long`, `wchar_t` y el tipo de tamaño siguen el destino. Los enums también admiten tipos subyacentes más estrechos y más anchos. La biblioteca estándar sigue en desarrollo.

Core v2 también admite desplazamientos y diferencias de punteros a objetos, incrementos/decrementos y asignaciones compuestas, incluidos recorridos de arrays y pasos multidimensionales. Las funciones auxiliares generadas conservan las reglas de C++17 para un puntero nulo más o menos cero y la diferencia entre dos punteros nulos. El orden entre punteros a objetos usa direcciones sin signo verificadas de forma independiente en destinos nativos x86/AArch64; los resultados nativos requieren CI de la revisión que lo implementa. Las conversiones puntero/entero en el código fuente y la STL completa siguen pendientes.

Core v2 contrasta los tamaños y las alineaciones ABI de los tipos de origen con el modelo de destino de NeverC, incluidos los tamaños de estructuras y los desplazamientos de campos. Las aserciones estáticas del código generado vuelven a comprobar la disposición al compilar, y el manifiesto registra estos datos.

Core v2 admite funciones miembro con nombre no virtuales de los tipos de registro aceptados, incluidas las sobrecargas const, los métodos cualificados para lvalue, los métodos estáticos y `this`. Las llamadas conservan la identidad del objeto original y evalúan el receptor antes que los argumentos.

Core v2 admite constructores ordinarios definidos por el usuario para registros con disposición estándar y operaciones de copia admitidas. Objetos locales, campos y elementos de array se construyen en su almacenamiento final; los campos se inicializan en orden de declaración. Sigue excluido el desenrollado de pila por excepciones.

Core v2 crea un objeto independiente para cada parámetro de registro pasado por valor y escribe el resultado en el destino del llamador. Constructores y métodos siguen las mismas reglas; se conservan las copias necesarias y los alias de las referencias. Para tipos triviales que cumplen los requisitos, otras implementaciones C++17 pueden añadir copias de argumentos o resultados.

Core v2 admite destructores ordinarios definidos por el usuario y destrucción implícita de miembros en salidas normales. Objetos locales, campos y elementos de array se destruyen en orden inverso; los temporales al finalizar la expresión completa, tras guardar los valores necesarios. Retornos, ramas, bucles, break y continue realizan la limpieza correspondiente. Los parámetros por valor se destruyen al salir de la función llamada; los objetos devueltos pertenecen al llamador. El desenrollado por excepciones sigue excluido.

Core v2 ejecuta constructores de copia y operadores de asignación por copia ordinarios definidos por el usuario, con un parámetro de origen `R&` o `const R&`. La copia usa el destino real y conserva los efectos secundarios y la referencia devuelta. La sintaxis de asignación evalúa primero el operando derecho; una llamada explícita a `operator=` evalúa primero el receptor.

Core v2 también admite constructores predeterminados generados o declarados default y destructores default, incluidos miembros de registros y arrays anidados. La construcción usa el destino real y el orden de declaración; la inicialización por valor solo pone a cero cuando C++ lo exige. Se conservan las reglas distintas del default definido fuera de la clase. Los constructores no usados o presentes solo en expresiones no evaluadas no requieren un cuerpo artificial.

Core v2 admite constructores de copia implícitos y explícitamente default, incluidos registros anidados y arrays multidimensionales. Las copias de miembros seleccionadas usan el destino real en orden de declaración y de elementos; la dirección del array fuente se evalúa una vez. Las copias triviales conservan los punteros almacenados. Los parámetros por valor y retornos desde objetos fuente mantienen los efectos de copia; transferir directamente una prvalue no añade copias.

Core v2 también admite la asignación por copia implícita o explícitamente default. Miembros y elementos de arrays conservan sus operaciones seleccionadas y su orden; las copias triviales generadas se convierten en asignaciones tipadas sin llamadas externas de copia de memoria. La asignación trivial conserva los punteros y devuelve el receptor real. La sintaxis de operador captura primero la referencia fuente, y la llamada explícita a miembro primero el receptor; los valores fuente se leen después de esos efectos. La asignación no construye ni destruye objetos adicionales.

Core v2 admite inicializadores predeterminados para los campos permitidos, con acceso a miembros anteriores, llamadas ordinarias, registros anidados y arrays. El valor seleccionado usa su objeto real como `this`; las cláusulas explícitas de agregado conservan el `this` del llamador. La inicialización explícita sustituye el valor predeterminado del miembro. La copia y asignación implícitas/default no lo repiten; un constructor de copia del usuario puede seleccionarlo para miembros omitidos. La inicialización de miembros del constructor y la de agregados conservan sus respectivos límites de destrucción de temporales. Plantillas y STL completo siguen en desarrollo.

Core v2 admite referencias rvalue a objetos ya vivos: alias de escalares, punteros, registros y arrays, parámetros y retornos por referencia, xvalues condicionales y métodos ordinarios cualificados `&&`. `static_cast<R&&>(live)` conserva el mismo objeto; una variable de referencia rvalue con nombre sigue siendo lvalue. Sobrecargas y copias existentes siguen la selección de Clang. Las referencias no crean responsables adicionales de destrucción.

Core v2 ejecuta constructores y asignaciones de movimiento ordinarios definidos por el usuario desde una fuente viva `R&&` o `const R&&`. La construcción usa el almacenamiento final; la asignación conserva las modificaciones de la fuente, el orden de evaluación y el alias `R&` devuelto, incluso con receptor cualificado `&`/`&&`. Las referencias rvalue con nombre siguen eligiendo sobrecargas lvalue y los constructores explícitos conservan sus reglas de inicialización. Mover no termina la vida de la fuente: fuente y destino se destruyen normalmente.

Core v2 también admite construcción y asignación de movimiento implícitas o explícitamente default, con registros anidados y arrays multidimensionales. Los miembros ejecutan la copia o movimiento elegido por C++ en orden de declaración y de elementos. Los movimientos generados no repiten inicializadores predeterminados; las operaciones triviales conservan los valores de los punteros. Las fuentes se destruyen normalmente y las operaciones generadas sin uso o triviales no necesitan un cuerpo inventado.

Core v2 admite declaraciones estándar resueltas `noexcept`, `noexcept(true/false)` y `throw()` de C++17, además de consultas constantes `noexcept(expression)`. Las consultas respetan las especificaciones de las funciones y destructores seleccionados sin ejecutar sus operandos. Todos los operandos y especificaciones escritas se inspeccionan, incluso en código no usado. Lanzamiento y captura de excepciones, desenrollado de pila, plantillas y STL completo siguen en desarrollo.

Core v2 admite operadores ordinarios sobrecargados, miembros o libres: aritmética, comparaciones, subíndices, desreferencia, incrementos, objetos función y firmas generales de asignación. Conserva funciones seleccionadas, alias de referencias y objetos resultado. La notación de operador respeta el orden de C++17; los operadores lógicos sobrecargados evalúan ambos operandos. Los operadores libres de asignación inicializan los parámetros de derecha a izquierda y los destruyen en orden inverso. Plantillas, asignación de memoria y STL completo siguen en desarrollo.

Core v2 también admite funciones ordinarias de conversión en objetos vivos: conversiones enteras, de enumeración y punteros implícitas o explícitas, `bool` explícito en condiciones y resultados por referencia u objeto. Cada conversión llama una vez al miembro seleccionado. Las referencias conservan sus alias; los prvalues de objeto inicializan directamente el destino real. Convertir una referencia en valor conserva la copia o el movimiento seleccionado. Los calificadores const y de referencia, constexpr y noexcept siguen C++17.

Core v2 admite receptores temporales y argumentos temporales escalares o de registro por referencia hasta el final de la expresión completa, en constructores, métodos, operadores y conversiones. Cada evaluación tiene almacenamiento real; los resultados objeto conservan su destino final y las referencias sus alias. Tras la llamada y la destrucción de parámetros, los temporales se destruyen en orden inverso de construcción, también en condiciones y bucles. Se admiten subobjetos array de registros temporales.

Core v2 amplía la vida de temporales mediante referencias locales automáticas ordinarias, incluidos `const T& r{T{...}}` y `T&& r = T{...}`. Escalares, enumeraciones, punteros y registros tienen almacenamiento real. Las referencias a miembros o elementos de array conservan el registro completo cuando C++ permite la extensión. La inicialización con llaves, también tras un signo igual, mantiene el mismo objeto. La destrucción sigue el ámbito de la referencia, incluidas condiciones, bucles y salidas anticipadas; los demás temporales del inicializador terminan con su expresión completa. Los alias no añaden propietarios y las copias o movimientos posteriores conservan las operaciones seleccionadas. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 también admite arrays temporales independientes de tamaño fijo en llamadas, conversión a puntero, indexación, expresiones descartadas y referencias locales automáticas. Cada array tiene un destino real; valores escalares, constructores e inicializadores predeterminados compartidos inicializan directamente cada elemento en orden, sin propietarios adicionales. Cuando C++ extiende la vida, las referencias a filas o elementos multidimensionales conservan el array completo. Los elementos se destruyen en orden inverso al terminar la expresión completa o el ámbito de la referencia. Se mantienen las direcciones tipadas y los límites existentes de tamaño, almacenamiento y expansión. C++/STL completo sigue en desarrollo.

Core v2 admite clases vacías de disposición estándar y objetos de función o conversión sin estado. Los objetos vacíos conservan el tamaño y la alineación de un byte de C++, almacenamiento separado cuando corresponde, constructores y operadores seleccionados y destrucción normal. Las copias triviales siguen evaluando sus operandos; los elementos vacíos de arrays y registros contenedores cuentan para el límite de almacenamiento. El NC generado usa un byte interno y la lista de campos fuente permanece vacía. La herencia, las plantillas y la STL completa siguen en desarrollo.

Core v2 admite conversiones a void, `void()` y `void{}`, con alias void compatibles, llamadas, retornos, expresiones coma y ramas condicionales. Descartar un lvalue no volatile conserva los efectos del receptor y del índice sin leer su valor almacenado. Los temporales siguen construyéndose y destruyéndose al final de la expresión completa original. No se generan variables ni valores void. Los operandos constantes y noexcept siguen verificándose. Los objetos volatile, tipos no admitidos, plantillas y STL completa quedan fuera de esta etapa.

Core v2 admite argumentos predeterminados en funciones, métodos, operadores de llamada y constructores de usuario compatibles, incluidos parámetros finales de constructores de copia y movimiento. Los nombres se resuelven en la declaración y las expresiones se evalúan en cada llamada que omite el argumento. Se conservan la identidad y la vida útil de referencias y valores. Los elementos de array sin inicializador y las copias generadas destruyen los temporales predeterminados antes del siguiente elemento; las cláusulas explícitas conservan la expresión completa exterior. También se comprueban los valores no usados o sustituidos. Las plantillas y la STL completa siguen pendientes.

## Proyectos de varios archivos

Seleccione explícitamente las unidades de traducción en una base de datos de compilación y especifique el directorio raíz del proyecto. El frontend integrado analiza cada unidad por separado; la fusión verifica definiciones, enlace, tipos compartidos y el cumplimiento de la regla de una sola definición (ODR) mediante comprobaciones conservadoras.

```sh
neverc translate --from cpp --profile cpp-project-v1 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/a.cpp src/b.cpp --out-dir generated
```

La salida del proyecto es `translated.nc` más `translated.h`. Solo se admiten cabeceras del proyecto situadas dentro de esa raíz. Si un archivo tiene varias configuraciones, seleccione su índice de base cero con `--compdb-entry src/a.cpp=4`. Los comandos de compilación almacenados se interpretan como datos y nunca se ejecutan.

## Matemáticas limitadas de doble precisión

`cpp-math-v1` añade a los proyectos `double`, las conversiones/comparaciones documentadas y exactamente `std::fabs(double)` y `std::floor(double)`. Utiliza las cabeceras integradas de Clang 20.1.8 / libc++ 200100 / macOS 15.5 y requiere un destino macOS 15.0 explícito (arm64 o x86_64). La aritmética general de coma flotante sigue excluida. Las excepciones deben estar enmascaradas y la conversión de subnormales a cero desactivada; se prueban los cuatro modos estándar de redondeo.

```sh
neverc translate --from cpp --profile cpp-math-v1 \
  --target arm64-apple-macosx15.0.0 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/math.cpp --out-dir generated-math
```

La traducción matemática también verifica las cabeceras NeverC instaladas, la identidad de las implementaciones integradas y un enlace real. Los módulos generados usan el runtime NeverC. Con `-fno-builtin-std`, la traducción solo falla antes de escribir los archivos si el código final requiere las correspondencias `fabs`/`floor`. El código matemático que no las necesita puede seguir traduciéndose.

## Validación y salida

Use `--check` en lugar de una opción de salida para realizar el mismo análisis, generación y validación de sintaxis y objetos sin conservar los archivos generados. `--report PATH` escribe diagnósticos estructurados. La traducción nunca ejecuta el programa fuente.

Las salidas y los archivos auxiliares nunca se sobrescriben. `--out-dir` requiere un directorio nuevo con un padre existente; `-o` requiere una ruta `.nc` nueva. El manifiesto registra requisitos del destino, hashes de entrada y salida y el procedimiento de compilación; el mapa fuente relaciona las líneas generadas con las ubicaciones originales.

Los resultados de CI, las verificaciones de ejecución e instalación y las pruebas omitidas se registran por plataforma. macOS arm64 nativo y macOS x86_64 mediante Rosetta siguen siendo entornos de validación distintos. El alcance anunciado no incluye C++/STL completo, excepciones, plantillas, cadenas ni `std::vector`. Consulte la [matriz de compatibilidad](../../utils/translate-frontends/docs/support-matrix.md), el [protocolo y las reglas de recuperación](../../utils/translate-frontends/docs/protocol.md) y el [proyecto de ejemplo](../../tests/neverc/Inputs/translate/cpp/project).
