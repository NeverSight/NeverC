**Langues**: [English](../translate.md) | [简体中文](../zh-CN/translate.md) | [繁體中文](../zh-TW/translate.md) | [日本語](../ja/translate.md) | [한국어](../ko/translate.md) | [Français](translate.md) | [Deutsch](../de/translate.md) | [Español](../es/translate.md) | [Italiano](../it/translate.md) | [Русский](../ru/translate.md) | [العربية](../ar/translate.md)

[← Documentation](README.md)

# Traduire C++ vers NeverC

Core v2 prend en charge le transfert sized delete par défaut : un sized delete/delete[] global implicite, sans redéclaration source, peut appeler son opérateur unsized défini dans le source. Les définitions sized explicites restent prioritaires et le cookie du tableau conserve sa disposition originale. Aucun allocateur par défaut ni runtime d’exceptions n’est ajouté ; la validation native exige la CI de cette révision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

Core v2 prend aussi en charge `__array_rank` et `__array_extent` avec des indices entiers constants non négatifs, y compris les indices de templates sur des tableaux fixes. Les deux opérandes restent vérifiés avant réduction. Les types non pris en charge et les indices convertis implicitement depuis une classe sont exclus. Les résultats natifs nécessitent la CI de la révision implémentée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#array-type-queries).

Core v2 ajoute la classification de types intégrée et vérifiée, notamment `__is_integral`, `__is_pointer` et `__is_same`. Les requêtes préservent l’identité des types C++ et vérifient les opérandes écrits avant de produire un booléen. Les types non pris en charge et les autres traits restent limités. Les vérifications O0/O2 et de relocalisation nécessitent la CI de la révision implémentée ; les en-têtes standard et le C++/STL complet restent inachevés. Les requêtes couvrent aussi les agrégats, classes vides, dispositions standard, propriétés trivial, trivially-copyable et POD, le polymorphisme, les classes abstraites et `__is_base_of` pour les types admis. Les classes standard `final` et les requêtes `__is_final`, `__is_literal` et `__has_unique_object_representations` sont également couvertes. Le remplissage source, la représentation des classes vides et les règles de type littéral C++17 restent déterminants ; les autres attributs et méthodes virtuelles conservent leurs restrictions. La destructibilité et la destruction triviale couvrent aussi les classes admises, leurs destructeurs privés ou supprimés et les références, sans instancier les corps de modèles ni résoudre leurs spécifications d’exception différées. Les requêtes concrètes de classification et de tableaux vérifient toutes les dépendances de source des types et valeurs avant la sortie, y compris les bornes d’alias et les constantes mémorisées ou générées, même pour un résultat faux ou un indice hors limites. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#builtin-type-classification).

Core v2 prend en charge les requêtes de construction, affectation, conversion et destruction pour les types non classe admis, les pointeurs, les références et les tableaux de taille fixe. Les expressions retenues des opérations de classe autorisent les opérations triviales implicites et les opérations ordinaires dont les définitions, conversions et dépendances de destruction sont vérifiées. Les requêtes de construction acceptent aussi les arguments par défaut ordinaires non réécrits après vérification du paramètre exact, de son expression et du nettoyage des temporaires, sans exécuter leurs effets. Les valeurs par défaut nothrow exigent aussi des spécifications d’exception déjà résolues pour les types réellement appelés. La destruction nothrow des valeurs de classe et des tableaux fixes conserve le destructeur choisi et le prototype résolu, puis vérifie la destruction implicite, les destructeurs ordinaires explicitement définis par défaut ou la destruction utilisateur ordinaire dont la définition a été contrôlée. Les résultats anticipés liés à la suppression, à l’accès et aux références sont préservés. La construction triviale implicite utilise la même preuve distincte de destruction. Les opérations de modèles déjà instanciées avec leurs définitions inline originales vérifiées sont aussi admises. Les corps non instanciés, les définitions séparées de modèles, les origines copiées de modèles de membres et les dépendances d’exception non résolues demandent encore des preuves de source. Les vérifications O0/O2, de relocalisation et sur huit ABI nécessitent la CI de cette révision ; les en-têtes standard et le support C++/STL complet restent à terminer. Le résultat de l’appel final dans `decltype`, y compris les parenthèses et l’opérande droit de la virgule intégrée, conserve la vérification différée de la destruction ; les arguments et la source de la fonction restent vérifiés. Les expressions d’exception reconstruites conservent leurs deux identités source ; les corps inutilisés des fonctions amies inline référencées restent différés après vérification de leur source exacte et de leur signature concrète. La source des requêtes peut utiliser des opérations triviales ordinaires définies par défaut et vérifiées, ou des opérations générées entièrement vérifiées, avec leurs dépendances d’initialisation, de corps et de disposition mémoire. Les opérations de modèle définies par défaut dans la classe exigent aussi les marqueurs réels, les signatures concrètes entièrement vérifiées et la déclaration originale exacte ; une interrogation de destructeur effectivement parcourue avec les preuves de sélection conservées peut déclencher la première vérification de sa signature concrète ; les corps de modèle inutilisés restent différés. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#non-record-operation-traits).

Core v2 ajoute les appels directs à `malloc/calloc/free` via des déclarations C globales exactes dans le code source. Les allocateurs C++ définis dans le source peuvent utiliser le tas natif et échanger puis libérer la mémoire avec un appelant C indépendant. Les contrôles des tailles, conventions d’appel, sources et IR restent actifs. La validation O0/O2 nécessite la CI de cette implémentation. `realloc`, l’allocation C++ standard avec exceptions, les en-têtes standard et la prise en charge complète de C++/STL restent inachevés. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#native-c-heap-calls).

Core v2 enregistre la destruction des objets statiques admis, tableaux et temporaires à durée de vie prolongée dès la fin de leur construction. Les valeurs constantes sont conservées ; les objets locaux sont enregistrés au premier passage, les autres au démarrage natif. Le CRT préserve l’ordre de sortie et de déchargement des modules. Les tests O0/O2, de déchargement et de premier accès concurrent nécessitent la CI de cette implémentation. TLS, déroulement des exceptions, tas par défaut, en-têtes standard et C++/STL complet restent inachevés ; le chargement manuel/DynCode est exclu. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-destruction).

Core v2 prend désormais en charge l’initialisation dynamique non locale des objets et références admis, y compris les instances de templates matérialisées. Un constructeur natif interne vérifié s’exécute avant main dans l’ordre des définitions, après l’initialisation à zéro ou constante, avec destruction des temporaires ordinaires après chaque initialiseur. Les statiques locales gardent leur initialisation au premier passage. Les tests O0/O2 du programme et du client C séparé nécessitent la CI de cette révision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#nonlocal-dynamic-initialization).

Les longueurs new[] à l’exécution acceptent aussi les allocateurs de classe noexcept définis dans le source, sans arguments placement. Une longueur invalide produit null avant tout appel à l’allocateur ; les éléments valides sont construits en boucle et les temporaires des arguments par défaut sont détruits après chaque élément. Les temporaires du préfixe explicite et de conversion de longueur restent vivants jusqu’à la fin de l’expression complète. Les agrégats répétés peuvent désormais initialiser directement chaque élément sans objet temporaire distinct, avec listes imbriquées et références à des objets existants. Les initialisations nécessitant de tels temporaires, les allocations avec exceptions ou placement à l’exécution, le tas par défaut et C++/STL complet restent inachevés. La vérification native attend la CI de cette version. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#runtime-array-allocation).

Core v2 prend en charge new/delete pour un seul objet via des fonctions d’allocation définies dans le code source et vérifiées, avec surcharges placement de classes et de modèles, identité du stockage et nettoyage des arguments. Destruction explicite et reconstruction placement conservent les obligations de nettoyage automatique. La vérification native exige la CI de cette version. Le runtime du tas par défaut, les longueurs new[] à l’exécution, les exceptions, les en-têtes standard et C++/STL complet restent inachevés. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

La commande expérimentale `neverc translate` produit du code source `.nc` vérifiable avec `cpp-core-v1`, `cpp-core-v2`, `cpp-project-v1` et `cpp-math-v1`.

**Seule la traduction de code C++ est actuellement implémentée.** La prise en charge d’E Language (易语言, `.e`), de Python, Go, Rust, TypeScript et JavaScript est prévue ; leurs traducteurs ne sont pas encore disponibles.

Les déclarations de spécialisation partielle conservent la vérification des arguments primaires, y compris les copies de variables membres dont les arguments internes restent dépendants. Les sources des types de paramètres déjà résolus sont vérifiées même sans sélection de la spécialisation. Les développements de longueur inconnue conservent le préfixe vérifié et le code réel sans inventer de positions. Les spécialisations complètes de variables membres encore dépendantes gardent les mêmes vérifications. C++/STL complet reste inachevé. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#partial-declaration-source-checks).

Conserver la vérification des arguments ne rend pas une spécialisation partielle valide : elle doit rester plus spécialisée et ses paramètres doivent être déductibles. Les motifs de paquets invalides et une instanciation extern après une définition d’instanciation restent des diagnostics C++ (`TR0202`).

Core v2 accepte les classes ordinaires imbriquées nommées dans les classes génériques admises, leurs copies concrètes et leurs spécialisations explicites propres. Ces corps n’ajoutent aucun niveau de paramètres de template. Les corps inutilisés restent différés ; champs instanciés, méthodes sélectionnées, templates membres, durées de vie et stockage statique scalaire conservent leur source et leur propriétaire exacts. Chaque instanciation explicite vérifie son qualificateur écrit, y compris extern et les répétitions sans effet. L’héritage, l’allocation et C++/STL complet restent à réaliser. Seule l’entrée C++ est implémentée ; E Language et Python sont prévus. Les bibliothèques Clang intégrées ne nécessitent aucun exécutable Clang externe. Les résultats natifs exigent la CI de la révision implémentée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-nested-classes-in-generic-owners).

Core v2 prend en charge les templates scalaires de variables membres statiques des enregistrements et instances de classes admis : arguments par défaut, paquets bornés, spécialisations partielles/complètes et définitions externes. Les instances équivalentes partagent le stockage ; des arguments internes ou externes différents gardent des objets distincts. Les valeurs const non inline vérifiées se lisent sans stockage ; les adresses et références évaluées exigent une définition. Les vérifications des déclarations et définitions, les effets du récepteur et la destruction des temporaires sont conservés. Les spécialisations complètes dans une classe externe dépendante conservent les sources des déclarations originales et concrètes. Les arguments, valeurs par défaut et types de paramètres résolus sont vérifiés immédiatement ; les sources dépendantes sont vérifiées après instanciation. Les autres résultats, C++/STL complet restent inachevés. Les bibliothèques Clang sont intégrées, sans Clang externe. Seule l’entrée C++ est implémentée ; E Language, Python et les autres langages restent prévus. La validation native nécessite le CI de l’implémentation. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-variable-templates).

Core v2 prend en charge les templates d’alias membres des records ordinaires, y compris imbriqués, et des instances de classes admis. Les arguments par défaut et paquets bornés peuvent utiliser les paramètres externes de la classe et internes de l’alias ; leurs arguments et types source substitués sont vérifiés séparément. Les alias conservent la sémantique des valeurs, références, tableaux et enregistrements sans objets de template à l’exécution. Les déclarations non dépendantes sont vérifiées même inutilisées ; les types dépendants restent différés jusqu’à la substitution. Les bibliothèques Clang sont intégrées, sans exécutable externe. La validation native nécessite le CI de l’implémentation. C++/STL complet reste inachevé. Seule l’entrée C++ est implémentée ; E Language, Python et les autres langages restent prévus. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-alias-templates).

Core v2 prend en charge les templates concrets de fonctions membres des enregistrements et instances de classes admis : méthodes nommées ou statiques, opérateurs, constructeurs et conversions, avec paquets bornés, valeurs par défaut, définitions externes et spécialisations. Les arguments internes et externes gardent des identités distinctes ; les appels équivalents partagent code et stockage statique. Les appels, valeurs par défaut et corps sélectionnés conservent les vérifications du code source et des durées de vie. La traduction utilise les bibliothèques Clang intégrées sans exécutable Clang externe. Les résultats natifs nécessitent le CI de cette implémentation. L’héritage, l’allocation et C++/STL complet restent inachevés. Seule l’entrée C++ est implémentée ; E Language, Python et les autres langages restent prévus. Les qualificateurs externes concrets des déclarations de templates membres sont vérifiés même sans corps instancié. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-function-templates).

Core v2 prend en charge les modèles de variables dans les espaces de noms à résultat entier, booléen ou enum, avec auto déduit, spécialisations partielles sélectionnées, spécialisations complètes, valeurs par défaut et paquets bornés de types ou de scalaires. Les traits C++17 peuvent alimenter static_assert, if constexpr et les arguments de modèles. Les instances équivalentes partagent un stockage global typé ; les instances distinctes gardent leurs propres valeurs et adresses. Une requête sur un type fixe ne force ni initialisation inutilisée ni allocation de stockage. Les arguments réels, valeurs par défaut sélectionnées, types de déclaration et de définition et initialiseurs instanciés restent vérifiés. les autres types de résultat, C++/STL complet restent inachevés ; la validation native nécessite le CI de la révision qui implémente cette prise en charge.

Un sizeof(T) ou alignof(T) direct dans le type écrit d’un paramètre non-type peut rester une métadonnée de déclaration si T est exactement le paramètre de type de la même liste. Les substitutions sélectionnées exigent toujours des types concrets vérifiés ; les autres requêtes dépendantes ne sont pas couvertes. Une déclaration extern template seule ne fournit pas la définition concrète requise par le contrat actuel de traduction d’une unité fermée.

Core v2 applique les règles standard de C++17 pour les modèles sur toutes les cibles prises en charge, y compris Windows x64 et ARM64. Les définitions sont analysées sans analyse différée de MSVC ; les définitions d’instanciation explicite en double et les spécifications d’exception incompatibles restent des erreurs du langage. Les corps dépendants inutilisés ne sont toujours instanciés qu’au besoin. La prise en charge complète de C++/STL reste inachevée. Les spécialisations explicites de fonctions nécessitent toujours leur propre définition dans l’unité source ; répéter les déclarations ne suffit pas.

Core v2 prend en charge les variables globales modifiables de type entier, booléen ou énumération dans un espace de noms, avec initialisation à zéro ou initialisation constante entièrement vérifiée. Leur stockage et leur adresse persistent entre les appels ; références, pointeurs et arguments par défaut accèdent à la même variable. Une déclaration `extern` doit avoir une définition dans la même unité source. Les globales const restent en lecture seule. Les restrictions propres aux records/tableaux/pointeurs/références globaux, au stockage local aux threads et aux variables locales statiques restent applicables.

Core v2 prend en charge les boucles `for` par plage de C++17 sur les tableaux fixes admis et les plages définies dans le source, avec résolution des appels membres ou ADL à `begin/end`. Les variables par valeur ou référence, les itérateurs de type record et les sentinelles de types distincts conservent les règles ordinaires d’appel et de durée de vie. La plage et `begin/end` sont initialisés une seule fois ; les objets de chaque itération sont détruits avant l’incrément ou la sortie, y compris avec `continue`, `break` et `return`. Les templates de classe, en-têtes standard, conteneurs STL, liaisons structurées et instructions d’initialisation C++20 restent hors de cette extension. La validation native exige le CI de la révision concernée.

Core v2 accepte les membres de données privés et protégés dans les classes à disposition standard par ailleurs prises en charge. Clang intégré vérifie les accès avant la traduction ; méthodes, constructeurs, fabriques, arguments par défaut et opérations de copie/déplacement autorisés utilisent le même stockage typé des membres. Les accès externes illégaux restent des diagnostics C++. Les spécificateurs d’accès sont des règles du langage source, sans garantie de confidentialité à l’exécution. Les classes hors disposition standard à accès mixtes, les déclarations d’amis templates, l’héritage et les types de champs non pris en charge restent limités. Les résultats natifs exigent le CI de la révision concernée.

Core v2 instancie les fonctions amies non templates des templates de classes admis, spécialisations partielles, templates membres et copies de classes complètes ou imbriquées ordinaires. ADL, accès privé, opérateurs libres, redéclarations, classes locales et variables statiques scalaires conservent leur identité, sans récepteur implicite. Les corps inutilisés restent différés ; les définitions sélectionnées et signatures originales sont vérifiées. Une autre déclaration normalisée exige des preuves de source non dépendante complètes, dont ses propres arguments par défaut et noexcept résolu. Formes dépendantes non prises en charge de templates de classes amies, expansions de types amis, C++/STL complet restent inachevés. Les règles des types amis non dépendants et membres amis sont conservées. Seul C++ est implémenté ; E Language et Python sont prévus. Clang est intégré sans exécutable externe. La validation native exige la CI du commit concerné. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-functions).

Core v2 accepte les types amis non templates résolus dans les classes génériques admises : `friend T;`, alias, types imbriqués dépendants et spécialisations de classes. Sema intégré vérifie les accès privés et les types non classes légalement ignorés. Les sources originales et substituées sont contrôlées ; les copies sans origine prouvée, formes dépendantes non prises en charge de templates de classes amies et expansions de packs restent exclus. Aucun récepteur ou stockage supplémentaire n’est créé. La CI du commit doit valider le comportement natif ; C++/STL complet reste inachevé. Seul C++ est implémenté ; E Language et Python sont prévus. Clang est intégré sans exécutable externe. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-types).

Core v2 accepte les templates de fonctions amies dans les classes ordinaires et génériques admises : opérateurs pris en charge, ADL, accès privé, paramètres internes et externes, valeurs par défaut et packs. Les références, durées de vie et identités des instances et stockages statiques sont conservées. Les déclarations originales et le contexte du corps sélectionné sont vérifiés. Formes dépendantes non prises en charge de templates de classes amies, copies sans origine prouvée et C++/STL complet restent inachevés ; la CI du commit doit valider le comportement natif. Seul C++ est implémenté ; E Language et Python sont prévus. Clang est intégré sans exécutable externe. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-function-templates).

Core v2 accepte les déclarations ordinaires de templates de classes amies dans les classes ordinaires et génériques admises : introductions dans un espace de noms, cibles existantes ou qualifiées prises en charge et accès par leurs spécialisations. Les autorisations répétées partagent la classe cible et son stockage statique. Les valeurs par défaut écrites dans la déclaration amie sont rejetées ; celles héritées restent vérifiées. Les formes dépendantes non prises en charge et C++/STL complet restent inachevés ; la CI du commit doit valider le comportement natif. Seul C++ est implémenté ; E Language et Python sont prévus. Clang est intégré sans exécutable externe. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-class-templates).

Core v2 accepte les pointeurs typés vers des fonctions libres ordinaires et des méthodes statiques définies : appels indirects, paramètres et résultats de rappel, références, tableaux, champs et stockage global, local statique ou membre statique ordinaire à initialisation constante. La cible est capturée avant les arguments. Seuls l’ABI par défaut et les signatures scalaires/par référence admises sont pris en charge. Objets par valeur, pointeurs de membres non statiques, variadiques, lambdas et rappels entre unités restent à faire. C++/STL complet reste inachevé ; la validation native nécessite CI. Seul C++ est implémenté ; E Language et Python sont prévus. Clang est intégré sans exécutable externe. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-function-pointers).

Core v2 accepte aussi les pointeurs vers les instances concrètes admises de templates de fonctions libres et de membres statiques, avec arguments explicites ou déduction depuis le type de pointeur attendu. Les contrôles du source, de la définition et de l’ABI restent requis. Appels directs et indirects partagent la même fonction et son stockage local statique ; les instances différentes restent distinctes. C++/STL complet reste inachevé ; la validation native nécessite CI. Seul C++ est implémenté ; E Language et Python sont prévus. Les bibliothèques Clang sont intégrées sans exécutable externe. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-function-template-pointers).

Core v2 accepte aussi les rappels à initialisation constante ou nulle dans les templates de variables de namespace et de membres statiques. Les instances équivalentes partagent le stockage ; les autres restent indépendantes même si elles ciblent la même fonction. Les contrôles du source de auto direct sont conservés ; arguments templates de valeur pointeur restent exclus. C++/STL complet reste inachevé ; la validation native nécessite CI. Entrée C++ uniquement ; E Language et Python sont prévus. Clang est intégré sans exécutable externe. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#callback-variable-templates).

Core v2 accepte les records imbriqués nommés non templates de classe, y compris les types privés ou protégés exposés par des alias ou fabriques autorisés. Chaque objet conserve son propre stockage et récepteur ; références explicites à l’objet externe, identité de type par portée, copies et déplacements de membres et tableaux, destruction et itérateurs imbriqués conservent leur sémantique habituelle. Les dépendances par valeur sont émises en premier. Records imbriqués anonymes, templates de classe, héritage et STL complète restent à réaliser ; la validation native exige le CI de la révision concernée.

Core v2 accepte les membres statiques définis de type entier, booléen ou énumération, avec définitions inline/constexpr ou hors classe et initialisation constante ou à zéro. Toutes les instances partagent le même stockage typé. Les effets du récepteur et la destruction des temporaires sont conservés ; les références aux membres statiques restent valides après un récepteur temporaire. Les membres const non inline de type entier, booléen ou énumération, avec une initialisation constante vérifiée dans la classe, permettent aussi la lecture de leur valeur ou son abandon sans définition séparée. Aucun objet global n’est créé ; prendre leur adresse ou leur lier une référence exige toujours une définition dans la même unité source. autres types statiques et STL complète restent inachevés ; la validation native exige le CI de la révision concernée.

Core v2 conserve l’initialisation constante et la première initialisation synchronisée des objets et références locaux statiques. Paramètres, variables automatiques et this restent utilisables. Une garde atomique 32 bits publie la construction après nettoyage des temporaires ; un objet constant nécessitant une destruction utilise la garde uniquement pour son enregistrement. Les références conservent les alias et prolongent les temporaires admis en stockage permanent. La destruction suit le contrat ci-dessus ; TLS et les déclarations statiques dans constexpr restent exclues. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-initialization) [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-temporaries)

Core v2 prend en charge les membres références vers des objets, y compris tableaux, pointeurs et pointeurs de fonction. La construction stocke la liaison ; accès, copie et déplacement conservent les alias. Les conteneurs const gardent leurs référents modifiables. L’initialisation des agrégats préserve les durées de vie prolongées et l’ordre de destruction, y compris les groupes statiques locaux synchronisés et les temporaires distincts des éléments implicites de tableaux à l’exécution. Les éléments implicites qui matérialisent des temporaires ont désormais des identités sémantiques distinctes dans les limites de ressources, avec conservation des frontières de nettoyage des arguments temporaires du constructeur par défaut qui initialise directement l’élément. C++/STL reste incomplet ; la validation native exige la CI de la révision implémentée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 accepte les espaces de noms inline, les alias d’espaces de noms, les directives using et les déclarations using résolues dans les espaces de noms et les blocs. Les fonctions, variables, types et énumérateurs non délimités importés conservent leur identité, la recherche de noms, les contrôles d’accès et la durée de vie des objets. Les espaces inline conservent la recherche combinée avec le parent et la recherche dépendante des arguments dans les deux sens, y compris après imbrication ou réouverture. Les imports de membres de classe, constructeurs hérités, templates de classe et imports d’énumérateurs ou syntaxe inline imbriquée explicite de C++20 restent exclus ; C++/STL complet reste inachevé.

Core v2 accepte if constexpr C++17 dans les fonctions ordinaires et les instances concrètes de templates de fonctions admis. Seule la branche choisie est émise, avec stockage des initialisations et destruction par portée. Les fonctions ordinaires vérifient les deux branches ; Clang n’instancie pas une branche dépendante écartée. Les exigences de définition des fonctions et globales ordinaires restent en vigueur. C++23 if consteval est exclu.

Core v2 accepte les templates de fonctions libres avec au plus 64 paramètres, combinant types pris en charge et valeurs entières, booléennes ou énumérées. Déduction, instanciation et spécialisation explicites, types par défaut, arguments par défaut résolus, récursion et imports produisent des fonctions typées ordinaires. Chaque instance conserve ses types locaux et son stockage statique scalaire, même entre templates primaires de signature identique. Les corps génériques et valeurs par défaut différées sont vérifiés à leur instanciation ; tout corps matérialisé est vérifié. Les appels limités à une signature sous sizeof/noexcept exigent encore une définition. Formes dépendantes non prises en charge de templates de classes amies, en-têtes standard et STL complet restent inachevés. La validation native exige le CI de la révision concernée.

Les arguments auto scalaires de C++17, les paramètres scalaires de type dépendant, la déduction des tailles de tableaux et la récursion constexpr finie sont pris en charge. Les valeurs deviennent des constantes typées, sans paramètre supplémentaire à l’exécution. Des arguments équivalents partagent une instance ; des valeurs ou types déduits différents conservent un stockage statique distinct. Les expressions écrites et les types des paramètres sont vérifiés avant leur réduction constante. Les arguments pointeurs, références ou objets de classe restent exclus de cet incrément.

Core v2 prend en charge les instances concrètes de templates de classes à disposition standard dans les espaces de noms, avec au plus 64 paramètres de type ou de valeur scalaire. Types par défaut, instanciations/spécialisations explicites, alias, tableaux fixes et initialiseurs de champs sélectionnés produisent des enregistrements typés ordinaires. Des arguments équivalents partagent un type ; les valeurs, types et templates primaires distincts gardent leurs identités. Les règles existantes de copie, référence, retour et destruction des membres restent applicables. Les types, valeurs par défaut de type et expressions écrites sont vérifiés avant effacement. Le support C++/STL complet reste inachevé ; les résultats natifs exigent le CI de la révision implémentée.

Ces templates de classes à disposition standard admettent aussi les fonctions membres ordinaires nommées : surcharges const et qualifiées par référence, fabriques statiques, parcours par membres et variables locales statiques scalaires propres à chaque instance. Les corps génériques et arguments par défaut inutilisés restent différés ; les appels sélectionnés et tous les corps instanciés passent les contrôles existants de types, définitions et durées de vie. Les types des paramètres de template externes écrits sur les définitions hors classe sont également vérifiés. Les méthodes utilisent l’ABI ordinaire du récepteur et du résultat, sans paramètres de template à l’exécution.

Ces templates admettent les constructeurs définis par l’utilisateur : par défaut, paramétrés, explicites, de conversion, de copie et de déplacement, avec définitions hors classe et instanciations/spécialisations explicites. Les corps, initialiseurs et arguments par défaut inutilisés restent différés ; toute construction sélectionnée exige une définition vérifiée. Les arguments de copie par défaut ne sont évalués que s’ils sont omis. L’initialisation écrit dans la destination selon l’ordre de déclaration des membres et conserve les durées de vie existantes des temporaires, champs et tableaux. Les constructeurs hérités restent exclus. Validation native : CI de la révision implémentée.

Ces templates admettent aussi les destructeurs non virtuels définis par l’utilisateur, leurs définitions hors classe et leurs instanciations/spécialisations explicites. Une définition est exigée lorsque le frontend intégré constate une utilisation, y compris pour un retour direct d’objet. Les requêtes de type et expressions non évaluées conservent les corps inutilisés différés, tout en vérifiant les spécifications d’exception sélectionnées. Le corps précède la destruction inverse des membres et éléments de tableaux, avec un stockage récepteur explicite. Les règles de durée de vie des temporaires, références prolongées et paramètres par valeur sont conservées. Appels explicites de destructeur et C++/STL complet restent inachevés ; la validation native exige le CI de la révision implémentée.

Ces templates admettent les constructeurs par défaut, de copie et de déplacement, les affectations par copie et déplacement et les destructeurs non virtuels définis par `= default`, dans ou hors de la classe. Les opérations sélectionnées conservent l’ordre des membres, les stockages source et destination, le résultat par référence de l’affectation et la destruction inverse. L’initialisation par valeur distingue le défaut à la première déclaration du défaut hors classe. Les corps inutilisés restent différés ; les requêtes vérifient les spécifications d’exception écrites et résolues. Les opérations non triviales utilisées exigent une définition générée appartenant aux sources. Une spécialisation explicite n’hérite pas du défaut du primaire. C++17/STL complet reste inachevé ; la validation native exige le CI de la révision implémentée.

Ces templates admettent aussi les opérateurs et fonctions de conversion définis par l’utilisateur : affectation par copie/déplacement, déréférencement, incrément, indexation, flèche, objets appelables et conversion bool explicite. Les surcharges sélectionnées conservent l’ordre C++17, les alias, le stockage final et les durées de vie. Les corps et arguments par défaut inutilisés restent différés ; les déclarations sélectionnées et tous les corps instanciés gardent leurs contrôles de source et de définition. Définitions hors classe, instanciations/spécialisations explicites et variables locales statiques par instance utilisent des fonctions typées sans paramètres de template à l’exécution. Allocation, en-têtes standard et C++17/STL complet restent inachevés ; les résultats natifs exigent le CI de la révision implémentée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-operators-and-conversions).

Les classes locales instanciées dans les templates de fonctions libres ou les membres de classes admis conservent méthodes, construction/destruction, opérateurs, conversions et opérations defaulted sélectionnées. Origines concrètes, types, stockage du récepteur et variables locales statiques par instance sont vérifiés ; tous les corps instanciés restent inspectés. Chaque instanciation explicite de fonction conserve ses arguments, types, noms de conversion, qualifications et contrôles d’attributs, même avec extern ou une répétition sans effet. Réutiliser une déclaration canonique ne peut donc masquer du code non pris en charge. Les résultats natifs exigent le CI correspondant ; C++17/STL complet reste inachevé. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-local-classes-and-written-directives).

Core v2 prend aussi en charge les templates de fonctions opérateurs ordinaires dans les espaces de noms. Arithmétique, comparaisons, références, opérateurs d’itérateur et retours de records conservent la surcharge choisie, l’ordre C++17, le stockage réel et la destruction des temporaires. ADL, appels explicites, spécialisations et instanciations utilisent les fonctions typées existantes, sans paramètres de template à l’exécution. Les corps inutilisés restent différés ; le code sélectionné, tous les corps instanciés et chaque directive écrite sont vérifiés. Formes dépendantes non prises en charge de templates de classes amies, allocation, en-têtes standard et C++17/STL complet restent inachevés ; la validation native exige le CI correspondant. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-operator-function-templates).

Ces templates de classes admettent aussi les membres statiques entiers, booléens et énumérés à initialisation nulle ou constante, avec inline/constexpr et définitions hors classe. Les arguments équivalents partagent le stockage ; les instances distinctes gardent leurs objets. Une valeur const non inline vérifiée ne crée pas de stockage, mais les adresses et références évaluées exigent une définition. Les initialiseurs inutilisés restent différés ; ceux instanciés et chaque directive explicite sont vérifiés dans le source. Effets du récepteur et destruction des temporaires restent préservés. autres types statiques, C++17/STL complet restent inachevés ; la validation native exige le CI correspondant. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-scalar-static-data).

Core v2 admet les valeurs par défaut des paramètres scalaires non types des templates de fonctions, opérateurs et classes pris en charge. Paramètres précédents, auto scalaire, valeurs héritées et conversions constexpr vérifiées suivent Clang. Arguments omis et explicites équivalents partagent fonctions, records et objets statiques. Le source non dépendant est vérifié ; les valeurs dépendantes inutilisées restent différées. Les conversions réussies gardent le source original et converti ; les échecs conservent le choix normal d’une autre surcharge. C++17/STL complet reste inachevé ; la validation native exige le CI correspondant. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#scalar-template-parameter-defaults).

Les conversions à type de retour déduit préservent les références et const, y compris decltype(auto) et le regroupement des références auto&&. Le frontal intégré déduit les types nécessaires à la sélection sans instancier les corps de modèles non pertinents qui retournent une valeur. La prise en charge complète de C++17/STL reste inachevée ; la validation native dépend de la CI de cette implémentation. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deduced-reference-conversions). Les nouveaux arguments de référence déduisent les résultats de conversion nécessaires avant le contrôle de compatibilité. Le filtre de liaison directe ne dispense pas un candidat de la déduction du type de retour nécessaire à une initialisation indirecte ultérieure ; un corps auto& concurrent invalide conserve le diagnostic C++ normal.

Core v2 accepte les packs concrets de types et de valeurs scalaires dans les templates autorisés de fonctions, opérateurs et classes des espaces de noms. La limite est de 64 paramètres par template primaire et 64 éléments par pack. Les paramètres développés gardent un stockage distinct ; sizeof... produit un nombre typé. Les expressions de repli résolues préservent ordre, court-circuit et nettoyage. Un nombre dépendant de la même liste peut rester une métadonnée dans le type écrit d’un paramètre. Les motifs des replis vides ne sont pas instanciés ; valeurs initiales et éléments matérialisés sont vérifiés. C++17/STL complet reste inachevé ; les résultats natifs exigent le CI de l’implémentation. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-parameter-packs).

Core v2 accepte les templates d’alias dans les espaces de noms propres pour les types scalaires, void, pointeurs, références, tableaux fixes et classes admises, avec arguments par défaut de type ou scalaires et paquets concrets. Les alias partagent types canoniques et stockage statique. Les arguments écrits, valeurs par défaut sélectionnées et types source substitués sont vérifiés lors des utilisations effectives ; candidats non retenus et corps dépendants non instanciés restent différés. Les arguments explicites conservent le contexte appelant, y compris en récursion et avec this. Les limites restent de 64 paramètres, éléments par paquet et niveaux, avec le budget source commun. C++17/STL complet reste inachevé ; la validation native dépend du CI de la révision. Seule l’entrée C++ est implémentée ; E Language, Python et les autres frontends sont prévus. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-alias-templates).
Les arguments de modèle non types sélectionnés conservent aussi le code source de leur type de paramètre, y compris pour les arguments explicites des modèles de fonction et les paquets vides déduits ; le code non pris en charge masqué par la simplification d’un alias reste vérifié.

Les types par défaut non dépendants sont vérifiés même dans les modèles inutilisés. Les appels qualifiés de modèles de fonctions et les définitions concrètes de fonctions de conversion conservent leur source exacte, y compris les alias dans les définitions hors classe. Une conversion lvalue peut créer un temporaire élargi pour un paramètre de référence const ; un paramètre de référence rvalue conserve la restriction de surcharge de C++.
Core v2 prend aussi en charge les spécialisations partielles de modèles de classes dans les espaces de noms du projet. Le motif choisi et ses paramètres déduits sont vérifiés séparément des arguments du modèle primaire. Les alias partagent le même type concret et le même stockage statique ; les membres pris en charge conservent appels, références et durées de vie. La limite reste de 64 paramètres et de 64 éléments par pack. La prise en charge complète de C++17/STL reste inachevée ; la validation native dépend de la CI de la révision concernée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-partial-specializations).

Core v2 prend en charge le type distinct `decltype(nullptr)` : alias, références, champs/tableaux, signatures de rappels et stockage scalaire statique ou de templates de variables initialisé à zéro ou par une constante. Les conversions en pointeurs conservent les effets des expressions et la destruction des temporaires ; les objets null distincts gardent leurs propres adresses. Les arguments de template `nullptr_t` acceptent les valeurs par défaut, les packs et les contextes source déjà admis, avec des contrôles conservés et une identité distincte de zéro entier ou false booléen. Les arguments null de type pointeur et les en-têtes standard restent exclus. La validation native nécessite la CI de la révision concernée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#null-pointer-values).

Core v2 prend en charge les constructeurs délégués des classes ordinaires et génériques admises, leurs chaînes et les templates de constructeurs sélectionnés. La cible et le corps déléguant utilisent le même objet final ; les membres sont initialisés une seule fois et les arguments temporaires sont détruits avant le corps déléguant. Les contrôles de sélection et de définition restent actifs. La validation native nécessite la CI de la révision concernée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#delegating-constructors).

Core v2 admet les membres const de types scalaires, enregistrements, pointeurs, rappels et tableaux pris en charge, y compris dans les classes génériques et imbriquées. Il conserve le stockage final, les copies/déplacements sélectionnés, les adresses distinctes et les références/pointeurs const. Clang rejette les écritures invalides et affectations supprimées. Les classes à clé constante définies dans le source sont possibles ; les en-têtes standard et le STL complet restent à réaliser. Validation native : CI de la révision concernée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#const-data-members).

Core v2 accepte les fonctions supprimées et les membres spéciaux par défaut définis comme supprimés, dans les formes de templates et d’amis admises. Clang conserve la résolution des surcharges et les diagnostics d’accès ; aucune définition exécutable n’est émise pour ces déclarations. Objets uniquement déplaçables, repli sur une copie sélectionnée et élision garantie de C++17 conservent la gestion de durée de vie. CI de la révision requise ; C++/STL complet reste inachevé. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deleted-function-declarations).

Core v2 accepte les types IEEE `float` et `double`, leur arithmétique et conversions, les références, champs/tableaux, callbacks et objets scalaires statiques initialisés à zéro ou par constante. Les bits exacts des littéraux et une vérification indépendante des dispositions préservent la représentation. L’exécution exige l’environnement flottant par défaut, sans calcul rapide, précision excédentaire ni contraction implicite. CI de la révision requise ; `long double`, les en-têtes standard et C++/STL complet restent inachevés. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#binary-floating-point-values).

Core v2 accepte les littéraux étroits, UTF-8, UTF-16, UTF-32 et larges avec leurs unités exactes et un stockage statique en lecture seule, l’initialisation des tableaux de caractères avec remplissage nul et les tableaux constants d’espace de noms. Alias, copies et durées de vie utilisent les opérations typées existantes. CI de la révision requise ; les en-têtes standard et les allocations/opérations de `std::string` restent inachevés. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#string-literals-and-constant-arrays).

Core v2 prend en charge les tableaux modifiables et constants initialisés à zéro ou par une constante, dans les espaces de noms, les variables statiques locales et les membres statiques de classe, y compris les instances de modèles admises. État et adresses persistent entre les appels ; les instances distinctes conservent un stockage séparé. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#fixed-array-static-storage).

Core v2 prend en charge l’initialisation nulle ou constante des pointeurs statiques vers des objets globaux, des littéraux, des éléments de tableau et des champs, y compris les adresses valides juste après la fin. Identité, mutabilité et accès const restent distincts pour les espaces de noms, fonctions, classes et modèles. Les adresses déclarées plus loin deviennent des constantes C23 sans initialisation à l’exécution. Références statiques, allocation et STL complète restent en développement. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-object-pointer-storage).

Les références statiques peuvent désigner des objets statiques existants, des sous-objets de tableaux ou de structures et des chaînes, y compris dans les variables statiques locales, les classes et les instances de modèles admises. Les lectures et écritures conservent l’objet initial et les droits const. Voir le [contrat des références statiques](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-bindings).

Core v2 prend en charge les temporaires scalaires, tableaux et enregistrements initialisés par constante dont la durée de vie est prolongée par une référence statique. Le stockage complet, les alias de sous-objets, les pointeurs internes, const et l’identité des instances restent préservés. La validation native exige la CI de cette implémentation. En C++17, l’initialisation constante de ces temporaires exige toujours une destruction triviale ; les temporaires statiques non triviaux utilisent l’initialisation dynamique et l’enregistrement. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-temporary-lifetime-extension).

Les enregistrements statiques prennent en charge l’initialisation constante et un état mutable partagé, y compris dans les variables statiques locales, les classes et les modèles admis. Les adresses internes, la construction constexpr et l’initialisation statique à zéro conservent l’identité de l’objet. Voir le [contrat des objets statiques](../../utils/translate-frontends/docs/cpp-core-v2.md#static-record-objects).


Core v2 accepte les chaînes vérifiées de bases vides triviales, avec les templates pris en charge, méthodes héritées, conversions et valeurs statiques. Chaque base occupe un véritable premier membre C dont la disposition est vérifiée indépendamment. Pointeurs nuls, identité des références, chemins constants et effets sont conservés. Les cycles de vie non triviaux et les en-têtes standard restent à compléter ; la validation native exige la CI de la révision concernée. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#trivial-empty-base-chains).
## Installation et traduction scalaire

Utilisez une installation normale de NeverC avec ses ressources standard. Le frontend C++ et les en-têtes SDK approuvés sont intégrés ; aucune installation séparée de Clang n’est nécessaire. Voir les [notes de compilation du frontend](../../utils/translate-frontends/cpp/README.md).

```sh
neverc translate --from cpp input.cpp -o output.nc
neverc output.nc -c -o output.o
```

Le profil `cpp-core-v1` accepte un unique fichier C++17 autonome sans include. Il prend en charge `int`, `unsigned int`, `bool`, `void`, les structures triviales, les fonctions non membres, les espaces de noms, surcharges et le contrôle de flux documenté. Toutes les déclarations du projet sont vérifiées, même inutilisées.

Sélectionnez `--profile cpp-core-v2` pour ajouter les alias `typedef`/`using` vérifiés, les énumérations de type sous-jacent entier pris en charge, `static_assert`, ainsi que des pointeurs d’objet et références de lvalue dans un périmètre limité. Les paramètres et résultats par référence conservent les alias ; les pointeurs nuls et les qualifications `const` imbriquées sont pris en charge. Les restrictions à un seul fichier source sans include restent applicables. Voir le [contrat core v2](../../utils/translate-frontends/docs/cpp-core-v2.md).

Core v2 ajoute les tableaux locaux de taille fixe et les champs tableau, l’indexation multidimensionnelle et les pointeurs ou références vers des tableaux. L’initialisation partielle met à zéro les éléments scalaires omis ; les enregistrements suivent l’initialisation sélectionnée et conserve l’ordre ainsi que les alias. La taille et l’expansion de l’initialisation sont limitées. Les tableaux globaux et de taille variable restent exclus.

Core v2 prend en charge `switch`/`case`/`default`, les instructions d’initialisation C++17, le passage au cas suivant et les annotations `[[fallthrough]]` validées. Le sélecteur est évalué une seule fois ; les switch et boucles imbriqués conservent les cibles de `break`/`continue`. Les plages case GNU et les autres attributs d’instruction restent exclus.

Core v2 prend désormais en charge les entiers signés et non signés de 8, 16, 32 et 64 bits, les types et littéraux caractères, ainsi que les requêtes constantes `sizeof`/`alignof`. Les promotions et la résolution des surcharges précèdent la normalisation ; `long`, `wchar_t` et le type de taille suivent la cible. Les types sous-jacents des enums peuvent aussi être plus étroits ou plus larges. La bibliothèque standard reste en développement.

Core v2 prend aussi en charge les décalages et différences de pointeurs sur objets, leur incrémentation/décrémentation et les affectations composées, avec parcours des tableaux et pas multidimensionnels. Les fonctions auxiliaires générées préservent les règles C++17 pour un pointeur nul plus ou moins zéro et la différence de deux pointeurs nuls. Les comparaisons d’ordre entre pointeurs sur objets utilisent des adresses non signées vérifiées indépendamment sur les cibles natives x86/AArch64 ; les résultats natifs nécessitent la CI de la révision concernée. Les conversions pointeur/entier du code source et la STL complète restent inachevées.

Core v2 compare les tailles et alignements ABI des types source au modèle cible propre à NeverC, y compris la taille des structures et les décalages des champs. Des assertions statiques vérifient à nouveau cette disposition à la compilation, et le manifeste en conserve les données.

Core v2 prend en charge les fonctions membres nommées non virtuelles des types enregistrement admis, dont les surcharges const, les méthodes qualifiées lvalue, les méthodes statiques et `this`. Les appels conservent l’identité de l’objet et évaluent le récepteur avant les arguments.

Core v2 prend en charge les constructeurs ordinaires définis par l’utilisateur pour les enregistrements à disposition standard et opérations de copie prises en charge. Objets locaux, champs et éléments de tableau sont construits dans leur stockage final ; les champs suivent l’ordre de déclaration. Le déroulement de pile par exception reste exclu.

Core v2 crée un objet distinct pour chaque paramètre enregistrement passé par valeur et écrit le résultat directement dans la destination de l’appelant. Les constructeurs et méthodes suivent les mêmes règles ; les copies requises et les alias des références sont conservés. Pour les types triviaux admissibles, d’autres implémentations C++17 peuvent ajouter des copies d’arguments ou de résultats.

Core v2 prend en charge les destructeurs ordinaires définis par l’utilisateur et la destruction implicite des membres lors des sorties normales. Objets locaux, champs et éléments de tableau sont détruits dans l’ordre inverse ; les temporaires à la fin de l’expression complète, après capture des valeurs nécessaires. Retours, branches, boucles, break et continue effectuent le nettoyage requis. Les paramètres par valeur sont détruits à la sortie de la fonction appelée ; les objets retournés appartiennent à l’appelant. Le déroulement des exceptions reste exclu.

Core v2 exécute les constructeurs de copie et opérateurs d’affectation par copie ordinaires définis par l’utilisateur, avec un paramètre source `R&` ou `const R&`. La copie utilise la destination réelle et conserve les effets de bord ainsi que la référence retournée. La syntaxe d’affectation évalue d’abord l’opérande droit ; un appel explicite à `operator=` évalue d’abord l’objet récepteur.

Core v2 prend aussi en charge les constructeurs par défaut générés ou marqués default et les destructeurs default, y compris les membres de type enregistrement ou tableau imbriqué. La construction utilise la destination réelle et l’ordre de déclaration ; l’initialisation par valeur ne met à zéro que lorsque C++ l’exige. Les règles distinctes du default défini hors classe sont conservées. Un constructeur inutilisé ou présent seulement dans une expression non évaluée ne nécessite aucun corps artificiel.

Core v2 prend en charge les constructeurs de copie implicites et explicitement default, y compris les enregistrements imbriqués et tableaux multidimensionnels. Les copies de membres sélectionnées utilisent la destination réelle, dans l’ordre des déclarations et des éléments ; l’adresse du tableau source est évaluée une fois. Les copies triviales conservent les pointeurs stockés. Les paramètres par valeur et retours depuis un objet source gardent les effets de copie ; transmettre directement une prvalue n’en ajoute pas.

Core v2 prend aussi en charge l’affectation par copie implicite ou explicitement default. Membres et éléments de tableau conservent leurs opérations sélectionnées et leur ordre ; les copies triviales générées deviennent des affectations typées sans appel externe de copie mémoire. L’affectation triviale conserve les pointeurs et renvoie le récepteur réel. La syntaxe opérateur capture d’abord la référence source, l’appel membre explicite d’abord le récepteur ; les valeurs sources sont lues après ces effets. Aucun objet supplémentaire n’est construit ou détruit pour l’affectation.

Core v2 prend en charge les initialiseurs par défaut des champs admis, avec accès aux membres précédents, appels ordinaires, records imbriqués et tableaux. Le défaut sélectionné utilise son objet réel comme `this` ; les clauses explicites d’agrégat conservent le `this` de l’appelant. Une initialisation explicite remplace le défaut du membre. Copie et affectation implicites/default ne relancent pas les défauts ; un constructeur de copie utilisateur peut les sélectionner pour les membres omis. Initialisation des membres du constructeur et initialisation d’agrégat conservent leurs limites respectives de destruction des temporaires. Modèles et STL complète restent en développement.

Core v2 accepte les références rvalue vers des objets déjà vivants : alias de scalaires, pointeurs, records et tableaux, paramètres et retours par référence, xvalues conditionnelles et méthodes ordinaires qualifiées `&&`. `static_cast<R&&>(live)` conserve le même objet ; une variable de référence rvalue nommée reste une lvalue. La surcharge et les copies existantes suivent le choix de Clang. Les références ne créent aucune responsabilité de destruction supplémentaire.

Core v2 exécute les constructeurs et affectations de déplacement ordinaires définis par l’utilisateur depuis une source vivante `R&&` ou `const R&&`. La construction utilise le stockage final ; l’affectation conserve les modifications de la source, l’ordre d’évaluation et l’alias `R&` retourné, y compris avec un receveur qualifié `&`/`&&`. Une référence rvalue nommée choisit toujours les surcharges lvalue ; les constructeurs explicites gardent leurs règles d’initialisation. Le déplacement ne termine pas la vie de la source : source et destination sont détruites normalement.

Core v2 prend aussi en charge la construction et l’affectation de déplacement implicites ou explicitement default, avec records imbriqués et tableaux multidimensionnels. Les membres exécutent la copie ou le déplacement choisi par C++, dans l’ordre de déclaration et des éléments. Ces déplacements ne relancent pas les initialiseurs par défaut ; les opérations triviales conservent les valeurs des pointeurs. Les sources sont détruites normalement et aucun corps fictif n’est requis pour une opération générée inutilisée ou triviale.

Core v2 accepte les déclarations standard résolues `noexcept`, `noexcept(true/false)` et `throw()` de C++17, ainsi que les requêtes constantes `noexcept(expression)`. Ces requêtes respectent les spécifications des fonctions et destructeurs sélectionnés sans exécuter leurs opérandes. Tous les opérandes et spécifications écrites sont inspectés, même dans le code inutilisé. Levée et capture d’exceptions, déroulement de pile, modèles et STL complète restent en développement.

Core v2 accepte les opérateurs ordinaires surchargés, membres ou libres : arithmétique, comparaisons, indexation, déréférencement, incréments, foncteurs et signatures générales d’affectation. Fonctions sélectionnées, alias de références et objets résultats sont conservés. La notation opérateur respecte l’ordre C++17 ; les opérateurs logiques surchargés évaluent les deux opérandes. Les opérateurs libres d’affectation initialisent les paramètres de droite à gauche puis les détruisent dans l’ordre inverse. Modèles, allocation et STL complète restent en développement.

Core v2 accepte aussi les fonctions de conversion ordinaires sur des objets vivants : conversions entières, énumérées et de pointeurs implicites ou explicites, `bool` explicite en contexte conditionnel, résultats par référence ou objet. Chaque conversion appelle une fois la fonction membre sélectionnée. Les références conservent leurs alias ; les prvalues objets initialisent directement leur destination. La conversion d’une référence en valeur conserve la copie ou le déplacement sélectionné. Qualificateurs const/référence, constexpr et noexcept suivent C++17.

Core v2 accepte désormais les objets temporaires récepteurs et les arguments temporaires scalaires ou enregistrement passés par référence jusqu’à la fin de l’expression complète, pour les constructeurs, méthodes, opérateurs et conversions. Chaque évaluation dispose d’un stockage réel ; objets résultats et alias conservent leur destination. Après l’appel et la destruction des paramètres, les temporaires sont détruits dans l’ordre inverse de construction, y compris dans les conditions et boucles. Les sous-objets tableaux des enregistrements temporaires sont acceptés.

Core v2 prolonge désormais la vie des temporaires liés à des références locales automatiques ordinaires, notamment `const T& r{T{...}}` et `T&& r = T{...}`. Scalaires, énumérations, pointeurs et enregistrements disposent d’un stockage réel. Une référence à un membre ou élément de tableau maintient l’enregistrement complet en vie lorsque C++ accorde cette prolongation. Les initialisations entre accolades, avec ou sans signe égal, conservent le même objet. La destruction suit la portée de la référence, y compris conditions, boucles et sorties anticipées ; les autres temporaires de l’initialiseur finissent avec leur expression complète. Les alias n’ajoutent aucun propriétaire ; copies et déplacements ultérieurs conservent les opérations sélectionnées. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 accepte aussi les tableaux temporaires autonomes de taille fixe dans les appels, conversions en pointeur, indexations, expressions ignorées et références locales automatiques. Chaque tableau a une destination réelle ; valeurs scalaires, constructeurs et initialisateurs par défaut communs initialisent directement les éléments dans l’ordre, sans propriétaires supplémentaires. Lorsque C++ prolonge la durée de vie, les références aux lignes ou éléments multidimensionnels conservent le tableau complet. Les éléments sont détruits en ordre inverse à la fin de l’expression complète ou de la portée de la référence. Adresses typées et limites existantes de taille, stockage et expansion sont conservées. C++/STL complet reste en développement.

Core v2 prend en charge les classes vides à disposition standard et les objets fonctionnels ou de conversion sans état. Les objets vides conservent une taille et un alignement C++ d’un octet, un stockage distinct lorsque nécessaire, les constructeurs et opérateurs sélectionnés et la destruction normale. Les copies triviales évaluent toujours leurs opérandes ; les éléments vides des tableaux et des enregistrements comptent dans les limites de stockage. Le NC généré utilise un octet interne et la liste des champs source reste vide. L’héritage, les templates et la STL complète restent en développement.

Core v2 prend en charge les conversions vers void, `void()` et `void{}`, avec les alias void compatibles, appels, retours, expressions virgule et branches conditionnelles. Ignorer une lvalue non volatile conserve les effets du récepteur et de l’index sans lire sa valeur stockée. Les temporaires sont toujours construits et détruits à la fin de l’expression complète d’origine. Aucune variable ni valeur void n’est générée. Les opérandes constants et noexcept restent vérifiés. Les objets volatile, types non pris en charge, templates et STL complète restent hors de cette étape.

Core v2 accepte les arguments par défaut des fonctions, méthodes, opérateurs d’appel et constructeurs utilisateur pris en charge, y compris les paramètres supplémentaires des constructeurs de copie et de déplacement. Les noms sont résolus au point de déclaration et les expressions évaluées à chaque appel qui omet l’argument. Identité et durée de vie des références et valeurs sont conservées. Pour les éléments de tableau sans initialiseur et les copies de tableau générées, les temporaires des arguments par défaut sont détruits avant l’élément suivant ; les clauses explicites gardent la limite de l’expression complète. Les valeurs inutilisées ou remplacées restent vérifiées. Les templates et la STL complète restent inachevés.

## Projets à plusieurs fichiers

Sélectionnez explicitement les unités de traduction dans une base de données de compilation et indiquez le répertoire racine du projet. Le frontend intégré analyse chaque unité séparément ; la fusion vérifie les définitions, la liaison, les types partagés et le respect de la règle de définition unique (ODR) de façon conservatrice.

```sh
neverc translate --from cpp --profile cpp-project-v1 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/a.cpp src/b.cpp --out-dir generated
```

La sortie comprend `translated.nc` et `translated.h`. Seuls les en-têtes du projet situés sous cette racine sont admis. Si un fichier possède plusieurs configurations, sélectionnez son indice de base zéro avec `--compdb-entry src/a.cpp=4`. Les commandes de compilation stockées sont lues comme des données et ne sont jamais exécutées.

## Mathématiques double précision limitées

`cpp-math-v1` ajoute aux projets `double`, les conversions/comparaisons documentées et les signatures exactes `std::fabs(double)` et `std::floor(double)`. Il utilise les en-têtes intégrés de Clang 20.1.8 / libc++ 200100 / macOS 15.5 et exige une cible macOS 15.0 explicite (arm64 ou x86_64). Les opérations arithmétiques flottantes générales restent exclues. Les interruptions flottantes doivent être masquées et la mise à zéro des subnormaux désactivée ; les quatre modes d’arrondi standard sont testés.

```sh
neverc translate --from cpp --profile cpp-math-v1 \
  --target arm64-apple-macosx15.0.0 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/math.cpp --out-dir generated-math
```

La traduction mathématique vérifie aussi les en-têtes NeverC installés, l’identité des implémentations intégrées et une édition de liens réelle. Les modules générés utilisent le runtime NeverC. Avec `-fno-builtin-std`, la traduction échoue avant l’écriture des fichiers uniquement si le code final nécessite les correspondances `fabs`/`floor`. Le code mathématique qui n’en a pas besoin reste traduisible.

## Validation et sortie

Remplacez l’option de sortie par `--check` pour effectuer les mêmes analyses, génération et validations syntaxiques et objet sans conserver les fichiers générés. `--report PATH` écrit les diagnostics structurés. La traduction n’exécute jamais le programme source.

Les sorties et fichiers annexes ne sont jamais écrasés. `--out-dir` exige un nouveau répertoire sous un parent existant ; `-o` exige un nouveau chemin `.nc`. Le manifeste indique les exigences de cible, les empreintes des entrées et sorties et la procédure de compilation ; la carte source relie les lignes générées aux positions d’origine.

Les résultats de CI, les vérifications d’exécution et d’installation ainsi que les tests ignorés sont consignés par plateforme. macOS arm64 natif et macOS x86_64 via Rosetta restent des environnements de validation distincts. Le périmètre annoncé ne couvre pas l’ensemble de C++/STL, ni les exceptions, les templates, les chaînes de caractères ou `std::vector`. Consultez la [matrice de prise en charge](../../utils/translate-frontends/docs/support-matrix.md), le [protocole et les règles de récupération](../../utils/translate-frontends/docs/protocol.md) et le [projet exemple](../../tests/neverc/Inputs/translate/cpp/project).
