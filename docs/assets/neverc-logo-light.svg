<svg xmlns="http://www.w3.org/2000/svg" viewBox="114 114 306 284" width="306" height="284" role="img" aria-labelledby="neverc-logo-title">
  <title id="neverc-logo-title">NeverC</title>

  <!-- Light theme. Same three syntax roles as the dark mark, in VS Code
       Light+: #267F99 type, #0070C1 keyword, #795E26 function. The dark
       mark's colours drop to ~1.4:1 on white, so the logo follows the theme
       the way syntax highlighting does. The core carries no glow here:
       on a white page a halo reads as a smudge, not as light. -->
  <defs>
    <linearGradient id="arc" x1="150" y1="372" x2="344" y2="140" gradientUnits="userSpaceOnUse">
      <stop offset="0" stop-color="#267F99"/>
      <stop offset="1" stop-color="#0070C1"/>
    </linearGradient>

    <linearGradient id="core" x1="354" y1="236" x2="388" y2="280" gradientUnits="userSpaceOnUse">
      <stop offset="0" stop-color="#8C6E2A"/>
      <stop offset="1" stop-color="#795E26"/>
    </linearGradient>
  </defs>

  <!-- The C is left open and a separate core sits in the gap: the language is
       the arc, what NeverC adds is the piece that was never part of it. -->
  <path d="M 326.19 166.16 A 114 114 0 1 0 326.19 345.84"
        fill="none" stroke="url(#arc)" stroke-width="44"/>

  <circle cx="370" cy="256" r="23" fill="url(#core)"/>
</svg>
