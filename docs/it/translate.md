**Lingue**: [English](../translate.md) | [简体中文](../zh-CN/translate.md) | [繁體中文](../zh-TW/translate.md) | [日本語](../ja/translate.md) | [한국어](../ko/translate.md) | [Français](../fr/translate.md) | [Deutsch](../de/translate.md) | [Español](../es/translate.md) | [Italiano](translate.md) | [Русский](../ru/translate.md) | [العربية](../ar/translate.md)

[← Documentazione](README.md)

# Tradurre C++ in NeverC

Core v2 supporta l’inoltro predefinito di sized delete: un sized delete/delete[] globale implicito senza ridichiarazioni nel sorgente può chiamare il corrispondente operatore unsized definito nel sorgente. Le definizioni sized esplicite hanno la precedenza e il cookie dell’array conserva la disposizione originale. Non vengono aggiunti allocatori predefiniti né runtime delle eccezioni; la verifica nativa richiede la CI della revisione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

Core v2 supporta anche `__array_rank` e `__array_extent` verificati con indici interi costanti non negativi, inclusi indici di template per tipi di array fissi. Entrambi gli operandi vengono controllati prima della valutazione costante. I tipi non supportati e gli indici convertiti implicitamente da classi restano esclusi. I risultati nativi richiedono la CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#array-type-queries).

Core v2 aggiunge la classificazione integrata e verificata dei tipi, tra cui `__is_integral`, `__is_pointer` e `__is_same`. Le query preservano l’identità dei tipi C++ e controllano gli operandi scritti prima di produrre un booleano. I tipi non supportati e gli altri tratti restano limitati. I test O0/O2 e di rilocazione richiedono la CI della revisione che implementa la modifica; gli header standard e il supporto completo C++/STL restano incompleti. Le query coprono anche aggregati, classi vuote, layout standard, proprietà trivial, trivially-copyable e POD, polimorfismo, astrazione e relazioni di base tramite `__is_base_of` per i tipi ammessi. Sono supportate anche le classi standard `final` e le query `__is_final`, `__is_literal` e `__has_unique_object_representations`. Restano determinanti il padding del tipo sorgente, la rappresentazione delle classi vuote e le regole dei tipi letterali C++17; gli altri attributi e metodi virtuali mantengono le restrizioni esistenti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#builtin-type-classification).

Core v2 aggiunge query di costruzione, assegnazione, conversione e distruzione per i tipi ammessi non di classe, inclusi riferimenti, array fissi e puntatori a classi ammesse. I tipi scritti e gli argomenti predefiniti selezionati restano verificati senza eseguirne gli effetti. I valori di classe, i loro riferimenti e array richiedono ancora la conservazione della selezione nel sorgente. O0/O2, rilocazione e otto ABI attendono la CI della revisione; header standard e C++/STL completo restano incompleti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#non-record-operation-traits).

Core v2 aggiunge chiamate dirette a `malloc/calloc/free` tramite dichiarazioni C globali esatte nel sorgente. Gli allocatori C++ definiti nel sorgente possono usare lo heap nativo e scambiare e liberare memoria con un chiamante C indipendente. Restano attivi i controlli su ampiezza, convenzione di chiamata, sorgente e IR. La verifica O0/O2 richiede la CI di questa implementazione. `realloc`, l’allocazione C++ predefinita con eccezioni, gli header standard e il supporto C++/STL completo restano incompleti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#native-c-heap-calls).

Core v2 registra la distruzione degli oggetti statici, array e temporanei ammessi con durata estesa al completamento di ciascuna costruzione. Conserva i valori costanti; registra i locali al primo passaggio e gli altri durante l’avvio nativo. Il CRT preserva l’ordine di uscita e scaricamento dei moduli. I test O0/O2, di scaricamento e primo accesso concorrente richiedono la CI dell’implementazione. TLS, unwinding, heap predefinito, header standard e C++/STL completo restano incompleti; il caricamento manuale/DynCode è escluso. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-destruction).

Core v2 supporta ora l’inizializzazione dinamica non locale di oggetti e riferimenti ammessi, comprese istanze di template materializzate. Un costruttore nativo interno verificato viene eseguito prima di main in ordine di definizione, dopo l’inizializzazione zero o costante, con pulizia dei temporanei ordinari dopo ogni inizializzatore. Gli statici locali conservano l’inizializzazione al primo uso. I test O0/O2 del programma e di un client C separato richiedono CI della revisione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#nonlocal-dynamic-initialization).

Le lunghezze new[] a runtime supportano anche allocatori di classe noexcept definiti nel sorgente, senza argomenti placement. Le lunghezze non valide restituiscono null prima della chiamata all’allocatore; gli elementi validi sono costruiti in un ciclo che distrugge i temporanei degli argomenti predefiniti dopo ogni elemento. I temporanei del prefisso esplicito e della conversione della lunghezza durano fino alla fine dell’espressione completa. Gli aggregati ripetuti possono inizializzare direttamente ogni elemento senza oggetti temporanei aggiuntivi, comprese liste annidate e riferimenti a oggetti esistenti. Le inizializzazioni che richiedono tali temporanei, le allocazioni con eccezioni o placement a runtime, heap predefinito e C++/STL completo restano incompleti. La verifica nativa attende la CI di questa versione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#runtime-array-allocation).

Core v2 supporta new/delete per singoli oggetti tramite funzioni di allocazione definite nel sorgente e verificate, inclusi overload placement di classi e template, identità della memoria e pulizia degli argomenti. Distruzione esplicita e ricostruzione placement mantengono gli obblighi di pulizia automatica successivi. La verifica nativa richiede la CI di questa implementazione. Runtime heap predefinito, lunghezze new[] a runtime, eccezioni, header standard e C++/STL completo restano incompleti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

Il comando sperimentale `neverc translate` genera sorgenti `.nc` verificabili con `cpp-core-v1`, `cpp-core-v2`, `cpp-project-v1` e `cpp-math-v1`.

**Attualmente è implementata solo la traduzione di codice C++.** Il supporto per E Language (易语言, `.e`), Python, Go, Rust, TypeScript e JavaScript è previsto per il futuro; i relativi traduttori non sono ancora disponibili.

Le dichiarazioni di specializzazione parziale conservano i controlli degli argomenti primari, incluse le copie di variabili membro con argomenti interni dipendenti. I tipi di parametro già risolti vengono controllati anche senza selezionare la specializzazione. Le espansioni di lunghezza ignota mantengono il prefisso verificato e il sorgente reale senza inventare posizioni. Le specializzazioni complete di variabili membro ancora dipendenti conservano gli stessi controlli. C++/STL completo resta incompleto. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#partial-declaration-source-checks).

Conservare il controllo degli argomenti non rende valida una specializzazione parziale: deve comunque essere più specializzata e i suoi parametri devono essere deducibili. I pattern di pacchetti non validi e un’istanziazione extern dopo una definizione di istanziazione restano diagnostiche C++ (`TR0202`).

Core v2 supporta classi ordinarie annidate con nome nelle classi generiche ammesse, le loro copie concrete e specializzazioni esplicite proprie. I corpi ordinari non aggiungono un livello di parametri template. I corpi inutilizzati restano differiti; campi istanziati, metodi selezionati, template membro, durata degli oggetti e memoria statica scalare conservano sorgente e proprietario esatti. Ogni istanziazione esplicita verifica il qualificatore scritto, anche per extern e ripetizioni senza effetto. Ereditarietà, allocazione e C++/STL completo restano incompleti. È implementato solo l’ingresso C++; E Language e Python sono previsti. Si usano librerie Clang integrate senza un eseguibile Clang esterno. I risultati nativi richiedono CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-nested-classes-in-generic-owners).

Core v2 supporta template scalari di variabili membro statiche nei record e nelle istanze di classi ammesse: argomenti predefiniti, pacchetti limitati, specializzazioni parziali/complete e definizioni esterne. Istanze equivalenti condividono memoria; argomenti interni o esterni diversi mantengono oggetti distinti. I valori const non inline verificati si leggono senza memoria, mentre indirizzi e riferimenti valutati richiedono una definizione. Si conservano controlli di dichiarazioni e definizioni, effetti del ricevente e distruzione dei temporanei. Le specializzazioni complete dentro classi esterne dipendenti conservano le fonti delle dichiarazioni originali e concrete. Argomenti, valori predefiniti e tipi dei parametri risolti vengono verificati subito; le fonti dipendenti vengono verificate dopo l’istanziazione. Altri risultati, C++/STL completo restano incompleti. Si usano librerie Clang integrate senza Clang esterno. Solo C++ è implementato come input; E Language, Python e altri frontend sono previsti. La verifica nativa richiede il CI dell’implementazione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-variable-templates).

Core v2 supporta template di alias membro nei record ordinari, compresi quelli annidati, e nelle istanze di classi ammesse. Argomenti predefiniti e pacchetti limitati possono usare parametri esterni della classe e interni dell’alias; argomenti e tipi sorgente sostituiti vengono verificati separatamente. Gli alias conservano la semantica di valori, riferimenti, array e record senza oggetti template a runtime. Le dichiarazioni non dipendenti sono controllate anche se inutilizzate; i tipi dipendenti restano rinviati alla sostituzione. Si usano librerie Clang integrate senza eseguibile esterno. La verifica nativa richiede il CI dell’implementazione. C++/STL completo resta incompleto. Solo C++ è implementato come input; E Language, Python e altri frontend sono previsti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-alias-templates).

Core v2 supporta template concreti di funzioni membro nei record e nelle istanze di classi ammessi: metodi normali e statici, operatori, costruttori e conversioni, con pacchetti limitati, valori predefiniti, definizioni esterne e specializzazioni. Gli argomenti interni ed esterni mantengono identità distinte; chiamate equivalenti condividono codice e memoria statica. Chiamate, valori predefiniti e corpi selezionati conservano i controlli sul sorgente e sulle durate di vita. La traduzione usa librerie Clang integrate senza avviare un eseguibile Clang esterno. I risultati nativi richiedono il CI dell’implementazione. Ereditarietà, allocazione e C++/STL completo restano incompleti. Solo C++ è implementato come input; E Language, Python e altri linguaggi sono previsti. I qualificatori esterni concreti nelle dichiarazioni dei template membro vengono controllati anche senza istanziare il corpo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-function-templates).

Core v2 supporta template di variabili nei namespace con risultati interi, booleani o enum, inclusi auto dedotto, specializzazioni parziali selezionate, specializzazioni complete, valori predefiniti e pacchetti limitati di tipi o scalari. I tratti C++17 possono essere usati in static_assert, if constexpr e altri argomenti di template. Le istanze equivalenti condividono memoria globale tipizzata; quelle diverse mantengono valori e indirizzi distinti. Le interrogazioni di un tipo fisso non forzano un inizializzatore inutilizzato né creano memoria. Argomenti effettivi, valori predefiniti selezionati, tipi di dichiarazione e definizione e inizializzatori istanziati restano controllati. altri risultati, C++/STL completo restano da completare; la verifica nativa richiede il CI della revisione che implementa il supporto.

Un sizeof(T) o alignof(T) diretto nel tipo scritto di un parametro non di tipo può restare un metadato di dichiarazione se T è esattamente il parametro di tipo della stessa lista. Le sostituzioni selezionate richiedono ancora tipi concreti verificati; le altre query dipendenti non rientrano nell’eccezione. Una dichiarazione extern template da sola non fornisce la definizione concreta richiesta dal contratto attuale di traduzione dell’unità chiusa.

Core v2 analizza i template secondo lo standard C++17 su tutte le destinazioni supportate, incluse Windows x64 e ARM64. Le definizioni vengono analizzate senza il parsing differito di MSVC; le definizioni duplicate di istanziazione esplicita e le specifiche di eccezione incompatibili restano errori del linguaggio. I corpi dipendenti non utilizzati vengono ancora istanziati solo quando necessario. Il supporto completo di C++/STL resta da completare. Le specializzazioni esplicite di funzione richiedono ancora una propria definizione nell’unità sorgente; le dichiarazioni ripetute non la forniscono.

Core v2 supporta variabili globali modificabili di tipo intero, booleano ed enumerazione nell’ambito di un namespace, con inizializzazione a zero o mediante una costante completamente verificata. Memoria e indirizzo persistono tra le chiamate; riferimenti, puntatori e argomenti predefiniti accedono alla stessa variabile. Una dichiarazione `extern` richiede una definizione nella stessa unità sorgente. Le globali const restano di sola lettura. Restano le restrizioni specifiche per record/array/puntatori/riferimenti globali, memoria locale al thread e variabili locali statiche.

Core v2 supporta i cicli `for` su intervalli di C++17 per gli array fissi ammessi e gli intervalli definiti nel sorgente, con chiamate risolte a `begin/end` tramite membri o ADL. Variabili per valore o riferimento, iteratori di tipo record e sentinelle di tipo diverso mantengono le normali regole di chiamata e durata. L’intervallo e `begin/end` vengono inizializzati una sola volta; gli oggetti di ogni iterazione vengono distrutti prima dell’incremento o dell’uscita, anche con `continue`, `break` e `return`. Template, header standard, contenitori STL, binding strutturati e istruzioni di inizializzazione C++20 restano fuori da questa estensione. La verifica nativa richiede il CI della revisione che la implementa.

Core v2 supporta membri dati privati e protetti nelle classi a layout standard che rispettano le altre restrizioni. Clang integrato controlla gli accessi prima della traduzione; metodi, costruttori, factory, argomenti predefiniti e operazioni di copia/spostamento autorizzati usano la stessa memoria tipizzata dei membri. Gli accessi esterni non consentiti restano diagnostici C++. Gli specificatori di accesso sono regole del linguaggio sorgente, senza garanzie di segretezza durante l’esecuzione. Restano le restrizioni per classi senza layout standard con accessi misti, friend template, ereditarietà e tipi di campo non supportati. I risultati nativi richiedono il CI della revisione che li implementa.

Core v2 istanzia funzioni friend non template da template di classi ammessi, specializzazioni parziali, template membri e copie di classi complete o annidate ordinarie. ADL, accesso privato, operatori liberi, ridichiarazioni, classi locali e variabili statiche scalari conservano la propria identità senza ricevitore implicito. I corpi inutilizzati restano differiti; definizioni selezionate e firme originali vengono controllate. La normalizzazione a un’altra dichiarazione richiede prove complete della sorgente non dipendente, inclusi argomenti predefiniti propri e noexcept risolto. Forme dipendenti non supportate di template di classi friend, espansioni di tipi friend, C++/STL completo restano incompleti. Restano le regole esistenti per tipi friend non dipendenti e membri friend. Solo C++ è implementato; E Language e Python sono previsti. Clang è integrato senza eseguibile esterno. La verifica nativa richiede CI del commit di implementazione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-functions).

Core v2 supporta tipi friend non template risolti nelle classi generiche ammesse: `friend T;`, alias, tipi annidati dipendenti e specializzazioni di classe. Sema integrato verifica l’accesso privato e i tipi non classe legalmente ignorati. Vengono controllate le sorgenti originali e sostituite; copie senza origine provata, forme dipendenti non supportate di template di classi friend ed espansioni di pacchetti restano esclusi. Non si aggiungono ricevitori o memoria. Il comportamento nativo richiede CI del commit; C++/STL completo resta incompleto. Solo C++ è implementato; E Language e Python sono previsti. Clang è integrato senza eseguibile esterno. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-types).

Core v2 supporta template di funzioni friend nelle classi ordinarie e generiche ammesse: operatori supportati, ADL, accesso privato, parametri interni ed esterni, valori predefiniti e pacchetti. Conserva riferimenti, durata degli oggetti e identità separate di istanze e memoria statica. Controlla dichiarazioni originali e contesto del corpo selezionato. Forme dipendenti non supportate di template di classi friend, copie senza origine provata e C++/STL completo restano incompleti; serve la CI del commit. Solo C++ è implementato; E Language e Python sono previsti. Clang è integrato senza eseguibile esterno. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-function-templates).

Core v2 supporta dichiarazioni ordinarie di template di classi friend nelle classi ordinarie e generiche ammesse: introduzioni nei namespace, destinazioni esistenti o qualificate supportate e accesso dalle loro specializzazioni. Le autorizzazioni ripetute condividono la classe destinazione e la memoria statica. Gli argomenti predefiniti scritti nella dichiarazione friend vengono rifiutati; quelli ereditati mantengono i controlli sulla sorgente. Le forme dipendenti non supportate e C++/STL completo restano incompleti; serve la CI del commit per la verifica nativa. Solo C++ è implementato; E Language e Python sono previsti. Clang è integrato senza eseguibile esterno. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-class-templates).

Core v2 supporta puntatori tipizzati a funzioni libere ordinarie e metodi statici definiti: chiamate indirette, parametri e risultati callback, riferimenti, array, campi e memoria globale, locale statica o di membri statici ordinari con inizializzazione costante. La destinazione viene salvata prima di valutare gli argomenti. Sono ammesse solo l’ABI predefinita e le firme scalari/per riferimento supportate. Oggetti per valore, puntatori a membri non statici, variadiche, lambda e callback tra unità restano da implementare. C++/STL completo è incompleto; serve CI per la verifica nativa. Solo C++ è implementato; E Language e Python sono previsti. Clang è integrato senza eseguibile esterno. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-function-pointers).

Core v2 supporta anche puntatori a istanze concrete ammesse di template di funzioni libere e membri statici, con argomenti espliciti o deduzione dal tipo di puntatore atteso. Restano obbligatori i controlli del sorgente, della definizione e dell’ABI. Le chiamate dirette e indirette condividono la funzione e i suoi oggetti locali statici; istanze diverse restano distinte. C++/STL completo è ancora incompleto; la verifica nativa richiede CI. Solo C++ è implementato; E Language e Python sono previsti. Le librerie Clang sono integrate senza eseguibile esterno. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-function-template-pointers).

Core v2 supporta anche callback con inizializzazione costante o nulla nei template di variabili di namespace e membri statici. Istanze equivalenti condividono la memoria; quelle diverse restano indipendenti anche se puntano alla stessa funzione. Restano i controlli del sorgente per auto scritto direttamente; argomenti template con valore puntatore sono esclusi. C++/STL completo è incompleto e la verifica nativa richiede CI. Input solo C++; E Language e Python sono previsti. Clang è integrato senza eseguibile esterno. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#callback-variable-templates).

Core v2 supporta record annidati con nome e non altre forme di template di classe, compresi tipi privati o protetti esposti tramite alias o factory consentiti. Ogni oggetto conserva memoria e ricevitore propri; riferimenti espliciti all’oggetto esterno, identità di tipo per ambito, copia e spostamento di membri e array, distruzione e iteratori annidati mantengono la normale semantica. Le dipendenze per valore sono emesse per prime. Record annidati anonimi, altre forme di template di classe, ereditarietà e STL completa restano da implementare; la verifica nativa richiede il CI della revisione corrispondente.

Core v2 supporta membri statici definiti di tipo intero, booleano o enumerazione, incluse definizioni inline/constexpr e fuori classe con inizializzazione costante o a zero. Tutte le istanze condividono la stessa memoria tipizzata. Gli effetti del ricevitore e la distruzione dei temporanei sono preservati; i riferimenti ai membri statici sopravvivono ai ricevitori temporanei. I membri const non inline di tipo intero, booleano o enumerazione con inizializzazione costante verificata nella classe consentono anche la lettura del valore o lo scarto del risultato senza una definizione separata. Non viene creato alcun oggetto globale; ottenere l’indirizzo o associare un riferimento al membro richiede ancora una definizione nella stessa unità sorgente. altri tipi statici e STL completa restano incompleti; la verifica nativa richiede il CI della revisione corrispondente.

Core v2 conserva l’inizializzazione costante e la prima inizializzazione sincronizzata di oggetti e riferimenti locali statici. Gli inizializzatori possono usare parametri, variabili automatiche e this. Una guardia a 32 bit senza lock pubblica la costruzione dopo la pulizia dei temporanei; gli oggetti costanti da distruggere la usano solo per registrarsi. I riferimenti mantengono alias e temporanei ammessi in memoria permanente. La distruzione segue il contratto sopra; TLS e dichiarazioni statiche in constexpr restano escluse. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-initialization) [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-temporaries)

Core v2 supporta membri riferimento a oggetti, inclusi array, puntatori e puntatori a funzione. La costruzione memorizza il legame; accesso, copia e spostamento conservano gli alias. I contenitori const mantengono referenti modificabili. L’inizializzazione degli aggregati preserva durate estese e ordine di distruzione, inclusi gruppi statici locali sincronizzati e temporanei distinti per gli elementi impliciti degli array a runtime. Gli elementi impliciti che materializzano temporanei hanno ora identità semantiche distinte entro i limiti di risorse e conservano i confini di pulizia degli argomenti temporanei del costruttore predefinito che inizializza direttamente l’elemento. C++/STL completo resta incompleto; la verifica nativa richiede la CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 supporta namespace inline, alias di namespace, direttive using e dichiarazioni using risolte nei namespace e nei blocchi. Funzioni, variabili, tipi ed enumeratori senza ambito importati conservano identità, ricerca dei nomi, controlli di accesso e durata degli oggetti. I namespace inline preservano la ricerca combinata con il namespace padre e la ricerca dipendente dagli argomenti in entrambe le direzioni, anche con annidamento e riapertura. Importazioni di membri di classe, costruttori ereditati, altre forme di template di classe e importazioni di enumeratori o sintassi inline annidata esplicita di C++20 restano esclusi; il supporto completo C++/STL non è ancora terminato.

Core v2 supporta if constexpr C++17 nelle funzioni ordinarie e nelle istanze concrete dei template di funzione ammessi. Emette soltanto il ramo scelto e conserva memoria di inizializzazione e distruzione per ambito. Nelle funzioni ordinarie controlla entrambi i rami; Clang non istanzia il ramo dipendente scartato. Restano i requisiti di definizione di funzioni e globali ordinarie. C++23 if consteval è escluso.

Core v2 supporta template di funzioni libere con al massimo 64 parametri, combinando tipi supportati con valori interi, booleani ed enumerati. Deduzione, istanziazione e specializzazione esplicite, tipi predefiniti, argomenti predefiniti risolti, ricorsione e importazioni producono funzioni tipizzate ordinarie. Ogni istanza mantiene tipi locali e memoria statica scalare propri, anche tra template primari con firme identiche. Corpi generici e valori predefiniti differiti sono controllati quando istanziati; ogni corpo materializzato viene controllato. Le chiamate con sola firma in sizeof/noexcept richiedono ancora una definizione. Forme dipendenti non supportate di template di classi friend, header standard e STL completo restano incompleti. La verifica nativa richiede il CI della revisione corrispondente.

Sono supportati argomenti auto scalari di C++17, parametri scalari di tipo dipendente, deduzione delle dimensioni degli array e ricorsione constexpr finita. I valori diventano costanti tipizzate senza aggiungere parametri a runtime. Argomenti equivalenti condividono un’istanza; valori o tipi dedotti diversi mantengono memoria statica separata. Espressioni scritte e tipi dei parametri vengono controllati prima della riduzione a costante. Argomenti puntatore, riferimento o valore di classe restano esclusi da questo incremento.

Core v2 supporta istanze concrete di template di classi a layout standard nei namespace, con al massimo 64 parametri di tipo o valore scalare. Tipi predefiniti, istanziazioni/specializzazioni esplicite, alias, array fissi e inizializzatori di campo selezionati producono normali record tipizzati. Argomenti equivalenti condividono un tipo; valori, tipi e template primari diversi mantengono identità distinte. Restano valide le regole esistenti di copia, riferimento, restituzione e distruzione dei membri. Tipi, tipi predefiniti ed espressioni scritte vengono controllati prima della rimozione. Il supporto C++/STL completo è incompleto; i risultati nativi richiedono CI della revisione implementata.

Questi template di classi a layout standard supportano anche normali funzioni membro con nome: overload const e qualificati per riferimento, factory statiche, cicli tramite membri e variabili locali statiche scalari per istanza. I corpi generici e gli argomenti predefiniti inutilizzati restano differiti; le chiamate selezionate e tutti i corpi istanziati passano i controlli esistenti su tipi, definizioni e durata di vita. Vengono controllati anche i tipi dei parametri di template esterni scritti nelle definizioni fuori dalla classe. I metodi usano la normale ABI di ricevitore e risultato, senza parametri di template a runtime.

Questi template supportano costruttori definiti dall’utente: predefiniti, con parametri, espliciti, di conversione, copia e spostamento, oltre a definizioni fuori classe e istanziazioni/specializzazioni esplicite. Corpi, inizializzatori e argomenti predefiniti inutilizzati restano differiti; ogni costruzione selezionata richiede una definizione verificata. Gli argomenti predefiniti di copia sono valutati solo se omessi. L’inizializzazione scrive nella destinazione secondo l’ordine di dichiarazione dei membri e conserva la durata di temporanei, campi e array. I costruttori ereditati restano esclusi. La verifica nativa richiede CI della revisione implementata.

Questi template supportano anche distruttori non virtuali definiti dall’utente, definizioni fuori classe e istanziazioni/specializzazioni esplicite. Serve una definizione quando il frontend integrato ne determina l’uso, anche per la restituzione diretta di un oggetto. Le interrogazioni di tipo e le espressioni non valutate mantengono differiti i corpi inutilizzati, verificando le specifiche di eccezione selezionate. Il corpo precede la distruzione di membri ed elementi degli array in ordine inverso, con memoria esplicita per l’oggetto ricevente. Restano valide le regole di durata per temporanei, riferimenti che prolungano la vita e parametri per valore. Chiamate esplicite al distruttore e C++/STL completo restano incompleti; la verifica nativa richiede CI della revisione implementata.

Questi template supportano costruttori predefiniti, di copia e spostamento, assegnazioni di copia e spostamento e distruttori non virtuali definiti con `= default`, dentro o fuori dalla classe. Le operazioni selezionate conservano ordine dei membri, memoria di origine e destinazione, risultato per riferimento dell’assegnazione e distruzione inversa. L’inizializzazione per valore distingue la prima dichiarazione dal defaulting fuori classe. I corpi inutilizzati restano differiti; le interrogazioni controllano le specifiche di eccezione scritte e risolte. Le operazioni non banali usate richiedono definizioni generate appartenenti ai sorgenti. Una specializzazione esplicita non eredita il defaulting del template primario. C++17/STL completo resta incompleto; la verifica nativa richiede CI della revisione implementata.

Questi template supportano anche operatori e funzioni di conversione definiti dall’utente: assegnazione per copia/spostamento, dereferenziazione, incremento, indice, freccia, oggetti invocabili e conversione bool esplicita. Gli overload selezionati conservano l’ordine C++17, gli alias, la memoria finale e le durate di vita. Corpi e argomenti predefiniti inutilizzati restano differiti; dichiarazioni selezionate e tutti i corpi istanziati mantengono i controlli su sorgenti e definizioni. Definizioni esterne, istanziazioni/specializzazioni esplicite e variabili locali statiche per istanza usano funzioni tipizzate senza parametri template a runtime. Allocazione, intestazioni standard e C++17/STL completo restano incompleti; i risultati nativi richiedono CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-operators-and-conversions).

Le classi locali istanziate nei template ammessi di funzioni libere o membri conservano metodi, costruzione/distruzione, operatori, conversioni e operazioni defaulted selezionate. Si controllano origine, tipi, memoria del ricevitore e variabili statiche per istanza; tutti i corpi istanziati restano ispezionati. Ogni istanziazione esplicita mantiene argomenti, tipi, nomi di conversione, qualificatori e controlli degli attributi, anche con extern o ripetizioni senza effetto. Il riuso di una dichiarazione canonica non nasconde sorgenti non supportati. I risultati nativi richiedono il CI corrispondente; C++17/STL completo resta incompleto. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-local-classes-and-written-directives).

Core v2 supporta anche template di funzioni operatore ordinarie negli spazi dei nomi. Aritmetica, confronti, riferimenti, operatori degli iteratori e risultati record conservano l’overload scelto, l’ordine C++17, la memoria effettiva e la distruzione dei temporanei. ADL, chiamate esplicite, specializzazioni e istanziazioni usano le funzioni tipizzate esistenti, senza parametri template a runtime. I corpi inutilizzati restano differiti; vengono verificati il codice selezionato, tutti i corpi istanziati e ogni direttiva scritta. Forme dipendenti non supportate di template di classi friend, allocazione, header standard e C++17/STL completo restano incompleti; la verifica nativa richiede il CI corrispondente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-operator-function-templates).

Questi template di classi supportano anche membri statici interi, booleani ed enumerati con inizializzazione a zero o costante, inclusi inline/constexpr e definizioni esterne alla classe. Argomenti equivalenti condividono memoria; istanze diverse mantengono oggetti distinti. I valori const non inline verificati non creano memoria, ma l’uso valutato di indirizzi e riferimenti richiede una definizione. Gli inizializzatori inutilizzati restano differiti; quelli istanziati e ogni direttiva esplicita vengono verificati nel sorgente. Effetti del ricevente e distruzione dei temporanei restano preservati. altri tipi statici, C++17/STL completo restano incompleti; la verifica nativa richiede il CI corrispondente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-scalar-static-data).

Core v2 supporta valori predefiniti dei parametri scalari non di tipo nei template di funzioni, operatori e classi ammessi. Parametri precedenti, auto scalare, valori ereditati e conversioni constexpr verificate seguono Clang. Argomenti omessi ed espliciti equivalenti condividono funzioni, record e oggetti statici. Il sorgente non dipendente viene verificato; i valori dipendenti inutilizzati restano differiti. Le conversioni riuscite conservano sorgente originale e convertito; gli errori mantengono la normale scelta di un altro overload. C++17/STL completo resta incompleto; la verifica nativa richiede il CI corrispondente. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#scalar-template-parameter-defaults).

Le conversioni con tipo di ritorno dedotto conservano riferimenti e const, inclusi decltype(auto) e il collasso dei riferimenti auto&&. Il frontend integrato deduce i tipi necessari alla selezione senza istanziare corpi di template non pertinenti che restituiscono valori. Il supporto completo di C++17/STL resta incompleto; la verifica nativa richiede la CI di questa implementazione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deduced-reference-conversions). I nuovi argomenti di riferimento deducono i risultati di conversione necessari prima del controllo di compatibilità. Il filtro per il collegamento diretto non esenta un candidato dalla deduzione del tipo restituito richiesta dalla successiva inizializzazione indiretta; un corpo auto& concorrente non valido mantiene la normale diagnostica C++.

Core v2 supporta pack concreti di tipi e valori scalari nei template ammessi di funzioni, operatori e classi dei namespace. Il limite è 64 parametri per template primario e 64 elementi per pack. I parametri espansi mantengono memoria distinta; sizeof... produce un conteggio tipizzato. Le fold expression risolte preservano ordine, cortocircuito e pulizia. Un conteggio dipendente della stessa lista può rimanere metadato nel tipo scritto di un parametro. I pattern delle fold vuote non vengono istanziati; valori iniziali ed elementi materializzati sono controllati. C++17/STL completo resta incompleto; i risultati nativi richiedono il CI dell’implementazione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-parameter-packs).

Core v2 ammette template alias negli spazi dei nomi propri per tipi scalari, void, puntatori, riferimenti, array fissi e classi supportate, con argomenti predefiniti di tipo o scalari e pacchetti concreti. Gli alias condividono tipi canonici e memoria statica. Argomenti scritti, valori predefiniti selezionati e tipi sorgente sostituiti sono verificati agli usi effettivi; candidati non scelti e corpi dipendenti non istanziati restano differiti. Gli argomenti espliciti conservano il contesto chiamante, anche nella ricorsione e nelle espressioni this. Restano i limiti di 64 parametri, elementi per pacchetto e livelli, oltre al budget sorgente comune. C++17/STL completo è ancora incompleto; la verifica nativa dipende dal CI della revisione. Solo C++ è implementato; E Language, Python e altri frontend sono previsti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-alias-templates).
Gli argomenti template non di tipo selezionati conservano anche il sorgente del tipo del parametro, inclusi gli argomenti espliciti dei template di funzione e i pacchetti vuoti dedotti; il sorgente non supportato eliminato dalla semplificazione di un alias viene comunque controllato.

I tipi predefiniti non dipendenti vengono controllati anche nei template inutilizzati. Le chiamate qualificate ai template di funzione e le definizioni concrete delle funzioni di conversione conservano il sorgente esatto, compresi gli alias nelle definizioni esterne alla classe. Una conversione lvalue può creare un temporaneo ampliato per un parametro con riferimento const; i parametri con riferimento rvalue mantengono la restrizione di overload del C++.
Core v2 supporta anche le specializzazioni parziali dei template di classe negli spazi dei nomi del progetto. Il modello selezionato e i parametri dedotti vengono verificati separatamente dagli argomenti del template primario. Gli alias condividono lo stesso tipo concreto e la stessa memoria statica; i membri supportati mantengono chiamate, riferimenti e durata degli oggetti. Il limite resta di 64 parametri e 64 elementi per pacchetto. Il supporto completo a C++17/STL resta incompleto; la verifica nativa dipende dalla CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-partial-specializations).

Core v2 supporta il tipo di valore distinto `decltype(nullptr)` per alias, riferimenti, campi/array, firme di callback e memoria scalare statica o di template di variabili inizializzata a zero o con costanti. Le conversioni a puntatore mantengono gli effetti delle espressioni e la distruzione dei temporanei; oggetti null distinti conservano indirizzi propri. Gli argomenti di template `nullptr_t` supportano valori predefiniti, pack e contesti sorgente già ammessi, mantenendo i controlli e identità distinte da zero intero e false booleano. Restano esclusi gli argomenti null di tipo puntatore e gli header standard. La verifica nativa richiede la CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#null-pointer-values).

Core v2 supporta costruttori deleganti nelle classi ordinarie e generiche ammesse, comprese catene e template di costruttori selezionati. Il destinatario e il corpo delegante usano lo stesso oggetto finale; i membri vengono inizializzati una volta e gli argomenti temporanei sono distrutti prima del corpo delegante. Restano attivi i controlli di selezione e definizione. La verifica nativa richiede la CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#delegating-constructors).

Core v2 ammette membri const di tipi scalari, record, puntatori, callback e array supportati, anche in classi generiche e annidate. Mantiene memoria finale, copie/spostamenti selezionati, indirizzi distinti e riferimenti/puntatori const. Clang rifiuta scritture non valide e assegnazioni eliminate. Sono possibili classi con chiavi costanti definite nel sorgente; gli header standard e lo STL completo restano da realizzare. La verifica nativa richiede la CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#const-data-members).

Core v2 ammette funzioni eliminate e membri speciali predefiniti definiti come eliminati, comprese le forme supportate di template e friend. Clang mantiene la selezione degli overload e le diagnostiche di accesso; queste dichiarazioni non producono definizioni eseguibili. Oggetti solo spostabili, copie alternative selezionate ed elisione garantita di C++17 conservano la gestione della durata. È richiesta la CI della revisione; C++/STL completo resta incompleto. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deleted-function-declarations).

Core v2 ammette IEEE `float` e `double`, aritmetica e conversioni, riferimenti, campi/array, callback e oggetti scalari statici inizializzati a zero o con costanti. I bit esatti dei letterali e controlli indipendenti della disposizione preservano la rappresentazione. L’esecuzione richiede l’ambiente floating point predefinito, senza matematica veloce, precisione in eccesso o contrazione implicita. È richiesta la CI della revisione; `long double`, header standard e C++/STL completo restano incompleti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#binary-floating-point-values).

Core v2 ammette letterali stretti, UTF-8, UTF-16, UTF-32 e larghi con unità esatte e memoria statica di sola lettura, inizializzazione degli array di caratteri con riempimento a zero e array costanti di namespace. Alias, copie dei campi e durata usano le operazioni tipizzate esistenti. È richiesta la CI della revisione; header standard, allocazione e operazioni di `std::string` restano incompleti. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#string-literals-and-constant-arrays).

Core v2 supporta array modificabili e costanti con inizializzazione a zero o costante negli spazi dei nomi, nelle variabili statiche locali e nei membri statici di classe, comprese le istanze di template ammesse. Stato e indirizzi persistono tra le chiamate; istanze diverse mantengono memoria separata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#fixed-array-static-storage).

Core v2 supporta l’inizializzazione a zero o costante dei puntatori statici a oggetti globali, letterali, elementi di array e campi, inclusi i puntatori validi subito dopo la fine. Memoria di namespace, funzioni, classi e template conserva identità, modificabilità e accesso const. Gli indirizzi definiti più avanti diventano costanti C23 senza inizializzazione a runtime. Riferimenti statici, allocazione e STL completa restano in sviluppo. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-object-pointer-storage).

I riferimenti statici possono legarsi a oggetti statici esistenti, sottooggetti di array o record e stringhe, inclusi riferimenti statici locali, di classe e istanze di template ammesse. Letture e scritture mantengono l’oggetto originale e i permessi const. Vedere il [contratto dei riferimenti statici](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-bindings).

Core v2 supporta temporanei scalari, array e record con inizializzazione costante e durata estesa da riferimenti statici. Conserva oggetti completi, alias dei sottooggetti, puntatori a sé, const e identità delle istanze. La verifica nativa richiede la CI di questa implementazione. In C++17, l’inizializzazione costante di questi temporanei richiede ancora distruzione triviale; i temporanei statici non triviali usano inizializzazione dinamica e registrazione. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-temporary-lifetime-extension).

I record statici supportano inizializzazione costante e stato modificabile condiviso, anche nelle variabili statiche locali, nelle classi e nelle istanze di template ammesse. Gli indirizzi interni, la costruzione constexpr e l’inizializzazione statica a zero preservano l’identità dell’oggetto. Vedere il [contratto dei record statici](../../utils/translate-frontends/docs/cpp-core-v2.md#static-record-objects).


Core v2 ammette catene verificate di basi vuote triviali, con template supportati, metodi ereditati, conversioni e valori statici. Ogni base usa un vero primo membro C con layout verificato indipendentemente. Restano preservati puntatori nulli, identità dei riferimenti, percorsi costanti ed effetti. Cicli di vita non triviali e header standard restano incompleti; la verifica nativa richiede la CI della revisione implementata. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#trivial-empty-base-chains).
## Installazione e traduzione scalare

Usare una normale installazione di NeverC con le risorse standard. Il frontend C++ e gli header SDK approvati sono integrati; non occorre installare Clang separatamente. Vedere le [note di compilazione del frontend](../../utils/translate-frontends/cpp/README.md).

```sh
neverc translate --from cpp input.cpp -o output.nc
neverc output.nc -c -o output.o
```

Il profilo `cpp-core-v1` accetta un singolo file C++17 autonomo senza include. Supporta `int`, `unsigned int`, `bool`, `void`, tipi aggregati semplici, funzioni non membro, spazi dei nomi, sovraccarichi e il flusso di controllo documentato. Ogni dichiarazione in ingresso viene controllata, incluso il codice inutilizzato.

Selezionare `--profile cpp-core-v2` per aggiungere alias `typedef`/`using` verificati, enumerazioni con tipi interi sottostanti supportati, `static_assert`, puntatori a oggetti e riferimenti lvalue entro un ambito limitato. Parametri e risultati per riferimento mantengono gli alias; sono supportati puntatori nulli e qualificazioni `const` annidate. Restano i vincoli di un solo file sorgente e nessun include. Vedere il [contratto core v2](../../utils/translate-frontends/docs/cpp-core-v2.md).

Core v2 aggiunge array locali di dimensione fissa e campi array, indicizzazione multidimensionale e puntatori o riferimenti ad array. L’inizializzazione parziale azzera gli elementi scalari omessi; i record seguono l’inizializzazione selezionata e mantiene l’ordine e gli alias. Dimensioni ed espansione dell’inizializzazione sono limitate. Restano esclusi gli array globali e quelli di lunghezza variabile.

Core v2 supporta `switch`/`case`/`default`, con istruzioni di inizializzazione C++17, passaggio al caso successivo e annotazioni `[[fallthrough]]` convalidate. Il selettore viene valutato una sola volta; switch e cicli annidati mantengono le destinazioni di `break`/`continue`. Restano esclusi gli intervalli case GNU e altri attributi di istruzione.

Core v2 supporta interi con e senza segno a 8, 16, 32 e 64 bit, tipi e letterali carattere e interrogazioni costanti `sizeof`/`alignof`. Promozioni e risoluzione degli overload precedono la normalizzazione; `long`, `wchar_t` e il tipo delle dimensioni seguono la piattaforma. Anche le enumerazioni ammettono tipi sottostanti più stretti o più ampi. La libreria standard è ancora in sviluppo.

Core v2 supporta anche spostamenti e differenze tra puntatori a oggetti, incrementi/decrementi e assegnazioni composte, inclusi attraversamenti di array e passi multidimensionali. Le funzioni ausiliarie generate preservano le regole C++17 per un puntatore nullo più o meno zero e per la differenza tra due puntatori nulli. I confronti d’ordine tra puntatori a oggetti usano indirizzi senza segno verificati indipendentemente sui target nativi x86/AArch64; i risultati nativi richiedono la CI della revisione interessata. Le conversioni puntatore/intero nel sorgente e la STL completa restano da completare.

Core v2 confronta dimensioni e allineamenti ABI dei tipi sorgente con il modello di destinazione di NeverC, comprese le dimensioni delle strutture e gli offset dei campi. Le asserzioni statiche nel codice generato verificano nuovamente la disposizione in compilazione; il manifest ne registra i dati.

Core v2 supporta le funzioni membro nominate non virtuali degli tipi record ammessi, inclusi overload const, metodi qualificati lvalue, metodi statici e `this`. Le chiamate preservano l’identità dell’oggetto originale e valutano il ricevente prima degli argomenti.

Core v2 supporta costruttori ordinari definiti dall’utente per record con layout standard e operazioni di copia supportate. Oggetti locali, campi ed elementi di array vengono costruiti nella memoria finale; i campi seguono l’ordine di dichiarazione. Resta escluso lo svolgimento dello stack per eccezioni.

Core v2 crea un oggetto distinto per ogni parametro record passato per valore e scrive il risultato nella destinazione del chiamante. Costruttori e metodi seguono le stesse regole; le copie richieste e gli alias dei riferimenti sono conservati. Per i tipi triviali idonei, altre implementazioni C++17 possono aggiungere copie di argomenti o risultati.

Core v2 supporta distruttori ordinari definiti dall’utente e distruzione implicita dei membri nelle uscite normali. Oggetti locali, campi ed elementi di array vengono distrutti in ordine inverso; i temporanei al termine dell’espressione completa, dopo aver salvato i valori necessari. Ritorni, rami, cicli, break e continue eseguono la pulizia prevista. I parametri per valore vengono distrutti all’uscita dalla funzione chiamata; gli oggetti restituiti appartengono al chiamante. L’unwinding delle eccezioni resta escluso.

Core v2 esegue costruttori di copia e operatori di assegnazione per copia ordinari definiti dall’utente, con parametro sorgente `R&` o `const R&`. La copia usa la destinazione effettiva e conserva gli effetti collaterali e il riferimento restituito. La sintassi di assegnazione valuta prima l’operando destro; una chiamata esplicita a `operator=` valuta prima il ricevente.

Core v2 supporta anche costruttori predefiniti generati o dichiarati default e distruttori default, inclusi membri record e array annidati. La costruzione usa la destinazione effettiva e l’ordine di dichiarazione; l’inizializzazione per valore azzera solo quando richiesto da C++. Restano valide le diverse regole per default definito fuori dalla classe. I costruttori inutilizzati o presenti solo in espressioni non valutate non richiedono un corpo artificiale.

Core v2 supporta costruttori di copia impliciti ed esplicitamente default, inclusi record annidati e array multidimensionali. Le copie selezionate dei membri usano la destinazione effettiva nell’ordine di dichiarazione e degli elementi; l’indirizzo dell’array sorgente viene valutato una volta. Le copie triviali conservano i puntatori memorizzati. Parametri per valore e ritorni da oggetti sorgente mantengono gli effetti della copia; il trasferimento diretto di prvalue non aggiunge copie.

Core v2 supporta anche l’assegnazione per copia implicita o esplicitamente default. Membri ed elementi degli array mantengono operazioni selezionate e ordine; le copie triviali generate diventano assegnazioni tipizzate senza chiamate esterne di copia della memoria. L’assegnazione triviale conserva i puntatori e restituisce il ricevente effettivo. La sintassi dell’operatore acquisisce prima il riferimento sorgente, la chiamata esplicita al membro prima il ricevente; i valori sorgente vengono letti dopo questi effetti. L’assegnazione non costruisce né distrugge oggetti aggiuntivi.

Core v2 supporta inizializzatori predefiniti dei campi ammessi, con accesso ai membri precedenti, chiamate ordinarie, record annidati e array. Il valore selezionato usa l’oggetto effettivo come `this`; le clausole esplicite dell’aggregato conservano il `this` del chiamante. L’inizializzazione esplicita sostituisce il valore predefinito del membro. Copia e assegnazione implicite/default non lo rieseguono; un costruttore di copia utente può selezionarlo per i membri omessi. Inizializzazione dei membri del costruttore e degli aggregati mantengono i rispettivi confini di distruzione dei temporanei. Template e STL completa restano in sviluppo.

Core v2 supporta riferimenti rvalue a oggetti già esistenti: alias di scalari, puntatori, record e array, parametri e risultati per riferimento, xvalue condizionali e normali metodi qualificati `&&`. `static_cast<R&&>(live)` conserva lo stesso oggetto; le variabili di riferimento rvalue con nome restano lvalue. Overload e copie esistenti seguono la scelta di Clang. I riferimenti non creano responsabilità di distruzione aggiuntive.

Core v2 esegue costruttori e assegnazioni di spostamento ordinari definiti dall’utente da una sorgente viva `R&&` o `const R&&`. La costruzione usa la memoria finale; l’assegnazione conserva modifiche alla sorgente, ordine di valutazione e alias `R&` restituito, anche con ricevitore qualificato `&`/`&&`. I riferimenti rvalue con nome scelgono ancora gli overload lvalue; i costruttori espliciti mantengono le regole di inizializzazione. Lo spostamento non termina la vita della sorgente: sorgente e destinazione vengono distrutte normalmente.

Core v2 supporta anche costruzione e assegnazione di spostamento implicite o esplicitamente default, con record annidati e array multidimensionali. I membri eseguono la copia o lo spostamento scelto da C++ nell’ordine di dichiarazione e degli elementi. Gli spostamenti generati non ripetono gli inizializzatori predefiniti; le operazioni triviali conservano i valori dei puntatori. Le sorgenti vengono distrutte normalmente e le operazioni generate inutilizzate o triviali non richiedono un corpo inventato.

Core v2 supporta dichiarazioni standard risolte `noexcept`, `noexcept(true/false)` e `throw()` di C++17, oltre alle query costanti `noexcept(expression)`. Le query rispettano le specifiche di funzioni e distruttori selezionati senza eseguire gli operandi. Tutti gli operandi e le specifiche scritte vengono controllati, anche nel codice inutilizzato. Lancio e cattura delle eccezioni, unwinding dello stack, template e STL completa restano in sviluppo.

Core v2 supporta operatori ordinari sovraccaricati, membri o liberi: aritmetica, confronti, indicizzazione, dereferenziazione, incrementi, oggetti funzione e firme generali di assegnazione. Conserva funzioni selezionate, alias di riferimenti e oggetti risultato. La notazione operatore rispetta l’ordine C++17; gli operatori logici sovraccaricati valutano entrambi gli operandi. Gli operatori liberi di assegnazione inizializzano i parametri da destra a sinistra e li distruggono in ordine inverso. Template, allocazione e STL completa restano in sviluppo.

Core v2 supporta anche funzioni di conversione ordinarie su oggetti vivi: conversioni intere, di enumerazioni e puntatori implicite o esplicite, `bool` esplicito nelle condizioni e risultati come riferimento o oggetto. Ogni conversione chiama una volta il membro selezionato. I riferimenti conservano gli alias; i prvalue oggetto inizializzano direttamente la destinazione effettiva. Le conversioni da riferimento a valore mantengono la copia o lo spostamento selezionato. Qualificatori const e di riferimento, constexpr e noexcept seguono C++17.

Core v2 supporta ora ricevitori temporanei e argomenti temporanei scalari o record passati per riferimento fino alla fine dell’espressione completa, per costruttori, metodi, operatori e conversioni. Ogni valutazione usa memoria effettiva; gli oggetti risultato conservano la destinazione finale e i riferimenti gli alias. Dopo la chiamata e la distruzione dei parametri, i temporanei vengono distrutti nell’ordine inverso di costruzione, anche nelle condizioni e nei cicli. Sono supportati i sotto-oggetti array dei record temporanei.

Core v2 estende ora la durata dei temporanei legati a normali riferimenti locali automatici, inclusi `const T& r{T{...}}` e `T&& r = T{...}`. Scalari, enumerazioni, puntatori e record hanno memoria effettiva. I riferimenti a membri o elementi di array mantengono vivo il record completo quando C++ consente l’estensione. L’inizializzazione con parentesi graffe, anche dopo un segno uguale, conserva lo stesso oggetto. La distruzione segue l’ambito del riferimento, incluse condizioni, cicli e uscite anticipate; gli altri temporanei nell’inizializzatore terminano con la propria espressione completa. Gli alias non aggiungono proprietari e copie o spostamenti successivi conservano le operazioni selezionate. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 supporta anche array temporanei autonomi a dimensione fissa nelle chiamate, conversioni a puntatore, indicizzazione, espressioni scartate e riferimenti locali automatici. Ogni array ha una destinazione effettiva; valori scalari, costruttori e inizializzatori predefiniti condivisi inizializzano direttamente gli elementi in ordine, senza proprietari aggiuntivi. Quando C++ estende la durata, i riferimenti a righe o elementi multidimensionali conservano l’array completo. Gli elementi vengono distrutti in ordine inverso alla fine dell’espressione completa o dell’ambito del riferimento. Restano gli indirizzi tipizzati e i limiti esistenti di dimensione, memoria ed espansione. C++/STL completa resta in sviluppo.

Core v2 supporta classi vuote a layout standard e oggetti funzione o conversione senza stato. Gli oggetti vuoti mantengono dimensione e allineamento C++ di un byte, memoria distinta quando richiesta, costruttori e operatori selezionati e distruzione normale. Le copie banali valutano comunque gli operandi; gli elementi vuoti di array e record contenitori rientrano nei limiti di memoria. Il codice NC generato usa un byte interno e la lista dei campi sorgente resta vuota. Ereditarietà, template e STL completa sono ancora in sviluppo.

Core v2 supporta conversioni a void, `void()` e `void{}`, insieme ad alias void compatibili, chiamate, ritorni, espressioni virgola e rami condizionali. Scartare un lvalue non volatile conserva gli effetti del ricevitore e dell’indice senza leggerne il valore memorizzato. Gli oggetti temporanei vengono comunque costruiti e distrutti alla fine dell’espressione completa originale. Non vengono generate variabili o valori void. Gli operandi costanti e noexcept restano verificati. Oggetti volatile, tipi non supportati, template e STL completa restano fuori da questa fase.

Core v2 supporta argomenti predefiniti per funzioni, metodi, operatori di chiamata e costruttori utente ammessi, inclusi parametri finali dei costruttori di copia e spostamento. I nomi vengono risolti alla dichiarazione e le espressioni valutate a ogni chiamata che omette l’argomento. Identità e durata di riferimenti e valori restano invariate. Gli elementi di array senza inizializzatore e le copie generate distruggono i temporanei degli argomenti predefiniti prima dell’elemento successivo; le clausole esplicite conservano il limite dell’espressione completa esterna. Anche i valori inutilizzati o sostituiti vengono verificati. Template e STL completa restano incompleti.

## Progetti con più file

Selezionare esplicitamente le unità di traduzione da un database di compilazione e specificare la directory radice del progetto. Il frontend integrato analizza ogni unità separatamente; la fusione verifica definizioni, collegamento, tipi condivisi e il rispetto della regola di definizione unica (ODR) mediante verifiche conservative.

```sh
neverc translate --from cpp --profile cpp-project-v1 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/a.cpp src/b.cpp --out-dir generated
```

L’output del progetto comprende `translated.nc` e `translated.h`. Sono ammessi solo gli header del progetto contenuti in tale directory. Se un file ha più configurazioni, selezionare l’indice a base zero con `--compdb-entry src/a.cpp=4`. I comandi del compilatore memorizzati vengono analizzati come dati e mai eseguiti.

## Matematica limitata a doppia precisione

`cpp-math-v1` aggiunge ai progetti `double`, le conversioni/i confronti documentati ed esattamente `std::fabs(double)` e `std::floor(double)`. Usa gli header integrati di Clang 20.1.8 / libc++ 200100 / macOS 15.5 e richiede un target macOS 15.0 esplicito (arm64 o x86_64). L’aritmetica generale in virgola mobile resta esclusa. Le eccezioni devono essere mascherate e l’azzeramento dei subnormali disattivato; sono testate tutte e quattro le modalità standard di arrotondamento.

```sh
neverc translate --from cpp --profile cpp-math-v1 \
  --target arm64-apple-macosx15.0.0 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/math.cpp --out-dir generated-math
```

La traduzione matematica verifica anche gli header NeverC installati, l’identità delle implementazioni incorporate e un collegamento effettivo. I moduli generati usano il runtime NeverC. Con `-fno-builtin-std`, la traduzione fallisce prima di scrivere l’output solo se il codice finale richiede le mappature `fabs`/`floor`. Il codice matematico che non le richiede può comunque essere tradotto.

## Convalida e output

Usare `--check` al posto di un’opzione di output per eseguire la stessa analisi, generazione e convalida sintattica e degli oggetti senza conservare i file generati. `--report PATH` scrive diagnostica strutturata. La traduzione non esegue mai il programma sorgente.

Output e file accessori non vengono sovrascritti. `--out-dir` richiede una nuova directory con una directory padre esistente; `-o` richiede un nuovo percorso `.nc`. Il manifesto registra requisiti del target, hash di input e output e procedura di compilazione; la mappa sorgente collega le righe generate alle posizioni originali.

I risultati CI, le verifiche di esecuzione e installazione e i test saltati sono documentati per piattaforma. macOS arm64 nativo e macOS x86_64 tramite Rosetta restano ambienti di convalida distinti. L’ambito dichiarato non comprende il supporto completo di C++/STL, né eccezioni, template, stringhe o `std::vector`. Consultare la [matrice di supporto](../../utils/translate-frontends/docs/support-matrix.md), il [protocollo e le regole di ripristino](../../utils/translate-frontends/docs/protocol.md) e il [progetto di esempio](../../tests/neverc/Inputs/translate/cpp/project).
