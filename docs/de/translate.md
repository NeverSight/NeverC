**Sprachen**: [English](../translate.md) | [简体中文](../zh-CN/translate.md) | [繁體中文](../zh-TW/translate.md) | [日本語](../ja/translate.md) | [한국어](../ko/translate.md) | [Français](../fr/translate.md) | [Deutsch](translate.md) | [Español](../es/translate.md) | [Italiano](../it/translate.md) | [Русский](../ru/translate.md) | [العربية](../ar/translate.md)

[← Dokumentation](README.md)

# C++ nach NeverC übersetzen

Core v2 unterstützt die standardmäßige Weiterleitung von sized delete: Ein implizites globales sized delete/delete[] ohne erneute Quelldeklaration kann den entsprechenden im Quelltext definierten unsized-Operator aufrufen. Explizite sized-Definitionen haben Vorrang; das ursprüngliche Array-Cookie-Layout bleibt erhalten. Standardallokator und Ausnahmelaufzeit werden nicht ergänzt; die native Prüfung benötigt die CI dieser Revision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

Core v2 unterstützt auch geprüfte `__array_rank`- und `__array_extent`-Abfragen mit nichtnegativen konstanten Ganzzahlindizes, einschließlich Templateindizes für feste Arraytypen. Beide Operanden werden vor der Konstantenfaltung geprüft. Nicht unterstützte Arraytypen und implizit aus Klassen konvertierte Indizes bleiben ausgeschlossen. Native Ergebnisse benötigen die CI der implementierenden Revision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#array-type-queries).

Core v2 ergänzt geprüfte eingebaute Typklassifizierung, darunter `__is_integral`, `__is_pointer` und `__is_same`. Abfragen bewahren die C++-Typidentität und prüfen die geschriebenen Operanden vor der Erzeugung eines booleschen Werts. Nicht unterstützte Typen und andere Typeigenschaften bleiben eingeschränkt. O0/O2- und Relokationstests benötigen die CI der implementierenden Revision; Standardheader und vollständiges C++/STL sind noch nicht fertig. Für unterstützte Typen lassen sich auch Aggregat-, Leerklassen-, Standardlayout-, Trivialitäts-, triviale Kopier- und POD-Eigenschaften, Polymorphie, Abstraktheit sowie Basisbeziehungen mit `__is_base_of` abfragen. Auch standardmäßige `final`-Klassen sowie `__is_final`, `__is_literal` und `__has_unique_object_representations` werden unterstützt. Quellseitige Füllbytes, die Darstellung leerer Klassen und C++17-Literaltypregeln bleiben maßgeblich; andere Attribute und virtuelle Methoden behalten ihre Einschränkungen. Destruierbarkeit und triviale Destruktion gelten nun auch für zugelassene Klassen, private oder gelöschte Destruktoren und Referenzen; Template-Rümpfe und Ausnahmespezifikationen bleiben verzögert. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#builtin-type-classification).

Core v2 unterstützt Abfragen zu Konstruktion, Zuweisung, Konvertierung und Zerstörung für zugelassene Nichtklassentypen, Zeiger, Referenzen und Arrays fester Länge. Gespeicherte Ausdrücke der Klassenoperationen erlauben implizite triviale Operationen und gewöhnliche Operationen mit geprüften Definitionen, Argumentkonvertierungen und Destruktorabhängigkeiten. Konstruktionsabfragen erlauben außerdem unveränderte gewöhnliche Standardargumente nach Prüfung des genauen Parameters, seines Ausdrucks und der Bereinigung temporärer Objekte. Die Abfragen führen keine Seiteneffekte aus. nothrow-Standardargumente erfordern außerdem bereits aufgelöste Ausnahmespezifikationen der tatsächlichen Aufruftypen. Die nothrow-Zerstörung von Klassenwerten und festen Arrays speichert den ausgewählten Destruktor und den aufgelösten Prototyp und prüft implizite Zerstörung, gewöhnliche explizit voreingestellte Destruktoren oder vollständig geprüfte gewöhnliche benutzerdefinierte Zerstörung. Frühe Ergebnisse bei gelöschten oder unzugänglichen Destruktoren und bei Referenzen bleiben erhalten. Template-Operationen, geschriebene Template-Destruktoren und unaufgelöste Ausnahmeabhängigkeiten bleiben unvollständig. O0/O2, Relokation und acht ABIs müssen in der CI dieser Revision geprüft werden; Standardheader und vollständiges C++/STL sind noch nicht fertig. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#non-record-operation-traits).

Core v2 ergänzt direkte `malloc/calloc/free`-Aufrufe über exakt geprüfte globale C-Deklarationen im Quelltext. Eigene C++-Allokatoren können den nativen Heap nutzen und Speicher mit einem unabhängigen C-Aufrufer austauschen und freigeben. Typbreite, Aufrufkonvention, Quelltext und IR bleiben geprüft. O0/O2-Prüfungen benötigen CI der Implementierung. `realloc`, standardmäßige C++-Allokation mit Ausnahmen, Standardheader und vollständiges C++/STL sind noch nicht fertig. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#native-c-heap-calls).

Core v2 registriert die Destruktion zugelassener statischer Objekte, Arrays und temporärer Objekte mit verlängerter Lebensdauer nach ihrer vollständigen Konstruktion. Konstante Werte bleiben erhalten; lokale Objekte registrieren sich beim ersten Erreichen, nichtlokale beim nativen Start. Die CRT bewahrt die Reihenfolge bei Programmende und Modulentladung. O0/O2-, Entlade- und konkurrierende Erstzugriffstests benötigen CI dieser Implementierung. TLS, Exception-Unwinding, Standard-Heap, Standardheader und vollständiges C++/STL bleiben offen; manuelles/DynCode-Laden ist ausgeschlossen. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-destruction).

Core v2 unterstützt jetzt die nichtlokale dynamische Initialisierung zugelassener Objekte und Referenzen einschließlich materialisierter Templateinstanzen. Ein geprüfter interner nativer Konstruktor läuft nach der Null- und Konstanteninitialisierung vor main in Definitionsreihenfolge; gewöhnliche temporäre Objekte werden nach jedem Initialisierer zerstört. Lokale statische Objekte behalten die Initialisierung bei erster Verwendung. O0/O2-Tests für Programme und separate C-Clients benötigen CI dieser Revision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#nonlocal-dynamic-initialization).

Laufzeitlängen für new[] unterstützen nun auch im Quellcode definierte noexcept-Klassenallokatoren ohne placement-Argumente. Ungültige Längen liefern null vor dem Allokatoraufruf; gültige Elemente werden in einer Schleife konstruiert, deren Standardargumente pro Element aufgeräumt werden. Temporäre Objekte expliziter Anfangselemente und der Längenkonvertierung leben bis zum Ende des vollständigen Ausdrucks. Wiederholte Aggregate können nun ohne zusätzliche temporäre Objekte direkt im jeweiligen Element initialisiert werden, auch mit verschachtelten Listen und Referenzen auf lebende Objekte. Initialisierungen mit weiteren temporären Objekten, werfende oder placement-Laufzeitallokation, Standard-Heap und vollständiges C++/STL bleiben offen. Native Prüfung steht in der Implementierungs-CI aus. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#runtime-array-allocation).

Core v2 unterstützt new/delete für einzelne Objekte über geprüfte Allokationsfunktionen im Quellcode, einschließlich Placement-Überladungen von Klassen und Templates, Speicheridentität und Argumentbereinigung. Explizite Destruktion und Placement-Neukonstruktion behalten die spätere automatische Bereinigung bei. Native Prüfung erfordert die CI dieser Implementierung. Standard-Heap-Laufzeit, Laufzeitlängen für new[], Ausnahmen, Standardheader und vollständiges C++/STL sind noch offen. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#single-object-allocation-and-placement-reuse).

Das experimentelle `neverc translate` erzeugt prüfbaren `.nc`-Quelltext mit `cpp-core-v1`, `cpp-core-v2`, `cpp-project-v1` und `cpp-math-v1`.

**Derzeit ist nur die Übersetzung von C++-Quelltext implementiert.** Unterstützung für E Language (易语言, `.e`), Python, Go, Rust, TypeScript und JavaScript ist geplant; entsprechende Übersetzer sind noch nicht verfügbar.

Partielle Spezialisierungsdeklarationen behalten die Prüfung ihrer primären Argumente, auch bei kopierten Member-Variablen mit abhängigen inneren Argumenten. Bereits aufgelöste Parametertypquellen werden auch ohne Auswahl der Spezialisierung geprüft. Pack-Erweiterungen unbekannter Länge behalten das geprüfte Präfix und den tatsächlichen Quelltext, ohne Argumentpositionen zu erfinden. Abhängige vollständige Member-Variablenspezialisierungen im Klassenbereich behalten dieselben Prüfungen. Vollständiges C++/STL bleibt offen. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#partial-declaration-source-checks).

Das Aufbewahren einer Argumentprüfung macht eine partielle Spezialisierung nicht automatisch gültig: Sie muss weiterhin stärker spezialisiert sein und ihre Parameter müssen deduzierbar sein. Ungültige Packmuster und eine extern-Instanziierung nach einer Instanziierungsdefinition bleiben C++-Diagnosen (`TR0202`).

Core v2 unterstützt benannte gewöhnliche verschachtelte Klassen in zugelassenen generischen Klassen, ihre konkreten Kopien und eigene explizite Mitgliedsspezialisierungen. Gewöhnliche Klassenrümpfe fügen keine Templateparameterebene hinzu. Unbenutzte Rümpfe bleiben verzögert; instanziierte Felder, ausgewählte Methoden, Mitgliedstemplates, Lebensdauer und skalare statische Objekte behalten ihre genaue Quelle und Zuordnung. Explizite Instanziierungen prüfen jeden geschriebenen Qualifizierer, auch bei extern und wirkungslosen Wiederholungen. Vererbung, Allokation und vollständiges C++/STL bleiben offen. Nur C++ ist als Eingabe implementiert; E Language und Python sind geplant. Eingebettete Clang-Bibliotheken benötigen kein externes Clang-Programm. Native Ergebnisse erfordern CI der Implementierungsrevision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-nested-classes-in-generic-owners).

Core v2 unterstützt skalare statische Member-Variablentemplates in zugelassenen Records und konkreten Klasseninstanzen, einschließlich Standardargumenten, begrenzten Packs, partiellen/vollständigen Spezialisierungen und Definitionen außerhalb der Klasse. Äquivalente Instanzen teilen Speicher; andere innere oder äußere Argumente behalten eigene Objekte. Geprüfte nicht-inline const-Werte benötigen zum Lesen keinen Speicher, ausgewertete Adressen und Referenzen aber eine Definition. Quellprüfungen beider Deklarationen, Empfängereffekte und die Zerstörung temporärer Objekte bleiben erhalten. Vollständige Spezialisierungen innerhalb abhängiger äußerer Klassen behalten die Quellen der ursprünglichen und konkreten Deklarationen. Aufgelöste Argumente, Standardwerte und Parametertypen werden sofort geprüft; äußerlich abhängige Quellen erst nach der Instanziierung. Andere Ergebnistypen, vollständiges C++/STL bleiben offen. Eingebettete Clang-Bibliotheken benötigen kein externes Clang. Nur C++ ist als Eingabe implementiert; E Language, Python und weitere Frontends sind geplant. Native Prüfung erfolgt im CI der Implementierung. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-variable-templates).

Core v2 unterstützt Member-Aliastemplates in zugelassenen gewöhnlichen Records einschließlich verschachtelter Records und konkreten Klassentemplate-Instanzen. Standardargumente und begrenzte Packs dürfen äußere Klassenparameter und innere Aliasparameter verwenden; ihre Argumente und substituierten Quelltypen werden getrennt geprüft. Aliase erhalten die vorhandene Wert-, Referenz-, Array- und Record-Semantik ohne Templateobjekte zur Laufzeit. Nichtabhängige Deklarationen werden auch unbenutzt geprüft; abhängige Typen bleiben bis zur Substitution verzögert. Eingebettete Clang-Bibliotheken benötigen kein externes Programm. Native Prüfung erfolgt im CI der Implementierung. Vollständiges C++/STL bleibt offen. Nur C++ ist als Eingabe implementiert; E Language, Python und weitere Frontends sind geplant. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-alias-templates).

Core v2 unterstützt konkrete Member-Funktionstemplates in zugelassenen Records und Klassentemplate-Instanzen: benannte/statische Methoden, Operatoren, Konstruktoren und Konvertierungen mit begrenzten Packs, Standardargumenten, externen Definitionen und Spezialisierungen. Innere und äußere Argumente behalten eigene Identitäten; äquivalente Aufrufe teilen Code und statischen Speicher. Gewählte Aufrufe, Standardwerte und instanziierte Körper behalten Quell- und Lebensdauerprüfungen. Die Übersetzung nutzt eingebettete Clang-Bibliotheken ohne externen Clang-Prozess. Native Ergebnisse erfordern die Implementierungs-CI. Vererbung, Allokation und vollständiges C++/STL bleiben offen. Nur C++ ist als Eingabe implementiert; E Language, Python und weitere Frontends sind geplant. Konkrete äußere Qualifizierungen an Member-Templatedeklarationen werden auch ohne instanziierten Funktionskörper geprüft. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-member-function-templates).

Core v2 unterstützt Variablen-Templates auf Namensraumebene mit Ganzzahl-, booleschen oder Enum-Ergebnissen, einschließlich deduziertem auto, ausgewählten partiellen und vollständigen Spezialisierungen, Standardargumenten und begrenzten Typ-/Wertepacks. C++17-Traits können static_assert, if constexpr und weitere Template-Argumente speisen. Äquivalente Instanzen teilen einen typisierten globalen Speicher; unterschiedliche Instanzen behalten eigene Werte und Adressen. Abfragen eines festen Typs erzwingen weder einen unbenutzten Initialisierer noch Speicher. Tatsächliche Argumente, ausgewählte Standardwerte, Deklarations- und Definitionstypen sowie instanziierte Initialisierer bleiben geprüft. andere Ergebnistypen, vollständiges C++/STL bleiben offen; native Validierung erfordert die CI der Implementierungsrevision.

Direktes sizeof(T) oder alignof(T) im geschriebenen Typ eines Nichttypparameters darf Deklarationsmetadatum bleiben, wenn T genau der Typparameter derselben Liste ist. Ausgewählte Substitutionen benötigen weiterhin konkrete geprüfte Typen; andere abhängige Abfrageformen sind nicht umfasst. Eine extern-template-Deklaration allein liefert nicht die konkrete Definition, die der aktuelle Übersetzungsvertrag für eine geschlossene Einheit verlangt.

Core v2 verwendet auf allen unterstützten Zielen, einschließlich Windows x64 und ARM64, die standardmäßige C++17-Templateanalyse. Template-Definitionen werden ohne verzögertes MSVC-Parsing verarbeitet; doppelte explizite Instanziierungsdefinitionen und unvereinbare Ausnahmespezifikationen bleiben Sprachfehler. Nicht verwendete abhängige Funktionsrümpfe werden weiterhin nur bei Bedarf instanziiert. Die vollständige Unterstützung von C++/STL ist noch nicht abgeschlossen. Explizite Funktionsspezialisierungen benötigen weiterhin eine eigene Definition in der Quelldatei; wiederholte Deklarationen allein liefern diese nicht.

Core v2 unterstützt veränderliche globale Ganzzahl-, boolesche und Enum-Variablen auf Namensraumebene mit Nullinitialisierung oder vollständig geprüfter konstanter Initialisierung. Speicher und Adresse bleiben über Aufrufe hinweg erhalten; Referenzen, Zeiger und Standardargumente greifen auf dieselbe Variable zu. Eine `extern`-Deklaration benötigt eine Definition in derselben Quelldatei. Const-Globale bleiben schreibgeschützt. Für globale Records/Arrays/Zeiger/Referenzen, threadlokalen Speicher und statische lokale Variablen gelten weiterhin die jeweiligen Einschränkungen.

Core v2 unterstützt C++17-Bereichsschleifen mit `for` für unterstützte feste Arrays und im Quelltext definierte Bereiche mit aufgelösten Member- oder ADL-Aufrufen von `begin/end`. Wert- und Referenzvariablen, Record-Iteratoren und unterschiedliche Endtypen behalten die normalen Aufruf- und Lebensdauerregeln. Bereichsinitialisierung und `begin/end` laufen einmal; Iterationsobjekte werden vor dem Inkrement oder Verlassen zerstört, auch bei `continue`, `break` und `return`. Klassen-Templates, Standardheader, STL-Container, strukturierte Bindungen und C++20-Initialisierungsanweisungen bleiben außerhalb dieser Erweiterung. Native Validierung erfordert CI der implementierenden Revision.

Core v2 unterstützt private und geschützte Datenmember in ansonsten unterstützten Standard-Layout-Klassen. Das eingebettete Clang prüft den Zugriff vor der Übersetzung; zulässige Methoden, Konstruktoren, Fabrikfunktionen, Standardargumente sowie generierte Kopier- und Verschiebeoperationen verwenden denselben typisierten Memberspeicher. Unzulässiger externer Zugriff bleibt eine C++-Diagnose. Zugriffsspezifizierer sind Quellsprachregeln und bieten keine Geheimhaltung zur Laufzeit. Klassen ohne Standardlayout mit gemischten Zugriffsebenen, Nicht unterstützte abhängige Friend-Klassentemplate-Formen, Vererbung und nicht unterstützte Feldtypen bleiben eingeschränkt. Native Ergebnisse erfordern CI der implementierenden Revision.

Core v2 instanziiert gewöhnliche Friend-Funktionen aus zugelassenen Klassentemplates, partiellen Spezialisierungen, Member-Klassentemplates sowie kopierten vollständigen oder gewöhnlichen verschachtelten Klassen. ADL, privater Zugriff, freie Operatoren, erneute Deklarationen, lokale Klassen und skalare statische Variablen behalten ihre Identität ohne impliziten Empfänger. Unbenutzte Rümpfe bleiben verzögert; ausgewählte Definitionen und ursprüngliche Signaturen werden geprüft. Eine andere normalisierte Deklaration erfordert vollständige unabhängige Quellnachweise samt eigenen Standardargumenten und aufgelöstem noexcept. Nicht unterstützte abhängige Friend-Klassentemplate-Formen, Friend-Typ-Pack-Erweiterungen, vollständiges C++/STL bleiben offen. Bestehende Regeln für unabhängige Friend-Typen und Member-Friends gelten weiter. Nur C++ ist implementiert; E Language und Python sind geplant. Eingebettete Clang-Bibliotheken benötigen kein externes Clang-Programm. Native Validierung erfordert CI des Implementierungscommits. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-functions).

Core v2 unterstützt aufgelöste Nicht-Template-Friend-Typen in zugelassenen generischen Klassen: `friend T;`, Aliase, abhängige verschachtelte Typen und Klassenspezialisierungen. Das eingebettete Sema prüft privaten Zugriff und rechtmäßig ignorierte Nicht-Klassentypen. Ursprüngliche und ersetzte Typquellen werden geprüft; Kopien ohne belegten Ursprung, Nicht unterstützte abhängige Friend-Klassentemplate-Formen und Typ-Pack-Erweiterungen bleiben ausgeschlossen. Es entstehen keine zusätzlichen Empfänger oder Speicherobjekte. Native Ergebnisse benötigen CI des Implementierungscommits; vollständiges C++/STL bleibt offen. Nur C++ ist implementiert; E Language und Python sind geplant. Clang ist ohne externes Programm eingebettet. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-non-template-friend-types).

Core v2 unterstützt Friend-Funktionstemplates in zugelassenen gewöhnlichen und generischen Klassen: unterstützte Operatoren, ADL, privaten Zugriff, innere und äußere Parameter, Standardwerte und Packs. Referenzen, Objektlebenszeiten sowie getrennte Instanz- und Speicheridentitäten bleiben erhalten. Ursprüngliche Deklarationen und der Kontext des gewählten Funktionskörpers werden geprüft. Nicht unterstützte abhängige Friend-Klassentemplate-Formen, Kopien ohne belegten Ursprung und vollständiges C++/STL bleiben offen; native Ergebnisse benötigen CI des Implementierungscommits. Nur C++ ist implementiert; E Language und Python sind geplant. Clang ist ohne externes Programm eingebettet. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-function-templates).

Core v2 unterstützt gewöhnliche Friend-Klassentemplate-Deklarationen in zugelassenen gewöhnlichen und generischen Klassen: neue Namen im Namensraum, vorhandene und unterstützte qualifizierte Ziele sowie Zugriff durch deren Spezialisierungen. Wiederholte Berechtigungen teilen die Zielklasse und ihren statischen Speicher. In der Friend-Deklaration geschriebene Standardargumente werden abgelehnt; geerbte Standardargumente behalten ihre Quellprüfung. Nicht unterstützte abhängige Friend-Formen und vollständiges C++/STL bleiben offen; native Ergebnisse benötigen CI des Implementierungscommits. Nur C++ ist implementiert; E Language und Python sind geplant. Clang ist ohne externes Programm eingebettet. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#friend-class-templates).

Core v2 unterstützt typisierte Zeiger auf definierte gewöhnliche freie Funktionen und statische Methoden: indirekte Aufrufe, Callback-Parameter und Rückgaben, Referenzen, Arrays, Felder sowie konstant initialisierte globale, lokal statische und gewöhnliche statische Member-Speicher. Das Aufrufziel wird vor den Argumenten gespeichert. Zulässig sind nur die Standard-ABI und unterstützte skalare beziehungsweise Referenzsignaturen. Objekte als Werte, nichtstatische Member-Zeiger, Variadics, Lambdas und Callbacks über Übersetzungseinheiten bleiben offen. Vollständiges C++/STL ist nicht fertig; native Validierung benötigt CI. Nur C++ ist implementiert; E Language und Python sind geplant. Clang ist ohne externes Programm eingebettet. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#ordinary-function-pointers).

Core v2 unterstützt auch Zeiger auf unterstützte konkrete Instanzen freier und statischer Member-Funktionstemplates, mit expliziten Argumenten oder Ableitung aus dem erwarteten Zeigertyp. Quelltext, Definition und ABI werden weiterhin geprüft. Direkte und indirekte Aufrufe teilen dieselbe Funktion und deren lokale statische Objekte; verschiedene Instanzen bleiben getrennt. Vollständiges C++/STL ist noch nicht fertig; native Prüfung erfolgt in CI. Nur C++ ist implementiert; E Language und Python sind geplant. Clang-Bibliotheken sind eingebettet, ohne externes Clang-Programm. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-function-template-pointers).

Core v2 unterstützt auch konstant oder mit null initialisierte Callbacks in Namespace- und statischen Member-Variablentemplates. Gleiche Instanzen teilen den Speicher; verschiedene bleiben auch bei gleichem Funktionsziel unabhängig. Quellprüfungen für direkt geschriebenes auto bleiben bestehen; Zeigerwerte als Templateargumente bleiben ausgeschlossen. Vollständiges C++/STL ist noch nicht fertig; native Prüfung erfolgt in CI. Eingabe nur C++; E Language und Python sind geplant. Clang ist ohne externes Programm eingebettet. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#callback-variable-templates).

Core v2 unterstützt benannte verschachtelte Records ohne Klassen-Templates, auch private und geschützte Typen über zulässige Aliase oder Fabrikfunktionen. Eigener Speicher und Empfänger, explizite Referenzen auf äußere Objekte, Typidentität pro Gültigkeitsbereich, Kopieren und Verschieben von Membern und Arrays, Zerstörung und verschachtelte Iteratoren behalten ihre normale Semantik. Wertabhängigkeiten werden zuerst ausgegeben. Anonyme verschachtelte Records, Klassen-Templates, Vererbung und vollständige STL-Unterstützung stehen noch aus; native Validierung benötigt CI der implementierenden Revision.

Core v2 unterstützt definierte statische Datenmember mit Ganzzahl-, booleschem oder Enum-Typ, einschließlich Inline-/constexpr- und ausgelagerter Definitionen mit konstanter oder Nullinitialisierung. Alle Instanzen teilen denselben typisierten Speicher. Empfängereffekte und temporäre Zerstörung bleiben erhalten; Referenzen auf statische Member überleben temporäre Empfänger. Nicht-inline const-Member mit Ganzzahl-, booleschem oder Enum-Typ und geprüfter konstanter Initialisierung in der Klasse können auch ohne separate Definition als Wert gelesen oder verworfen werden. Dabei entsteht kein globales Objekt; für die Adresse oder eine Referenz auf das Member bleibt eine Definition in derselben Quelldatei erforderlich. andere statische Typen und vollständige STL-Unterstützung stehen aus; native Validierung benötigt CI der implementierenden Revision.

Core v2 erhält konstante und synchronisierte Erstinitialisierung statischer lokaler Objekte und Referenzen. Initialisierer dürfen Parameter, automatische Variablen und this verwenden. Ein lockfreier 32-Bit-Guard veröffentlicht Konstruktion und temporäre Bereinigung; konstante Objekte mit Destruktion verwenden ihn nur zur Registrierung. Referenzen bewahren Aliasse und verlängern zulässige temporäre Lebensdauern in dauerhaftem Speicher. Destruktion folgt dem obigen Vertrag; TLS und statische Deklarationen in constexpr-Funktionen bleiben ausgeschlossen. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-initialization) [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#dynamic-local-static-temporaries)

Core v2 unterstützt Objektreferenz-Member, auch für Arrays, Zeiger und Funktionszeiger. Konstruktion speichert die Bindung; Memberzugriff und Kopieren/Verschieben erhalten Aliasse. Const-Container behalten veränderbare Referenzziele. Aggregatinitialisierung erhält verlängerte temporäre Lebenszeiten und die Destruktionsreihenfolge, einschließlich synchronisierter lokaler statischer Gruppen und eigener temporärer Objekte für Laufzeit-Arrayfüller. Temporäre Objekte in ausgelassenen Arrayelementen erhalten innerhalb der Ressourcengrenzen eigene semantische Identitäten; direkte Standardkonstruktoren der Elemente behalten ihre Bereinigungsgrenzen für temporäre Argumente. Vollständiges C++/STL bleibt unvollendet; native Validierung erfordert CI der implementierenden Revision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 unterstützt Inline-Namespaces, Namespace-Aliasse sowie using-Direktiven und aufgelöste using-Deklarationen in Namespace- und Blockbereichen. Importierte Funktionen, Variablen, Typen und unbeschränkte Enumeratoren behalten ihre Identität, Namensauflösung, Zugriffskontrollen und Objektlebensdauer. Inline-Namespaces bewahren die gemeinsame Suche mit dem übergeordneten Namespace und die argumentabhängige Suche in beide Richtungen, auch bei Verschachtelung und Wiederöffnung. Klassenmember-Importe, geerbte Konstruktoren, Klassen-Templates sowie C++20-Enumeratorimporte und explizite verschachtelte inline-Syntax sind nicht enthalten; vollständiges C++/STL ist weiterhin nicht erreicht.

Core v2 unterstützt C++17 if constexpr in gewöhnlichen Funktionen und zugelassenen konkreten Funktionstemplate-Instanzen. Nur der gewählte Zweig wird erzeugt; Initialisierungsspeicher und Bereinigung bleiben erhalten. Bei gewöhnlichen Funktionen werden beide Quellzweige geprüft; Clang instanziiert verworfene abhängige Zweige nicht. Die bisherigen Definitionsanforderungen für gewöhnliche Funktionen und globale Variablen gelten weiter. C++23 if consteval bleibt ausgeschlossen.

Core v2 unterstützt freie Funktionstemplates mit bis zu 64 Parametern, die unterstützte Typen mit Ganzzahl-, Bool- und Enum-Werten kombinieren. Ableitung, explizite Instanziierung und Spezialisierung, Standardtypen, aufgelöste Standardargumente, Rekursion und Namespace-Importe ergeben gewöhnliche typisierte Funktionen. Jede Instanz behält eigene lokale Typen und statischen skalaren Speicher, auch bei identischen Signaturen verschiedener Primärtemplates. Generische Körper und verzögerte Standardargumente werden bei Instanziierung geprüft; jeder erzeugte Körper wird geprüft. Aufrufe mit nur einer Signatur unter sizeof/noexcept benötigen weiterhin eine Definition. Nicht unterstützte abhängige Friend-Klassentemplate-Formen, Standardheader und vollständiges STL bleiben unvollständig. Native Validierung erfordert CI der implementierenden Revision.

Skalare auto-Argumente aus C++17, skalare Parameter mit abhängigem Typ, die Ableitung von Arraygrößen und endliche constexpr-Templaterekursion werden unterstützt. Wertargumente werden zu typisierten Konstanten und erzeugen keine Laufzeitparameter. Äquivalente Argumente teilen eine Instanz; verschiedene Werte oder abgeleitete Typen behalten getrennten statischen Speicher. Geschriebene Argumentausdrücke und Parametertypen werden vor dem Wegfalten geprüft. Zeiger-, Referenz- und Klassenwertargumente bleiben außerhalb dieses Schritts.

Core v2 unterstützt konkrete Instanzen von Standardlayout-Klassentemplates auf Namensraumebene mit bis zu 64 Typ- oder skalaren Wertparametern. Standardtypen, explizite Instanziierungen/Spezialisierungen, Typaliase, feste Arrays und ausgewählte Feldinitialisierungen erzeugen gewöhnliche typisierte Records. Äquivalente Argumente teilen einen Typ; verschiedene Werte, Typen und Primärtemplates behalten eigene Identitäten. Bestehende Regeln für Kopie, Referenzen, Rückgabe und Zerstörung der Mitglieder gelten weiter. Geschriebene Typen, Standardtypen und Argumentausdrücke werden vor dem Entfernen geprüft. Vollständiges C++/STL bleibt offen; native Ergebnisse erfordern CI der implementierenden Revision.

Diese Standardlayout-Klassentemplates unterstützen auch gewöhnliche benannte Memberfunktionen: const- und referenzqualifizierte Überladungen, statische Fabrikfunktionen, Member-Bereichsschleifen und skalare statische lokale Variablen je Instanz. Ungenutzte generische Funktionsrümpfe und Standardargumente bleiben bis zur Verwendung uninstanziiert; ausgewählte Aufrufe und alle instanziierten Rümpfe durchlaufen die bestehenden Typ-, Definitions- und Lebensdauerprüfungen. Auch geschriebene äußere Templateparametertypen an Definitionen außerhalb der Klasse werden geprüft. Methoden nutzen die gewöhnliche Empfänger- und Ergebnis-ABI ohne Templateparameter zur Laufzeit.

Diese Templates unterstützen benutzerdefinierte Standard-, Parameter-, explizite, Konvertierungs-, Kopier- und Verschiebekonstruktoren sowie Definitionen außerhalb der Klasse und explizite Instanziierungen/Spezialisierungen. Ungenutzte Rümpfe, Initialisierer und Standardargumente bleiben uninstanziiert; ausgewählte Konstruktionen benötigen eine geprüfte Definition. Kopier-Standardargumente werden nur bei Auslassung ausgewertet. Die Initialisierung schreibt in der Deklarationsreihenfolge der Mitglieder direkt in den Zielspeicher und erhält bestehende Lebensdauern temporärer Objekte, Felder und Arrays. Geerbte Konstruktoren bleiben ausgeschlossen. Native Prüfung erfordert CI der implementierenden Revision.

Diese Klassentemplates unterstützen auch benutzerdefinierte nichtvirtuelle Destruktoren, Definitionen außerhalb der Klasse und explizite Instanziierungen/Spezialisierungen. Stellt das eingebettete Frontend eine Verwendung fest, ist eine Definition nötig, auch bei direkter Objektrückgabe. Typabfragen und nicht ausgewertete Ausdrücke lassen ungenutzte Rümpfe uninstanziiert, prüfen aber ausgewählte Ausnahmespezifikationen. Der Rumpf läuft vor der Zerstörung von Mitgliedern und Arrayelementen in umgekehrter Reihenfolge; der Empfängerspeicher ist explizit. Bestehende Lebensdauerregeln für temporäre Objekte, verlängernde Referenzen und Wertparameter bleiben erhalten. Explizite Destruktoraufrufe und vollständiges C++/STL bleiben offen; native Prüfung erfordert CI der implementierenden Revision.

Diese Templates unterstützen mit `= default` definierte Standard-, Kopier- und Verschiebekonstruktoren, Kopier- und Verschiebezuweisungen sowie nichtvirtuelle Destruktoren innerhalb und außerhalb der Klasse. Ausgewählte Operationen erhalten Memberreihenfolge, Quell- und Zielspeicher, Referenzergebnisse der Zuweisung und umgekehrte Zerstörung. Wertinitialisierung unterscheidet die erste Deklaration von späterem Defaulting außerhalb der Klasse. Ungenutzte Rümpfe bleiben uninstanziiert; Abfragen prüfen geschriebene, aufgelöste Ausnahmespezifikationen. Verwendete nichttriviale Operationen benötigen eine generierte Definition aus den Eingabequellen. Explizite Spezialisierungen erben das Defaulting des Primärtemplates nicht. Vollständiges C++17/STL bleibt offen; native Prüfung erfordert CI der implementierenden Revision.

Diese Templates unterstützen auch benutzerdefinierte Operatoren und Konvertierungsfunktionen: Kopier-/Verschiebezuweisung, Dereferenzierung, Inkrement, Indexierung, Pfeilzugriff, Funktionsobjekte und explizite bool-Konvertierung. Ausgewählte Überladungen erhalten die C++17-Auswertungsreihenfolge, Referenzaliasse, endgültigen Ergebnisspeicher und Lebensdauern. Ungenutzte Rümpfe und Standardargumente bleiben verzögert; ausgewählte Deklarationen und alle instanziierten Rümpfe werden auf Quellen und Definitionen geprüft. Definitionen außerhalb der Klasse, explizite Instanziierungen/Spezialisierungen und instanzbezogene skalare statische lokale Variablen verwenden gewöhnliche typisierte Funktionen ohne Laufzeit-Templateparameter. Allokationsoperatoren, Standardheader und vollständiges C++17/STL bleiben offen; native Ergebnisse erfordern CI der implementierenden Revision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-operators-and-conversions).

Lokale Klassen in zugelassenen Funktions- oder Klassenmember-Templateinstanzen behalten gewöhnliche Methoden, Konstruktoren/Destruktoren, Operatoren, Konvertierungen und ausgewählte defaulted Operationen. Memberherkunft, Quelltypen, Empfängerspeicher und instanzbezogene statische lokale Variablen werden geprüft; jeder instanziierte Rumpf bleibt sichtbar. Jede explizite Funktionsinstanziierung bewahrt geschriebene Argumente, Typen, Konvertierungsnamen, Qualifizierungen und Attributprüfungen, auch bei extern und wirkungslosen Wiederholungen. Die Wiederverwendung einer kanonischen Deklaration kann nicht unterstützten Quelltext nicht verbergen. Native Ergebnisse erfordern das zugehörige CI; vollständiges C++17/STL bleibt offen. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#instantiated-local-classes-and-written-directives).

Core v2 unterstützt außerdem gewöhnliche Operatorfunktionstemplates auf Namensraumebene. Arithmetik, Vergleiche, Referenzrückgaben, Iteratoroperatoren und Record-Ergebnisse behalten die gewählte Überladung, C++17-Auswertungsreihenfolge, tatsächlichen Speicher und die Zerstörung temporärer Objekte bei. ADL, explizite Aufrufe, Spezialisierung und Instanziierung verwenden bestehende typisierte Funktionen ohne Templateparameter zur Laufzeit. Unbenutzte Rümpfe bleiben verzögert; gewählter Quelltext, alle instanziierten Rümpfe und geschriebene Direktiven werden geprüft. Nicht unterstützte abhängige Friend-Klassentemplate-Formen, Speicherallokation, Standardheader und vollständiges C++17/STL bleiben offen; native Validierung erfordert das zugehörige CI. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-operator-function-templates).

Diese Klassentemplates unterstützen auch statische Integer-, Bool- und Enum-Daten mit Null- oder Konstanteninitialisierung, einschließlich inline/constexpr und Definitionen außerhalb der Klasse. Gleiche Argumente teilen den Speicher; verschiedene Instanzen behalten eigene Objekte. Geprüfte nicht-inline const-Werte erzeugen keinen Speicher, ausgewertete Adress- und Referenzverwendungen brauchen jedoch eine Definition. Unbenutzte Initialisierer bleiben verzögert; instanziierte Initialisierer und jede explizite Direktive werden im Quelltext geprüft. Empfängereffekte und die Zerstörung temporärer Objekte bleiben erhalten. andere statische Typen, vollständiges C++17/STL bleiben offen; native Validierung erfordert das zugehörige CI. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-scalar-static-data).

Core v2 unterstützt skalare Nichttyp-Standardargumente für die zugelassenen Funktions-, Operator- und Klassentemplates. Vorherige Parameter, skalares auto, geerbte Vorgaben und geprüfte constexpr-Konvertierungen folgen Clang. Ausgelassene und äquivalente explizite Argumente teilen Funktionen, Records und statische Objekte. Nichtabhängiger Quelltext wird geprüft; unbenutzte abhängige Vorgaben bleiben verzögert. Erfolgreiche Konvertierungen behalten ursprünglichen und konvertierten Quelltext; Fehler erlauben die normale Überladungsauswahl. Vollständiges C++17/STL bleibt offen; native Validierung erfordert das zugehörige CI. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#scalar-template-parameter-defaults).

Referenzkonvertierungen mit abgeleitetem Rückgabetyp bewahren Aliase und const, einschließlich decltype(auto) und der Referenzkollabierung bei auto&&. Das integrierte Frontend leitet die für die Kandidatenauswahl nötigen Typen ab, ohne unbeteiligte Template-Funktionskörper mit Wertrückgabe zu instanziieren. Vollständiges C++17/STL bleibt unvollständig; die native Prüfung erfordert die CI dieser Implementierung. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deduced-reference-conversions). Bei neuen Referenzargumenten werden nötige Konversionsergebnisse vor der Kompatibilitätsprüfung deduziert. Der Filter für direkte Bindung befreit einen Kandidaten nicht von der Rückgabetypableitung für eine spätere indirekte Initialisierung; ein ungültiger konkurrierender auto&-Funktionsrumpf erhält weiterhin die normale C++-Diagnose.

Core v2 unterstützt konkrete Typ- und skalare Parameter-Packs in zugelassenen Funktions-, Operator- und Klassentemplates auf Namensraumebene. Pro Primärtemplate gelten höchstens 64 Parameter und pro Pack 64 Elemente. Expandierte Parameter behalten getrennten Speicher; sizeof... liefert eine typisierte Anzahl. Aufgelöste Fold-Ausdrücke erhalten Reihenfolge, Kurzschlussauswertung und Bereinigung. Eine abhängige Anzahl derselben Templateparameterliste darf im geschriebenen Parametertyp Metadatum bleiben. Leere Fold-Muster werden nicht instanziiert; Startwerte und erzeugte Elemente werden geprüft. Vollständiges C++17/STL bleibt offen; native Ergebnisse erfordern die implementierende CI. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#concrete-parameter-packs).

Core v2 unterstützt Alias-Templates in eigenen Namensräumen für zugelassene skalare Typen, void, Zeiger, Referenzen, feste Arrays und Klassen, einschließlich Typ-/Skalarstandardargumenten und konkreten Parameterpaketen. Aliase teilen kanonische Typen und statischen Speicher. Geschriebene Argumente, ausgewählte Standardargumente und ersetzter Quelltyp werden an tatsächlichen Verwendungen geprüft; nicht ausgewählte Kandidaten und nicht instanziierte abhängige Rümpfe bleiben verzögert. Explizite Argumente behalten den Aufruferkontext, auch bei Rekursion und this-Ausdrücken. Es gelten je 64 Parameter, Paketelemente und Ebenen sowie das gemeinsame Quellbudget. Vollständiges C++17/STL ist noch nicht umgesetzt; native Ergebnisse erfordern CI für die Implementierung. Nur C++ ist implementiert; E Language, Python und weitere Frontends sind geplant. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#namespace-alias-templates).
Ausgewählte Nichttyp-Templateargumente behalten außerdem den Quelltext ihres Parametertyps, auch bei expliziten Funktionstemplateargumenten und deduzierten leeren Packs; durch einen Alias weggefalteter, nicht unterstützter Quelltext wird weiterhin geprüft.

Nicht abhängige Typvorgaben werden auch in unbenutzten Templates geprüft. Qualifizierte Aufrufe von Funktionstemplates und konkrete Definitionen von Konvertierungsfunktionen behalten ihren genauen Quelltext, einschließlich Aliasnamen in Definitionen außerhalb der Klasse. Eine lvalue-Konvertierung kann ein erweitertes temporäres Objekt für einen const-Referenzparameter erzeugen; für rvalue-Referenzparameter gilt weiterhin die C++-Überladungsregel.
Core v2 unterstützt auch partielle Spezialisierungen von Klassentemplates in projekteigenen Namensräumen. Das gewählte Muster und seine abgeleiteten Parameter werden getrennt von den Argumenten des primären Templates geprüft. Aliase teilen denselben konkreten Typ und statischen Speicher; unterstützte Member behalten Aufrufe, Referenzen und Lebensdauer. Für Parameter und Elemente je Pack gilt weiterhin die Grenze 64. Vollständiges C++17/STL ist noch nicht fertig; die native Validierung erfolgt durch die CI der implementierenden Revision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#class-template-partial-specializations).

Core v2 unterstützt den eigenständigen Werttyp `decltype(nullptr)` für Aliase, Referenzen, Felder/Arrays, Callback-Signaturen sowie mit null oder Konstanten initialisierte skalare statische Objekte und Variablen-Templates. Zeigerkonvertierungen erhalten Seiteneffekte und die Zerstörung temporärer Objekte; verschiedene Nullwertobjekte behalten eigene Adressen. `nullptr_t`-Template-Argumente unterstützen geprüfte Standardwerte, Packs und bestehende Template-Quellkontexte; ihre Instanzen bleiben von Integer-Null und booleschem false getrennt. Null-Template-Argumente mit Zeigertyp und Standard-Header bleiben ausgenommen. Die native Prüfung erfordert die CI der Implementierungsrevision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#null-pointer-values).

Core v2 unterstützt delegierende Konstruktoren in zulässigen gewöhnlichen und generischen Klassen, einschließlich Ketten und ausgewählter Konstruktor-Templates. Ziel und delegierender Rumpf verwenden dasselbe endgültige Objekt; Mitglieder werden einmal initialisiert, temporäre Argumente vor dem delegierenden Rumpf zerstört. Auswahl und Definitionen werden weiterhin geprüft. Native Prüfung erfordert die CI der Implementierungsrevision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#delegating-constructors).

Core v2 unterstützt const-Datenmitglieder mit zulässigen Skalar-, Record-, Zeiger-, Callback- und Arraytypen, auch in generischen und verschachtelten Klassen. Initialisierung im endgültigen Speicher, ausgewählte Kopier-/Move-Operationen, eigene Adressen und const-Referenzen/-Zeiger bleiben erhalten. Clang weist ungültige Schreibzugriffe und gelöschte Zuweisungen zurück. Eigene Klassen mit konstantem Schlüssel sind möglich; Standard-Header und vollständige STL stehen noch aus. Native Prüfung erfordert die CI der Implementierungsrevision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#const-data-members).

Core v2 akzeptiert unterstützte gelöschte Funktionen und als gelöscht definierte Standard-Spezialfunktionen, einschließlich zulässiger Template- und Friend-Formen. Clang prüft Überladung und Zugriff; für gelöschte Deklarationen entsteht keine ausführbare Definition. Nur verschiebbare Objekte, ausgewählte Kopieroperationen als Ersatz und garantierte C++17-Kopierelision behalten die bestehende Lebensdauerbehandlung. Die Implementierungsrevision benötigt CI; vollständiges C++/STL steht noch aus. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#deleted-function-declarations).

Core v2 unterstützt IEEE `float` und `double`, Arithmetik und Konvertierungen, Referenzen, Felder/Arrays, Callbacks sowie mit Null oder Konstanten initialisierte statische Skalarobjekte. Exakte Literalbits und unabhängige Layoutprüfungen erhalten die Darstellung. Die Ausführung erfordert die Standard-Gleitkommaumgebung ohne Fast Math, überschüssige Präzision oder implizite Multiplikations-Additions-Fusion. CI der Implementierungsrevision ist erforderlich; `long double`, Standardheader und vollständiges C++/STL stehen noch aus. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#binary-floating-point-values).

Core v2 unterstützt schmale, UTF-8-, UTF-16-, UTF-32- und breite Stringliterale mit exakten Codeeinheiten und statischem schreibgeschütztem Speicher, Zeichenarray-Initialisierung mit Nullauffüllung und konstant initialisierte Namespace-Arrays. Aliase, Feldkopien und Lebensdauern nutzen die vorhandenen typisierten Operationen. CI der Implementierungsrevision ist erforderlich; Standardheader sowie Speicherverwaltung und Operationen von `std::string` stehen noch aus. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#string-literals-and-constant-arrays).

Core v2 unterstützt null- oder konstantinitialisierte veränderliche und konstante Arrays auf Namespace-Ebene, als lokale statische Variablen und als statische Klassenmember, einschließlich zugelassener Template-Instanzen. Zustand und Adressen bleiben über Aufrufe hinweg erhalten; verschiedene Instanzen haben getrennten Speicher. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#fixed-array-static-storage).

Core v2 unterstützt null- oder konstantinitialisierte statische Objektzeiger auf globale Objekte, Literale, Arrayelemente und Felder sowie gültige Zeiger hinter dem letzten Element. Namespace-, lokale, Klassen- und Template-Speicher behalten Identität, Veränderlichkeit und const-Zugriff. Vorwärtsadressen werden C23-Konstanten ohne Laufzeitinitialisierung. Statische Referenzen, Allokation und die vollständige STL bleiben in Entwicklung. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-object-pointer-storage).

Statische Referenzen können vorhandene statische Objekte, Teilobjekte von Arrays oder Strukturen und Zeichenketten binden, auch als lokale oder Klassenvariablen und in unterstützten Templateinstanzen. Lesen und Schreiben erhalten das ursprüngliche Objekt und dessen const-Rechte. Siehe den [Vertrag für statische Referenzen](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-bindings).

Core v2 unterstützt konstant initialisierte temporäre Skalare, Arrays und Records, deren Lebensdauer eine statische Referenz verlängert. Vollständige Objekte, Teilobjekt-Aliase, Selbstzeiger, const-Regeln und Template-Identitäten behalten dauerhaften Speicher. Native Validierung erfordert CI dieser Implementierung. Konstante Initialisierung dieser temporären Objekte erfordert in C++17 weiterhin triviale Destruktion; nichttriviale statische temporäre Objekte verwenden dynamische Initialisierung und Registrierung. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#static-reference-temporary-lifetime-extension).

Statische Datensätze unterstützen konstante Initialisierung und gemeinsamen veränderlichen Zustand, auch lokal, in Klassen und in unterstützten Templateinstanzen. Eigene Adressen, constexpr-Konstruktion und statische Nullinitialisierung erhalten die Objektidentität. Siehe den [Vertrag für statische Datensätze](../../utils/translate-frontends/docs/cpp-core-v2.md#static-record-objects).


Core v2 unterstützt geprüfte Ketten trivialer leerer Basisklassen samt unterstützten Templates, geerbten Methoden, Konvertierungen und statischen Werten. Jede Basis erhält einen echten ersten C-Member mit unabhängig geprüftem Layout. Nullzeiger, Referenzidentität, konstante Pfade und Seiteneffekte bleiben erhalten. Nichttriviale Basislebenszyklen und Standardheader sind noch unvollständig; native Nachweise erfordern CI für die Implementierungsrevision. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#trivial-empty-base-chains).
## Einrichtung und skalare Übersetzung

Verwenden Sie eine normale NeverC-Installation mit den Standardressourcen. Das C++-Frontend und die freigegebenen SDK-Header sind integriert; eine separate Clang-Installation ist nicht erforderlich. Einzelheiten enthält die [Frontend-Bauanleitung](../../utils/translate-frontends/cpp/README.md).

```sh
neverc translate --from cpp input.cpp -o output.nc
neverc output.nc -c -o output.o
```

Das Profil `cpp-core-v1` akzeptiert eine eigenständige C++17-Datei ohne Includes. Unterstützt werden `int`, `unsigned int`, `bool`, `void`, triviale Aggregattypen, freie Funktionen, Namensräume, Überladungen und der dokumentierte Kontrollfluss. Alle projekteigenen Deklarationen werden geprüft, auch unbenutzter Code.

Mit `--profile cpp-core-v2` werden geprüfte `typedef`-/`using`-Typaliase, Aufzählungen mit unterstützten ganzzahligen Basistypen, `static_assert` sowie Objektzeiger und Lvalue-Referenzen in begrenztem Umfang unterstützt. Referenzparameter und zurückgegebene Referenzen erhalten die Aliasbeziehungen; Nullzeiger und verschachtelte `const`-Qualifikationen werden unterstützt. Die Beschränkung auf eine Quelldatei ohne Includes bleibt bestehen. Siehe den [core-v2-Vertrag](../../utils/translate-frontends/docs/cpp-core-v2.md).

Core v2 ergänzt lokale Arrays fester Länge und Array-Felder, mehrdimensionale Indizierung sowie Zeiger und Referenzen auf Arrays. Bei Teilinitialisierung werden ausgelassene skalare Elemente mit null initialisiert; Record-Elemente folgen ihrer ausgewählten Initialisierung; Reihenfolge und Aliasbeziehungen bleiben erhalten. Array-Länge und Initialisierungsexpansion sind begrenzt. Globale Arrays und Arrays variabler Länge bleiben ausgeschlossen.

Core v2 unterstützt `switch`/`case`/`default` mit C++17-Initialisierungsanweisungen, Fallthrough und geprüften `[[fallthrough]]`-Annotationen. Der Selektor wird einmal ausgewertet; verschachtelte Switches und Schleifen behalten ihre `break`-/`continue`-Ziele. GNU-Case-Bereiche und andere Anweisungsattribute bleiben ausgeschlossen.

Core v2 unterstützt jetzt vorzeichenbehaftete und vorzeichenlose Ganzzahlen mit 8, 16, 32 und 64 Bit, Zeichentypen und Zeichenliterale sowie konstante `sizeof`-/`alignof`-Abfragen. Integer-Promotions und Überladungsauflösung erfolgen vor der Normalisierung; `long`, `wchar_t` und der Größentyp folgen der Zielplattform. Auch Enum-Basistypen dürfen schmaler oder breiter sein. Die Standardbibliothek ist noch in Entwicklung.

Core v2 unterstützt außerdem Objektzeiger-Offsets, Zeigerdifferenzen, Inkrement/Dekrement und zusammengesetzte Zuweisungen, einschließlich Array-Durchläufen und mehrdimensionaler Schrittweiten. Generierte Hilfsfunktionen erhalten die C++17-Regeln für Nullzeiger plus/minus null und die Differenz zweier Nullzeiger. Objektzeigervergleiche verwenden auf nativen x86/AArch64-Zielen unabhängig geprüfte vorzeichenlose Adressen; native Ergebnisse müssen durch CI der implementierenden Revision bestätigt werden. Zeiger/Ganzzahl-Konvertierungen im Quelltext und die vollständige STL bleiben unvollständig.

Core v2 vergleicht Quelltypgrößen und ABI-Ausrichtungen mit dem eigenen Zielmodell von NeverC, einschließlich Strukturgrößen und Feldoffsets. Statische Zusicherungen im erzeugten Code prüfen das Layout bei der Kompilierung erneut; das Manifest hält diese Angaben fest.

Core v2 unterstützt benannte nichtvirtuelle Memberfunktionen der zugelassenen Record-Typen, einschließlich const-Überladungen, lvalue-qualifizierter Methoden, statischer Methoden und `this`. Aufrufe erhalten die Identität des ursprünglichen Objekts und werten das Empfängerobjekt vor den Argumenten aus.

Core v2 unterstützt gewöhnliche benutzerdefinierte Konstruktoren für Record-Typen mit Standardlayout und unterstützten Kopieroperationen. Lokale Objekte, Felder und Array-Elemente entstehen direkt im endgültigen Speicher; Felder werden in Deklarationsreihenfolge initialisiert. Stack-Abwicklung bei Ausnahmen fehlt noch.

Core v2 erzeugt für jeden per Wert übergebenen Record-Parameter ein eigenes Objekt und schreibt Record-Rückgaben direkt in den Zielspeicher des Aufrufers. Dies gilt auch für Konstruktoren und Methoden; erforderliche Kopien und Referenzaliase bleiben erhalten. Andere C++17-Implementierungen dürfen bei geeigneten trivialen Typen zusätzliche Kopien von Argumenten oder Ergebnissen erzeugen.

Core v2 unterstützt gewöhnliche benutzerdefinierte Destruktoren und implizite Member-Destruktion bei normalem Verlassen. Lokale Objekte, Felder und Array-Elemente werden in umgekehrter Reihenfolge zerstört; temporäre Objekte am Ende des vollständigen Ausdrucks, nachdem benötigte Werte gesichert wurden. Rückgaben, Verzweigungen, Schleifen, break und continue führen die nötige Bereinigung aus. Wertparameter werden beim Verlassen der aufgerufenen Funktion zerstört; Rückgabeobjekte gehören dem Aufrufer. Exception-Unwinding fehlt noch.

Core v2 führt gewöhnliche benutzerdefinierte Kopierkonstruktoren und Kopierzuweisungsoperatoren mit einem Quellparameter `R&` oder `const R&` aus. Kopien verwenden das tatsächliche Ziel und erhalten Seiteneffekte sowie die zurückgegebene Referenz. Zuweisungssyntax wertet zuerst den rechten Operanden aus; explizite `operator=`-Aufrufe zuerst das Empfängerobjekt.

Core v2 unterstützt auch generierte/default-Standardkonstruktoren und default-Destruktoren, einschließlich verschachtelter Record- und Array-Member. Die Konstruktion erfolgt am tatsächlichen Ziel in Deklarationsreihenfolge; Wertinitialisierung führt nur die von C++ verlangte Nullinitialisierung aus. Für default-Definitionen außerhalb der Klasse bleiben die abweichenden Regeln erhalten. Ungenutzte oder nur in nicht ausgewerteten Ausdrücken verwendete Konstruktoren benötigen keinen künstlichen Funktionsrumpf.

Core v2 unterstützt implizite und explizit mit default definierte Kopierkonstruktoren für verschachtelte Records und mehrdimensionale Arrays. Ausgewählte Member-Kopierkonstruktoren laufen am tatsächlichen Ziel in Deklarations- und Elementreihenfolge; die Quelladresse wird einmal ausgewertet. Triviale Kopien erhalten gespeicherte Zeigerwerte. Wertparameter und Rückgaben aus Quellobjekten erhalten Kopiereffekte, direkte prvalue-Weitergabe erzeugt keine zusätzliche Kopie.

Core v2 unterstützt auch implizite und explizit mit default definierte Kopierzuweisung. Member und Array-Elemente behalten ihre ausgewählten Operationen und Reihenfolge; generierte triviale Array-Kopien werden zu typisierten Elementzuweisungen ohne externen Speicherkopieraufruf. Triviale Zuweisung erhält Zeigerwerte und gibt den tatsächlichen Empfänger zurück. Operatorsyntax erfasst zuerst die Quellreferenz, explizite Membersyntax zuerst den Empfänger; Quellwerte werden erst nach diesen Effekten gelesen. Zuweisung erzeugt oder zerstört keine zusätzlichen Objekte.

Core v2 unterstützt Standard-Member-Initialisierer zugelassener Felder, einschließlich Zugriff auf frühere Member, gewöhnlicher Aufrufe, verschachtelter Records und Arrays. Das ausgewählte Default verwendet das tatsächliche Objekt als `this`; explizite Aggregatklammern behalten das `this` des Aufrufers. Explizite Initialisierung ersetzt das jeweilige Default. Implizites/defaulted Kopieren und Zuweisen wiederholt keine Defaults; ein benutzerdefinierter Kopierkonstruktor kann sie für ausgelassene Member auswählen. Konstruktor-Member und Aggregate behalten ihre jeweiligen Grenzen für die Zerstörung temporärer Objekte. Templates und die vollständige STL bleiben in Entwicklung.

Core v2 unterstützt Rvalue-Referenzen auf bereits lebende Objekte: Skalar-, Zeiger-, Record- und Array-Aliase, Referenzparameter und -rückgaben, bedingte Xvalues sowie gewöhnliche `&&`-qualifizierte Methoden. `static_cast<R&&>(live)` erhält dasselbe Objekt; benannte Rvalue-Referenzvariablen bleiben Lvalues. Überladungen und bestehendes Kopierverhalten folgen Clangs Auswahl. Referenzen erzeugen keine zusätzliche Zerstörungsverantwortung.

Core v2 führt gewöhnliche benutzerdefinierte Move-Konstruktoren und Move-Zuweisungen mit lebenden `R&&`- oder `const R&&`-Quellen aus. Konstruktion verwendet den tatsächlichen Zielspeicher; Zuweisung erhält Quelländerungen, Auswertungsreihenfolge und den zurückgegebenen `R&`-Alias, auch mit `&`/`&&`-qualifiziertem Empfänger. Benannte Rvalue-Referenzen wählen weiterhin Lvalue-Überladungen; explizite Konstruktoren behalten ihre Initialisierungsregeln. Ein Move beendet die Quelllebensdauer nicht: Quelle und Ziel werden normal zerstört.

Core v2 unterstützt auch implizite und explizit mit default definierte Move-Konstruktion und -Zuweisung für verschachtelte Records und mehrdimensionale Arrays. Member führen die von C++ ausgewählte Kopie oder Verschiebung in Deklarations- und Elementreihenfolge aus. Generierte Moves wiederholen keine Member-Defaults; triviale Operationen erhalten gespeicherte Zeigerwerte. Quellen werden normal zerstört, unbenutzte oder triviale generierte Operationen benötigen keinen erfundenen Funktionsrumpf.

Core v2 unterstützt aufgelöste Standarddeklarationen mit `noexcept`, `noexcept(true/false)` und C++17-`throw()` sowie konstante `noexcept(expression)`-Abfragen. Abfragen berücksichtigen die Spezifikationen der gewählten Funktionen und Destruktoren, ohne ihre Operanden auszuführen. Alle Operanden und geschriebenen Spezifikationen werden auch in ungenutztem Code geprüft. Werfen und Fangen von Ausnahmen, Stack-Unwinding, Templates und die vollständige STL bleiben in Entwicklung.

Core v2 unterstützt gewöhnliche überladene Member- und freie Operatoren für Arithmetik, Vergleiche, Indexierung, Dereferenzierung, Inkremente, Funktoren und allgemeine Zuweisungssignaturen. Gewählte Funktionen, Referenzaliasse und Objektergebnisse bleiben erhalten. Die Operatornotation wahrt die C++17-Reihenfolge; überladene logische Operatoren werten beide Operanden aus. Freie Zuweisungsoperatoren initialisieren Parameter von rechts nach links und zerstören sie umgekehrt. Templates, Speicherallokation und vollständige STL bleiben in Entwicklung.

Core v2 unterstützt außerdem gewöhnliche Konvertierungsfunktionen lebender Objekte: implizite und explizite Ganzzahl-, Enum- und Zeigerkonvertierungen, kontextuelles explizites `bool` sowie Referenz- und Objektergebnisse. Jede Konvertierung ruft die gewählte Memberfunktion einmal auf. Referenzen erhalten ihre Aliasse; Objekt-prvalues initialisieren das tatsächliche Ziel direkt. Referenz-zu-Wert-Konvertierungen behalten den gewählten Kopier- oder Verschiebevorgang. Const-/Referenzqualifizierer, constexpr und noexcept folgen C++17.

Core v2 unterstützt nun temporäre Empfängerobjekte und temporäre Skalar-/Record-Referenzargumente bis zum Ende des vollständigen Ausdrucks, auch für Konstruktoren, Methoden, Operatoren und Konvertierungsfunktionen. Jede Auswertung erhält tatsächlichen Speicher; Objektergebnisse behalten ihr endgültiges Ziel und Referenzen ihre Aliasse. Nach Aufruf und Zerstörung der Funktionsparameter erfolgt die temporäre Bereinigung in umgekehrter Konstruktionsreihenfolge, auch in Bedingungen und Schleifen. Array-Teilobjekte temporärer Records sind unterstützt.

Core v2 verlängert nun die Lebensdauer temporärer Werte durch gewöhnliche automatische lokale Referenzen, einschließlich `const T& r{T{...}}` und `T&& r = T{...}`. Skalare, Enums, Zeiger und Records erhalten tatsächlichen Speicher. Referenzen auf Mitglieder oder Array-Elemente halten den gesamten Record am Leben, wenn C++ die Verlängerung vorsieht. Initialisierung mit geschweiften Klammern, auch nach einem Gleichheitszeichen, bewahrt dasselbe Objekt. Die Zerstörung folgt dem Referenzbereich, auch in Bedingungen, Schleifen und bei frühem Verlassen; andere temporäre Werte im Initialisierer enden weiterhin mit ihrem vollständigen Ausdruck. Aliasse erzeugen keinen weiteren Besitzer, spätere Kopien und Moves behalten die gewählten Operationen. [C++17](../../utils/translate-frontends/docs/cpp-core-v2.md#reference-members).

Core v2 unterstützt auch eigenständige temporäre Arrays fester Größe in Aufrufen, bei Zeigerumwandlung/Indizierung, verworfenen Ausdrücken und automatischen lokalen Referenzen. Jedes Array besitzt ein tatsächliches Ziel; Skalare, Record-Konstruktoren und gemeinsame Standardinitialisierer initialisieren die Elemente direkt in Reihenfolge, ohne zusätzliche Elementbesitzer. Bei zulässiger C++-Lebensdauerverlängerung halten Zeilen-/Elementreferenzen mehrdimensionaler Arrays das gesamte Array am Leben. Die Elemente werden am Ende des vollständigen Ausdrucks oder Referenzbereichs rückwärts zerstört. Typisierte Adressen sowie bestehende Größen-, Speicher- und Expansionsgrenzen bleiben erhalten. Vollständiges C++/STL bleibt in Entwicklung.

Core v2 unterstützt leere Standard-Layout-Klassen sowie zustandslose Funktions- und Konvertierungsobjekte. Leere Objekte behalten C++-Größe und Ausrichtung von je einem Byte, erforderlichen getrennten Speicher, ausgewählte Konstruktoren und Operatoren sowie normale Destruktion. Triviale Kopien werten ihre Operanden weiterhin aus; leere Elemente in Arrays und umschließenden Records zählen zum Speicherlimit. Der erzeugte NC-Code nutzt ein internes Speicherbyte, die Quellfeldliste bleibt leer. Vererbung, Templates und die vollständige STL sind weiter in Entwicklung.

Core v2 unterstützt Konvertierungen zu void, `void()` und `void{}` sowie unterstützte void-Aliase, Aufrufe, Rückgaben, Kommaausdrücke und bedingte Zweige. Beim Verwerfen eines nicht volatilen Lvalues bleiben Empfänger- und Indexeffekte erhalten, ohne dessen gespeicherten Wert zu lesen. Temporäre Objekte werden weiterhin konstruiert und am bisherigen Ende des vollständigen Ausdrucks zerstört. Es entstehen keine void-Variablen oder Wertträger. Konstanten- und noexcept-Operanden werden weiterhin geprüft. Volatile Objekte, nicht unterstützte Typen, Templates und die vollständige STL bleiben außerhalb dieses Schritts.

Core v2 unterstützt Standardargumente für zugelassene Funktionen, Methoden, Aufrufoperatoren und benutzerdefinierte Konstruktoren, einschließlich nachfolgender Standardargumente bei Kopier- und Verschiebekonstruktoren. Namen werden am Deklarationsort aufgelöst; die Auswertung erfolgt bei jedem Aufruf mit ausgelassenem Argument. Identität und Lebensdauer von Referenz- und Wertargumenten bleiben erhalten. Bei ausgelassenen Arrayinitialisierern und erzeugten Arraykopien werden temporäre Standardargumente vor dem nächsten Element zerstört; explizite Initialisierer behalten die äußere vollständige Ausdrucksgrenze. Auch unbenutzte oder überschriebene Standardausdrücke werden geprüft. Templates und die vollständige STL sind weiterhin unvollständig.

## Projekte mit mehreren Dateien

Wählen Sie Übersetzungseinheiten ausdrücklich aus einer Kompilierungsdatenbank aus und geben Sie das Projektstammverzeichnis an. Das integrierte Frontend analysiert jede Einheit einzeln; die Zusammenführung prüft Definitionen, Bindung, gemeinsame Typen und die Einhaltung der Ein-Definitions-Regel (ODR) anhand konservativer Prüfungen.

```sh
neverc translate --from cpp --profile cpp-project-v1 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/a.cpp src/b.cpp --out-dir generated
```

Die Projektausgabe besteht aus `translated.nc` und `translated.h`. Nur Projekt-Header innerhalb dieses Stammverzeichnisses werden zugelassen. Bei mehreren Konfigurationen wählen Sie den nullbasierten Datenbankindex mit `--compdb-entry src/a.cpp=4`. Gespeicherte Compilerbefehle werden als Daten gelesen und niemals ausgeführt.

## Begrenzte Mathematik mit double

`cpp-math-v1` ergänzt Projekte um `double`, dokumentierte Konvertierungen/Vergleiche und exakt `std::fabs(double)` sowie `std::floor(double)`. Verwendet werden die integrierten Header von Clang 20.1.8 / libc++ 200100 / macOS 15.5; ein explizites macOS-15.0-Ziel (arm64 oder x86_64) ist erforderlich. Allgemeine Gleitkommaarithmetik bleibt ausgeschlossen. Gleitkomma-Traps müssen maskiert und das Nullsetzen subnormaler Zahlen deaktiviert sein; alle vier Standard-Rundungsmodi sind getestet.

```sh
neverc translate --from cpp --profile cpp-math-v1 \
  --target arm64-apple-macosx15.0.0 \
  --project-root "$PWD" --compdb build/compile_commands.json \
  src/math.cpp --out-dir generated-math
```

Die mathematische Übersetzung prüft außerdem installierte NeverC-Mathematikheader, die Identität eingebetteter Implementierungen und einen echten Linkvorgang. Generierte Mathematikmodule verwenden die NeverC-Laufzeit. Mit `-fno-builtin-std` schlägt die Übersetzung nur dann vor dem Schreiben der Ausgabedateien fehl, wenn der endgültige Code Zuordnungen für `fabs`/`floor` benötigt. Mathematikcode ohne diese Zuordnungen kann weiterhin übersetzt werden.

## Validierung und Ausgabe

Mit `--check` anstelle einer Ausgabeoption durchlaufen Sie dieselbe Analyse, Erzeugung sowie Syntax- und Objektvalidierung, ohne erzeugte Dateien zu behalten. `--report PATH` schreibt strukturierte Diagnosen. Die Übersetzung führt das Quellprogramm niemals aus.

Ausgaben und Begleitdateien werden nicht überschrieben. `--out-dir` verlangt ein neues Verzeichnis unter einem vorhandenen Elternverzeichnis; `-o` einen neuen `.nc`-Pfad. Das Manifest enthält Zielanforderungen, Ein- und Ausgabehashes sowie die Kompilierungsanleitung; die Quellzuordnung verbindet erzeugte Zeilen mit ursprünglichen Positionen.

CI-Ergebnisse, Ausführungs- und Installationsprüfungen sowie übersprungene Tests werden nach Plattform dokumentiert. Natives macOS arm64 und macOS x86_64 unter Rosetta bleiben getrennte Prüfungsumgebungen. Der angegebene Umfang umfasst weder vollständiges C++/STL noch Ausnahmen, Templates, Zeichenketten oder `std::vector`. Siehe [Unterstützungsmatrix](../../utils/translate-frontends/docs/support-matrix.md), [Protokoll und Wiederherstellungsregeln](../../utils/translate-frontends/docs/protocol.md) und [Beispielprojekt](../../tests/neverc/Inputs/translate/cpp/project).
