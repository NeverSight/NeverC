NeverC builtin C++ frontend: upstream source and modifications

Release: LLVM/Clang 20.1.8 (llvmorg-20.1.8)
Source archive: llvm-project-20.1.8.src.tar.xz
URL: https://github.com/llvm/llvm-project/releases/download/llvmorg-20.1.8/llvm-project-20.1.8.src.tar.xz
SHA256: 6898f963c8e938981e6c4a302e83ec5beb4630147c7311183cf61069af16333d

A checked private SemaExceptionSpec.cpp repair includes the actual receiver of
a scalar pseudo-destructor expression in canThrow. The call itself remains
nonthrowing, while receiver calls retain their exception specifications and
dependency. Exact original/rewritten source blocks, idempotence and rejection of
missing, duplicate, drifted or orphaned blocks are audited before source writes.

The archive is fetched and verified during the NeverC build. An offline copy
can be selected with CMake's NEVERC_CPP_LLVM_SOURCE_ARCHIVE option; the same
SHA256 is required. No upstream executable or shared library is installed.

A checked private ASTConsumer.h/Sema.h/TemplateDeduction.h/SemaTemplate.cpp/
SemaTemplateDeduction.cpp/SemaTemplateInstantiateDecl.cpp patch preserves each explicit function/static-member directive and successful
template-use source event. A default-false consumer request opts only core v2
into single-pass alias TypeSourceInfo substitution and retained substitution
wrappers on type/scalar defaults and non-type parameter types. Exact deduction
Info carries preliminary explicit parameter-type evidence to completed uses;
rechecked packs retain fresh complete records. Empty deduced NTTP packs preserve
their actual separate type substitution without inventing an element. Temporary
parameter-type evidence is bounded to 4096 records per use. Other consumers retain
the upstream paths and
finality. Bounded per-deduction records retain selected parameter identities,
original/converted defaults and an overflow marker; successful type/function uses
carry their complete canonical/sugared arguments. Explicit class specialization,
class instantiation and function specialization preserve each declaration's own
source, including unused/repeated occurrences. Existing directive evidence still
precedes no-effect returns. No deduction, overload, access or SFINAE rule changes.
The six sources must all be pristine or fully rewritten; duplicate, partial,
missing, mixed, drifted and orphan-marker states fail before any group write.
Independent literal fixtures check all anchors, exact results and idempotence.
NeverC bounds persistent evidence before copying and indexes it after Sema.
For requesting consumers, conversion-function definition instantiation substitutes
the actual definition's declaration-name type after constructing its existing
Final=false argument list. This replaces the generic name TypeSourceInfo copied
by upstream, retaining concrete in-class/out-of-line definition syntax. Empty
substitution results retain their diagnostics and invalidate the definition.
Ordinary functions and nonrequesting consumers keep their upstream behavior.
Only reached concrete types, selected callees and explicit declarations consume
their exact source evidence. Explicit arguments keep caller context; selected
defaults and alias underlying source use checked parameter frames. Type sugar
and written locations are distinct from canonical runtime identities. No external
Clang executable or opaque translation fallback is introduced.

The same checked source group also adds a default-neutral ASTConsumer decision
and provenance hook in SemaInit.cpp's actual empty-array-element initialization.
Core v2 can request separate semantic initializer expressions when a filler
contains materializations, including selected default arguments and default member
initializers. Its consumer bounds the array extent and reserves a global weighted
source budget before the list grows. Sema retains the first constructed filler,
rebuilds each remaining missing slot through PerformEmptyInit at its actual index,
updates NumInits, and records the exact omitted-element expression. Lowering gives
direct element default construction its C++17 default-argument cleanup boundary;
constructors of aggregate fields retain the enclosing initializer full-expression.
Single-element fillers also need semantic slots because later outer-reference
lifetime revisits do not traverse ArrayFiller. Second passes retain those slots.
VerifyOnly, NoInit, vectors and default consumers preserve upstream behavior.
This repairs repeated static MTE/APValue identity loss without rewriting constant
results or changing the ordinary C23 compiler. Independent literal fixtures check
complete group states, including reservation/provenance loss and stale list counts.
Source/protocol and O0/O2 fixtures check distinct self addresses, static/automatic
lifetimes and default-argument cleanup; native acceptance requires implementing CI.

A four-file checked array-query repair defers SemaExprCXX.cpp's extent evaluation
while the dimension is type/value dependent, even when the queried array type is
already fixed. TreeTransform.h transforms the dimension before its unchanged-node
shortcut and reuses the original node only when both type and dimension stay the
same. ParseExprCXX.cpp and TreeTransform.h give the dimension a constant-evaluated
context so required constexpr bodies are available even below sizeof/noexcept.
SemaTemplateVariadic.cpp explicitly collects packs from the dimension, which has
no ordinary Stmt-child edge. The exact dimension and type source remain retained;
no canonical replacement AST is synthesized. Pure source fixtures require both
files to be pristine or fully rewritten before either file changes and cover
idempotence, mixed states, missing/duplicate/drifted blocks and orphan markers.
The frontend separately visits the dimension omitted by the upstream RAV and
requires an admitted nonnegative integer or unscoped-enum constant index. Native
template-index substitution and saved-NC behavior require implementing CI.

A separate checked SemaExpr.cpp repair enables lazy default member initializer
substitution for copied class-scope full specializations in requesting consumers.
Their explicit specialization kind coexists with an implicit member-class origin;
the existing initializer gate otherwise overlooks that origin. The repair requires
a concrete class-scope full and the actual member instantiation metadata, then
reuses the existing field pattern, outer argument collection and initializer
instantiation. Ordinary full declarations, nonrequesting consumers, cycle checks
and unparsed-initializer diagnostics retain their original behavior. Complete
original/rewritten blocks are checked exactly; missing, duplicate, partial, drifted
and orphan-marker states fail before this file changes. Independent setup fixtures
cover first/repeated execution and rejected states.

A checked SemaInit.cpp patch resolves potentially lvalue-reference placeholder
conversion results before the reference-only candidate filter. It handles bare
decltype(auto) and unqualified bare auto&&, preserving lazy value-returning
template bodies, the existing reference filter and normal overload diagnostics.
The complete original or rewritten block must occur exactly once; missing,
partial, duplicate and drifted states fail before this file is changed. Independent
literal fixtures and paired semantic/runtime regressions cover the correction.

A separate checked SemaOverload.cpp patch applies the same ordering to fresh
reference arguments in FindConversionForRefInit. Result deduction precedes the
compatibility filter. Its direct reference-candidate search excludes written
non-function lvalue results from direct rvalue-reference binding. Later full
initialization still deduces results needed for indirect standard conversions.
Overload analysis still rejects an lvalue-reference conversion result for an
rvalue-reference argument; const-reference temporary widening remains admitted.
An invalid competing auto& body retains its normal diagnostic. All original
category, explicitness, access and candidate-selection checks remain active.
Independent literal source fixtures cover exact output, idempotence and drift;
paired source/protocol and native O0/O2 cases cover fresh argument conversions.

The shared private ten-file source group preserves completed namespace class
partial-specialization deductions in SemaTemplateDeduction.cpp. For requesting
consumers only, the existing successful class-partial pattern substitution keeps
source sugar (Final=false); partial ordering keeps its original path. After conversion, substituted-primary equality, trap and
constraint checks, two bounded callback records preserve deduced partial slots
and the actual substituted pattern/primary slots. The exact canonical deduced
argument-list object is retained as identity: upstream SemaTemplateInstantiate.cpp
moves Info.takeCanonical() unchanged into the chosen match and setInstantiationOf,
and getTemplateInstantiationArgs() exposes it on the concrete class. No additional
selection, deduction or instantiation is performed. Other consumers retain the
original AST and no-op callback behavior. Independent literal archive fixtures
cover the extended group and failure-before-write states; paired native/protocol
fixtures cover selected source, canonical storage and lifetime behavior.

The shared ten-file group retains namespace variable-template source only for
requesting consumers. Successful CheckVarTemplateId and explicit full declaration
checks preserve actual variables, locations, written arguments, defaults and
non-type parameter types. Each full declaration also retains its written
storage-class flag, including no-use declarations, so nonstandard spellings
cannot escape through an unevaluated-use path. Successful variable-partial
conversion checks retain both deduced slots and substituted primary pattern;
the exact Info.takeSugared() object transferred to the chosen variable identifies
the pair, independently of class selection's takeCanonical() identity.
SemaTemplateInstantiateDecl.cpp callbacks retain the TypeSourceInfo already
produced by first declaration construction and later completion. The latter may
replace VarDecl's type without replacing its first TypeSourceInfo. No extra
substitution or initializer instantiation is introduced. Existing partial
ordering, error traps, reference conversion and conversion-definition-name
repairs remain intact. Source retention remains bounded and opt-in, with no-op
callbacks and the original semantic paths for other consumers. Independent
literal fixtures cover exact ten-file output, mixed states and drift before any
write. Native/protocol fixtures check source, scalar storage and lazy demand.

The shared source-retention group also records successful member-template
construction and conversion expressions with the exact selected FunctionDecl
and original deduction location. ASTConsumer.h adds an opt-in default-noop
HandleNeverCSelectedTemplateCallSource callback. Overload.h carries a default-
initialized SourceLocation outside the ImplicitConversionSequence union, copies
it explicitly and clears it when conversion kind changes. Successful overload
selection populates it after constructor ranking, preserving Standard.CopyConstructor
cases without changing ranking or semantic flags. SemaOverload.cpp, SemaInit.cpp
and SemaExprCXX.cpp report direct successful expressions before conversion and
lifetime wrappers. No additional deduction or instantiation is performed.
The source group now spans ten files and is preflighted before group writes.
Independent literal contracts cover copied/reset metadata, exact result identity,
selection locations, includes and callbacks. Mixed, missing, duplicated and
partially patched states are rejected. Shared SemaInit/SemaOverload fixtures
also retain the independent existing deduced-reference fixes.

The isolated build renames namespace llvm and selected global C/C++ symbols.
IsolateSymbols.py applies checked, idempotent changes to the extracted
IntrinsicInst.cpp, Debugify.h, and LoopUtils.cpp to isolate otherwise global
identifiers.
Six calls in Signals.cpp, APFixedPoint.cpp, and ConstantFolding.cpp explicitly
perform the same double promotions as the integer/mixed cmath overloads. This
avoids emitting shared global MSVC math templates while preserving getter
evaluation, integer negation, and the existing outer float/half conversions.
All three math sources are checked before any of those files is rewritten.
The same checked patch step corrects SemaAccess.cpp's effective-context walk
for friend functions declared in nested classes. C++17 [class.nest]/4 gives
these functions no implicit enclosing-class access; canonical function grants
still permit explicit friendship from their own or enclosing classes. Ordinary
nested member access and non-nested friend contexts are unchanged. The patch
checks original/rewritten source states and rejects partial or duplicate edits.
The supplemental declaration check keeps immediate nominated-name access
validation before redeclaration merging and rechecks only completed friend
functions with their canonical grants. A private DelayedDiagnostic flag leaves
type-friend and existing diagnostic behavior unchanged. Late-parsed nested
friend defaults use a matching temporary function scope/context, preserving
lexical lookup and restoring parser/Sema state afterwards. SemaAccess.cpp,
DelayedDiagnostic.h and ParseCXXInlineMethods.cpp are validated as one source
group before any supplemental rewrite; mixed or partial groups are rejected.
A checked DeclCXX.h/DeclCXX.cpp pair also propagates later namespace default
arguments through existing using declarations, as required by C++17
[dcl.fct.default]/9. It selects only a later declaration of the same non-template
function in the same semantic namespace with additional defaults. Lexical
block/friend declarations and different namespace default sets remain excluded;
new overloads are not imported. Both source states are validated before either
file is rewritten, with idempotent complete pairs and rejected partial pairs.
Other symbol changes use compilation definitions. NoCrashReporter.h disables
the private PrettyStackTrace.cpp Darwin process annotation; NeverC owns it.
On Windows, RewriteSetupCoffSymbols.py isolates five Setup COM GUID identities
in the aggregated COFF archive. It replaces each 42-byte GUID identifier with
an equal-length private ASCII name, including the exact observed MSVC template
and local metadata spellings listed in SetupGuidSymbols.py. The first linker
index keeps its order; the second index stably reorders name/member-index pairs.
Original member selection and duplicate multiplicity are preserved. Object
records, auxiliary slots, code, data, relocations and member offsets are compared
byte for byte; only the authorized name bytes and paired index ordering may
change. Unknown spellings, conflicting shared strings and changed GUID values
are rejected. The same private LLVM 20.1.8 nm/readobj independently inspect the
result, and the full no-host ABI audit runs before the final archive is replaced.
This is a build-time Python transformation, with no external Clang executable
or COFF writer required by the installed NeverC translator. Internal Clang
libraries remain statically included. The Windows CI contract retains separate
baseline/isolated SDK smart-pointer and deterministic COM runtime evidence;
it does not claim to test COM activation, VS discovery or private LTO behavior.
The opt-in variable type callback also retains the exact previous concrete
VarTemplateSpecializationDecl when Sema creates a separate out-of-line member
definition. In-place completion carries no previous-node argument. This lets
source checks validate both declarations without replaying substitution or
matching an unrelated use by template argument values. The same atomic ten-file
source contract covers the callback signature and both producer paths.
For a class-scope full member variable copied into a concrete outer class, the
existing successful argument check is retained with the exact resulting variable
and type source. The original written storage specifier belongs to the original
declaration; inherited static storage on a copy is not a written specifier. No
argument conversion or type substitution is repeated for this source event.
Written and copied class/variable partial declarations also retain the successful
primary argument check, including resolved non-type parameter source types when
some argument values remain dependent. Copied events retain the exact original
partial declaration; direct events remain separate from later selected-partial
deduction. Collection never requests another substitution or instantiation.
A non-inline static member variable template reuses the initializer from its
actual redeclaration chain. Repeated references to an out-of-line definition do
not instantiate its in-class constant initializer a second time. This private
pinned-source correction preserves the first instantiation and C++ diagnostics
for genuinely repeated written initializers.
Class-scope full member class copies retain the actual declaration, original
full declaration and successful canonical/sugared/default/parameter-type source
after the existing eager class instantiation completes. Original dependent full
declarations retain the source from the existing successful declaration hook.
No additional substitution or instantiation is requested.
Explicit ordinary member-class instantiation directives retain the exact record,
original member, written qualifier, name/template/extern locations and attributes.
Both normal success and successful no-effect directives preserve their own source
without adding an AST declaration or repeating instantiation.
Instantiated non-template friends retain two exact source events: the incoming
and selected function declarations plus the actual function and granting class,
and the actual/written FriendDecl pair. The function event follows successful
substitution without repeating normalization or lookup. Namespace redeclaration
merging does not replace the written friend source with canonical flags.
Ordinary instantiated friend types reuse the exact actual/written FriendDecl
pair after successful type substitution. Unsupported, expanded and friend-type
template-header paths are excluded; no substitution or access lookup is repeated.
Original and actual TypeSourceInfo remain distinct from canonical type identity.
Friend function-template primary copies retain exact actual primary, incoming
and selected function declarations and granting class, paired with the actual
and written FriendDecl. Concrete free template bodies also retain Sema's selected
compatible declaration, pattern and lexical context after successful body creation.
Shared member-template origin metadata is not per-declaration source evidence.
Class-friend template copies retain their exact actual and original targets,
granting record, resolved semantic context and selected previous target after
successful parameter merging and visibility. Their FriendDecl pair is retained
separately; these declarations have no ordinary member-template origin link.
The exact scripts and compilation rules are part of the NeverC source tree at
neverc/lib/Translate/Cpp/Frontend and neverc/cmake/modules/BuiltinCppFrontend.cmake.

Operation-trait evaluation preserves hypothetical construction, conversion and
assignment roots for the opt-in NeverC consumer. A state pointer passed through
the actual Sema helpers belongs to one exact TypeTraitExpr; nested queries retain
separate state. A bounded reservation precedes ASTContext allocation of synthetic
operands, replacing their temporary BumpPtrAllocator/stack lifetime only for this
consumer. Callback arrays are copied before return. Complete roots survive false
trivial/nothrow results; incomplete attempts are marked separately from metadata
early exits and cannot prove absence of a selected function/default argument.
All fifteen anchors in the two-file source group are validated for consistent
pristine or rewritten state before either file is written. Audits cover drift,
duplicates, missing anchors/files, mixed states, orphan markers and repeat runs.
This retention does not request extra instantiation, change query booleans or
serialize hypothetical operations. The adapter separately checks complete implicit
trivial record operations and exact synthetic reference/decay paths. Selected
operation families must be implicit through owning bases and fields. Ordinary
user operations additionally require exact non-template definitions that completed
normal source traversal. Deferred roots are checked after successful traversal,
with deferral closed before validation and lowering. Independent lifetime checks
include owning subobject destruction, not just a written destructor body. No
extra Sema instantiation is requested. The three retained nothrow operations also
check already-resolved standard exception specifications on each selected root
callee and bound destructor, without asking Sema to resolve additional dependencies.
The same source proof covers owning-subobject destructor inference. Constructor
queries can select unchanged ordinary parameter defaults after exact
parameter/initializer source completion. The adapter independently validates the
FullExpr envelope stripped by ParmVarDecl::getDefaultArg(), source ownership,
selected owner/slot, implicit special-member families and nested destruction.
Normal parameter traversal collects exact family and record-prvalue destruction
dependencies even through unevaluated operands and written TypeLoc expressions;
trivial destructor binding nodes may be absent after specification resolution.
Collections finish by exact parameter/initializer identity. Exact semantic
initializer cache entries retain dependencies and edges even on cache hits;
every reachable entry must complete before query validation. This covers nested
defaults and initializers first checked outside a parameter collector without
replaying source traversal. Graph nodes, edges and frames remain bounded by the
shared budget and depth limit. Lazy template destructor prvalues still need
separate signature proof.
The same graph retains exact noexcept expression completion from normal Type,
TypeLoc, concrete-template and friend-signature traversal. Calls and function
references keep edges to actual and selected declaration exception expressions;
independent function declarations isolate unused defaults and bodies. Selected
operation/default/destruction source consumes the complete transitive graph,
including ordinary non-nothrow queries, without replay or extra Sema work.
Variable and enum references retain actual initializer dependencies, including
implicit-enumerator predecessors and previously cached variable templates.
Already materialized user-provided constexpr bodies and constructor initializers
are checked through their original synchronous RAV roots. Parameter references
do not select defaults; implicit methods retain their existing family proof.
No body is materialized for source checking, and semantic visit-once state stays
separate from completion records for these value roots.
Exact TypeLoc completion nodes retain type metadata dependencies from normal
source traversal, including alias/template source, array bounds and decltype.
Consumed declarations use their actual TypeSourceInfo; canonical type metadata
cannot replace source. Layout dependencies follow existing owning base/field type
source, while pointer/reference spelling keeps pointee layout lazy. Signature
parameter types retain their own edges with unused defaults isolated. The same
bounded worklist checks completed type and expression nodes without replaying
Sema or instantiating missing definitions.
All resolved operation queries consume this source proof after normal traversal,
including implicit assignments, scalar types and pre-operation false results.
Enum layout retains written underlying types even without an enumerator body.
Implicit family checks also apply to non-nothrow queries because marking the
selected function referenced can resolve its inferred exception specification.
Nothrow defaults additionally check already-resolved actual callee prototypes,
selected function declarations, constructors, bound destructors and allocation/
deallocation dependencies. These checks do not rewrite or evaluate the initializer.
Lazy templates, template-owned or rewritten defaults, incomplete selection,
unresolved exception dependencies and
record-value nothrow destruction still need further evidence. The independent
source check never fills runtime construction/default caches or queues helpers.

LICENSE.TXT contains LLVM's Apache 2.0 license with LLVM exceptions and legacy
notice. BLAKE3/LICENSE and NeverCCppThirdPartyNotices.txt retain the additional
notices for the statically included Support sources. The separate cpp-sdk
directory contains the exact header source snapshot, hashes, provenance,
generation script, and notices for the embedded C++ header distribution.
