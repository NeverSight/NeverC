// Generated by utils/plugin-api/gen-python-abi.py; DO NOT EDIT.
// header-sha256: 72baa530c8e126c2ae47ea4be0b66f2624f76ea0b63e3920b957c8b3394e47f1

// NevercASTLifecycleObserverDescriptor.Callback
static NevercStatus NEVERC_CALL pythonTrampoline_NevercASTLifecycleObserverDescriptor_Callback(NevercTaskHandle Argument0, const NevercASTLifecycleEvent * Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("const NevercASTLifecycleEvent *", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercASTLifecycleObserverDescriptor.Callback", Arguments, std::size(Arguments));
}

// NevercBinaryImageVerifierDescriptor.Verify
static NevercStatus NEVERC_CALL pythonTrampoline_NevercBinaryImageVerifierDescriptor_Verify(void * Argument0, NevercTaskHandle Argument1, const NevercLinkRequest * Argument2, NevercBinaryImageHandle Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercLinkRequest *", Argument2),
      makePythonCallbackArgument("NevercBinaryImageHandle", Argument3),
  };
  return invokePythonStatusCallback(Argument0, "NevercBinaryImageVerifierDescriptor.Verify", Arguments, std::size(Arguments));
}

// NevercBinaryImageVerifierDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercBinaryImageVerifierDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercBinaryImageVerifierDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercCallingConventionDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercCallingConventionDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercCallingConventionDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercCallingConventionDescriptor.PlanCallingConvention
static NevercStatus NEVERC_CALL pythonTrampoline_NevercCallingConventionDescriptor_PlanCallingConvention(void * Argument0, const NevercCallingConventionQuery * Argument1, NevercCallingConventionPlan * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercCallingConventionQuery *", Argument1),
      makePythonCallbackArgument("NevercCallingConventionPlan *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercCallingConventionDescriptor.PlanCallingConvention", Arguments, std::size(Arguments));
}

// NevercCodeGenEdgeDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercCodeGenEdgeDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercCodeGenEdgeDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercCodeGenEdgeDescriptor.CoarseLower
static NevercStatus NEVERC_CALL pythonTrampoline_NevercCodeGenEdgeDescriptor_CoarseLower(void * Argument0, NevercTaskHandle Argument1, const NevercCodeGenRequest * Argument2, NevercCodeGenProductCandidate * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercCodeGenRequest *", Argument2),
      makePythonCallbackArgument("NevercCodeGenProductCandidate *", Argument3),
  };
  return invokePythonStatusCallback(Argument0, "NevercCodeGenEdgeDescriptor.CoarseLower", Arguments, std::size(Arguments));
}

// NevercCodeGenEdgeDescriptor.VerifyProduct
static NevercStatus NEVERC_CALL pythonTrampoline_NevercCodeGenEdgeDescriptor_VerifyProduct(void * Argument0, NevercTaskHandle Argument1, const NevercCodeGenRequest * Argument2, const NevercCodeGenProductCandidate * Argument3, NevercCodeGenVerificationObligations Argument4) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercCodeGenRequest *", Argument2),
      makePythonCallbackArgument("const NevercCodeGenProductCandidate *", Argument3),
      makePythonCallbackArgument("NevercCodeGenVerificationObligations", Argument4),
  };
  return invokePythonStatusCallback(Argument0, "NevercCodeGenEdgeDescriptor.VerifyProduct", Arguments, std::size(Arguments));
}

// NevercDynCodeBinaryVerifierDescriptor.Verify
static NevercStatus NEVERC_CALL pythonTrampoline_NevercDynCodeBinaryVerifierDescriptor_Verify(void * Argument0, const NevercDynCodeBinaryVerifyRequest * Argument1, NevercDynCodeBinaryVerifyResult * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercDynCodeBinaryVerifyRequest *", Argument1),
      makePythonCallbackArgument("NevercDynCodeBinaryVerifyResult *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercDynCodeBinaryVerifierDescriptor.Verify", Arguments, std::size(Arguments));
}

// NevercDynCodeBinaryVerifierDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercDynCodeBinaryVerifierDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercDynCodeBinaryVerifierDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercDynCodeCharsetEncoderDescriptor.Encode
static NevercStatus NEVERC_CALL pythonTrampoline_NevercDynCodeCharsetEncoderDescriptor_Encode(void * Argument0, const NevercDynCodeCharsetEncodeRequest * Argument1, NevercDynCodeCharsetEncodeResult * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercDynCodeCharsetEncodeRequest *", Argument1),
      makePythonCallbackArgument("NevercDynCodeCharsetEncodeResult *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercDynCodeCharsetEncoderDescriptor.Encode", Arguments, std::size(Arguments));
}

// NevercDynCodeCharsetEncoderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercDynCodeCharsetEncoderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercDynCodeCharsetEncoderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercDynCodeExtractorDescriptor.Extract
static NevercStatus NEVERC_CALL pythonTrampoline_NevercDynCodeExtractorDescriptor_Extract(void * Argument0, const NevercDynCodeExtractionRequest * Argument1) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercDynCodeExtractionRequest *", Argument1),
  };
  return invokePythonStatusCallback(Argument0, "NevercDynCodeExtractorDescriptor.Extract", Arguments, std::size(Arguments));
}

// NevercDynCodeExtractorDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercDynCodeExtractorDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercDynCodeExtractorDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercDynCodeImportProviderDescriptor.Import
static NevercStatus NEVERC_CALL pythonTrampoline_NevercDynCodeImportProviderDescriptor_Import(void * Argument0, const NevercDynCodeImportRequest * Argument1, NevercDynCodeImportResult * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercDynCodeImportRequest *", Argument1),
      makePythonCallbackArgument("NevercDynCodeImportResult *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercDynCodeImportProviderDescriptor.Import", Arguments, std::size(Arguments));
}

// NevercDynCodeImportProviderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercDynCodeImportProviderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercDynCodeImportProviderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercDynCodeTargetDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercDynCodeTargetDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercDynCodeTargetDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercIRAnalysisDescriptor.Compute
static NevercStatus NEVERC_CALL pythonTrampoline_NevercIRAnalysisDescriptor_Compute(const NevercIRPassInvocation * Argument0, void ** Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercIRPassInvocation *", Argument0),
      makePythonCallbackArgument("void **", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercIRAnalysisDescriptor.Compute", Arguments, std::size(Arguments));
}

// NevercIRAnalysisDescriptor.Query
static NevercStatus NEVERC_CALL pythonTrampoline_NevercIRAnalysisDescriptor_Query(const void * Argument0, NevercByteView * Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const void *", Argument0),
      makePythonCallbackArgument("NevercByteView *", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercIRAnalysisDescriptor.Query", Arguments, std::size(Arguments));
}

// NevercIRAnalysisDescriptor.Invalidate
static NevercStatus NEVERC_CALL pythonTrampoline_NevercIRAnalysisDescriptor_Invalidate(void * Argument0, NevercIRAnalysisInvalidationReason Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("void *", Argument0),
      makePythonCallbackArgument("NevercIRAnalysisInvalidationReason", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercIRAnalysisDescriptor.Invalidate", Arguments, std::size(Arguments));
}

// NevercIRAnalysisDescriptor.Destroy
static void NEVERC_CALL pythonTrampoline_NevercIRAnalysisDescriptor_Destroy(void * Argument0, void * Argument1) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("void *", Argument0),
  };
  invokePythonVoidCallback(Argument1, "NevercIRAnalysisDescriptor.Destroy", Arguments, std::size(Arguments));
}

// NevercIRAnalysisDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercIRAnalysisDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercIRAnalysisDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercIRPassDescriptor.Run
static NevercStatus NEVERC_CALL pythonTrampoline_NevercIRPassDescriptor_Run(const NevercIRPassInvocation * Argument0, NevercIRPreservedAnalyses * Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercIRPassInvocation *", Argument0),
      makePythonCallbackArgument("NevercIRPreservedAnalyses *", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercIRPassDescriptor.Run", Arguments, std::size(Arguments));
}

// NevercIRPassDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercIRPassDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercIRPassDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercInterceptorDescriptor.Callback
static NevercStatus NEVERC_CALL pythonTrampoline_NevercInterceptorDescriptor_Callback(const NevercPhaseFrame * Argument0, NevercPhaseContinuation * Argument1, NevercPhaseResult * Argument2, void * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercPhaseFrame *", Argument0),
      makePythonCallbackArgument("NevercPhaseContinuation *", Argument1),
      makePythonCallbackArgument("NevercPhaseResult *", Argument2),
  };
  return invokePythonStatusCallback(Argument3, "NevercInterceptorDescriptor.Callback", Arguments, std::size(Arguments));
}

// NevercInterceptorDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercInterceptorDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercInterceptorDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercJobDescriptor.Callback
static NevercStatus NEVERC_CALL pythonTrampoline_NevercJobDescriptor_Callback(const NevercPluginJobContext * Argument0, int32_t * Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercPluginJobContext *", Argument0),
      makePythonCallbackArgument("int32_t *", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercJobDescriptor.Callback", Arguments, std::size(Arguments));
}

// NevercLTOProviderDescriptor.BuildCacheKey
static NevercStatus NEVERC_CALL pythonTrampoline_NevercLTOProviderDescriptor_BuildCacheKey(void * Argument0, NevercTaskHandle Argument1, const NevercLTORequest * Argument2, NevercMutableByteView Argument3, uint64_t * Argument4) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercLTORequest *", Argument2),
      makePythonCallbackArgument("NevercMutableByteView", Argument3),
      makePythonCallbackArgument("uint64_t *", Argument4),
  };
  return invokePythonStatusCallback(Argument0, "NevercLTOProviderDescriptor.BuildCacheKey", Arguments, std::size(Arguments));
}

// NevercLTOProviderDescriptor.Codegen
static NevercStatus NEVERC_CALL pythonTrampoline_NevercLTOProviderDescriptor_Codegen(void * Argument0, NevercTaskHandle Argument1, const NevercLTORequest * Argument2, NevercLTOProductCandidate * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercLTORequest *", Argument2),
      makePythonCallbackArgument("NevercLTOProductCandidate *", Argument3),
  };
  return invokePythonStatusCallback(Argument0, "NevercLTOProviderDescriptor.Codegen", Arguments, std::size(Arguments));
}

// NevercLTOProviderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercLTOProviderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercLTOProviderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercLinkerProviderDescriptor.Link
static NevercStatus NEVERC_CALL pythonTrampoline_NevercLinkerProviderDescriptor_Link(void * Argument0, NevercTaskHandle Argument1, const NevercLinkRequest * Argument2, const NevercRawLinkInputSet * Argument3, NevercLinkerProductCandidate * Argument4) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercLinkRequest *", Argument2),
      makePythonCallbackArgument("const NevercRawLinkInputSet *", Argument3),
      makePythonCallbackArgument("NevercLinkerProductCandidate *", Argument4),
  };
  return invokePythonStatusCallback(Argument0, "NevercLinkerProviderDescriptor.Link", Arguments, std::size(Arguments));
}

// NevercLinkerProviderDescriptor.VerifyImage
static NevercStatus NEVERC_CALL pythonTrampoline_NevercLinkerProviderDescriptor_VerifyImage(void * Argument0, NevercTaskHandle Argument1, const NevercLinkRequest * Argument2, NevercBinaryImageHandle Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercLinkRequest *", Argument2),
      makePythonCallbackArgument("NevercBinaryImageHandle", Argument3),
  };
  return invokePythonStatusCallback(Argument0, "NevercLinkerProviderDescriptor.VerifyImage", Arguments, std::size(Arguments));
}

// NevercLinkerProviderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercLinkerProviderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercLinkerProviderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercMCAsmBackendDescriptor.GetFixupKindInfo
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCAsmBackendDescriptor_GetFixupKindInfo(void * Argument0, NevercMCFixupKind Argument1, uint32_t Argument2, NevercMCFixupKindInfo * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercMCFixupKind", Argument1),
      makePythonCallbackArgument("uint32_t", Argument2),
      makePythonCallbackArgument("NevercMCFixupKindInfo *", Argument3),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCAsmBackendDescriptor.GetFixupKindInfo", Arguments, std::size(Arguments));
}

// NevercMCAsmBackendDescriptor.MapRelocation
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCAsmBackendDescriptor_MapRelocation(void * Argument0, const NevercMCLayoutFixupRequest * Argument1, uint32_t * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercMCLayoutFixupRequest *", Argument1),
      makePythonCallbackArgument("uint32_t *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCAsmBackendDescriptor.MapRelocation", Arguments, std::size(Arguments));
}

// NevercMCAsmBackendDescriptor.ShouldRelaxFixup
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCAsmBackendDescriptor_ShouldRelaxFixup(void * Argument0, const NevercMCLayoutFixupRequest * Argument1, NevercBool * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercMCLayoutFixupRequest *", Argument1),
      makePythonCallbackArgument("NevercBool *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCAsmBackendDescriptor.ShouldRelaxFixup", Arguments, std::size(Arguments));
}

// NevercMCAsmBackendDescriptor.RelaxFragment
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCAsmBackendDescriptor_RelaxFragment(void * Argument0, const NevercMCLayoutFixupRequest * Argument1, NevercMCRelaxationResult * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercMCLayoutFixupRequest *", Argument1),
      makePythonCallbackArgument("NevercMCRelaxationResult *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCAsmBackendDescriptor.RelaxFragment", Arguments, std::size(Arguments));
}

// NevercMCAsmBackendDescriptor.ApplyFixup
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCAsmBackendDescriptor_ApplyFixup(void * Argument0, const NevercMCLayoutFixupRequest * Argument1, NevercMutableByteView Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercMCLayoutFixupRequest *", Argument1),
      makePythonCallbackArgument("NevercMutableByteView", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCAsmBackendDescriptor.ApplyFixup", Arguments, std::size(Arguments));
}

// NevercMCAsmBackendDescriptor.WriteNops
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCAsmBackendDescriptor_WriteNops(void * Argument0, uint64_t Argument1, const NevercMCByteSink * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("uint64_t", Argument1),
      makePythonCallbackArgument("const NevercMCByteSink *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCAsmBackendDescriptor.WriteNops", Arguments, std::size(Arguments));
}

// NevercMCAsmBackendDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercMCAsmBackendDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercMCAsmBackendDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercMCDecoderDescriptor.DecodeInstruction
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCDecoderDescriptor_DecodeInstruction(void * Argument0, const NevercMCDecodeRequest * Argument1, NevercMCDecodeResult * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercMCDecodeRequest *", Argument1),
      makePythonCallbackArgument("NevercMCDecodeResult *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCDecoderDescriptor.DecodeInstruction", Arguments, std::size(Arguments));
}

// NevercMCDecoderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercMCDecoderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercMCDecoderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercMCEncoderDescriptor.EncodeInstruction
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMCEncoderDescriptor_EncodeInstruction(void * Argument0, const NevercMCEncodeRequest * Argument1, const NevercMCEncodeSink * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercMCEncodeRequest *", Argument1),
      makePythonCallbackArgument("const NevercMCEncodeSink *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercMCEncoderDescriptor.EncodeInstruction", Arguments, std::size(Arguments));
}

// NevercMCEncoderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercMCEncoderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercMCEncoderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercMCSchemaDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercMCSchemaDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercMCSchemaDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercMIRPassDescriptor.Run
static NevercStatus NEVERC_CALL pythonTrampoline_NevercMIRPassDescriptor_Run(const NevercMIRPassInvocation * Argument0, NevercMIRPreservedAnalyses * Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercMIRPassInvocation *", Argument0),
      makePythonCallbackArgument("NevercMIRPreservedAnalyses *", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercMIRPassDescriptor.Run", Arguments, std::size(Arguments));
}

// NevercMIRPassDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercMIRPassDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercMIRPassDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercObjectFormatDescriptor.Probe
static NevercStatus NEVERC_CALL pythonTrampoline_NevercObjectFormatDescriptor_Probe(void * Argument0, const NevercObjectProbeRequest * Argument1, NevercObjectProbeResult * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercObjectProbeRequest *", Argument1),
      makePythonCallbackArgument("NevercObjectProbeResult *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercObjectFormatDescriptor.Probe", Arguments, std::size(Arguments));
}

// NevercObjectFormatDescriptor.Reader
static NevercStatus NEVERC_CALL pythonTrampoline_NevercObjectFormatDescriptor_Reader(void * Argument0, const NevercObjectReadRequest * Argument1) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercObjectReadRequest *", Argument1),
  };
  return invokePythonStatusCallback(Argument0, "NevercObjectFormatDescriptor.Reader", Arguments, std::size(Arguments));
}

// NevercObjectFormatDescriptor.Writer
static NevercStatus NEVERC_CALL pythonTrampoline_NevercObjectFormatDescriptor_Writer(void * Argument0, const NevercObjectWriteRequest * Argument1) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercObjectWriteRequest *", Argument1),
  };
  return invokePythonStatusCallback(Argument0, "NevercObjectFormatDescriptor.Writer", Arguments, std::size(Arguments));
}

// NevercObjectFormatDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercObjectFormatDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercObjectFormatDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercObjectMergeProviderDescriptor.Merge
static NevercStatus NEVERC_CALL pythonTrampoline_NevercObjectMergeProviderDescriptor_Merge(void * Argument0, NevercTaskHandle Argument1, const NevercObjectMergeRequest * Argument2, NevercObjectMergeCandidate * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument1),
      makePythonCallbackArgument("const NevercObjectMergeRequest *", Argument2),
      makePythonCallbackArgument("NevercObjectMergeCandidate *", Argument3),
  };
  return invokePythonStatusCallback(Argument0, "NevercObjectMergeProviderDescriptor.Merge", Arguments, std::size(Arguments));
}

// NevercObjectMergeProviderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercObjectMergeProviderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercObjectMergeProviderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercObserverDescriptor.Callback
static NevercStatus NEVERC_CALL pythonTrampoline_NevercObserverDescriptor_Callback(const NevercPhaseFrame * Argument0, NevercObserverPoint Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercPhaseFrame *", Argument0),
      makePythonCallbackArgument("NevercObserverPoint", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercObserverDescriptor.Callback", Arguments, std::size(Arguments));
}

// NevercObserverDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercObserverDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercObserverDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercOptionDescriptor.Validator
static NevercStatus NEVERC_CALL pythonTrampoline_NevercOptionDescriptor_Validator(const NevercOptionValidationContext * Argument0, NevercStringView Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercOptionValidationContext *", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercOptionDescriptor.Validator", Arguments, std::size(Arguments));
}

// NevercOptionDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercOptionDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercOptionDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercPrepObserverDescriptor.Callback
static NevercStatus NEVERC_CALL pythonTrampoline_NevercPrepObserverDescriptor_Callback(NevercTaskHandle Argument0, const NevercPrepEvent * Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("const NevercPrepEvent *", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercPrepObserverDescriptor.Callback", Arguments, std::size(Arguments));
}

// NevercProviderDescriptor.Callback
static NevercStatus NEVERC_CALL pythonTrampoline_NevercProviderDescriptor_Callback(const NevercPhaseFrame * Argument0, NevercPhaseResult * Argument1, void * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercPhaseFrame *", Argument0),
      makePythonCallbackArgument("NevercPhaseResult *", Argument1),
  };
  return invokePythonStatusCallback(Argument2, "NevercProviderDescriptor.Callback", Arguments, std::size(Arguments));
}

// NevercProviderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercProviderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercProviderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercTargetABIDescriptor.ClassifyFunction
static NevercStatus NEVERC_CALL pythonTrampoline_NevercTargetABIDescriptor_ClassifyFunction(void * Argument0, const NevercABIFunctionQuery * Argument1, NevercABIArgumentClassification * Argument2, NevercABIArgumentClassificationArray * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercABIFunctionQuery *", Argument1),
      makePythonCallbackArgument("NevercABIArgumentClassification *", Argument2),
      makePythonCallbackArgument("NevercABIArgumentClassificationArray *", Argument3),
  };
  return invokePythonStatusCallback(Argument0, "NevercTargetABIDescriptor.ClassifyFunction", Arguments, std::size(Arguments));
}

// NevercTargetABIDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercTargetABIDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercTargetABIDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercTargetDescriptor.ValidateCPU
static NevercStatus NEVERC_CALL pythonTrampoline_NevercTargetDescriptor_ValidateCPU(NevercTaskHandle Argument0, NevercStringView Argument1, void * Argument2, NevercBool * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercBool *", Argument3),
  };
  return invokePythonStatusCallback(Argument2, "NevercTargetDescriptor.ValidateCPU", Arguments, std::size(Arguments));
}

// NevercTargetDescriptor.CanonicalizeCPU
static NevercStatus NEVERC_CALL pythonTrampoline_NevercTargetDescriptor_CanonicalizeCPU(NevercTaskHandle Argument0, NevercStringView Argument1, void * Argument2, NevercStringView * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercStringView *", Argument3),
  };
  return invokePythonStatusCallback(Argument2, "NevercTargetDescriptor.CanonicalizeCPU", Arguments, std::size(Arguments));
}

// NevercTargetDescriptor.ListCPUs
static NevercStatus NEVERC_CALL pythonTrampoline_NevercTargetDescriptor_ListCPUs(NevercTaskHandle Argument0, void * Argument1, NevercStringArrayView * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringArrayView *", Argument2),
  };
  return invokePythonStatusCallback(Argument1, "NevercTargetDescriptor.ListCPUs", Arguments, std::size(Arguments));
}

// NevercTargetDescriptor.ResolveFeatures
static NevercStatus NEVERC_CALL pythonTrampoline_NevercTargetDescriptor_ResolveFeatures(NevercTaskHandle Argument0, NevercStringView Argument1, NevercStringArrayView Argument2, void * Argument3, NevercStructArrayView * Argument4) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercStringArrayView", Argument2),
      makePythonCallbackArgument("NevercStructArrayView *", Argument4),
  };
  return invokePythonStatusCallback(Argument3, "NevercTargetDescriptor.ResolveFeatures", Arguments, std::size(Arguments));
}

// NevercTargetDescriptor.CreateTargetMachine
static NevercStatus NEVERC_CALL pythonTrampoline_NevercTargetDescriptor_CreateTargetMachine(void * Argument0, const NevercTargetMachineCreateRequest * Argument1, NevercTargetMachineHandle * Argument2) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("const NevercTargetMachineCreateRequest *", Argument1),
      makePythonCallbackArgument("NevercTargetMachineHandle *", Argument2),
  };
  return invokePythonStatusCallback(Argument0, "NevercTargetDescriptor.CreateTargetMachine", Arguments, std::size(Arguments));
}

// NevercTargetDescriptor.DestroyTargetMachine
static void NEVERC_CALL pythonTrampoline_NevercTargetDescriptor_DestroyTargetMachine(void * Argument0, NevercTargetMachineHandle Argument1) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTargetMachineHandle", Argument1),
  };
  invokePythonVoidCallback(Argument0, "NevercTargetDescriptor.DestroyTargetMachine", Arguments, std::size(Arguments));
}

// NevercTargetDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercTargetDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercTargetDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

// NevercVFSProviderDescriptor.MatchesPath
static NevercStatus NEVERC_CALL pythonTrampoline_NevercVFSProviderDescriptor_MatchesPath(NevercTaskHandle Argument0, NevercStringView Argument1, void * Argument2, NevercBool * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercBool *", Argument3),
  };
  return invokePythonStatusCallback(Argument2, "NevercVFSProviderDescriptor.MatchesPath", Arguments, std::size(Arguments));
}

// NevercVFSProviderDescriptor.Status
static NevercStatus NEVERC_CALL pythonTrampoline_NevercVFSProviderDescriptor_Status(NevercTaskHandle Argument0, NevercStringView Argument1, void * Argument2, NevercVFSStatusResult * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercVFSStatusResult *", Argument3),
  };
  return invokePythonStatusCallback(Argument2, "NevercVFSProviderDescriptor.Status", Arguments, std::size(Arguments));
}

// NevercVFSProviderDescriptor.OpenRead
static NevercStatus NEVERC_CALL pythonTrampoline_NevercVFSProviderDescriptor_OpenRead(NevercTaskHandle Argument0, NevercStringView Argument1, void * Argument2, NevercVFSOpenReadResult * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercVFSOpenReadResult *", Argument3),
  };
  return invokePythonStatusCallback(Argument2, "NevercVFSProviderDescriptor.OpenRead", Arguments, std::size(Arguments));
}

// NevercVFSProviderDescriptor.ReadDirectory
static NevercStatus NEVERC_CALL pythonTrampoline_NevercVFSProviderDescriptor_ReadDirectory(NevercTaskHandle Argument0, NevercStringView Argument1, void * Argument2, NevercVFSDirectoryResult * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercVFSDirectoryResult *", Argument3),
  };
  return invokePythonStatusCallback(Argument2, "NevercVFSProviderDescriptor.ReadDirectory", Arguments, std::size(Arguments));
}

// NevercVFSProviderDescriptor.Canonicalize
static NevercStatus NEVERC_CALL pythonTrampoline_NevercVFSProviderDescriptor_Canonicalize(NevercTaskHandle Argument0, NevercStringView Argument1, void * Argument2, NevercVFSCanonicalPathResult * Argument3) {
  const PythonCallbackArgument Arguments[] = {
      makePythonCallbackArgument("NevercTaskHandle", Argument0),
      makePythonCallbackArgument("NevercStringView", Argument1),
      makePythonCallbackArgument("NevercVFSCanonicalPathResult *", Argument3),
  };
  return invokePythonStatusCallback(Argument2, "NevercVFSProviderDescriptor.Canonicalize", Arguments, std::size(Arguments));
}

// NevercVFSProviderDescriptor.DestroyUserData
static void NEVERC_CALL pythonTrampoline_NevercVFSProviderDescriptor_DestroyUserData(void * Argument0) {
  invokePythonVoidCallback(Argument0, "NevercVFSProviderDescriptor.DestroyUserData", nullptr, 0);
  pythonCallbackUserDataDestroyed(Argument0);
}

static bool configurePythonCallbackRecord(
    PythonCallbackBinding &Binding, const std::string &RecordName,
    std::vector<uint8_t> &Bytes, std::string &Error) {
  if (RecordName == "NevercASTLifecycleObserverDescriptor") {
    if (Bytes.size() != sizeof(NevercASTLifecycleObserverDescriptor)) {
      Error = "descriptor byte size does not match NevercASTLifecycleObserverDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Callback"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercASTLifecycleObserverDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, nullptr);
    if (Descriptor->Callback && !Binding.hasCallback("Callback")) {
      Error = "native callback requires a Python replacement: NevercASTLifecycleObserverDescriptor.Callback";
      return false;
    }
    Descriptor->Callback = Binding.hasCallback("Callback") ? pythonTrampoline_NevercASTLifecycleObserverDescriptor_Callback : nullptr;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercBinaryImageVerifierDescriptor") {
    if (Bytes.size() != sizeof(NevercBinaryImageVerifierDescriptor)) {
      Error = "descriptor byte size does not match NevercBinaryImageVerifierDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Verify", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercBinaryImageVerifierDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Verify && !Binding.hasCallback("Verify")) {
      Error = "native callback requires a Python replacement: NevercBinaryImageVerifierDescriptor.Verify";
      return false;
    }
    Descriptor->Verify = Binding.hasCallback("Verify") ? pythonTrampoline_NevercBinaryImageVerifierDescriptor_Verify : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercBinaryImageVerifierDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercCallingConventionDescriptor") {
    if (Bytes.size() != sizeof(NevercCallingConventionDescriptor)) {
      Error = "descriptor byte size does not match NevercCallingConventionDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"DestroyUserData", "PlanCallingConvention"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercCallingConventionDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    Descriptor->DestroyUserData = pythonTrampoline_NevercCallingConventionDescriptor_DestroyUserData;
    if (Descriptor->PlanCallingConvention && !Binding.hasCallback("PlanCallingConvention")) {
      Error = "native callback requires a Python replacement: NevercCallingConventionDescriptor.PlanCallingConvention";
      return false;
    }
    Descriptor->PlanCallingConvention = Binding.hasCallback("PlanCallingConvention") ? pythonTrampoline_NevercCallingConventionDescriptor_PlanCallingConvention : nullptr;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercCodeGenEdgeDescriptor") {
    if (Bytes.size() != sizeof(NevercCodeGenEdgeDescriptor)) {
      Error = "descriptor byte size does not match NevercCodeGenEdgeDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"DestroyUserData", "CoarseLower", "VerifyProduct"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercCodeGenEdgeDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    Descriptor->DestroyUserData = pythonTrampoline_NevercCodeGenEdgeDescriptor_DestroyUserData;
    if (Descriptor->CoarseLower && !Binding.hasCallback("CoarseLower")) {
      Error = "native callback requires a Python replacement: NevercCodeGenEdgeDescriptor.CoarseLower";
      return false;
    }
    Descriptor->CoarseLower = Binding.hasCallback("CoarseLower") ? pythonTrampoline_NevercCodeGenEdgeDescriptor_CoarseLower : nullptr;
    if (Descriptor->VerifyProduct && !Binding.hasCallback("VerifyProduct")) {
      Error = "native callback requires a Python replacement: NevercCodeGenEdgeDescriptor.VerifyProduct";
      return false;
    }
    Descriptor->VerifyProduct = Binding.hasCallback("VerifyProduct") ? pythonTrampoline_NevercCodeGenEdgeDescriptor_VerifyProduct : nullptr;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercDynCodeBinaryVerifierDescriptor") {
    if (Bytes.size() != sizeof(NevercDynCodeBinaryVerifierDescriptor)) {
      Error = "descriptor byte size does not match NevercDynCodeBinaryVerifierDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Verify", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercDynCodeBinaryVerifierDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Verify && !Binding.hasCallback("Verify")) {
      Error = "native callback requires a Python replacement: NevercDynCodeBinaryVerifierDescriptor.Verify";
      return false;
    }
    Descriptor->Verify = Binding.hasCallback("Verify") ? pythonTrampoline_NevercDynCodeBinaryVerifierDescriptor_Verify : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercDynCodeBinaryVerifierDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercDynCodeCharsetEncoderDescriptor") {
    if (Bytes.size() != sizeof(NevercDynCodeCharsetEncoderDescriptor)) {
      Error = "descriptor byte size does not match NevercDynCodeCharsetEncoderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Encode", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercDynCodeCharsetEncoderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Encode && !Binding.hasCallback("Encode")) {
      Error = "native callback requires a Python replacement: NevercDynCodeCharsetEncoderDescriptor.Encode";
      return false;
    }
    Descriptor->Encode = Binding.hasCallback("Encode") ? pythonTrampoline_NevercDynCodeCharsetEncoderDescriptor_Encode : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercDynCodeCharsetEncoderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercDynCodeExtractorDescriptor") {
    if (Bytes.size() != sizeof(NevercDynCodeExtractorDescriptor)) {
      Error = "descriptor byte size does not match NevercDynCodeExtractorDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Extract", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercDynCodeExtractorDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Extract && !Binding.hasCallback("Extract")) {
      Error = "native callback requires a Python replacement: NevercDynCodeExtractorDescriptor.Extract";
      return false;
    }
    Descriptor->Extract = Binding.hasCallback("Extract") ? pythonTrampoline_NevercDynCodeExtractorDescriptor_Extract : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercDynCodeExtractorDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercDynCodeImportProviderDescriptor") {
    if (Bytes.size() != sizeof(NevercDynCodeImportProviderDescriptor)) {
      Error = "descriptor byte size does not match NevercDynCodeImportProviderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Import", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercDynCodeImportProviderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Import && !Binding.hasCallback("Import")) {
      Error = "native callback requires a Python replacement: NevercDynCodeImportProviderDescriptor.Import";
      return false;
    }
    Descriptor->Import = Binding.hasCallback("Import") ? pythonTrampoline_NevercDynCodeImportProviderDescriptor_Import : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercDynCodeImportProviderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercDynCodeTargetDescriptor") {
    if (Bytes.size() != sizeof(NevercDynCodeTargetDescriptor)) {
      Error = "descriptor byte size does not match NevercDynCodeTargetDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercDynCodeTargetDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    Descriptor->DestroyUserData = pythonTrampoline_NevercDynCodeTargetDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercIRAnalysisDescriptor") {
    if (Bytes.size() != sizeof(NevercIRAnalysisDescriptor)) {
      Error = "descriptor byte size does not match NevercIRAnalysisDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Compute", "Query", "Invalidate", "Destroy", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercIRAnalysisDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Compute && !Binding.hasCallback("Compute")) {
      Error = "native callback requires a Python replacement: NevercIRAnalysisDescriptor.Compute";
      return false;
    }
    Descriptor->Compute = Binding.hasCallback("Compute") ? pythonTrampoline_NevercIRAnalysisDescriptor_Compute : nullptr;
    if (Descriptor->Query && !Binding.hasCallback("Query")) {
      Error = "native callback requires a Python replacement: NevercIRAnalysisDescriptor.Query";
      return false;
    }
    Descriptor->Query = Binding.hasCallback("Query") ? pythonTrampoline_NevercIRAnalysisDescriptor_Query : nullptr;
    if (Descriptor->Invalidate && !Binding.hasCallback("Invalidate")) {
      Error = "native callback requires a Python replacement: NevercIRAnalysisDescriptor.Invalidate";
      return false;
    }
    Descriptor->Invalidate = Binding.hasCallback("Invalidate") ? pythonTrampoline_NevercIRAnalysisDescriptor_Invalidate : nullptr;
    if (Descriptor->Destroy && !Binding.hasCallback("Destroy")) {
      Error = "native callback requires a Python replacement: NevercIRAnalysisDescriptor.Destroy";
      return false;
    }
    Descriptor->Destroy = Binding.hasCallback("Destroy") ? pythonTrampoline_NevercIRAnalysisDescriptor_Destroy : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercIRAnalysisDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercIRPassDescriptor") {
    if (Bytes.size() != sizeof(NevercIRPassDescriptor)) {
      Error = "descriptor byte size does not match NevercIRPassDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Run", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercIRPassDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Run && !Binding.hasCallback("Run")) {
      Error = "native callback requires a Python replacement: NevercIRPassDescriptor.Run";
      return false;
    }
    Descriptor->Run = Binding.hasCallback("Run") ? pythonTrampoline_NevercIRPassDescriptor_Run : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercIRPassDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercInterceptorDescriptor") {
    if (Bytes.size() != sizeof(NevercInterceptorDescriptor)) {
      Error = "descriptor byte size does not match NevercInterceptorDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Callback", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercInterceptorDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Callback && !Binding.hasCallback("Callback")) {
      Error = "native callback requires a Python replacement: NevercInterceptorDescriptor.Callback";
      return false;
    }
    Descriptor->Callback = Binding.hasCallback("Callback") ? pythonTrampoline_NevercInterceptorDescriptor_Callback : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercInterceptorDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercJobDescriptor") {
    if (Bytes.size() != sizeof(NevercJobDescriptor)) {
      Error = "descriptor byte size does not match NevercJobDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Callback"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercJobDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, nullptr);
    if (Descriptor->Callback && !Binding.hasCallback("Callback")) {
      Error = "native callback requires a Python replacement: NevercJobDescriptor.Callback";
      return false;
    }
    Descriptor->Callback = Binding.hasCallback("Callback") ? pythonTrampoline_NevercJobDescriptor_Callback : nullptr;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercLTOProviderDescriptor") {
    if (Bytes.size() != sizeof(NevercLTOProviderDescriptor)) {
      Error = "descriptor byte size does not match NevercLTOProviderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"BuildCacheKey", "Codegen", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercLTOProviderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->BuildCacheKey && !Binding.hasCallback("BuildCacheKey")) {
      Error = "native callback requires a Python replacement: NevercLTOProviderDescriptor.BuildCacheKey";
      return false;
    }
    Descriptor->BuildCacheKey = Binding.hasCallback("BuildCacheKey") ? pythonTrampoline_NevercLTOProviderDescriptor_BuildCacheKey : nullptr;
    if (Descriptor->Codegen && !Binding.hasCallback("Codegen")) {
      Error = "native callback requires a Python replacement: NevercLTOProviderDescriptor.Codegen";
      return false;
    }
    Descriptor->Codegen = Binding.hasCallback("Codegen") ? pythonTrampoline_NevercLTOProviderDescriptor_Codegen : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercLTOProviderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercLinkerProviderDescriptor") {
    if (Bytes.size() != sizeof(NevercLinkerProviderDescriptor)) {
      Error = "descriptor byte size does not match NevercLinkerProviderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Link", "VerifyImage", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercLinkerProviderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Link && !Binding.hasCallback("Link")) {
      Error = "native callback requires a Python replacement: NevercLinkerProviderDescriptor.Link";
      return false;
    }
    Descriptor->Link = Binding.hasCallback("Link") ? pythonTrampoline_NevercLinkerProviderDescriptor_Link : nullptr;
    if (Descriptor->VerifyImage && !Binding.hasCallback("VerifyImage")) {
      Error = "native callback requires a Python replacement: NevercLinkerProviderDescriptor.VerifyImage";
      return false;
    }
    Descriptor->VerifyImage = Binding.hasCallback("VerifyImage") ? pythonTrampoline_NevercLinkerProviderDescriptor_VerifyImage : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercLinkerProviderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercMCAsmBackendDescriptor") {
    if (Bytes.size() != sizeof(NevercMCAsmBackendDescriptor)) {
      Error = "descriptor byte size does not match NevercMCAsmBackendDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"GetFixupKindInfo", "MapRelocation", "ShouldRelaxFixup", "RelaxFragment", "ApplyFixup", "WriteNops", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercMCAsmBackendDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->GetFixupKindInfo && !Binding.hasCallback("GetFixupKindInfo")) {
      Error = "native callback requires a Python replacement: NevercMCAsmBackendDescriptor.GetFixupKindInfo";
      return false;
    }
    Descriptor->GetFixupKindInfo = Binding.hasCallback("GetFixupKindInfo") ? pythonTrampoline_NevercMCAsmBackendDescriptor_GetFixupKindInfo : nullptr;
    if (Descriptor->MapRelocation && !Binding.hasCallback("MapRelocation")) {
      Error = "native callback requires a Python replacement: NevercMCAsmBackendDescriptor.MapRelocation";
      return false;
    }
    Descriptor->MapRelocation = Binding.hasCallback("MapRelocation") ? pythonTrampoline_NevercMCAsmBackendDescriptor_MapRelocation : nullptr;
    if (Descriptor->ShouldRelaxFixup && !Binding.hasCallback("ShouldRelaxFixup")) {
      Error = "native callback requires a Python replacement: NevercMCAsmBackendDescriptor.ShouldRelaxFixup";
      return false;
    }
    Descriptor->ShouldRelaxFixup = Binding.hasCallback("ShouldRelaxFixup") ? pythonTrampoline_NevercMCAsmBackendDescriptor_ShouldRelaxFixup : nullptr;
    if (Descriptor->RelaxFragment && !Binding.hasCallback("RelaxFragment")) {
      Error = "native callback requires a Python replacement: NevercMCAsmBackendDescriptor.RelaxFragment";
      return false;
    }
    Descriptor->RelaxFragment = Binding.hasCallback("RelaxFragment") ? pythonTrampoline_NevercMCAsmBackendDescriptor_RelaxFragment : nullptr;
    if (Descriptor->ApplyFixup && !Binding.hasCallback("ApplyFixup")) {
      Error = "native callback requires a Python replacement: NevercMCAsmBackendDescriptor.ApplyFixup";
      return false;
    }
    Descriptor->ApplyFixup = Binding.hasCallback("ApplyFixup") ? pythonTrampoline_NevercMCAsmBackendDescriptor_ApplyFixup : nullptr;
    if (Descriptor->WriteNops && !Binding.hasCallback("WriteNops")) {
      Error = "native callback requires a Python replacement: NevercMCAsmBackendDescriptor.WriteNops";
      return false;
    }
    Descriptor->WriteNops = Binding.hasCallback("WriteNops") ? pythonTrampoline_NevercMCAsmBackendDescriptor_WriteNops : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercMCAsmBackendDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercMCDecoderDescriptor") {
    if (Bytes.size() != sizeof(NevercMCDecoderDescriptor)) {
      Error = "descriptor byte size does not match NevercMCDecoderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"DecodeInstruction", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercMCDecoderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->DecodeInstruction && !Binding.hasCallback("DecodeInstruction")) {
      Error = "native callback requires a Python replacement: NevercMCDecoderDescriptor.DecodeInstruction";
      return false;
    }
    Descriptor->DecodeInstruction = Binding.hasCallback("DecodeInstruction") ? pythonTrampoline_NevercMCDecoderDescriptor_DecodeInstruction : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercMCDecoderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercMCEncoderDescriptor") {
    if (Bytes.size() != sizeof(NevercMCEncoderDescriptor)) {
      Error = "descriptor byte size does not match NevercMCEncoderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"EncodeInstruction", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercMCEncoderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->EncodeInstruction && !Binding.hasCallback("EncodeInstruction")) {
      Error = "native callback requires a Python replacement: NevercMCEncoderDescriptor.EncodeInstruction";
      return false;
    }
    Descriptor->EncodeInstruction = Binding.hasCallback("EncodeInstruction") ? pythonTrampoline_NevercMCEncoderDescriptor_EncodeInstruction : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercMCEncoderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercMCSchemaDescriptor") {
    if (Bytes.size() != sizeof(NevercMCSchemaDescriptor)) {
      Error = "descriptor byte size does not match NevercMCSchemaDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercMCSchemaDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    Descriptor->DestroyUserData = pythonTrampoline_NevercMCSchemaDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercMIRPassDescriptor") {
    if (Bytes.size() != sizeof(NevercMIRPassDescriptor)) {
      Error = "descriptor byte size does not match NevercMIRPassDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Run", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercMIRPassDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Run && !Binding.hasCallback("Run")) {
      Error = "native callback requires a Python replacement: NevercMIRPassDescriptor.Run";
      return false;
    }
    Descriptor->Run = Binding.hasCallback("Run") ? pythonTrampoline_NevercMIRPassDescriptor_Run : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercMIRPassDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercObjectFormatDescriptor") {
    if (Bytes.size() != sizeof(NevercObjectFormatDescriptor)) {
      Error = "descriptor byte size does not match NevercObjectFormatDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Probe", "Reader", "Writer", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercObjectFormatDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Probe && !Binding.hasCallback("Probe")) {
      Error = "native callback requires a Python replacement: NevercObjectFormatDescriptor.Probe";
      return false;
    }
    Descriptor->Probe = Binding.hasCallback("Probe") ? pythonTrampoline_NevercObjectFormatDescriptor_Probe : nullptr;
    if (Descriptor->Reader && !Binding.hasCallback("Reader")) {
      Error = "native callback requires a Python replacement: NevercObjectFormatDescriptor.Reader";
      return false;
    }
    Descriptor->Reader = Binding.hasCallback("Reader") ? pythonTrampoline_NevercObjectFormatDescriptor_Reader : nullptr;
    if (Descriptor->Writer && !Binding.hasCallback("Writer")) {
      Error = "native callback requires a Python replacement: NevercObjectFormatDescriptor.Writer";
      return false;
    }
    Descriptor->Writer = Binding.hasCallback("Writer") ? pythonTrampoline_NevercObjectFormatDescriptor_Writer : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercObjectFormatDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercObjectMergeProviderDescriptor") {
    if (Bytes.size() != sizeof(NevercObjectMergeProviderDescriptor)) {
      Error = "descriptor byte size does not match NevercObjectMergeProviderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Merge", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercObjectMergeProviderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Merge && !Binding.hasCallback("Merge")) {
      Error = "native callback requires a Python replacement: NevercObjectMergeProviderDescriptor.Merge";
      return false;
    }
    Descriptor->Merge = Binding.hasCallback("Merge") ? pythonTrampoline_NevercObjectMergeProviderDescriptor_Merge : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercObjectMergeProviderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercObserverDescriptor") {
    if (Bytes.size() != sizeof(NevercObserverDescriptor)) {
      Error = "descriptor byte size does not match NevercObserverDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Callback", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercObserverDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Callback && !Binding.hasCallback("Callback")) {
      Error = "native callback requires a Python replacement: NevercObserverDescriptor.Callback";
      return false;
    }
    Descriptor->Callback = Binding.hasCallback("Callback") ? pythonTrampoline_NevercObserverDescriptor_Callback : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercObserverDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercOptionDescriptor") {
    if (Bytes.size() != sizeof(NevercOptionDescriptor)) {
      Error = "descriptor byte size does not match NevercOptionDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Validator", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercOptionDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Validator && !Binding.hasCallback("Validator")) {
      Error = "native callback requires a Python replacement: NevercOptionDescriptor.Validator";
      return false;
    }
    Descriptor->Validator = Binding.hasCallback("Validator") ? pythonTrampoline_NevercOptionDescriptor_Validator : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercOptionDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercPrepObserverDescriptor") {
    if (Bytes.size() != sizeof(NevercPrepObserverDescriptor)) {
      Error = "descriptor byte size does not match NevercPrepObserverDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Callback"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercPrepObserverDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, nullptr);
    if (Descriptor->Callback && !Binding.hasCallback("Callback")) {
      Error = "native callback requires a Python replacement: NevercPrepObserverDescriptor.Callback";
      return false;
    }
    Descriptor->Callback = Binding.hasCallback("Callback") ? pythonTrampoline_NevercPrepObserverDescriptor_Callback : nullptr;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercProviderDescriptor") {
    if (Bytes.size() != sizeof(NevercProviderDescriptor)) {
      Error = "descriptor byte size does not match NevercProviderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"Callback", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercProviderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->Callback && !Binding.hasCallback("Callback")) {
      Error = "native callback requires a Python replacement: NevercProviderDescriptor.Callback";
      return false;
    }
    Descriptor->Callback = Binding.hasCallback("Callback") ? pythonTrampoline_NevercProviderDescriptor_Callback : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercProviderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercTargetABIDescriptor") {
    if (Bytes.size() != sizeof(NevercTargetABIDescriptor)) {
      Error = "descriptor byte size does not match NevercTargetABIDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"ClassifyFunction", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercTargetABIDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->ClassifyFunction && !Binding.hasCallback("ClassifyFunction")) {
      Error = "native callback requires a Python replacement: NevercTargetABIDescriptor.ClassifyFunction";
      return false;
    }
    Descriptor->ClassifyFunction = Binding.hasCallback("ClassifyFunction") ? pythonTrampoline_NevercTargetABIDescriptor_ClassifyFunction : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercTargetABIDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercTargetDescriptor") {
    if (Bytes.size() != sizeof(NevercTargetDescriptor)) {
      Error = "descriptor byte size does not match NevercTargetDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"ValidateCPU", "CanonicalizeCPU", "ListCPUs", "ResolveFeatures", "CreateTargetMachine", "DestroyTargetMachine", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercTargetDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->ValidateCPU && !Binding.hasCallback("ValidateCPU")) {
      Error = "native callback requires a Python replacement: NevercTargetDescriptor.ValidateCPU";
      return false;
    }
    Descriptor->ValidateCPU = Binding.hasCallback("ValidateCPU") ? pythonTrampoline_NevercTargetDescriptor_ValidateCPU : nullptr;
    if (Descriptor->CanonicalizeCPU && !Binding.hasCallback("CanonicalizeCPU")) {
      Error = "native callback requires a Python replacement: NevercTargetDescriptor.CanonicalizeCPU";
      return false;
    }
    Descriptor->CanonicalizeCPU = Binding.hasCallback("CanonicalizeCPU") ? pythonTrampoline_NevercTargetDescriptor_CanonicalizeCPU : nullptr;
    if (Descriptor->ListCPUs && !Binding.hasCallback("ListCPUs")) {
      Error = "native callback requires a Python replacement: NevercTargetDescriptor.ListCPUs";
      return false;
    }
    Descriptor->ListCPUs = Binding.hasCallback("ListCPUs") ? pythonTrampoline_NevercTargetDescriptor_ListCPUs : nullptr;
    if (Descriptor->ResolveFeatures && !Binding.hasCallback("ResolveFeatures")) {
      Error = "native callback requires a Python replacement: NevercTargetDescriptor.ResolveFeatures";
      return false;
    }
    Descriptor->ResolveFeatures = Binding.hasCallback("ResolveFeatures") ? pythonTrampoline_NevercTargetDescriptor_ResolveFeatures : nullptr;
    if (Descriptor->CreateTargetMachine && !Binding.hasCallback("CreateTargetMachine")) {
      Error = "native callback requires a Python replacement: NevercTargetDescriptor.CreateTargetMachine";
      return false;
    }
    Descriptor->CreateTargetMachine = Binding.hasCallback("CreateTargetMachine") ? pythonTrampoline_NevercTargetDescriptor_CreateTargetMachine : nullptr;
    if (Descriptor->DestroyTargetMachine && !Binding.hasCallback("DestroyTargetMachine")) {
      Error = "native callback requires a Python replacement: NevercTargetDescriptor.DestroyTargetMachine";
      return false;
    }
    Descriptor->DestroyTargetMachine = Binding.hasCallback("DestroyTargetMachine") ? pythonTrampoline_NevercTargetDescriptor_DestroyTargetMachine : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercTargetDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  if (RecordName == "NevercVFSProviderDescriptor") {
    if (Bytes.size() != sizeof(NevercVFSProviderDescriptor)) {
      Error = "descriptor byte size does not match NevercVFSProviderDescriptor";
      return false;
    }
    if (!Binding.validateCallbacks({"MatchesPath", "Status", "OpenRead", "ReadDirectory", "Canonicalize", "DestroyUserData"}, Error))
      return false;
    auto *Descriptor = reinterpret_cast<NevercVFSProviderDescriptor *>(Bytes.data());
    Binding.setOriginalUserData(Descriptor->UserData, Descriptor->DestroyUserData);
    if (Descriptor->MatchesPath && !Binding.hasCallback("MatchesPath")) {
      Error = "native callback requires a Python replacement: NevercVFSProviderDescriptor.MatchesPath";
      return false;
    }
    Descriptor->MatchesPath = Binding.hasCallback("MatchesPath") ? pythonTrampoline_NevercVFSProviderDescriptor_MatchesPath : nullptr;
    if (Descriptor->Status && !Binding.hasCallback("Status")) {
      Error = "native callback requires a Python replacement: NevercVFSProviderDescriptor.Status";
      return false;
    }
    Descriptor->Status = Binding.hasCallback("Status") ? pythonTrampoline_NevercVFSProviderDescriptor_Status : nullptr;
    if (Descriptor->OpenRead && !Binding.hasCallback("OpenRead")) {
      Error = "native callback requires a Python replacement: NevercVFSProviderDescriptor.OpenRead";
      return false;
    }
    Descriptor->OpenRead = Binding.hasCallback("OpenRead") ? pythonTrampoline_NevercVFSProviderDescriptor_OpenRead : nullptr;
    if (Descriptor->ReadDirectory && !Binding.hasCallback("ReadDirectory")) {
      Error = "native callback requires a Python replacement: NevercVFSProviderDescriptor.ReadDirectory";
      return false;
    }
    Descriptor->ReadDirectory = Binding.hasCallback("ReadDirectory") ? pythonTrampoline_NevercVFSProviderDescriptor_ReadDirectory : nullptr;
    if (Descriptor->Canonicalize && !Binding.hasCallback("Canonicalize")) {
      Error = "native callback requires a Python replacement: NevercVFSProviderDescriptor.Canonicalize";
      return false;
    }
    Descriptor->Canonicalize = Binding.hasCallback("Canonicalize") ? pythonTrampoline_NevercVFSProviderDescriptor_Canonicalize : nullptr;
    Descriptor->DestroyUserData = pythonTrampoline_NevercVFSProviderDescriptor_DestroyUserData;
    Descriptor->UserData = &Binding;
    return true;
  }
  Error = "record is not an official callback-bearing NeverC descriptor: " + RecordName;
  return false;
}
