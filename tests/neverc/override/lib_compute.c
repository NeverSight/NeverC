// Library translation unit: defines `compute` with its default implementation.
// The override tests link this together with an override-marked TU and expect
// the override to win.
int compute(void) { return 1; }
