# C++ translation support matrix

Status: experimental built-in C++ implementation of `cpp-core-v1`,
`cpp-core-v2`, `cpp-project-v1`, and `cpp-math-v1`. NeverC statically contains the pinned full
Clang frontend and approved SDK headers. No separate frontend executable,
Clang/LLVM installation or SDK descriptor is needed for translation. Only C++
input translation is implemented; other language adapters remain future work.
The [design](design.md) defines commands, diagnostics, and artifacts.

## Implementation status

| Capability | Evidence/status |
| --- | --- |
| Built-in full-Clang frontend | Private LLVM/Clang 20.1.8 static build, isolated symbols and C ABI, invoked through the current NeverC executable. The separate `utils/translate-prototype/` experiment is historical feasibility evidence. |
| C++ SDK parsing | The approved `<cmath>` header union and minimal SDK version metadata are embedded. Historical `<string>` and `<vector>` syntax probes establish no translation support. See [source provenance](../../../neverc/lib/Translate/Cpp/SDK/README.md). |
| Builtin driver, semantic protocol, artifact checks | Implemented with CLI/IR/artifact tests; production protocol is separate from the P0 prototype format. |
| Experimental `cpp-core-v1` | Implemented: programs/modules, `-O0`/`-O2` full-value comparison, rejection, relocation, output ownership and cancellation tests. Platform and delivery evidence is listed below. |
| Experimental `cpp-core-v2` | Adds 8/16/32/64-bit integers, character literals, constant size/alignment queries, checked type aliases and integral enums, static assertions, bounded object pointers, live lvalue/rvalue references, fixed local arrays/array fields, array decay/indexing and switch control flow to the single-source core contract. Source/carrier sizes, ABI alignments and record field offsets are independently checked against NeverC and guarded in generated code. See the [core v2 contract](cpp-core-v2.md); earlier v1 CI evidence does not establish v2 execution support. |
| Null pointer values in `cpp-core-v2` | Distinct `decltype(nullptr)` storage, aliases, deduction, references, fields/arrays, supported callback signatures and zero/constant scalar static or variable-template objects. Pointer conversions retain source effects and cleanup; independent object addresses, layout, boolean/equality rules and checked source folding are covered by paired source/protocol, IR and O0/O2 fixtures. Dynamic static initialization, null non-type template arguments and standard headers remain unsupported. See [null values](cpp-core-v2.md#null-pointer-values); native CI of the implementing revision is required. |
| Fixed arrays in `cpp-core-v2` | Implemented with O0/O2 execution and protocol/forged-IR regression cases. Includes multidimensional arrays, pointer/reference aliasing, source-order initialization and subscript evaluation. Extent/expansion limits and global/VLA exclusions are in the [core v2 contract](cpp-core-v2.md). New array cases still require revision-specific native CI evidence; the older results below do not verify them. |
| Defined scalar static data members in `cpp-core-v2` | Defined integer/bool/enum members, including inline/constexpr and out-of-line definitions, share ordinary typed global storage. Constant/zero initialization, receiver effects, temporary cleanup and lasting static aliases are covered by fixtures. Uses requiring object identity need a definition in this source unit; declaration-only const scalar values follow the next row; dynamic initialization and other static types remain restricted. Native validation requires the implementing revision. |
| Declaration-only static constants in `cpp-core-v2` | Non-inline const integer/bool/enum members with checked in-class constant initializers support non-ODR value reads, discarded results and unevaluated uses without a separate definition. They emit typed values, no source global or field, and preserve receiver effects and temporary cleanup. Address/reference uses, including discarded or skipped uses, still require a definition (`TR0203`). Full C++/STL remains unfinished; native validation requires the implementing revision. |
| Concrete standard-layout class templates in `cpp-core-v2` | Up to 64 type/scalar parameters, type defaults, explicit instantiation/specialization, aliases, fixed fields/arrays and selected field initializers. Instances use canonical record/field identities, explicit layout and existing copy/reference/return/destruction operations. Written defaults/types/arguments remain checked. Nested classes and instantiated non-template friends follow the contracts below; full STL remains unfinished; native results require implementing CI. |
| Class-template scalar static data in `cpp-core-v2` | Integer/bool/enum zero or constant initialization, inline/constexpr and out-of-line definitions, explicit instantiation/specialization, shared canonical globals and separate template instances. Unused members stay lazy; materialized initializers and every written directive retain source checks. Declaration-only const reads create no storage; evaluated address/reference uses require definitions. Receiver effects and cleanup use ordinary typed operations. Other static types, dynamic initialization, full C++17/STL remain unfinished; native validation requires implementing CI. |
| Ordinary named class-template methods in `cpp-core-v2` | Selected instance/static methods, const/ref overloads, defaults/noexcept, explicit instantiation/specialization, static factories, scalar static locals and member ranges of admitted standard-layout templates. Bodies/defaults stay lazy until instantiated; selected calls and explicit declarations require definitions. Ordinary receiver/result ABI and per-instance method/local/storage identity; written outer parameter types remain checked. Own member function templates follow their separate row; class-template operators follow their separate row. Native validation requires implementing CI; full C++/STL remains unfinished. |
| User-provided class-template constructors in `cpp-core-v2` | Ordinary/default/explicit/converting/copy/move constructors, checked defaults, out-of-line definitions and explicit instantiation/specialization of standard-layout instances. Existing destination/source ABI, member initialization order, field/array lifetimes and local/static identities. Selected construction requires a definition; unused bodies/defaults stay lazy. Inherited constructors remain excluded; delegation follows its separate row. Native validation requires implementing CI; full C++/STL remains unfinished. |
| Const data members in `cpp-core-v2` | Supported scalar/record/pointer/callback/array fields, generic and nested classes, final-destination initialization and source-selected copy/move construction. Addresses and const reference/decay types remain intact, including const pointers with mutable pointees. Source-invalid writes and deleted assignments retain language diagnostics; explicitly deleted/defaulted-deleted declarations retain existing restrictions. Paired source/protocol and O0/O2 fixtures require implementing CI. See the [const-member contract](cpp-core-v2.md#const-data-members). |
| Delegating constructors in `cpp-core-v2` | Ordinary and concrete generic classes, chains, copy/move delegation and selected constructor templates reuse the same final receiver. Target construction runs once; argument cleanup precedes the delegating body, and members are not initialized twice. Source selection, missing definitions and unsupported operations retain their checks. Paired source/protocol and O0/O2 fixtures require implementing CI; [inheritance and exception unwinding remain separate](cpp-core-v2.md#delegating-constructors). |
| User-provided class-template destructors in `cpp-core-v2` | Nonvirtual destructors, out-of-line definitions, explicit instantiation/specialization and checked selected exception specifications. Used definitions are required even for direct object returns; type-only/unevaluated queries keep unused bodies lazy. Internal receiver-pointer helpers preserve body-before-reverse-member/array cleanup and existing temporary/reference/parameter/result lifetimes. Bounded demand queue avoids forcing unused implicit wrappers. Explicit destructor calls and full C++17/STL remain unfinished; native validation requires implementing CI. |
| Defaulted class-template special members in `cpp-core-v2` | In-class/out-of-line default/copy/move constructors, copy/move assignment and nonvirtual destruction of admitted instances. Exact defaulting origin, concrete signatures, lazy queries and owned generated definitions retain Clang semantics. Member order, zeroing distinctions, source/destination identity, reference results and reverse cleanup use ordinary typed helpers. Explicit specialization cannot inherit primary defaulting. Native O0/O2 and protocol fixtures require implementing CI; complete C++17/STL remains unfinished. |
| Class-template operators and conversions in `cpp-core-v2` | User-provided operators, copy/move assignment and explicit/implicit conversions of admitted instances retain selected overloads, C++17 sequencing, aliases, result storage and lifetimes. Out-of-line definitions, explicit instantiation/specialization and per-instance scalar static locals use ordinary typed calls without runtime template parameters. Unused bodies/defaults remain lazy; selected declarations and all materialized bodies require source/definition checks. Allocation, standard headers and complete C++17/STL remain unfinished; native results require implementing CI. |
| Instantiated local classes and written directives in `cpp-core-v2` | Local-class members inside admitted function/member instances preserve concrete origin, ordinary calls, storage, lifecycle and per-instance scalar static locals; all materialized bodies are checked. Every explicit function-instantiation directive keeps written arguments, types, conversion names, qualifiers and attribute checks, including extern/no-effect repetitions. Canonical reuse creates no duplicate runtime function. Source budgets and existing restrictions remain; native results require implementing CI and full C++17/STL remains unfinished. |
| Concrete free function templates in `cpp-core-v2` | Up to 64 type/integer/bool/enum parameters, scalar C++17 auto and dependent scalar types, array-bound deduction, explicit specialization/instantiation, resolved defaults, recursion and imports. Values become typed constants with no runtime template parameter. Equivalent arguments share instances; different values/types and primaries keep separate local types/static storage. Written value arguments and parameter types remain checked; unused generic bodies/defaults stay lazy. Signature-only calls under sizeof/noexcept still require definitions. Unsupported dependent friend class-template forms, pointer/reference/class values and complete STL remain unfinished; native validation requires implementing CI. |
| Concrete parameter packs in `cpp-core-v2` | Type/scalar packs in admitted namespace function/operator and class templates; at most 64 primary parameters and 64 elements per pack. Expanded storage, typed sizeof... counts, resolved folds, reference collapse and existing lifetimes; exact owned origins and every materialized element remain checked. The same-list dependent count TypeLoc exception is metadata only. Empty fold patterns stay lazy. Unsupported dependent friend class-template forms, bases and complete C++17/STL remain unfinished; native results require implementing CI. |
| Scalar template parameter defaults in `cpp-core-v2` | Integer/bool/enum and scalar auto defaults for admitted function/operator/class/namespace-alias templates, earlier-parameter expressions, inherited defaults, deduction, specialization/instantiation and constexpr conversions. Equivalent explicit/omitted arguments share typed identities and static storage. Nondependent source is checked; dependent defaults stay lazy until a reached selected use or explicit declaration. Original/converted evidence preserves source checks, while failed substitution/conversion retains SFINAE fallback. No runtime template parameters. Pointer/reference/record values and complete C++17/STL remain unfinished; native validation requires implementing CI. |
| Partial declaration source checks in `cpp-core-v2` | Written class/variable partials and copied member variable partials retain successful primary argument and resolved parameter type source, even when inner arguments remain dependent or no partial is selected. Retained pack source does not waive C++ specialization ordering or deduction rules; invalid patterns remain TR0202. Generic full member variable source collection also retains pending arguments. Native CI is required. |
| Friend function templates in `cpp-core-v2` | Admitted ordinary/generic class bodies support free named/operator templates, ADL/private access, inner/outer defaults and packs, references, lifetimes, local classes and separate static identities. Exact copied/written declarations and compatible body context remain distinct. Canonical namespace merges preserve their existing identities. Unproved normalization/copy chains and unsupported dependent friend class-template forms remain separate work; native behavior requires implementing-revision CI. |
| Friend class templates in `cpp-core-v2` | Ordinary/generic granting classes support exact class-template targets, namespace introductions, existing and supported qualified targets, access by specializations and shared target/static identity. Original written defaults are invalid; inherited defaults remain checked. Exact written/copied target sources, lookup/default headers and actual definitions stay distinct. Unsupported dependent friend representations remain separate work; native behavior requires implementing-revision CI. |
| Ordinary function pointers in `cpp-core-v2` | Typed default-ABI scalar/reference signatures, defined ordinary free/static targets, indirect postfix capture before arguments, callback parameters/results, fields/arrays and constant global/static callback storage. Unsupported callback forms, independent ABI/layout checks and standalone-only scope are specified in the [callback contract](cpp-core-v2.md#ordinary-function-pointers). Native validation requires implementing-revision CI. |
| Concrete function-template pointers in `cpp-core-v2` | Explicit and target-deduced free/static template instances, supported owners and visible friends, exact selected source checks, shared canonical direct/indirect function and static-local identity. Default callback ABI/signature restrictions and standalone scope remain in force. See the [template callback contract](cpp-core-v2.md#concrete-function-template-pointers); native validation requires implementing-revision CI. |
| Callback variable templates in `cpp-core-v2` | Constant/zero-initialized namespace and static-member instances, supported partial/full owners, fixed/dependent callback types and direct auto, exact declaration/type/initializer sources, canonical storage and independent reseating. Default callback and standalone limits remain; see the [storage contract](cpp-core-v2.md#callback-variable-templates). Native validation requires implementing-revision CI. |
| Instantiated non-template friend types in `cpp-core-v2` | Admitted generic class bodies retain type-parameter, alias, nested-type and specialization grants using exact original/substituted sources. Embedded Sema checks access; ignored nonclass types still need source checks. No runtime friendship entry is added. Unsupported dependent friend class-template forms, type expansions and unproved copy chains remain separate work; native validation requires the implementing revision. |
| Instantiated non-template friends in `cpp-core-v2` | Admitted generic class bodies retain named/free-operator friends, ADL/private access, canonical namespace merging, local classes and scalar static locals without a receiver. Exact original/selected/granting sources remain separate; different normalized original signatures require independent complete nondependent type/default/noexcept evidence. Unsupported dependent friend class-template forms/type expansions remain unfinished. Native CI is required; normal source fixtures do not prove AST-merge normalization coverage. |
| Member class templates in `cpp-core-v2` | Ordinary named record owners support primaries, partial/full specializations, defaults, bounded packs and out-of-line definitions. Concrete types, field dependency order, method receivers, lifetimes and scalar static identity compose with supported member function/alias/variable templates. Inheritance, allocation and full C++/STL remain unfinished. Eighteen O0/O2 checkpoints and paired protocol fixtures require implementing-revision native CI. |
| Member class templates with template owners in `cpp-core-v2` | Nested primary/partial instances, direct full specializations in concrete outer scopes and own member-template specializations preserve distinct outer/inner parameter levels, definition origins, layouts, receivers and scalar static objects. Hidden copied partials retain resolved source checks even when unselected. Copied full declarations follow the separate full-source contract; ordinary nested generic records follow their own checked body contract. Native CI of the implementing revision is required. |
| Copied class-scope full member classes in `cpp-core-v2` | Original dependent full declarations and actual concrete copies retain separate checked argument/default/type sources, exact full body origins and outer substitutions. The full body adds no parameter level. Existing layouts, member templates, receivers, lifetimes and scalar static identity apply; unused copies are checked. Inheritance, allocation and complete C++/STL remain unfinished. Native CI of the implementing revision is required. |
| Ordinary nested classes in generic owners in `cpp-core-v2` | Named ordinary member records, concrete copies and own written specializations preserve zero template levels, exact body origins, lazy uninstantiated bodies and existing layout/receiver/lifetime/static-storage rules. Member templates compose with checked outer substitutions. Every explicit member-class directive retains its written qualifier, including extern/no-effect repetitions. Anonymous records, unions, unsupported dependent friend class-template forms, friend-type expansions, inheritance, allocation and full C++/STL remain unfinished. Paired protocol fixtures and seventeen O0/O2 checkpoints require implementing-revision native CI. |
| Concrete member variable templates in `cpp-core-v2` | Scalar static members in admitted records and concrete outer primary/partial/full instances; defaults, bounded packs, inner partial/full and own member specializations, inline and out-of-line definitions. Exact prior declarations and selected source are checked; equivalent instances share canonical storage, while other arguments retain independent objects. Non-inline const reads need no storage; evaluated addresses/references require definitions. Twenty-two O0/O2 checkpoints and paired protocol/storage/receiver/relocation fixtures require implementing CI. Dependent outer class-scope full specializations retain original and concrete source: resolved argument/default/parameter-type syntax is checked immediately and pending source after instantiation. Callback results follow their separate row; other results, dynamic initialization and full C++/STL remain unfinished. |
| Concrete member alias templates in `cpp-core-v2` | Admitted ordinary records, including nested records, and concrete class primary/partial/full instances; type/scalar/auto/enum arguments, defaults and bounded packs; supported value, pointer, reference, array and record results. Exact outer/inner source and origin checks precede erasure. Nondependent declarations are checked even when unused; dependent underlying types remain lazy until substitution. Paired source/protocol, nine O0/O2 checkpoints and relocation require implementing CI. Allocation and full C++/STL remain unfinished. |
| Concrete member function templates in `cpp-core-v2` | Named/static methods, operators, constructors and conversions of admitted records and class instances, bounded type/scalar packs, defaults, external definitions and specialization. Inner/outer identities retain shared or distinct code and storage. Selected calls, defaults, materialized bodies and explicit directives preserve exact source; receivers, references, results and lifetimes use existing typed operations. Paired source/protocol, 20 O0/O2 checkpoints, closure and relocation require implementing CI. Inheritance, allocation and full C++/STL remain unfinished. |
| Namespace scalar variable templates in `cpp-core-v2` | Integer/bool/enum zero or constant initialization, resolved auto, selected partials, full specializations, defaults and bounded type/scalar packs. Source-defined C++17 traits feed assertions, if constexpr and other template arguments. Equivalent instances share global storage; type-only uses do not force unused definitions. Actual use/default, first/definition type and materialized initializer source remain checked in the exact selected instance context. Paired source/protocol, 20 O0/O2 storage checkpoints, closure and relocation fixtures require implementing CI. Member variables and callbacks follow their separate rows; other result types, dynamic initialization and complete C++/STL remain unfinished. |
| Namespace class-template partial specializations in `cpp-core-v2` | Selected type/scalar/auto/pack patterns, primary fallback, ordering, full-specialization precedence and admitted concrete members retain separate primary and deduced argument identities. Exact successful deduction and substituted pattern source are checked, including non-type parameter types and empty packs. Paired source/protocol, 18 O0/O2 runtime checkpoints, storage/lifetime, closure and relocation fixtures are included; native results require the implementing revision's CI. Existing 64-entry bounds apply; template-template parameters, bases and full standard headers remain follow-up work. |
| Namespace alias templates in `cpp-core-v2` | Owned type/scalar/auto parameters, concrete packs, defaults, alias chains, namespace imports and supported scalar/void/pointer/reference/array/class types. Exact reached source uses retain written arguments and substituted underlying TypeLocs; canonical records/functions/static objects are reused. Selected defaults and non-type parameter types preserve caller context, exact deduction evidence and explicit declarations; empty deduced function packs retain their parameter type substitution. Limits: 64 parameters/elements/depth and 200000 source units. Member aliases follow their separate row; template-template parameters and full STL remain unfinished. |
| Namespace operator function templates in `cpp-core-v2` | Ordinary free operator templates retain deduction, ADL, explicit calls, specialization/instantiation, canonical local/static identity and checked source/definitions. Explicit parameters, references and record result destinations use existing typed calls with C++17 sequencing and full lifetime cleanup. Iterator free operators are included. Unsupported dependent friend class-template forms, allocation, standard headers and complete C++17/STL remain unfinished; native validation requires implementing CI. |
| Resolved constexpr-if in `cpp-core-v2` | Ordinary and admitted concrete-template constant branch selection emits only the selected substatement, preserves runtime init/condition-variable storage and reverse cleanup, and avoids discarded automatic storage under outer switch registration. Ordinary functions check both source branches; Clang omits discarded dependent template branches. The stricter definition requirement remains; C++23 consteval branches stay excluded. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Inline namespaces in `cpp-core-v2` | Owned named/anonymous inline namespaces, transitive visibility, reopening, combined qualified lookup and bidirectional ADL retain original global/function/type identities. Different version namespaces remain distinct. Aliases, using imports and macro-spelled C++17 forms are included; explicit C++20 nested inline syntax and unsupported source remain excluded. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Resolved namespace imports in `cpp-core-v2` | Namespace aliases, using-directives and ordinary using-declarations in namespace/block scopes retain original function, object and type identities, resolved lookup, defaults and cleanup. Repeated/comma imports and unscoped namespace/local enumerators are included. Class imports, inherited constructors, unsupported template forms, foreign targets and C++20 enumerator imports remain excluded. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Statically initialized scalar locals in `cpp-core-v2` | Non-volatile integer/bool/enum static locals use zero or fully checked constant initialization and preserve values, addresses and escaped aliases across calls, recursion and lexical exits. Different scopes and overloads retain distinct objects; switch/loop entry does not create an automatic shadow or repeat initialization. Static locals in constexpr functions, dynamic initialization, TLS and other static local types remain excluded. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Named nested records in `cpp-core-v2` | Named non-template nested classes preserve source access, canonical scope identity and their own storage/receiver. By-value field and array dependencies are emitted first; pointer cycles keep forward declarations. Fixtures cover aliases, factories, out-of-line/local types, copy/move, cleanup and nested iterators. Anonymous nested records, templates and inheritance remain restricted; native validation requires the implementing revision. |
| Non-template friends in `cpp-core-v2` | Resolved friend functions/types preserve source access, hidden-friend ADL, selected operators, defaults and canonical redeclarations. Runtime behavior uses ordinary typed calls and storage. Fixtures cover legal grants, illegal access/lookup, factories, aliases, ADL ranges and cleanup. Unsupported dependent friend class-template forms, friend-type expansions and unsupported friend forms stay rejected; native results require the implementing revision. |
| Nonpublic data members in `cpp-core-v2` | Private/protected fields in supported standard-layout classes retain Clang access checks and ordinary typed storage. Fixtures cover factories, defaults, aliases, generated copies/moves, private arrays and cleanup; illegal external access stays rejected. Mixed-access non-standard-layout classes, unsupported dependent friend class-template forms, inheritance and anonymous nested records remain separate work. Native results require the implementing revision. |
| Range-based `for` in `cpp-core-v2` | Supported fixed arrays and source-defined member/ADL ranges use typed calls and separate range/iteration cleanup scopes. Includes values, references, record iterators, distinct sentinels and abrupt exits. O0/O2 and protocol fixtures check one-time initialization, storage identity and destruction order. Native results require the implementing revision; unsupported class-template forms, standard headers and STL containers remain separate work. |
| Switch in `cpp-core-v2` | Implemented using existing verified branch/label/jump IR, with O0/O2 execution and source rejection cases for selector effects, fallthrough, nested entry and loop control. GNU ranges and other attributes remain excluded. New switch cases still require revision-specific native CI evidence. |
| Pointer arithmetic in `cpp-core-v2` | Object-pointer offsets/differences and increment/compound assignment use verified strides and typed null/zero helpers. Includes forged-IR bounds checks and O0/O2 execution fixtures for arrays, rows, records and operand sequencing. Native results must be established for the implementing revision; pointer ordering and STL remain excluded. |
| Live rvalue references in `cpp-core-v2` | `T&&` aliases, parameters/results, conditional xvalues and ordinary `&&` methods retain the address of existing live scalar, pointer, record and array objects. Named rvalue references remain lvalues; resolved overloads and admitted source copies are preserved. Fixtures cover signatures, aliases, sequencing, arrays, cleanup counts and relocation. Static lifetime extension and unsupported moves remain excluded; full-expression temporary calls follow their separate row; native results require the implementing revision. |
| Ordinary methods in `cpp-core-v2` | Named nonvirtual instance/const/ref-qualified and static methods of admitted records use verified ordinary calls with an explicit this pointer where required. Protocol checks cover signatures, identity and receiver copies; O0/O2 no-inline fixtures cover mutation, const overloads, reference returns and receiver/argument effects. Unsupported special-member forms and STL remain excluded; temporary receivers follow their separate row; native results require the implementing revision. |
| Ordinary constructors in `cpp-core-v2` | User-provided default/converting/explicit constructors for standard-layout records with admitted copying and destruction. Fixtures check final destination identity, field declaration order, per-element array construction, materialization, hidden signatures and old-profile rejection. Exceptions and STL remain outside this increment; native results require the implementing revision. |
| Record call storage in `cpp-core-v2` | By-value record parameters use separate caller-prepared objects; record results use explicit destination pointers. Source-required copies and reference aliases remain intact. Protocol fixtures cover hidden signatures, direct forwarding, parameter separation and v1 compatibility; O0/O2 no-inline fixtures cover identity, mutation, recursion, receiver sequencing and nested initialization. User moves follow the separate row below; this convention does not admit STL; native results require the implementing revision. |
| Record destruction in `cpp-core-v2` | Ordinary user destructors and implicit containing-record destruction use typed calls and explicit cleanup at normal scope/full-expression exits. O0/O2 no-inline fixtures cover reverse local/member/array order, branches, loop jumps, parameter/result ownership, temporary completion order and captured conditions/return values. Protocol fixtures check helper signatures, ownership, reverse members and relocation. Explicit destructor calls, static destruction, exception unwinding and static lifetime extension remain excluded; native results require the implementing revision. |
| User copy operations in `cpp-core-v2` | Ordinary user-provided copy constructors and copy assignment with lvalue-reference sources execute through checked destination/receiver calls. O0/O2 fixtures cover selected source overloads, side effects, actual object identities, returned aliases, lifetime counts and operator-vs-member-call ordering. Protocol fixtures check selected functions, no extra prvalue copying, full signatures and relocation. Broader ordinary operators follow their separate row; native results require the implementing revision. |
| User move operations in `cpp-core-v2` | Ordinary user-provided move construction and assignment from live `R&&`/`const R&&` sources preserve selected overloads, actual destinations, source mutations, returned aliases and syntax-dependent operand order. Explicit constructors and ref-qualified assignment keep their source rules. Moving leaves normal source/target cleanup intact. O0/O2 and protocol fixtures cover these effects, complete signatures, direct prvalue forwarding and relocation. Static lifetime extension, exceptions, templates and full STL remain in development; native results require the implementing revision. |
| Generated moves in `cpp-core-v2` | Implicit and explicitly defaulted move construction/assignment preserve selected member copy or move operations, nested array order, actual storage and normal source/target cleanup. Trivial operations preserve stored pointers; generated field defaults do not rerun. Assignment reads source values after operand effects. Existing implicit trivial inline temporary operations remain supported with separate full-expression and automatic-reference lifetime contracts. O0/O2 and protocol fixtures cover fallback, order, aliases, defaults, owners, lazy definitions, typed array stores and relocation. Full C++/STL remains in development; native results require the implementing revision. |
| Ordinary operators in `cpp-core-v2` | Member/free overloads preserve selected calls, C++17 sequencing, reference aliases, by-value parameters and record results. Overloaded logical operators evaluate both operands. Free assignment parameters initialize RHS-first in both syntaxes and are destroyed in reverse order. Ordinary assignment signatures admit supported value/result types; generated special-member restrictions remain. O0/O2 and protocol fixtures cover operator categories, order, lifetimes, signatures and relocation. Operator function templates, allocation and complete STL remain in development; native results require the implementing revision. |
| Conversion functions in `cpp-core-v2` | Ordinary live-receiver conversions preserve the selected call, explicit/contextual selection, reference aliases and direct object-result destinations. Following standard conversions and selected copy/move operations remain visible. Const/ref qualifiers, constexpr and noexcept follow C++17. O0/O2 and protocol fixtures cover effects, lifetimes, signatures, cleanup and relocation. Static lifetime extension and complete STL remain in development; native results require the implementing revision. |
| Full-expression temporary calls in `cpp-core-v2` | Scalar/record temporary reference arguments and temporary receivers retain actual storage until the full-expression ends, after the selected call and callee parameter cleanup. Record subobjects share the same owner; conditions and loops preserve guarded reverse destruction. Fixtures cover real addresses, operators/conversions, user/generated moves, result destinations, cleanup and relocation. Static lifetime extension remains excluded; standalone fixed-array temporaries follow their separate contract; native results require the implementing revision. |
| Automatic reference lifetime extension in `cpp-core-v2` | Ordinary automatic local references retain admitted scalar/enum/pointer/record temporaries in one actual destination until their lexical scope ends. Braces preserve aliases; record subobjects keep the complete owner where C++ grants extension. Nested full-expression temporaries retain shorter lifetimes. Fixtures cover copies/moves, returns, conditional owners, loop exits, guarded cleanup and relocation. Static/global/TLS references, reference fields and full STL remain excluded; standalone fixed arrays follow their separate contract; native results require the implementing revision. |
| Default arguments in `cpp-core-v2` | Admitted functions, methods, call operators and user constructors evaluate resolved defaults at each omitted argument, preserving declaration lookup, references and actual value destinations. User copy/move constructors can have trailing defaults. Array omitted elements and generated copies clean default temporaries per element; explicit clauses retain the enclosing full expression. Unused and overridden non-template defaults remain inspected; template defaults are checked once instantiated. Native evidence requires implementing CI; templates and complete STL remain unfinished. |
| Void expressions in `cpp-core-v2` | Void casts, void()/void{}, supported aliases, calls, returns, comma expressions and conditional branches retain operand effects and existing temporary cleanup. Discarded nonvolatile lvalues are not loaded; record/array temporaries still construct and destroy normally. The protocol contains no void storage or value operands. Full source checks remain active in constant, query, dead and discarded contexts; unsupported types, volatile objects and complete STL remain excluded. Native evidence requires implementing CI. |
| Empty records in `cpp-core-v2` | Empty standard-layout records and stateless callable/conversion objects retain one-byte size/alignment, actual storage, selected operations and cleanup. Source fields/offsets stay empty; an internal NC byte provides storage. Trivial copies still evaluate operands, arrays count empty elements against budgets, and the consumer verifies layout and initializer arity. Fixtures cover identity, parameter/result destinations, lifetimes and manifest consistency; native evidence requires implementing CI. Inheritance, virtual dispatch, templates and complete STL remain excluded. |
| Standalone array temporaries in `cpp-core-v2` | Bounded scalar/enum/pointer/record arrays have one actual destination and complete owner, including multidimensional arrays, omitted fillers, discarded values, calls, decay/indexing and automatic reference extension. Elements initialize in order and are destroyed in reverse order; aliases retain typed addresses. Fixtures check element identity, cleanup, query purity, resource limits and relocation. Static/global/TLS lifetimes, reference fields and complete STL remain excluded; native results require the implementing revision. |
| Noexcept in `cpp-core-v2` | Standard resolved `noexcept`, computed true/false specifications and C++17 `throw()` retain source semantics. Constant queries account for selected functions and temporary destruction without executing operands or creating owners. Written operands/specifications remain inspected, even in unused code. O0/O2 and protocol fixtures cover effects, special members, redeclarations, bool literals, signatures and relocation. Throwing, catching and stack unwinding remain in development; native results require the implementing revision. |
| Generated/defaulted lifecycle in `cpp-core-v2` | Implicit and explicitly defaulted default constructors preserve final destinations, declaration order, nested member/array construction and C++ default/value initialization. In-class/out-of-line defaulted destructors use reverse member cleanup. Fixtures cover lazy trivial and unevaluated definitions, transitive discovery, complete signatures, identity, cleanup, zero-initialization differences and relocation. Exception unwinding and STL remain outside this milestone; native results require the implementing revision. |
| Default member initializers in `cpp-core-v2` | Supported field defaults retain declaration/element order, actual destination identity and nested `this` contexts. Explicit initialization overrides the selected field; implicit/defaulted copies and assignment do not rerun defaults, while user copy constructors can select defaults for omitted members. Constructor and aggregate temporary cleanup boundaries remain distinct. Written unused/overridden defaults are checked. Fixtures include partial arrays, const complete objects, lazy construction, signatures and relocation; native results require the implementing revision. |
| Generated copy construction in `cpp-core-v2` | Implicit and explicitly defaulted copy constructors retain selected member copying for nested records and multidimensional arrays. Source arrays are captured once; elements use actual destinations in increasing index order. O0/O2 fixtures check copy effects, mutable sources, pointers, by-value/return storage and destruction counts; protocol fixtures check canonical signatures, selected calls, exact source/destination indices, trivial field copies, lazy definitions and relocation. Broader C++/STL remain in development; native results require the implementing revision. |
| Generated copy assignment in `cpp-core-v2` | Implicit and explicitly defaulted assignment preserves selected member calls, nested array loops, source order and receiver reference results. Generated trivial array copies use bounded typed stores without external memory-copy calls, including records with nontrivial lifecycle. Trivial assignment captures references before reading source values. O0/O2 fixtures cover aliases, mutable sources, self/chained assignment, side effects and destruction counts; protocol fixtures check helper signatures, loops, typed stores, captures, builtin rejection, lazy definitions and relocation. Templates and complete STL remain in development; native results require the implementing revision. |
| Multi-file project translation | P3A implemented: compilation-database selection, owned headers, per-unit semantic analysis, ODR/linkage checks, combined source/header emission, and original/generated program/library comparison at `-O0`/`-O2`. |
| NeverC math mappings | P3B implemented behind explicit `cpp-math-v1`, pinned SDK/declaration provenance, exact runtime capability checks, and a runtime link probe. The [mapping gates](runtime-mapping.md) include numeric/environment differentials and installed output execution. |
| C++ byte strings / trivial vectors | Follow-up profiles; no translation support implied by parsing their headers. |
| Built-in frontend/SDK delivery | Implemented in normal NeverC builds; upstream Clang is a pinned build-time source input. Runtime and installation evidence is tracked separately below. |
| Deduced reference conversions in `cpp-core-v2` | `decltype(auto)` and collapsing `auto&&` preserve supported reference results, receiver constness, selected calls and actual aliases. Ineligible value-returning template bodies stay lazy during reference-only candidate filtering. Fresh reference arguments deduce necessary results before compatibility checks; the direct-binding filter does not exempt a candidate from result deduction needed for later indirect initialization. Source checks and C++ diagnostics remain active. Full C++17/STL is unfinished; native evidence requires implementing CI. [Contract](cpp-core-v2.md#deduced-reference-conversions). |

## `cpp-core-v1` acceptance contract — P2

One C++17 source unit, native hosted user space, no includes. The profile is
immutable after release; broader behavior requires an explicitly selected new
profile. All unlisted constructs remain unsupported until entered into this
matrix with lowering and rejection tests.

| Construct | Contract |
| --- | --- |
| Types | `int`, `unsigned int` (both exactly 32 bits in this initial profile), `bool`, `void`; source target widths/conversions must agree with the generated target. |
| Aggregates | Nonempty simple records composed of supported fields, explicit aggregate initialization, copy, and field access. Reject recursive by-value records, user-defined constructors/destructors/member functions, inheritance, unions, bitfields, access/lifetime features outside this subset. |
| Functions | Supported by-value arguments/results and locals; ordinary functions and scalar recursion; namespace and overload resolution performed by Clang. No external function dependencies. |
| Initialization | Preserve explicit initialization and permitted source initialization semantics. Do not silently zero-initialize an uninitialized C++ local. No dynamic global initialization or static locals. |
| Arithmetic/conversions | Source integer promotions, usual signed/unsigned conversions, unsigned wrap, comparisons, and bool conversion. The guarantee covers defined source executions; it does not promise complete static detection of arbitrary undefined behavior. |
| Control flow | Branches/loops, early return, short-circuit `&&`/`||`, and supported scalar expressions. Lower effects explicitly; do not inherit C evaluation order accidentally. Additional statement forms require explicit support/tests. |
| Names | Namespaces and resolved overloads; deterministic emitted identities, with file identity for internal linkage. Avoid NeverC/runtime reserved names. |
| Globals | V1 profiles retain compile-time constants. Core v2 also supports mutable integer/bool/enum namespace globals with zero or fully checked constant initialization and one owned definition; reads, references and pointers share that storage. Mutable object/array/pointer/reference globals and dynamic initialization remain outside this increment. Native results require the implementing revision. |
| Entry points | No-argument `int main()` when present; modules may omit `main`. Never invent an entry point. |
| C ABI exports | Explicit supported scalar parameter/result signatures; no aggregate ABI, class ABI, or foreign-object identity guarantee. |
| Preprocessing | All includes rejected, including unused includes. Time-dependent and path-observing macros (`__FILE__`, `__BASE_FILE__`, `__FILE_NAME__`) are rejected. Admitted macro inputs are recorded. Unsupported directives/pragma behavior must be diagnosed. |

The following are rejected: pointers/references, arrays, floating-point types,
enums, unions/bitfields, user-defined special members/member functions,
templates, inheritance/virtual dispatch, exceptions, RTTI, lambdas, variadics,
volatile/atomic operations, static locals, dynamic global initialization,
foreign-function dependencies, and unsupported preprocessing/options. Rejection
applies to all selected owned declarations/definitions, even unused code.

This is a source migration contract. It does not preserve arbitrary C++ binary
ABI, support the whole C++ standard, or automatically translate an ecosystem.
Naïve source-to-source printing is not an accepted implementation.

## Explicit extension profiles

| Profile | Extension | Status |
| --- | --- | --- |
| `cpp-project-v1` | `cpp-core-v1` plus explicitly selected compilation-database inputs, owned project headers, dependency closure, shared types/linkage, scalar C ABI boundaries, and explicit target validation. | Implemented; host and delivery evidence below. |
| `cpp-math-v1` | `cpp-project-v1` plus the bounded binary64 operations below and approved `std::fabs(double)`/`std::floor(double)` mappings. | Implemented; SDK/runtime gates and host evidence below. |
| String follow-up | Exact byte operations with encoding/NUL/aliasing/ownership/lifetime/allocation-error contract. NeverC UTF-8 `string` is not automatically `std::string`. | Unversioned and unimplemented until the operation list is fixed. |
| Trivial-vector follow-up | Exact element/operation list with bounds, invalidation, storage, lifetime, growth/overflow, and allocation-error behavior. | Unversioned and unimplemented. |

Project and math profiles require `--compdb`, `--project-root`, explicitly selected
source files, and `--out-dir` or `--check`. They emit one `translated.nc` and
`translated.h`, after analyzing each original translation unit independently.
The [context contract](p3-compilation-context.md) defines supported options,
response files, selection, and raw versus normalized input hashes; the
[project contract](p3-project-emission.md) defines linkage and ODR restrictions.

Math accepts binary64 literals, value copies, parameters/results and aggregate
fields; conversions between `double` and supported bool/int32/uint32 values;
unary `+`/`-`; and comparisons. General double arithmetic, compound arithmetic,
increment/decrement, `float`, `long double`, and source floating-environment APIs
remain rejected. Source builtin NaN/infinity constructors, bit casts, and
`numeric_limits` are rejected; runtime NaNs and infinities may enter through
module parameters. Constant folding cannot hide an unsupported source construct.
The accepted source options fix no fast math and disabled FP contraction. The
FP contract `cpp.math.binary64.masked.v1` requires masked traps and the documented
binary64 environment; it does not authorize arbitrary FP options or operations.

Math requires an explicit x86_64 or arm64 macOS 15.0 target and the approved
`neverc-embedded-clang20.1.8-libcxx200100-macos15.5` distribution. Equivalent Darwin
target spellings are accepted only when their effective deployment is macOS
15.0. The immutable built-in catalog approves the 209 original headers and
NeverC-authored version metadata; there is no external SDK selection or host
discovery. Standard-library overloads or distributions outside this boundary fail.

P3B uses P3A include/context handling. Full Clang may parse standard-library
internals to resolve approved operations, but those bodies are not recursively
translated. An ignored header must not hide owned code, custom specializations,
or unapproved calls. Broader profiles are never inferred from an include.

## Host/target evidence

The original process-separated implementation passed 126 integrated tests on
both native arm64 and Rosetta x86_64, including SDK/runtime gates, environment
isolation, O0/O2 execution and full-value differentials. Its installed generated
output also ran independently of the old frontend and SDK descriptor. Those
runs are a semantic regression baseline, not proof of the new built-in delivery.

| Host / source target | Built-in implementation evidence | Support boundary |
| --- | --- | --- |
| macOS arm64 / `arm64-apple-macosx15.0.0` | Earlier recorded built-in core protocol suite: 73 passed. Earlier built-in project and math suites passed 33 and 49 cases respectively. Isolated fresh-prefix installation, translation and O0/O2 execution passed with default mimalloc, Python plugins and bundled Python enabled. Full integrated-suite status is recorded below. | Native macOS runner; bounded profiles only. |
| macOS under Rosetta x86_64 / `x86_64-apple-darwin24.6.0` | Prior 126-test process-separated baseline above. Local built-in validation was not completed. | Math target is explicitly macOS 15.0; built-in and native Intel execution are not established. |
| Linux x64 / arm64 | At `b02468ad`, the [x64](https://github.com/NeverSight/NeverC/actions/runs/34596323748) and [arm64](https://github.com/NeverSight/NeverC/actions/runs/34596323441) formal CI workflows completed successfully. | Workflow completion evidence only; per-test execution and installed-prefix translation were not separately reviewed here. The math profile remains restricted to its approved macOS targets. |
| Windows x64 / arm64 | At `b02468ad`, the [x64 Clang](https://github.com/NeverSight/NeverC/actions/runs/34596323650) and [arm64 Clang](https://github.com/NeverSight/NeverC/actions/runs/34596323464) logs verify the built-in build, embedded-runtime relink, installation and full CTest run. Counts and skips are recorded below. | Bounded profiles and explicit test skips; installation of the compiler is distinct from an installed-prefix C++ translation smoke. The math profile remains restricted to its approved macOS targets. |
| Android, kernel, freestanding, dyncode | Outside initial hosted translation profile. Existing NeverC support for such modes is not translator validation. | Not advertised. |

The arm64 SDK probes explicitly select their target. A NeverC build under Rosetta
can default to x86_64 on the same arm64 machine; its source and generated targets
must be aligned explicitly and its execution evidence recorded separately.

The passing arm64 installation smoke used the real CMake installation components
for NeverC, resource headers, standard resources and the macOS arm64 runtime.
Its sandbox denied both development source/build trees, Homebrew and the system
developer directory. Inside that sandbox, the installed compiler translated
core, project and math fixtures, built and executed their output at `-O0` and
`-O2`, and linked separate C clients for the generated modules. Recursive dynamic
dependency inspection found no external Clang/LLVM libraries. Installed license
notices and all 210 catalog source entries were verified by hash: 209 original
headers and NeverC's version metadata. The installed compiler retained its
bundled Python and ordinary system dependencies. This smoke does not substitute
for a complete regression run on every advertised host.

Before built-in integration, an additional arm64-compiler → x86_64-target probe with default mimalloc enabled
could not complete its runtime link on this host. This combination is not in the
accepted matrix. After the process-cleanup fix, the driver returned `TR0404` at
the link stage in about 60 seconds, published a failure report, and removed staging
without publishing generated artifacts. The OS kept the killed compiler child
unreapable; the driver reported that condition instead of waiting indefinitely.
The old x86_64 baseline used the matching Rosetta compiler with mimalloc off.

NeverC's existing compiler target matrix must not be copied into the translator's
support claims. Each advertised combination requires matching source/generated
data models, SDK provenance, runtime capabilities when used, and execution on a
matching runner. Cross-compilation alone is insufficient.

Other language adapters remain planned in issue #16's order: E Language after
the C++ P3 baseline, then Python, Go, Rust, TS, and JS. C compilation continues
through the existing direct NeverC path.

## Repository regression validation

The early local built-in arm64 integrated run passed 131 of 132 tests. Its
remaining shadow-header assertion was corrected; the second local run stopped
when full validation moved to GitHub CI. These are historical results. The
earlier local protocol and installation evidence above predates the numeric byte
initializer for MSVC, Windows path normalization and full-host symbol audit.

At code revision [`b02468ad1ccda72b16393e4fab9147c201f2ee1f`](https://github.com/NeverSight/NeverC/commit/b02468ad1ccda72b16393e4fab9147c201f2ee1f),
all ten formal CI workflows completed successfully: seven platform builds,
the C++ frontend tools, code quality, and documentation/test-dependency checks.
This includes the [macOS arm64 workflow](https://github.com/NeverSight/NeverC/actions/runs/34596323438).
Linux and macOS evidence for this revision is workflow completion only; no
additional per-test, numerical-observation or installed-prefix smoke results
are inferred here from those workflow statuses.

The Windows Clang build, installation and test logs were reviewed separately.
Both architectures completed the initial and embedded-runtime executable links,
passed the private/host ABI audit, and installed `neverc.exe`. Each CTest
inventory contained 2,899 tests:

| Windows Clang runner | Passed | Skipped | Failed |
| --- | ---: | ---: | ---: |
| x64 | 2,829 | 70 | 0 |
| arm64 | 2,780 | 119 | 0 |

Each runner's `neverc-translate.*` group contained 130 passed and 21 skipped
tests. The skips cover three independent reference-compiler comparisons,
15 macOS-specific math tests, two POSIX process fixtures and one controlled
long-path test. The logs do not identify which long-path environment condition
caused that skip. Core/project protocol tests and the executable core translation
fixtures passed. Windows does not register the math protocol suite.

The separate `cpp-frontend-tools` workflow exercises the archive-index parser
and ABI collision checks on Linux, Windows and macOS before the full compiler
builds finish. Compiler builds also run those checks against their real archives,
including Clang LTO and MSVC LTCG inputs.
The macOS workflow includes the fresh-prefix installation smoke with reads of
the checkout, selected Xcode developer directory and installed Homebrew/developer
roots denied to NeverC and its generated programs. That gate checks installed
core, project and math translation at O0/O2. Its current detailed smoke log was
not separately reviewed for the CI summary above; earlier measured installation
evidence remains recorded in its own section.

The repository gate requires default mimalloc and Python-plugin options enabled,
with bundled Python for the installed configuration. `PluginGlobalState`
validates symbols from the enabled Python plugin implementation; a development build with that option disabled does not
satisfy that artifact check. Python plugins are an existing compiler extension
mechanism and do not provide Python-to-NeverC source translation.

The external Clang reference compiler and its own headers/SDK are independent
test oracles only. Production translation and built-in SDK admission tests do
not select an external frontend or require an SDK environment variable.
Arm64 CI builds must use a native execution environment so CMake's recorded host
architecture matches the compiler and managed Python runtime. The DynCode test
loader also explicitly selects arm64 when its test harness is arm64.

The focused translation suite covers source/IR equivalence, SDK/runtime
admission, project closure, bounded file and JSON input, process cancellation,
artifact publication and environment isolation. The frontend protocol suites
exercise all owned declarations, including unreachable unsupported source, and
ensure pruned mapped calls leave no unused mapping metadata. Installed-prefix
checks use normal CMake installation components and verify translation plus
generated project/math execution at O0/O2 with development Clang/SDK/source paths
unavailable. They also inspect symbols and dependencies outside that sandbox.
No repository test exclusion is added by this change.
