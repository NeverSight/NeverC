# C++ translation support matrix

Status: experimental built-in C++ implementation of `cpp-core-v1`,
`cpp-core-v2`, `cpp-project-v1`, and `cpp-math-v1`. NeverC statically contains the pinned full
Clang frontend and approved SDK headers. No separate frontend executable,
Clang/LLVM installation or SDK descriptor is needed for translation. Only C++
input translation is implemented; other language adapters remain future work.
The [design](design.md) defines commands, diagnostics, and artifacts.

## Implementation status

| Capability | Evidence/status |
| --- | --- |
| Built-in full-Clang frontend | Private LLVM/Clang 20.1.8 static build, isolated symbols and C ABI, invoked through the current NeverC executable. The separate `utils/translate-prototype/` experiment is historical feasibility evidence. |
| C++ SDK parsing | Core v2 admits the embedded `<type_traits>`, `<cstdint>`, `<limits>`, `<ratio>`, `<cstddef>`, `<utility>`, `<tuple>`, `<array>`, `<initializer_list>`, `<optional>`, `<iterator>`, `<algorithm>`, `<numeric>`, `<new>`, `<memory>` and `<functional>` closures for compile-time aliases and constants, numeric bounds and rational arithmetic, fundamental metadata, byte operations, scalar utilities, scalar or composite pairs and tuples, fixed value arrays, initializer-list views, scalar or composite optional values, pointer and reverse-iterator operations, bounded pointer algorithms, allocation metadata and authenticated memory/functional-header parsing on supported macOS, Linux and Windows targets. Runtime standard-library behavior outside the documented direct lowerings and all other core headers remain rejected. Math v1 separately admits the approved `<cmath>` union and minimal macOS SDK metadata. Historical `<string>` and `<vector>` syntax probes establish no translation support. See [source provenance](../../../neverc/lib/Translate/Cpp/SDK/README.md). |
| Compile-time `<type_traits>` in `cpp-core-v2` | Exact angle inclusion uses only the pinned libc++/resource VFS. Resolved aliases and integral/enum constants, including `remove_cv_t`, `is_same_v`, construction/destruction traits and `integral_constant::value`, lower to existing core types and literals. Every consumed header path/hash is driver-approved and recorded in IR/manifests; platform roots, quoted/shadow headers, runtime objects/calls and constant storage identity are rejected. Frontend checks cover the eight supported target triples; O0/O2 execution requires implementing CI. See the [type-traits contract](cpp-core-v2.md#compile-time-type_traits). |
| Fixed-width `<cstdint>` in `cpp-core-v2` | Exact angle inclusion exposes target-defined fixed, least, fast, pointer and maximum-width aliases plus the standard limit and constant macros. The nine-file standalone libc++/resource closure is authenticated and recorded; `<type_traits>` composes in either include order. Quoted `"cstdint"`, `<stdint.h>`, platform roots and user shadows remain rejected. Frontend checks cover all eight supported target triples; O0/O2 execution requires implementing CI. See the [cstdint contract](cpp-core-v2.md#fixed-width-integers-from-cstdint). |
| Numeric bounds from `<limits>` in `cpp-core-v2` | Integral/enum static members and the documented zero-argument `numeric_limits` methods for admitted integer, float and double types fold through pinned Clang into exact scalar literals. The 16-file libc++/resource closure is authenticated on all eight target triples. Objects, storage/method identities, object-qualified calls, long double results, quoted/shadow headers and runtime libc++ calls remain rejected. O0/O2 execution requires implementing CI. See the [limits contract](cpp-core-v2.md#numeric-bounds-from-limits). |
| Compile-time rational arithmetic from `<ratio>` in `cpp-core-v2` | Normalized `ratio` types, arithmetic aliases, comparison traits and C++17 `*_v` constants, plus representable standard SI aliases, fold through pinned Clang without runtime storage or calls. The 15-file libc++/resource closure is identical and platform-free on all eight targets. Runtime ratio objects, static-member addresses and quoted/shadow headers remain rejected. O0/O2 execution requires implementing CI. See the [ratio contract](cpp-core-v2.md#compile-time-rational-arithmetic-from-ratio). |
| Fundamental metadata and `std::byte` from `<cstddef>` in `cpp-core-v2` | Target `size_t`, `ptrdiff_t` and `nullptr_t` aliases, including exact pinned `std::size_t` function metadata, pinned `NULL`, `max_align_t` size/alignment queries and checked `offsetof` fold through pinned Clang. `std::byte` storage, all standard bitwise/shift operators and `to_integer` lower directly to scalar IR. The 29-file libc++/resource closure is authenticated on all eight target triples. Quoted/C-header forms, raw builtins, union/base offsets, forged declarations and standard-function addresses remain rejected. O0/O2 execution requires implementing CI. See the [cstddef contract](cpp-core-v2.md#fundamental-types-and-bytes-from-cstddef). |
| Scalar utilities and pairs from `<utility>` in `cpp-core-v2` | Exact pinned libc++ declarations provide scalar `move`, `forward`, `move_if_noexcept`, `as_const`, `exchange` and `swap`; scalar or recursively composite `pair` construction, assignment, swap, `make_pair` and `get`; recursive comparisons over authenticated arrays and nested pairs; tuple traits; and compile-time integer-sequence metadata. Corresponding comparison leaves use the heterogeneous scalar arithmetic/pointer boundary; source-record comparisons remain rejected. Composite elements may be source-owned trivial standard-layout records, admitted arrays or nested pairs, with recursive assignability required for mutation. The authenticated standalone utility closure has 87 files and the array-plus-utility union has 222; neither adds a libc++ runtime dependency on any of the eight targets. Native-array swap, function addresses, forged declarations and ordinary reference-valued pairs remain rejected; the exact `std::minmax` result is the sole reference-pair exception. O0/O2 execution requires implementing CI. See the [utility contract](cpp-core-v2.md#scalar-utilities-and-pairs-from-utility). |
| Value tuples from `<tuple>` in `cpp-core-v2` | Exact pinned libc++ `std::tuple<T...>` specializations authenticate the nonempty implementation's private outer field, indexed leaf bases, admitted scalar or recursively composite values and target offsets, or the dedicated empty specialization's zero-field one-byte layout, before flattening into protocol records. Composite elements may be source-owned nonempty trivial standard-layout records, admitted arrays or pairs, or nonempty nested tuples. Default, element-wise, trivial copy/move and per-element converting construction and assignment, pair conversion, `make_tuple`, member/free swap, recursive same-length heterogeneous comparisons, tuple traits and index-based or unique-type `get` lower without a libc++ runtime dependency; composite conversions require the exact unqualified type and mutation requires recursive assignability. Exact `apply` over empty or nonempty scalar tuples accepts named functions or stored function pointers with directly convertible by-value scalar parameters and a scalar or void result, evaluates the callable and tuple once, and emits one ordinary indirect call. Exact `tuple_cat` accepts zero arguments or authenticated scalar or recursively composite tuple/pair/array sources, evaluates all sources once before reading their elements, and directly constructs the exact concatenated tuple. Comparisons recurse through authenticated arrays, pairs and tuples to heterogeneous scalar leaves; source-record comparisons remain rejected. The 98-file tuple closure, 108-file scalar-pair union and 231-file array-plus-tuple-plus-utility composite union are platform-free on all eight targets. Reference, empty-base-optimized composite, nontrivial-record, long-double and function-pointer elements, `tie` and `forward_as_tuple` remain rejected; callable objects, reference/record callback parameters and reference/record apply results also remain outside the boundary. O0/O2 execution and eight-target protocol fixtures cover layout and operations. See the [tuple contract](cpp-core-v2.md#value-tuples-from-tuple). |
| Fixed value arrays from `<array>` in `cpp-core-v2` | Exact pinned libc++ `std::array<T, N>` declarations retain their fixed field layout and directly lower capacity, forward and reverse pointer iterators, `data`, indexed/front/back access, constant in-range `at`, `fill`, member/free `swap`, all six scalar or recursively nested-array comparisons and index-based `get`. The zero-length partial specialization authenticates libc++'s aligned private byte storage and exposes an inaccessible synthetic `T` carrier with the same target size/alignment; capacity, null iterators, no-op fill/swap, copy/assignment, empty comparisons and nested zero leaves preserve evaluation effects without reading that carrier. Elements may also be source-owned trivial standard-layout records or nested admitted arrays; copy/move construction and assignment use ordinary aggregate values. The authenticated 217-file libc++/resource closure is platform-free and adds no runtime libc++ dependency on all eight supported targets. Zero-array element access, nontrivial record elements, record comparisons and dynamic or out-of-range `at` remain rejected. O0/O2 execution requires implementing CI. See the [array contract](cpp-core-v2.md#fixed-value-arrays-from-array). |
| Initializer-list views from `<initializer_list>` in `cpp-core-v2` | Exact pinned libc++ `std::initializer_list<T>` specializations retain their checked `const T *` plus `size_t` layout. Braced backing arrays use existing automatic, full-expression and static lifetime rules, including ordered initialization, nontrivial source-record destruction and nested lists. Default, copy/move and assignment operations, member/free `begin` and `end`, `size`, and the matching `<iterator>` range-access overloads lower without a libc++ runtime dependency. The authenticated 10-file closure is identical and platform-free on all eight targets. Volatile, incomplete, reference and function elements, function addresses, quoted headers and forged declarations remain rejected. O0/O2 fixtures cover local, temporary, global, nested and nontrivial-element lifetimes. See the [initializer-list contract](cpp-core-v2.md#initializer-list-views-from-initializer_list). |
| Value optionals from `<optional>` in `cpp-core-v2` | Exact pinned libc++ `std::optional<T>` specializations retain an authenticated private base and union layout exposed as a synthetic value-plus-engagement record. Elements may be admitted scalars, source-owned trivial standard-layout records, arrays, pairs or tuples, recursively and with optional top-level `const`. Default, `nullopt`, direct, zero/single-value `in_place`, copy/move and converting construction; copy/move, converting, value and `nullopt` assignment; `has_value`, contextual bool, dereference, implicit or explicit arrow, `reset`, single-value `emplace`, `value_or`, member/free `swap` and zero/single-value `make_optional` lower without libc++. Composite conversion requires exact unqualified types and mutation requires recursive assignability. All six value comparisons recurse through authenticated arrays, pairs and tuples to the scalar arithmetic/pointer leaf boundary, while `nullopt` comparisons work for every admitted element; source-record comparisons remain rejected. The standalone 136-file closure and 253-file array-plus-tuple-plus-utility-plus-optional union are identical and platform-free on all eight targets. References, volatile, `long double`, function pointers, nontrivial destruction, throwing `value`, initializer-list emplacement and unrelated in-place tags stay rejected. O0/O2 execution and eight-target protocol fixtures cover layout, operations and negative boundaries. See the [optional contract](cpp-core-v2.md#value-optionals-from-optional). |
| Pointer iterator operations from `<iterator>` in `cpp-core-v2` | The exact pinned public header exposes pointer `iterator_traits`, native/admitted-array range access, pointer `advance`, `distance`, `next` and `prev`, and authenticated raw-pointer `reverse_iterator` construction, access, movement, offsets, differences and comparisons. The 171 consumed libc++/resource files are identical on all eight targets and add no platform or runtime dependency. Custom and function-pointer iterators remain rejected; four stream-iterator headers stay disabled until a cross-target C runtime `mbstate_t` ABI is present. See the [iterator contract](cpp-core-v2.md#iterator-metadata-from-iterator). |
| Pointer algorithms from `<algorithm>` in `cpp-core-v2` | The exact pinned public header and its 354-file libc++/resource closure are identical and platform-free on all eight targets. Scalar-pointer `copy`, `copy_n`, `move`, `copy_backward`, `move_backward`, `fill`, `fill_n`, `iter_swap`, `swap_ranges`, `reverse`, `reverse_copy`, `rotate` and `rotate_copy` calls lower to pointer loops without a libc++ runtime call; copy, move, reverse-copy and rotate-copy outputs and fill values accept checked direct scalar conversions, while `iter_swap`, `swap_ranges`, `reverse` and `rotate` retain their same-element boundary. Built-in equality elements, including enums without a source `operator==`, additionally admit `find`, `count`, three- and four-iterator `equal`, `adjacent_find`, `remove`, `remove_copy`, `replace`, `replace_copy`, `unique`, `unique_copy`, `search`, `find_end`, `find_first_of`, `search_n`, three- or four-iterator `mismatch` and three- or four-iterator `is_permutation`; `find`, `count`, `remove`, `remove_copy` and default `search_n` accept a heterogeneous scalar value through a checked equality common type; `replace` and `replace_copy` also accept a heterogeneous old/new value whose new value converts directly to the destination. Copy-producing forms accept checked direct scalar output conversions. Built-in arithmetic elements, enums without a source `operator<`, and complete non-function object pointers admit default-order `min`, `max`, `clamp`, `minmax`, `min_element`, `max_element`, `minmax_element`, `lower_bound`, `upper_bound`, `equal_range`, `binary_search`, `is_sorted`, `is_sorted_until`, `is_heap`, `is_heap_until`, `make_heap`, `push_heap`, `pop_heap`, `sort_heap`, `sort`, `stable_sort`, `inplace_merge`, `partial_sort`, `partial_sort_copy`, `nth_element`, `next_permutation`, `prev_permutation`, `lexicographical_compare`, `includes`, `merge`, `set_union`, `set_intersection`, `set_difference` and `set_symmetric_difference`. The default two-range equality and ordered forms accept heterogeneous scalar elements through checked common comparison types; ordered bound queries accept a heterogeneous scalar value through a checked ordered common type. Checked unary boolean function-pointer predicates with directly convertible by-value scalar parameters admit `find_if`, `find_if_not`, `count_if`, `all_of`, `any_of`, `none_of`, `copy_if`, `remove_if`, `remove_copy_if`, `replace_if`, `replace_copy_if`, `is_partitioned`, `partition`, `stable_partition`, `partition_copy` and `partition_point`; their copy-producing forms and predicate replacement values accept checked direct scalar output conversions. Checked binary boolean function-pointer predicates with directly convertible by-value scalar parameters additionally admit `adjacent_find`, three- and four-iterator `equal`, three- and four-iterator `mismatch`, three- and four-iterator `is_permutation`, `unique`, `unique_copy`, `search`, `find_end`, `find_first_of` and `search_n`; the two-range scans and `search_n` value retain their independent heterogeneous scalar source types, and `unique_copy` accepts a checked direct scalar output conversion. Checked scalar function-pointer comparators with directly convertible by-value scalar parameters admit `min_element`, `max_element`, `lower_bound`, `upper_bound`, `equal_range`, `binary_search`, `is_sorted`, `is_sorted_until`, `min`, `max`, `clamp`, `minmax`, `minmax_element`, `lexicographical_compare`, `includes`, `merge`, `set_union`, `set_intersection`, `set_difference`, `set_symmetric_difference`, `is_heap`, `is_heap_until`, `make_heap`, `push_heap`, `pop_heap`, `sort_heap`, `sort`, `stable_sort`, `inplace_merge`, `partial_sort`, `partial_sort_copy`, `nth_element`, `next_permutation` and `prev_permutation`. Checked function-pointer callbacks with directly convertible by-value scalar parameters also admit `for_each`, `for_each_n`, unary and binary `transform`; transform and zero-parameter generator results may directly convert to their writable output scalar. Single-range comparator forms retain one element type. Ordered bound queries accept an independent scalar value in each comparison direction they use, while the two-range lexicographical, inclusion and ordered-output forms accept independent scalar element types. Predicate mutation destinations must be writable; ordered outputs accept checked direct scalar conversions from both input elements; `copy_n`, `fill_n` and `search_n` admit integral and non-scoped enum counts through 64 promoted bits. `mismatch`, `equal_range` and `minmax_element` return authenticated scalar-pointer pairs, while `minmax` returns its exact authenticated pair of const references. Source enum comparison overloads, undocumented heterogeneous values and predicates, records and custom iterators remain rejected. The authenticated `shuffle` and `sample` component headers stay disabled until their random-distribution dependency has a checked cross-target C runtime `mbstate_t` ABI. See the [algorithm contract](cpp-core-v2.md#algorithm-header-from-algorithm). |
| Numeric algorithms from `<numeric>` in `cpp-core-v2` | The exact pinned public header and its 126-file libc++/resource closure are identical and platform-free on all eight targets. Default `iota`, `accumulate`, `inner_product`, `partial_sum`, `adjacent_difference`, `reduce`, two-range `transform_reduce`, `inclusive_scan` and `exclusive_scan` calls on admitted non-promoted integer, `float` or `double` pointer ranges lower directly. `iota` accepts an independent arithmetic initial value through a checked direct conversion to the writable output element. Default `accumulate`, `inner_product`, initialized `reduce`, two-range `transform_reduce` and `exclusive_scan` accept an independent arithmetic initial value through checked common arithmetic, while `inner_product` and two-range `transform_reduce` also accept an independent numeric second-range element. The corresponding operation-taking forms also accept function pointers with directly convertible scalar results and by-value scalar parameters, including heterogeneous second-range callbacks for `inner_product` and two-range `transform_reduce`, unary `transform_reduce`, initialized `inclusive_scan`, both `transform_inclusive_scan` forms and `transform_exclusive_scan`; initialized scans may combine in an independent arithmetic accumulator type and convert it directly to the writable output element. Output ranges are writable, arguments and callbacks are retained once, and empty or in-place scans retain their sequential behavior. `gcd` and `lcm` additionally accept non-boolean built-in integer combinations through 64 bits, including narrow and mixed-signedness arguments, and return the exact common type. Callable objects, reference callback parameters or results, record callback results, heterogeneous accumulator/value forms outside the documented operations, enums, records, custom iterators, wider integers, function addresses for the numeric algorithms and the remaining numeric algorithms are rejected. See the [numeric contract](cpp-core-v2.md#numeric-header-from-numeric). |
| Standard function objects, reference wrappers and invocation from `<functional>` in `cpp-core-v2` | The exact pinned C++17 public header and its 346-file libc++/resource closure are identical and platform-free on all eight targets. Calls on typed and transparent specializations of all 19 C++17 arithmetic, bitwise, comparison and logical standard function objects lower directly for integral types through 64 bits, `float` and `double` where the selected operator is valid. Transparent specializations accept heterogeneous admitted operands with Clang-selected built-in conversions. Exact empty objects use an authenticated one-byte carrier and support trivial construction, assignment, local and global storage, copying and by-value passing. Exact integral `std::hash` specializations from `bool` through `unsigned long`, plus `long long` and `unsigned long long`, also use that carrier and lower to target `size_t` for temporary, stored, copied and by-value objects, support same-type copy and move assignment, and call directly or through `std::invoke`; the receiver is evaluated before the argument. Wide values preserve their bits on 64-bit targets and reproduce libc++'s Murmur2 hash of eight little-endian bytes on 32-bit targets after authenticating the exact inherited `__scalar_hash` implementation. The exact C++17 `nullptr_t` hash specialization returns its pinned libc++ constant through the same object, assignment and invocation boundary. Exact `float` and `double` hash specializations authenticate the scalar base and zero-normalizing outer operator: both signed zeros hash to zero, while other values hash their IEEE representation; `double` uses the eight-byte Murmur2 path on 32-bit targets. Complete enum hashes authenticate the primary `hash`, its exact `__enum_hash<E, true>` base, the underlying-type cast and the nested integer hash, then reuse the matching narrow or wide algorithm through the same object forms. Exact non-volatile object-pointer hashes authenticate the pinned pointer partial specialization and union body, then reproduce four-byte Murmur2 on 32-bit targets or ABI-v1 eight-byte CityHash on 64-bit targets through those object forms. Exact object `std::reference_wrapper<T>` and `std::reference_wrapper<const T>`, plus exact fixed-arity function wrappers with admitted scalar, object-pointer or fixed-arity ordinary function-pointer values and exact lvalue- or rvalue-reference parameters and results, retain the authenticated target ABI layout: one pointer slot under the Itanium ABI and an empty-base storage slot plus the pointer under the Microsoft ABI; they support direct matching-lvalue construction, direct and wrapper-taking `std::ref`/`std::cref`, trivial copy/move construction, same-type assignment and `get()` with preserved qualification and one-time evaluation. Wrapper-taking calls preserve the referenced target, with `cref` adding `const` for object targets. Object forms also support implicit reference conversion. Wrappers around admitted standard function objects, stored fixed-arity function pointers and admitted function referents call directly or through `std::invoke`; exact scalar, object-pointer and function-pointer lvalue or rvalue references preserve their source storage and qualification, and the wrapper and referenced callable are retained before argument evaluation. Wrappers around exact source-owned record callables use the same forms when Clang selects an admitted defined lvalue or const-lvalue `operator()`, retaining the referenced object identity, mutations and reference results. Exact specialization, member template, `std::forward`, body and source provenance are authenticated; narrow integer promotion, result conversion and one-time eager argument evaluation are preserved. Exact C++17 `std::invoke` calls additionally lower fixed-arity ordinary functions and stored function pointers with directly convertible by-value scalar parameters, exact fixed-arity ordinary function pointers including function-name decay, or exact admitted lvalue or rvalue references to those values, preserving scalar, object-pointer, function-pointer, lvalue- or rvalue-reference or `void` results and retaining the callable before evaluating arguments. They also accept every admitted typed or transparent standard function object, including temporary and stored objects, through the same authenticated operations and conversions. Exact source-owned record callables lower when Clang selects an admitted defined nonstatic `operator()` with the same argument and result boundary; lvalue, const-lvalue and rvalue-qualified overloads, reference results, receiver-first evaluation and temporary lifetime are preserved. Direct named addresses of owned nonstatic member functions and admitted non-volatile scalar, object-pointer or fixed-arity ordinary function-pointer fields lower on exact-class lvalue, full-expression temporary, pointer or `std::reference_wrapper` receivers. Method calls preserve receiver-before-argument sequencing; field projections preserve their qualified lvalue or xvalue category; materialized temporary receivers are destroyed at their C++ lifetime boundary, object-pointer values use checked qualification-preserving conversions, exact scalar, object-pointer or function-pointer lvalue- or rvalue-reference parameters and results preserve storage identity, and field calls retain their qualified lvalue result, including reads of declared-const fields; projected function pointers may be called or reseated through that result. Directly initialized local member pointers for admitted methods or data fields may be retained across statements and passed to `std::invoke`; their storage is authenticated and erased before the same direct method call or field projection is emitted. Exact same-type local copy/move-initializer chains, including the admitted `<utility>` reference adapters, are also authenticated and erased; those adapters may wrap the authenticated pointer at its final `std::invoke`, native member operation or `std::mem_fn` factory use. Admitted direct addresses and stored data-member pointers may additionally be applied with native `.*` or `->*` on an exact lvalue, pointer or full-expression temporary receiver, preserving the field glvalue and const qualification contributed by either the field or receiver, including native `.*` lifetime extension by a local reference. Admitted direct addresses and stored member-function pointers may also be called with native `.*` or `->*` on an exact object, pointer or full-expression temporary through the same fixed-arity method boundary, preserving receiver-before-argument sequencing and admitted lvalue- or rvalue-reference parameters and admitted lvalue- or rvalue-reference results; `&&`-qualified methods require the temporary receiver. Exact `std::mem_fn` wrappers built from those direct named addresses or authenticated local member-pointer initializer chains authenticate and erase the pinned wrapper before calling directly or as the immediate callable of `std::invoke` through the same boundary. Directly initialized local wrappers for admitted methods or data fields may be retained across statements and called through either form; exact same-type local copy/move-initializer chains, including the admitted `<utility>` reference adapters, are authenticated and erased with their carriers. The same adapters may wrap an authenticated local wrapper at its final direct call or `std::invoke` use. Cv-qualified typed specializations, function-object addresses or pointers, user-defined operands, `long double`, `void *`, volatile-object or function-pointer hashes, `std::function`, binders, searchers, reassigned or null member pointers, `mem_fn` copies or moves from parameters, reassigned `mem_fn` objects, base adjustments, volatile reference wrappers or receivers, other reference signatures and variadic targets remain rejected. O0/O2 execution and eight-target protocol fixtures cover the admitted operations, storage, reference wrappers and invocation. See the [functional contract](cpp-core-v2.md#functional-header-from-functional). |
| Allocation metadata and placement from `<new>` in `cpp-core-v2` | The exact pinned C++17 public header and its 37-file libc++/resource closure are identical and platform-free on all eight targets. `std::nothrow_t`, scalar `std::align_val_t` values and the interference-size constants remain compile-time or scalar metadata. Exact `std::launder` calls on admitted non-volatile scalar, fixed-array and source-record object pointers return the retained pointer once without a libc++ call. Exact standard single-object and constant-array placement new expressions evaluate the storage argument once and reuse it without an allocation call; the checked array ABI supplies any required cookie offset. Volatile, void and function pointees, function addresses, runtime nothrow tags, direct or redeclared placement functions, default heap operations, runtime placement-array bounds, quoted/shadow headers and forged declarations remain rejected. See the [`<new>` contract](cpp-core-v2.md#new-header-from-new). |
| Memory utilities from `<memory>` in `cpp-core-v2` | The exact pinned C++17 public header and its 267-file libc++/resource closure are identical and platform-free on all eight targets. Raw-pointer `pointer_traits`, exact `std::allocator<T>` and exact `std::allocator_traits<std::allocator<T>>` identities, nested aliases, rebinds and constants resolve through authenticated compile-time metadata. Exact `uses_allocator<T, std::allocator<U>>` identities, inherited aliases and compatible values also resolve. Exact single-object `std::default_delete<T>` and unbounded-array `std::default_delete<T[]>` specializations, including complete bounded inner extents, preserve authenticated one-byte stateless carriers; default, copy/move and admitted cv-converting construction plus calls through checked object or flattened reverse array destruction and a Clang-selected source-defined scalar or array class/global delete lower directly. Exact single-object `std::unique_ptr<T, D>` and unbounded-array `std::unique_ptr<T[], D>` specializations, including multidimensional owners, preserve authenticated pointer-sized carriers when `D` is the matching default deleter or a source-owned by-value empty, standard-layout, trivial one-byte deleter with one exact source-defined `void operator()(pointer) noexcept`; default, null, compatible raw-pointer, matching raw-pointer/null plus an argument bound to the exact deleter lvalue or rvalue parameter, same-type move, assignment, `nullptr` assignment, pointer observation, mutable/const `get_deleter` references, boolean conversion, release, reset, member/free swap, all six comparisons between admitted same-category owners with qualification-compatible raw pointers regardless of deleter specialization, all six bidirectional `nullptr` comparisons, and automatic or static destruction lower through the matching checked default scalar or array class/global-delete path, or a guarded custom operator call. Default deleters additionally admit const-adding converting moves. Scalar owners expose dereference and arrow; array owners expose subscript. Every admitted nonstatic member accepts an object or exact raw-pointer receiver, preserving pointee `const`, owner identity and one-time evaluation. Exact single-object `std::make_unique<T>(args...)` plus unbounded-array `std::make_unique<T[]>(count)`, including complete bounded inner extents, for integer constant expressions from zero through 65536 authenticate the pinned factory, matching selected scalar or array global/class allocation, and owner construction, then value-initialize flattened scalars or call the selected source-owned non-template `noexcept` record constructor, including authenticated source-owned trailing defaults evaluated once per constructed object, and return that same owner without a libc++ call. Exact runtime allocator objects preserve the authenticated one-byte stateless libc++ representation through a synthetic byte field. Their default, copy/move and non-void converting construction, same-type assignment, heterogeneous comparison, deprecated C++17 `address` and complete-element `max_size` lower directly without libc++ calls. Exact allocator and allocator-traits `construct` authenticate the pinned placement-new/forwarding body and directly value-initialize writable scalars or invoke supported source-owned `noexcept` default, multi-argument, copy and move constructors for complete source-owned records, including authenticated source-owned trailing defaults evaluated once. Exact allocator and traits `allocate`/`deallocate` forwarding evaluates receivers, hints, pointers and counts and calls checked source-defined global new/delete for complete default-new-aligned elements; allocation counts must be constant and proven within `max_size`, while deallocation counts may be runtime values. Exact allocator member `destroy` accepts its matching raw element pointer; exact allocator-traits `destroy` preserves the pinned member or `destroy_at` fallback for admitted scalar or complete source-record pointers. Traits `max_size` and copy selection reuse the checked size and stateless-record paths. Exact `std::addressof(T&)` and raw-pointer `pointer_traits<T *>::pointer_to(T&)` calls directly return checked addresses of non-volatile scalar, pointer, array or source-record lvalues, preserve pointee `const`, evaluate their argument once and bypass overloaded `operator&`. Exact `destroy_at`, `destroy` and `destroy_n` calls accept raw pointers to scalar objects and complete source-owned non-union records. Trivial destruction has no runtime body; nontrivial records call their checked destruction helper in forward order, with exact argument evaluation, counted return advancement and the lifetime alias policy. The ten C++17 `uninitialized_*` copy, move, fill, default and value construction forms lower for writable raw pointers to matching scalars or complete source-owned non-union records. Trivial records use direct storage operations; zero-parameter nothrow default constructors support default/value construction, while authenticated helpers may select exact source-owned non-template nothrow copy or move constructors for copy/fill/move. Pointer and pointer-pair results, argument evaluation and the lifetime alias policy remain exact. Volatile objects, rvalues for address utilities, function pointers, fancy pointers, custom allocator traits, default-heap, dynamic-count or over-aligned allocator allocation, other allocator-traits forwarding calls, throwing/converted allocator construction, array-element destruction, custom iterators, heterogeneous, union or non-source-record uninitialized construction, function addresses, quoted/shadow headers and forged declarations remain rejected. Stateful, reference, non-raw-pointer, nontrivial, overloaded, ref-qualified or throwing custom deleters, const-removing or base-adjusting converting moves, base-adjusting heterogeneous ownership comparisons, `get_deleter` member-function addresses, runtime-count/throwing/over-aligned `make_unique`, other smart pointers and other ownership factories await separate runtime contracts. See the [memory-header contract](cpp-core-v2.md#memory-header-from-memory). |
| Builtin driver, semantic protocol, artifact checks | Implemented with CLI/IR/artifact tests; production protocol is separate from the P0 prototype format. |
| Experimental `cpp-core-v1` | Implemented: programs/modules, `-O0`/`-O2` full-value comparison, rejection, relocation, output ownership and cancellation tests. Platform and delivery evidence is listed below. |
| Experimental `cpp-core-v2` | Adds 8/16/32/64-bit integers, IEEE float/double, character literals, constant size/alignment queries, checked type aliases and integral enums, static assertions, bounded object pointers, live lvalue/rvalue references, fixed local arrays/array fields, array decay/indexing and switch control flow to the single-source core contract. Source/carrier sizes, ABI alignments and record field offsets are independently checked against NeverC and guarded in generated code. See the [core v2 contract](cpp-core-v2.md); earlier v1 CI evidence does not establish v2 execution support. |
| Empty base chains in `cpp-core-v2` | Source-owned one-byte standard-layout nonvirtual single-base chains use real first-member C storage. Checked ordinary/template construction, generated copying/moving/assignment and destruction preserve base effects, order and identity. Internal base constructor entries preserve initialization roles through delegation and share local static guards with complete-object entries. Nonempty objects, inherited constructors and other layouts remain excluded. Paired source/protocol, eight-target layout, native exit order, saved-NC O0/O2 and relocation checks require implementing CI. See the [empty-chain contract](cpp-core-v2.md#empty-base-chains). |
| Function type metadata in `cpp-core-v2` | Ordinary callback function types and their direct lvalue/rvalue references pass through templates, aliases, defaults, packs, deduction, queries and transforms while retaining exact signature source, adjusted bounds and noexcept dependencies. Actual pointers/callbacks keep existing carriers and body requirements; runtime function-reference carriers and record/array values in signatures remain restricted. Paired controls, eight-ABI identities, 23 runtime checkpoints and relocation require implementing CI. See the [function metadata contract](cpp-core-v2.md#bare-function-type-metadata). |
| Unary type transforms in `cpp-core-v2` | Sixteen pinned transforms check both original input and result, exact substituted operand source, and final consumed dependencies. Erased aliases retain template source; dependent parameter declarations and irrelevant pointee layouts remain lazy. No new runtime carrier or helper. Paired cases, eight-ABI identities/widths, thirty runtime checkpoints, O0/O2 and relocation require implementing CI. See the [type-transform contract](cpp-core-v2.md#unary-type-transforms). |
| Array type queries in `cpp-core-v2` | __array_rank and __array_extent use checked types and nonnegative constant integer indices, preserving native size_t values without operand effects. Fixed-type template indices receive deferred evaluation and actual substitution in the private frontend; constant-evaluated dimension contexts and explicit pack collection preserve nested constexpr calls and folds. The final source graph retains exact type and dimension roots, including cached/default/generated value dependencies even for zero or out-of-range results. Unknown-bound arrays retain rank and zero outer extent, checked inner bounds and alias/template source. Owned incomplete non-union record elements retain type identity without an invented layout; real definitions retain full checks. Metadata pointer/reference wrappers preserve source identity without adding runtime carriers. Unsupported elements and implicit class-conversion indices remain excluded. Paired source/protocol, producer source-state audits, O0/O2 and relocation require implementing CI. See the [array-query contract](cpp-core-v2.md#array-type-queries). |
| Builtin type classification in `cpp-core-v2` | Thirty-seven unary predicates, binary __is_same (also __is_same_as), and __is_base_of classify admitted source types, including void and checked function signatures, final classes, C++17 literal types and source padding/trivial-copy representation properties. Destructibility and trivial destruction preserve deletion/access and lazy template bodies/specifications for admitted records; nothrow record values/arrays use the checked operation contract while reference destruction uses its unconditional result. Standard record final is admitted across existing generic and nested class shapes; other attributes and virtual methods retain their restrictions. Source identity survives shared C carriers; all retained TypeLoc operands, selected defaults and substitutions remain checked without executing unevaluated effects. Every supported concrete result closes its type/expression dependency graph, including alias bounds, consumed layout and generated value source, without selecting metadata-only hypothetical operations. Unknown-bound arrays and owned incomplete non-union record metadata retain exact type identity in aliases and template arguments, including ordinary/trivial destructibility short circuits. Real definitions retain full checks; runtime carriers remain restricted, and operation queries use their separate retained-source proof, including incomplete record identities where no referent operation is selected. Unsupported operand types and other trait kinds remain rejected. Paired source/protocol, O0/O2 and relocation checks require implementing CI; standard headers and complete C++/STL remain unfinished. See the [classification contract](cpp-core-v2.md#builtin-type-classification). |
| Checked operation traits in `cpp-core-v2` | Construction, assignment, implicit conversion and nothrow destruction predicates preserve Clang results for admitted non-record operands, including references, fixed arrays and pointers to admitted records. Unknown-bound array operands admit checked metadata, exact reference binding/decay and construction/destruction short circuits without runtime carriers; failed record operations still need complete retained source. Owned incomplete non-union records can supply identity for nonowning pointer/reference operations and exact pre-operation false results; selected lazy method return signatures retain explicit complete-carrier checks. Construction has at most 64 argument types; all written type sources and defaults remain checked. Complete implicit trivial record operations additionally use independently checked retained roots, synthetic reference/decay operands and implicit owning-subobject operation families. User operations use exact completed ordinary or original inline template definitions, converted synthetic operands and recursive destruction proofs in a deferred source pass before lowering. Concrete template source requires raw original-body identity and completed selected/definition signatures; no unused body is instantiated, and implicit/defaulted bodies cannot supply user-body completion. Retained nothrow construction/assignment/conversion also check already-resolved selected exception specifications without requesting resolution. Constructor queries also check exact completed ordinary or instantiated template parameter defaults, unchanged initializer wrappers and nested destruction dependencies, with source collection through unevaluated operands and TypeLoc; lazy template destructor prvalues still need signature proof when destruction is consumed. A written `decltype` terminal call and its parentheses/built-in comma-right result wrappers add no temporary destruction; actual arguments, comma-left temporaries and callee source remain checked. Nothrow defaults additionally check already-resolved actual callee specifications. Rebuilt nondependent exception expressions require completion of both original and resolved nodes. Shared written type nodes across specializations do not replace each actual resolved expression’s first source check. Referenced unused inline friends require exact paired source and concrete signature completion while retaining lazy bodies and unselected defaults. Source dependencies can use ordinary trivial defaulting with implicit subobject families or separately completed generated initializer/body/layout nodes; record-array memcpy retains its omitted assignment-family proof. Inline template defaulting additionally requires actual defaulting, completed concrete signatures and the exact single-stage inline defaulting origin. Actually visited retained nothrow-destruction queries can trigger the first check of a missing inline class-template destructor signature; exact TSI nodes are never replayed, unused body events stay lazy, and signature/generated work drains before final validation. The exact owned base/field/array graph can supply further already resolved inline-defaulted class signatures for first checking, bounded by record identity and 64 levels. Child source nodes do not alter the root event or result; unresolved template-child completion and local-class context are not inferred. Complete retained record-prvalue operation results can request the same existing owning signatures; reference results do not consume referent destruction. Construction, assignment and conversion traits can compose checked ordinary class-template constructor/operator signatures while unused inline bodies remain uninstantiated. Each exact selected expression in the retained operation graph requires its own completion, including nested arguments and conversions. Actual redeclarations, defaults, resolved exceptions and owning destruction of consumed record temporaries remain checked; opaque operands and default initializers are traversal leaves. Authorized ordinary destructor signatures may supply query-source dependencies. Runtime operations still require checked bodies. Dependent operation queries in non-type parameter declaration types admit direct type parameters from the same exact written list; concrete substitutions retain their own semantic/source checks. Member-template constructors, assignments and conversions also require exact successful selection evidence, checked canonical arguments and a separate completed dependency graph for consumed template defaults, even when the body already exists; actual signature frames cannot borrow caller arguments. Template assignment checks the exact direct selected reference and original operator location against existing deduction source. Copied primaries additionally require a bounded raw origin chain with matching checked source ordinals, exact selected class edges and a terminal inline body; member-specialization stops and borrowed later definitions remain excluded. Actual copied primaries keep their inner argument frames, while outer substitutions retain their actual class instances. Exact defaulted construction/assignment roots can use the final completed generated-source proof, preserving original operand, exception and lifetime checks; nontrivial roots need an existing generated body, and selected ordinary defaults can use the same final proof with exact initializer, callee and lifetime checks. Earlier selected declarations retain their later defaulted family's generated-source dependencies; family classification does not replace actual signature/body completion. Template defaults require normal completion of the exact actual parameter/initializer under its matching concrete context and a completed inline-origin constructor definition; explicit arguments preserve unused default laziness. Record-value and fixed-array nothrow destruction retain the exact selected destructor and resolved prototype snapshot, requiring completed source and implicit, checked explicitly defaulted or completed user owning destruction. Written defaulting requires the actual declaration family and every original type-source node; implicit nontrivial owners retain the recursive proof without instantiating a body. Deleted/access short circuits preserve false without consuming a lazy specification; reference nothrow destruction performs no destructor lookup. Other unmaterialized template bodies, separate template definitions, copied member-template origins, template defaulted destructors lacking signature completion, uninstantiated or rewritten defaults, incomplete selection and unresolved exception dependencies still need further source evidence. Implicit trivial construction composes its own family proof with the separate exact record-prvalue destruction proof, including selected defaults; nothrow construction and destruction retain their independent Clang booleans. Paired diagnostics, 24 scalar, 18 implicit-record and 120 user-operation saved-NC O0/O2 checkpoints, relocation and eighteen scalar booleans across eight ABIs require implementing CI. Standard headers and full C++/STL remain unfinished. See the [operation contract](cpp-core-v2.md#non-record-operation-traits). |
| Native C heap in `cpp-core-v2` | Exact source-owned global C declarations admit direct malloc/calloc/realloc/free through checked IR and independent native CRT/size_t evidence. A volatile indirect realloc bridge preserves the process allocator's zero-size and failure behavior under generated C23 optimization. Definitions take priority; function values, arbitrary imports and explicit attributes remain rejected. Win32 cdecl, saved-target guards, source/protocol/IR checks and O0/O2 bidirectional C-client allocation/resize/free interoperability require implementing CI. Default throwing C++ allocation, headers and full C++/STL remain unfinished. See [native heap](cpp-core-v2.md#native-c-heap-calls). |
| Constant array allocation in `cpp-core-v2` | Source-owned new[] admits strict constant outer lengths, including zero, fixed inner dimensions and selected placement/templates. delete[] recovers runtime count and original storage before reverse destruction. Generic Itanium, Apple ARM64 and MSVC cookies require independent ABI metadata; nullable allocation skips cookie writes and initialization. Eight-target protocol, malformed metadata, relocation and O0/O2 C-client/runtime checks require implementing CI. Throwing runtime new bounds, cookie-free MSVC sized deletion, default heap, exceptions and full C++/STL remain unfinished. See the [array contract](cpp-core-v2.md#constant-array-allocation). |
| Runtime class array allocation in `cpp-core-v2` | Source-owned nonthrowing class new[] without placement evaluates the bound once, preserves pre-conversion negative checks and normal positive-wide size_t conversion, and rejects invalid byte/prefix lengths with null before any allocator call. Explicit prefixes retain enclosing temporary lifetimes; direct omitted constructors have per-element cleanup. Eight-target width, paired source/protocol, relocation and O0/O2 C-client checks require implementing CI. Repeated aggregate/list fillers may initialize actual element storage after a role-aware proof excludes separate temporary objects; selected defaults, nested arrays and live references are checked. Arbitrary temporary-producing fillers, throwing/placement runtime new, default C++ heap and full C++/STL remain unfinished. See the [runtime contract](cpp-core-v2.md#runtime-array-allocation). |
| Static destruction in `cpp-core-v2` | Complete static records, arrays and lifetime-extended temporaries register native CRT cleanup on completion; root registration precedes ordinary full-expression cleanup. Constant roots preserve their data and guard only registration. Linux/Darwin use __cxa_atexit with module ownership; explicit MSVC Windows uses atexit with exact x86 C calling convention. Independent ABI checks, typed cleanup helpers, CFG ownership and cyclic/conditional registration verification retain const rules. Source/protocol, malformed IR, O0/O2 C callback interleaving, native library unload and sixteen-thread first-use fixtures require implementing CI. TLS, exceptions, default heap/standard headers, manual/DynCode loading and complete C++/STL remain unfinished. See the [destruction contract](cpp-core-v2.md#static-destruction). |
| Nonlocal dynamic initialization in `cpp-core-v2` | Admitted scalar/pointer/callback/record/array/reference definitions and materialized template instances initialize before main through a checked internal native constructor. Definition order, macro ordering, zero/constant phase, permanent temporary children, full-expression cleanup and source-owned new use existing typed operations. Local statics retain first-use semantics. Program and separate C-client O0/O2 fixtures require implementing CI. TLS, exceptions, default heap/standard headers and manual/DynCode loading remain separate work. See the [startup contract](cpp-core-v2.md#nonlocal-dynamic-initialization). |
| Binary floating values in `cpp-core-v2` | IEEE `float`/`double` values, arithmetic, source-selected conversions, references/pointers, fields/arrays, callbacks and zero/constant scalar static storage. Exact literal bits, independently checked carrier/record layouts, default floating environment and no implicit contraction; fast-math and excess precision are rejected. Source/protocol, IR and O0/O2 fixtures require implementing-revision CI. Long double, complex types, standard headers remain unfinished. See the [floating contract](cpp-core-v2.md#binary-floating-point-values). |
| nullptr_t template values in `cpp-core-v2` | Concrete nullptr_t, auto and dependent scalar arguments, defaults and bounded packs reuse typed null IR across admitted function/class/alias/variable/member/partial/full contexts. Exact canonical type/value and retained written/default/parameter sources remain checked. Pointer-typed null arguments stay rejected. Paired 33 accepted, 20 source-boundary rejection and 5 invalid-C++ cases, O0/O2 identity and relocation fixtures require implementing CI. See [template null values](cpp-core-v2.md#nullptr-template-values); full C++/STL remains unfinished. |
| Null pointer values in `cpp-core-v2` | Distinct `decltype(nullptr)` storage, aliases, deduction, references, fields/arrays, supported callback signatures and zero/constant scalar static or variable-template objects. Pointer conversions retain source effects and cleanup; independent object addresses, layout, boolean/equality rules and checked source folding are covered by paired source/protocol, IR and O0/O2 fixtures. nullptr_t non-type arguments follow the template-value row; standard headers beyond the pinned set remain unsupported. See [null values](cpp-core-v2.md#null-pointer-values); native CI of the implementing revision is required. |
| Fixed arrays in `cpp-core-v2` | Implemented with O0/O2 execution and protocol/forged-IR regression cases. Includes multidimensional arrays, pointer/reference aliasing, source-order initialization and subscript evaluation. Extent/expansion limits and VLA exclusions are in the [core v2 contract](cpp-core-v2.md). New array cases still require revision-specific native CI evidence; the older results below do not verify them. |
| Defined scalar static data members in `cpp-core-v2` | Defined integer/bool/enum/float/double members, including inline/constexpr and out-of-line definitions, share ordinary typed global storage. Constant/zero initialization, receiver effects, temporary cleanup and lasting static aliases are covered by fixtures. Uses requiring object identity need a definition in this source unit; declaration-only const scalar values follow the next row; other static types remain restricted. Native validation requires the implementing revision. |
| Declaration-only static constants in `cpp-core-v2` | Non-inline const integer/bool/enum members with checked in-class constant initializers support non-ODR value reads, discarded results and unevaluated uses without a separate definition. They emit typed values, no source global or field, and preserve receiver effects and temporary cleanup. Address/reference uses, including discarded or skipped uses, still require a definition (`TR0203`). Full C++/STL remains unfinished; native validation requires the implementing revision. |
| Concrete standard-layout class templates in `cpp-core-v2` | Up to 64 type/scalar parameters, type defaults, explicit instantiation/specialization, aliases, fixed fields/arrays and selected field initializers. Instances use canonical record/field identities, explicit layout and existing copy/reference/return/destruction operations. Written defaults/types/arguments remain checked. Nested classes and instantiated non-template friends follow the contracts below; full STL remains unfinished; native results require implementing CI. |
| Class-template scalar static data in `cpp-core-v2` | Integer/bool/enum/float/double zero or constant initialization, inline/constexpr and out-of-line definitions, explicit instantiation/specialization, shared canonical globals and separate template instances. Unused members stay lazy; materialized initializers and every written directive retain source checks. Declaration-only const reads create no storage; evaluated address/reference uses require definitions. Receiver effects and cleanup use ordinary typed operations. Other static types, full C++17/STL remain unfinished; native validation requires implementing CI. |
| Ordinary named class-template methods in `cpp-core-v2` | Selected instance/static methods, const/ref overloads, defaults/noexcept, explicit instantiation/specialization, static factories, scalar static locals and member ranges of admitted standard-layout templates. Bodies/defaults stay lazy until instantiated; selected calls and explicit declarations require definitions. Ordinary receiver/result ABI and per-instance method/local/storage identity; written outer parameter types remain checked. Own member function templates follow their separate row; class-template operators follow their separate row. Native validation requires implementing CI; full C++/STL remains unfinished. |
| User-provided class-template constructors in `cpp-core-v2` | Ordinary/default/explicit/converting/copy/move constructors, checked defaults, out-of-line definitions and explicit instantiation/specialization of standard-layout instances. Existing destination/source ABI, member initialization order, field/array lifetimes and local/static identities. Selected construction requires a definition; unused bodies/defaults stay lazy. Inherited constructors remain excluded; delegation follows its separate row. Native validation requires implementing CI; full C++/STL remains unfinished. |
| Deleted function declarations in `cpp-core-v2` | Supported ordinary, special-member, operator, friend and template declarations participate in Clang checking and emit no executable definition. Selected deleted calls/addresses retain `TR0202`; other missing definitions retain `TR0203`. Deleted defaulted moves preserve selected copy fallbacks; guaranteed elision and move-only objects use existing lifetime lowering. Source/protocol and O0/O2 fixtures require implementing CI. See the [declaration contract](cpp-core-v2.md#deleted-function-declarations). |
| Const data members in `cpp-core-v2` | Supported scalar/record/pointer/callback/array fields, generic and nested classes, final-destination initialization and source-selected copy/move construction. Addresses and const reference/decay types remain intact, including const pointers with mutable pointees. Source-invalid writes and deleted assignments retain language diagnostics; deleted/defaulted-deleted declarations follow their separate contract. Paired source/protocol and O0/O2 fixtures require implementing CI. See the [const-member contract](cpp-core-v2.md#const-data-members). |
| Delegating constructors in `cpp-core-v2` | Ordinary and concrete generic classes, chains, copy/move delegation and selected constructor templates reuse the same final receiver. Target construction runs once; argument cleanup precedes the delegating body, and members are not initialized twice. Source selection, missing definitions and unsupported operations retain their checks. Paired source/protocol and O0/O2 fixtures require implementing CI; [inheritance and exception unwinding remain separate](cpp-core-v2.md#delegating-constructors). |
| User-provided class-template destructors in `cpp-core-v2` | Nonvirtual destructors, out-of-line definitions, explicit instantiation/specialization and checked selected exception specifications. Used definitions are required even for direct object returns; type-only/unevaluated queries keep unused bodies lazy. Internal receiver-pointer helpers preserve body-before-reverse-member/array cleanup and existing temporary/reference/parameter/result lifetimes. Bounded demand queue avoids forcing unused implicit wrappers. Explicit destructor calls and full C++17/STL remain unfinished; native validation requires implementing CI. |
| Defaulted class-template special members in `cpp-core-v2` | In-class/out-of-line default/copy/move constructors, copy/move assignment and nonvirtual destruction of admitted instances. Exact defaulting origin, concrete signatures, lazy queries and owned generated definitions retain Clang semantics. Member order, zeroing distinctions, source/destination identity, reference results and reverse cleanup use ordinary typed helpers. Explicit specialization cannot inherit primary defaulting. Native O0/O2 and protocol fixtures require implementing CI; complete C++17/STL remains unfinished. |
| Class-template operators and conversions in `cpp-core-v2` | User-provided operators, copy/move assignment and explicit/implicit conversions of admitted instances retain selected overloads, C++17 sequencing, aliases, result storage and lifetimes. Out-of-line definitions, explicit instantiation/specialization and per-instance scalar static locals use ordinary typed calls without runtime template parameters. Unused bodies/defaults remain lazy; selected declarations and all materialized bodies require source/definition checks. Allocation, standard headers and complete C++17/STL remain unfinished; native results require implementing CI. |
| Instantiated local classes and written directives in `cpp-core-v2` | Local-class members inside admitted function/member instances preserve concrete origin, ordinary calls, storage, lifecycle and per-instance scalar static locals; all materialized bodies are checked. Every explicit function-instantiation directive keeps written arguments, types, conversion names, qualifiers and attribute checks, including extern/no-effect repetitions. Canonical reuse creates no duplicate runtime function. Source budgets and existing restrictions remain; native results require implementing CI and full C++17/STL remains unfinished. |
| Concrete free function templates in `cpp-core-v2` | Up to 64 type/integer/bool/enum/nullptr_t parameters, scalar C++17 auto and dependent scalar types, array-bound deduction, explicit specialization/instantiation, resolved defaults, recursion and imports. Values become typed constants with no runtime template parameter. Equivalent arguments share instances; different values/types and primaries keep separate local types/static storage. Written value arguments and parameter types remain checked; unused generic bodies/defaults stay lazy. Unused implicit free specializations under sizeof/decltype/noexcept retain checked signatures and resolved specifications when the owned primary has a definition or is a non-friend namespace declaration with no definition; existing bodies stay lazy. Unused non-friend namespace ordinary/operator calls, with or without an owned primary definition, also require a completed source graph for selection, erased template defaults, actual arguments and consumed function defaults, including false results. Used instances and explicit instantiations still require concrete definitions. Unsupported dependent friend class-template forms, pointer/reference/class values and complete STL remain unfinished; native validation requires implementing CI. |
| Concrete parameter packs in `cpp-core-v2` | Type/scalar packs in admitted namespace function/operator and class templates; at most 64 primary parameters and 64 elements per pack. Expanded storage, typed sizeof... counts, resolved folds, reference collapse and existing lifetimes; exact owned origins and every materialized element remain checked. The same-list dependent count TypeLoc exception is metadata only. Empty fold patterns stay lazy. Unsupported dependent friend class-template forms, bases and complete C++17/STL remain unfinished; native results require implementing CI. |
| Scalar template parameter defaults in `cpp-core-v2` | Integer/bool/enum and scalar auto defaults for admitted function/operator/class/namespace-alias templates, earlier-parameter expressions, inherited defaults, deduction, specialization/instantiation and constexpr conversions. Equivalent explicit/omitted arguments share typed identities and static storage. Nondependent source is checked; dependent defaults stay lazy until a reached selected use or explicit declaration. Original/converted evidence preserves source checks, while failed substitution/conversion retains SFINAE fallback. No runtime template parameters. Pointer/reference/record values and complete C++17/STL remain unfinished; native validation requires implementing CI. |
| Partial declaration source checks in `cpp-core-v2` | Written class/variable partials and copied member variable partials retain successful primary argument and resolved parameter type source, even when inner arguments remain dependent or no partial is selected. Retained pack source does not waive C++ specialization ordering or deduction rules; invalid patterns remain TR0202. Generic full member variable source collection also retains pending arguments. Native CI is required. |
| Friend function templates in `cpp-core-v2` | Admitted ordinary/generic class bodies support free named/operator templates, ADL/private access, inner/outer defaults and packs, references, lifetimes, local classes and separate static identities. Exact copied/written declarations and compatible body context remain distinct. Canonical namespace merges preserve their existing identities. Unproved normalization/copy chains and unsupported dependent friend class-template forms remain separate work; native behavior requires implementing-revision CI. |
| Friend class templates in `cpp-core-v2` | Ordinary/generic granting classes support exact class-template targets, namespace introductions, existing and supported qualified targets, access by specializations and shared target/static identity. Original written defaults are invalid; inherited defaults remain checked. Exact written/copied target sources, lookup/default headers and actual definitions stay distinct. Unsupported dependent friend representations remain separate work; native behavior requires implementing-revision CI. |
| Ordinary function pointers in `cpp-core-v2` | Typed default-ABI scalar/reference signatures, defined ordinary free/static targets, indirect postfix capture before arguments, callback parameters/results, fields/arrays and constant global/static callback storage. Unsupported callback forms, independent ABI/layout checks and standalone-only scope are specified in the [callback contract](cpp-core-v2.md#ordinary-function-pointers). Native validation requires implementing-revision CI. |
| Concrete function-template pointers in `cpp-core-v2` | Explicit and target-deduced free/static template instances, supported owners and visible friends, exact selected source checks, shared canonical direct/indirect function and static-local identity. Default callback ABI/signature restrictions and standalone scope remain in force. See the [template callback contract](cpp-core-v2.md#concrete-function-template-pointers); native validation requires implementing-revision CI. |
| Callback variable templates in `cpp-core-v2` | Constant/zero-initialized namespace and static-member instances, supported partial/full owners, fixed/dependent callback types and direct auto, exact declaration/type/initializer sources, canonical storage and independent reseating. Default callback and standalone limits remain; see the [storage contract](cpp-core-v2.md#callback-variable-templates). Native validation requires implementing-revision CI. |
| Instantiated non-template friend types in `cpp-core-v2` | Admitted generic class bodies retain type-parameter, alias, nested-type and specialization grants using exact original/substituted sources. Embedded Sema checks access; ignored nonclass types still need source checks. No runtime friendship entry is added. Unsupported dependent friend class-template forms, type expansions and unproved copy chains remain separate work; native validation requires the implementing revision. |
| Instantiated non-template friends in `cpp-core-v2` | Admitted generic class bodies retain named/free-operator friends, ADL/private access, canonical namespace merging, local classes and scalar static locals without a receiver. Exact original/selected/granting sources remain separate; different normalized original signatures require independent complete nondependent type/default/noexcept evidence. Unsupported dependent friend class-template forms/type expansions remain unfinished. Native CI is required; normal source fixtures do not prove AST-merge normalization coverage. |
| Member class templates in `cpp-core-v2` | Ordinary named record owners support primaries, partial/full specializations, defaults, bounded packs and out-of-line definitions. Concrete types, field dependency order, method receivers, lifetimes and scalar static identity compose with supported member function/alias/variable templates. Inheritance, default heap runtime and full C++/STL remain unfinished. Eighteen O0/O2 checkpoints and paired protocol fixtures require implementing-revision native CI. |
| Member class templates with template owners in `cpp-core-v2` | Nested primary/partial instances, direct full specializations in concrete outer scopes and own member-template specializations preserve distinct outer/inner parameter levels, definition origins, layouts, receivers and scalar static objects. Hidden copied partials retain resolved source checks even when unselected. Copied full declarations follow the separate full-source contract; ordinary nested generic records follow their own checked body contract. Native CI of the implementing revision is required. |
| Copied class-scope full member classes in `cpp-core-v2` | Original dependent full declarations and actual concrete copies retain separate checked argument/default/type sources, exact full body origins and outer substitutions. The full body adds no parameter level. Existing layouts, member templates, receivers, lifetimes and scalar static identity apply; unused copies are checked. Inheritance, default heap runtime and complete C++/STL remain unfinished. Native CI of the implementing revision is required. |
| Ordinary nested classes in generic owners in `cpp-core-v2` | Named ordinary member records, concrete copies and own written specializations preserve zero template levels, exact body origins, lazy uninstantiated bodies and existing layout/receiver/lifetime/static-storage rules. Member templates compose with checked outer substitutions. Every explicit member-class directive retains its written qualifier, including extern/no-effect repetitions. Anonymous records, unions, unsupported dependent friend class-template forms, friend-type expansions, inheritance, default heap runtime and full C++/STL remain unfinished. Paired protocol fixtures and seventeen O0/O2 checkpoints require implementing-revision native CI. |
| Concrete member variable templates in `cpp-core-v2` | Scalar static members in admitted records and concrete outer primary/partial/full instances; defaults, bounded packs, inner partial/full and own member specializations, inline and out-of-line definitions. Exact prior declarations and selected source are checked; equivalent instances share canonical storage, while other arguments retain independent objects. Non-inline const reads need no storage; evaluated addresses/references require definitions. Twenty-two O0/O2 checkpoints and paired protocol/storage/receiver/relocation fixtures require implementing CI. Dependent outer class-scope full specializations retain original and concrete source: resolved argument/default/parameter-type syntax is checked immediately and pending source after instantiation. Callback results follow their separate row; other results, full C++/STL remain unfinished. |
| Concrete member alias templates in `cpp-core-v2` | Admitted ordinary records, including nested records, and concrete class primary/partial/full instances; type/scalar/auto/enum arguments, defaults and bounded packs; supported value, pointer, reference, array and record results. Exact outer/inner source and origin checks precede erasure. Nondependent declarations are checked even when unused; dependent underlying types remain lazy until substitution. Paired source/protocol, nine O0/O2 checkpoints and relocation require implementing CI. Allocation and full C++/STL remain unfinished. |
| Concrete member function templates in `cpp-core-v2` | Named/static methods, operators, constructors and conversions of admitted records and class instances, bounded type/scalar packs, defaults, external definitions and specialization. Inner/outer identities retain shared or distinct code and storage. Selected calls, defaults, materialized bodies and explicit directives preserve exact source; receivers, references, results and lifetimes use existing typed operations. Paired source/protocol, 20 O0/O2 checkpoints, closure and relocation require implementing CI. Inheritance, default heap runtime and full C++/STL remain unfinished. |
| Namespace scalar variable templates in `cpp-core-v2` | Integer/bool/enum zero or constant initialization, resolved auto, selected partials, full specializations, defaults and bounded type/scalar packs. Source-defined C++17 traits feed assertions, if constexpr and other template arguments. Equivalent instances share global storage; type-only uses do not force unused definitions. Actual use/default, first/definition type and materialized initializer source remain checked in the exact selected instance context. Paired source/protocol, 20 O0/O2 storage checkpoints, closure and relocation fixtures require implementing CI. Member variables and callbacks follow their separate rows; other result types, complete C++/STL remain unfinished. |
| Namespace class-template partial specializations in `cpp-core-v2` | Selected type/scalar/auto/pack patterns, primary fallback, ordering, full-specialization precedence and admitted concrete members retain separate primary and deduced argument identities. Exact successful deduction and substituted pattern source are checked, including non-type parameter types and empty packs. Paired source/protocol, 18 O0/O2 runtime checkpoints, storage/lifetime, closure and relocation fixtures are included; native results require the implementing revision's CI. Existing 64-entry bounds apply; template-template parameters, bases and full standard headers remain follow-up work. |
| Namespace alias templates in `cpp-core-v2` | Owned type/scalar/auto parameters, concrete packs, defaults, alias chains, namespace imports and supported scalar/void/pointer/reference/array/class types. Exact reached source uses retain written arguments and substituted underlying TypeLocs; canonical records/functions/static objects are reused. Selected defaults and non-type parameter types preserve caller context, exact deduction evidence and explicit declarations; empty deduced function packs retain their parameter type substitution. Limits: 64 parameters/elements/depth and 200000 source units. Member aliases follow their separate row; template-template parameters and full STL remain unfinished. |
| Namespace operator function templates in `cpp-core-v2` | Ordinary free operator templates retain deduction, ADL, explicit calls, specialization/instantiation, canonical local/static identity and checked source/definitions. Explicit parameters, references and record result destinations use existing typed calls with C++17 sequencing and full lifetime cleanup. Iterator free operators are included. Unsupported dependent friend class-template forms, default heap runtime, standard headers and complete C++17/STL remain unfinished; native validation requires implementing CI. |
| Resolved constexpr-if in `cpp-core-v2` | Ordinary and admitted concrete-template constant branch selection emits only the selected substatement, preserves runtime init/condition-variable storage and reverse cleanup, and avoids discarded automatic storage under outer switch registration. Ordinary functions check both source branches; Clang omits discarded dependent template branches. The stricter definition requirement remains; C++23 consteval branches stay excluded. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Inline namespaces in `cpp-core-v2` | Owned named/anonymous inline namespaces, transitive visibility, reopening, combined qualified lookup and bidirectional ADL retain original global/function/type identities. Different version namespaces remain distinct. Aliases, using imports and macro-spelled C++17 forms are included; explicit C++20 nested inline syntax and unsupported source remain excluded. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Resolved namespace imports in `cpp-core-v2` | Namespace aliases, using-directives and ordinary using-declarations in namespace/block scopes retain original function, object and type identities, resolved lookup, defaults and cleanup. Repeated/comma imports and unscoped namespace/local enumerators are included. Class imports, inherited constructors, unsupported template forms, foreign targets and C++20 enumerator imports remain excluded. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Statically initialized scalar locals in `cpp-core-v2` | Non-volatile integer/bool/enum static locals use zero or fully checked constant initialization and preserve values, addresses and escaped aliases across calls, recursion and lexical exits. Different scopes and overloads retain distinct objects; switch/loop entry does not create an automatic shadow or repeat initialization. Static locals in constexpr functions, TLS and unsupported static local types retain their separate restrictions. Dynamic local initialization follows the next row. Native validation requires the implementing revision; full C++/STL remains unfinished. |
| Dynamic local static initialization in `cpp-core-v2` | First-use scalar/pointer/callback/record/fixed-array initialization and reference bindings to existing objects in non-constexpr functions, including const objects; required destructors follow the static destruction contract. Actual parameters/locals/this, canonical storage, zero initialization, constructor destinations and full-expression cleanup are preserved. CFG-verified ownership and native lock-free 32-bit acquire/release guards synchronize callers; AArch64 helpers disable outlining and inlining. Paired source/protocol, malformed-IR, O0/O2, sixteen-thread visibility and assembly checks require implementing CI. Dynamic lifetime-extended temporaries and nonlocal startup follow their own contracts. TLS, exceptions/retry and complete C++/STL remain unfinished. |
| Named nested records in `cpp-core-v2` | Named non-template nested classes preserve source access, canonical scope identity and their own storage/receiver. By-value field and array dependencies are emitted first; pointer cycles keep forward declarations. Fixtures cover aliases, factories, out-of-line/local types, copy/move, cleanup and nested iterators. Anonymous nested records, templates and inheritance remain restricted; native validation requires the implementing revision. |
| Non-template friends in `cpp-core-v2` | Resolved friend functions/types preserve source access, hidden-friend ADL, selected operators, defaults and canonical redeclarations. Runtime behavior uses ordinary typed calls and storage. Fixtures cover legal grants, illegal access/lookup, factories, aliases, ADL ranges and cleanup. Unsupported dependent friend class-template forms, friend-type expansions and unsupported friend forms stay rejected; native results require the implementing revision. |
| Nonpublic data members in `cpp-core-v2` | Private/protected fields in supported standard-layout classes retain Clang access checks and ordinary typed storage. Fixtures cover factories, defaults, aliases, generated copies/moves, private arrays and cleanup; illegal external access stays rejected. Mixed-access non-standard-layout classes, unsupported dependent friend class-template forms, inheritance and anonymous nested records remain separate work. Native results require the implementing revision. |
| Range-based `for` in `cpp-core-v2` | Supported fixed arrays and source-defined member/ADL ranges use typed calls and separate range/iteration cleanup scopes. Includes values, references, record iterators, distinct sentinels and abrupt exits. O0/O2 and protocol fixtures check one-time initialization, storage identity and destruction order. Native results require the implementing revision; unsupported class-template forms, standard headers and STL containers remain separate work. |
| Switch in `cpp-core-v2` | Implemented using existing verified branch/label/jump IR, with O0/O2 execution and source rejection cases for selector effects, fallthrough, nested entry and loop control. GNU ranges and other attributes remain excluded. New switch cases still require revision-specific native CI evidence. |
| Pointer arithmetic in `cpp-core-v2` | Object-pointer offsets/differences and increment/compound assignment use verified strides and typed null/zero helpers. Includes forged-IR bounds checks and O0/O2 execution fixtures for arrays, rows, records and operand sequencing. Native results must be established for the implementing revision; full STL remains unfinished. |
| Object pointer ordering in `cpp-core-v2` | Relational comparisons preserve array/subobject/equal-pointer rules and choose a consistent order for unrelated objects through an independently verified unsigned address carrier on native x86/AArch64. Composite types, qualifiers, one-time operand evaluation and source/wire cast restrictions remain checked. Source, forged-IR and O0/O2 fixtures require implementing CI. See the [ordering contract](cpp-core-v2.md#object-pointer-ordering). |
| Reference members in `cpp-core-v2` | Object-reference fields use checked pointer carriers: construction binds, ordinary access dereferences, and copy/move preserves aliases. Const containers retain mutable referents; source-deleted operations remain deleted. Automatic aggregate temporaries follow exact Clang ownership and container-before-temporary cleanup. Constant static aggregates and dynamic local aggregate groups retain storage identity; constant and runtime materializing default elements retain distinct identities. A bounded private semantic expansion preserves outer-reference lifetimes and exact omitted-element cleanup, including one-element arrays; ordinary AST consumers keep their existing behavior. Source/protocol, IR, O0/O2 and concurrent-reader fixtures require implementing CI; full C++/STL remains unfinished. See the [binding contract](cpp-core-v2.md#reference-members). |
| Live rvalue references in `cpp-core-v2` | `T&&` aliases, parameters/results, conditional xvalues and ordinary `&&` methods retain the address of existing live scalar, pointer, record and array objects. Named rvalue references remain lvalues; resolved overloads and admitted source copies are preserved. Fixtures cover signatures, aliases, sequencing, arrays, cleanup counts and relocation. Static lifetime extension and unsupported moves remain excluded; full-expression temporary calls follow their separate row; native results require the implementing revision. |
| Ordinary methods in `cpp-core-v2` | Named nonvirtual instance/const/ref-qualified and static methods of admitted records use verified ordinary calls with an explicit this pointer where required. Protocol checks cover signatures, identity and receiver copies; O0/O2 no-inline fixtures cover mutation, const overloads, reference returns and receiver/argument effects. Unsupported special-member forms and STL remain excluded; temporary receivers follow their separate row; native results require the implementing revision. |
| Ordinary constructors in `cpp-core-v2` | User-provided default/converting/explicit constructors for standard-layout records with admitted copying and destruction. Fixtures check final destination identity, field declaration order, per-element array construction, materialization, hidden signatures and old-profile rejection. Exceptions and STL remain outside this increment; native results require the implementing revision. |
| Record call storage in `cpp-core-v2` | By-value record parameters use separate caller-prepared objects; record results use explicit destination pointers. Source-required copies and reference aliases remain intact. Protocol fixtures cover hidden signatures, direct forwarding, parameter separation and v1 compatibility; O0/O2 no-inline fixtures cover identity, mutation, recursion, receiver sequencing and nested initialization. User moves follow the separate row below; this convention does not admit STL; native results require the implementing revision. |
| Record destruction in `cpp-core-v2` | Ordinary user destructors and implicit containing-record destruction use typed calls and explicit cleanup at normal scope/full-expression exits. O0/O2 no-inline fixtures cover reverse local/member/array order, branches, loop jumps, parameter/result ownership, temporary completion order and captured conditions/return values. Protocol fixtures check helper signatures, ownership, reverse members and relocation. Exception unwinding remains excluded; explicit destruction and static lifetime extension/destruction follow their own contracts; native results require the implementing revision. |
| User copy operations in `cpp-core-v2` | Ordinary user-provided copy constructors and copy assignment with lvalue-reference sources execute through checked destination/receiver calls. O0/O2 fixtures cover selected source overloads, side effects, actual object identities, returned aliases, lifetime counts and operator-vs-member-call ordering. Protocol fixtures check selected functions, no extra prvalue copying, full signatures and relocation. Broader ordinary operators follow their separate row; native results require the implementing revision. |
| User move operations in `cpp-core-v2` | Ordinary user-provided move construction and assignment from live `R&&`/`const R&&` sources preserve selected overloads, actual destinations, source mutations, returned aliases and syntax-dependent operand order. Explicit constructors and ref-qualified assignment keep their source rules. Moving leaves normal source/target cleanup intact. O0/O2 and protocol fixtures cover these effects, complete signatures, direct prvalue forwarding and relocation. Static lifetime extension, exceptions, templates and full STL remain in development; native results require the implementing revision. |
| Generated moves in `cpp-core-v2` | Implicit and explicitly defaulted move construction/assignment preserve selected member copy or move operations, nested array order, actual storage and normal source/target cleanup. Trivial operations preserve stored pointers; generated field defaults do not rerun. Assignment reads source values after operand effects. Existing implicit trivial inline temporary operations remain supported with separate full-expression and automatic-reference lifetime contracts. O0/O2 and protocol fixtures cover fallback, order, aliases, defaults, owners, lazy definitions, typed array stores and relocation. Full C++/STL remains in development; native results require the implementing revision. |
| Ordinary operators in `cpp-core-v2` | Member/free overloads preserve selected calls, C++17 sequencing, reference aliases, by-value parameters and record results. Overloaded logical operators evaluate both operands. Free assignment parameters initialize RHS-first in both syntaxes and are destroyed in reverse order. Ordinary assignment signatures admit supported value/result types; generated special-member restrictions remain. O0/O2 and protocol fixtures cover operator categories, order, lifetimes, signatures and relocation. Default heap runtime and complete STL remain in development; native results require the implementing revision. |
| Conversion functions in `cpp-core-v2` | Ordinary live-receiver conversions preserve the selected call, explicit/contextual selection, reference aliases and direct object-result destinations. Following standard conversions and selected copy/move operations remain visible. Const/ref qualifiers, constexpr and noexcept follow C++17. O0/O2 and protocol fixtures cover effects, lifetimes, signatures, cleanup and relocation. Static lifetime extension and complete STL remain in development; native results require the implementing revision. |
| Full-expression temporary calls in `cpp-core-v2` | Scalar/record temporary reference arguments and temporary receivers retain actual storage until the full-expression ends, after the selected call and callee parameter cleanup. Record subobjects share the same owner; conditions and loops preserve guarded reverse destruction. Fixtures cover real addresses, operators/conversions, user/generated moves, result destinations, cleanup and relocation. Static lifetime extension remains excluded; standalone fixed-array temporaries follow their separate contract; native results require the implementing revision. |
| Automatic reference lifetime extension in `cpp-core-v2` | Ordinary automatic local references retain admitted scalar/enum/pointer/record temporaries in one actual destination until their lexical scope ends. Braces preserve aliases; record subobjects keep the complete owner where C++ grants extension. Nested full-expression temporaries retain shorter lifetimes. Fixtures cover copies/moves, returns, conditional owners, loop exits, guarded cleanup and relocation. Static references and reference fields follow their separate contracts; TLS and full STL remain unfinished; standalone fixed arrays follow their separate contract; native results require the implementing revision. |
| Default arguments in `cpp-core-v2` | Admitted functions, methods, call operators and user constructors evaluate resolved defaults at each omitted argument, preserving declaration lookup, references and actual value destinations. User copy/move constructors can have trailing defaults. Array omitted elements and generated copies clean default temporaries per element; explicit clauses retain the enclosing full expression. Unused and overridden non-template defaults remain inspected; template defaults are checked once instantiated. Native evidence requires implementing CI; templates and complete STL remain unfinished. |
| Void expressions in `cpp-core-v2` | Void casts, void()/void{}, supported aliases, calls, returns, comma expressions and conditional branches retain operand effects and existing temporary cleanup. Discarded nonvolatile lvalues are not loaded; record/array temporaries still construct and destroy normally. The protocol contains no void storage or value operands. Full source checks remain active in constant, query, dead and discarded contexts; unsupported types, volatile objects and complete STL remain excluded. Native evidence requires implementing CI. |
| Empty records in `cpp-core-v2` | Empty standard-layout records and stateless callable/conversion objects retain one-byte size/alignment, actual storage, selected operations and cleanup. Source fields/offsets stay empty; an internal NC byte provides storage. Trivial copies still evaluate operands, arrays count empty elements against budgets, and the consumer verifies layout and initializer arity. Fixtures cover identity, parameter/result destinations, lifetimes and manifest consistency; native evidence requires implementing CI. Inheritance, virtual dispatch, templates and complete STL remain excluded. |
| Standalone array temporaries in `cpp-core-v2` | Bounded scalar/enum/pointer/record arrays have one actual destination and complete owner, including multidimensional arrays, omitted fillers, discarded values, calls, decay/indexing and automatic reference extension. Elements initialize in order and are destroyed in reverse order; aliases retain typed addresses. Fixtures check element identity, cleanup, query purity, resource limits and relocation. Static lifetimes and reference fields follow their separate contracts; TLS and complete STL remain unfinished; native results require the implementing revision. |
| Source-defined allocation in `cpp-core-v2` | Single-object new/delete preserve selected global/class/template allocator calls, exact layout size, placement/default-argument lifetimes, returned storage identity, source cv and nonthrowing null behavior. Delete captures once, destroys first and passes the saved address plus selected size/alignment. Explicit placement reuse shares checked alias carriers; new objects have no lexical cleanup. Untouched implicit global sized delete/delete[] may forward to the matching checked namespace unsized definition; explicit sized replacements remain authoritative, and original array cookie metadata is retained. Other missing default/reserved runtime remains TR0203. Paired protocol cases and 27 O0/O2 runtime checkpoints require implementing CI. Array expressions follow their separate row; default heap runtime, exceptions, standard headers and full C++/STL remain unfinished. See the [contract](cpp-core-v2.md#single-object-allocation-and-placement-reuse). |
| Explicit destruction in `cpp-core-v2` | Direct nonvirtual record destruction and scalar pseudo-destructor calls preserve receiver effects, const receiver addresses and existing reverse cleanup. Unevaluated template queries keep bodies lazy; explicit calls retain automatic cleanup obligations. Independently checked `memory_lifetimes` enables recursive `may_alias` carriers without weakening types, layout or const permissions. Source/protocol and saved-NC O0/O2/strict-aliasing fixtures require implementing CI. Default heap runtime, throwing runtime array-new bounds, exceptions, standard headers and full C++/STL remain unfinished. See the [contract](cpp-core-v2.md#explicit-destruction). |
| Noexcept in `cpp-core-v2` | Standard resolved `noexcept`, computed true/false specifications and C++17 `throw()` retain source semantics. Constant queries account for selected functions and temporary destruction without executing operands or creating owners. Written operands/specifications remain inspected, even in unused code. O0/O2 and protocol fixtures cover effects, special members, redeclarations, bool literals, signatures and relocation. Throwing, catching and stack unwinding remain in development; native results require the implementing revision. |
| Generated/defaulted lifecycle in `cpp-core-v2` | Implicit and explicitly defaulted default constructors preserve final destinations, declaration order, nested member/array construction and C++ default/value initialization. In-class/out-of-line defaulted destructors use reverse member cleanup. Fixtures cover lazy trivial and unevaluated definitions, transitive discovery, complete signatures, identity, cleanup, zero-initialization differences and relocation. Exception unwinding and STL remain outside this milestone; native results require the implementing revision. |
| Default member initializers in `cpp-core-v2` | Supported field defaults retain declaration/element order, actual destination identity and nested `this` contexts. Explicit initialization overrides the selected field; implicit/defaulted copies and assignment do not rerun defaults, while user copy constructors can select defaults for omitted members. Constructor and aggregate temporary cleanup boundaries remain distinct. Written unused/overridden defaults are checked. Fixtures include partial arrays, const complete objects, lazy construction, signatures and relocation; native results require the implementing revision. |
| Generated copy construction in `cpp-core-v2` | Implicit and explicitly defaulted copy constructors retain selected member copying for nested records and multidimensional arrays. Source arrays are captured once; elements use actual destinations in increasing index order. O0/O2 fixtures check copy effects, mutable sources, pointers, by-value/return storage and destruction counts; protocol fixtures check canonical signatures, selected calls, exact source/destination indices, trivial field copies, lazy definitions and relocation. Broader C++/STL remain in development; native results require the implementing revision. |
| Generated copy assignment in `cpp-core-v2` | Implicit and explicitly defaulted assignment preserves selected member calls, nested array loops, source order and receiver reference results. Generated trivial array copies use bounded typed stores without external memory-copy calls, including records with nontrivial lifecycle. Trivial assignment captures references before reading source values. O0/O2 fixtures cover aliases, mutable sources, self/chained assignment, side effects and destruction counts; protocol fixtures check helper signatures, loops, typed stores, captures, builtin rejection, lazy definitions and relocation. Templates and complete STL remain in development; native results require the implementing revision. |
| Multi-file project translation | P3A implemented: compilation-database selection, owned headers, per-unit semantic analysis, ODR/linkage checks, combined source/header emission, and original/generated program/library comparison at `-O0`/`-O2`. |
| NeverC math mappings | P3B implemented behind explicit `cpp-math-v1`, pinned SDK/declaration provenance, exact runtime capability checks, and a runtime link probe. The [mapping gates](runtime-mapping.md) include numeric/environment differentials and installed output execution. |
| String literals and constant arrays in `cpp-core-v2` | Exact narrow/UTF-8/UTF-16/UTF-32/wide code units, static read-only literal lvalues, const aliases, character-array initialization/zero fill, record field copies and source-owned constant namespace arrays. Folded complete trees, const storage, layout and relocation have source/protocol/IR and O0/O2 fixtures; native results require implementing-revision CI. Dynamic lifetime and actual string/vector libraries remain unfinished. See the [storage contract](cpp-core-v2.md#string-literals-and-constant-arrays). |
| Fixed-array static storage in `cpp-core-v2` | Zero/constant-initialized mutable and const namespace arrays, function-local statics, defined class static arrays and admitted class/variable-template instances. Canonical storage, nested record elements, source const rules, receiver effects and instance separation retain typed checks and O0/O2 fixtures. Dynamic initialization/guards and static reference lifetime extension/destruction follow their own contracts; TLS remains unfinished. See the [static array contract](cpp-core-v2.md#fixed-array-static-storage). |
| Static object pointers in `cpp-core-v2` | Zero or checked symbolic addresses in namespace/local/member static pointers, arrays and admitted class/variable-template instances. Exact global/literal targets, typed array/field paths, final one-past addresses, const rules, source definitions and stable instance identity. C23 constant emission supports forward object declarations without runtime initialization. Dynamic pointer initializers follow the first-use and startup contracts. TLS, default heap runtime and full STL remain unfinished; native evidence requires implementing CI. See the [address contract](cpp-core-v2.md#static-object-pointer-storage). |
| Static reference bindings in `cpp-core-v2` | Constant bindings in namespace/local/member static references and admitted template instances. Immutable pointer carriers retain original object identity, const permissions, array extents, chains, strings and receiver cleanup; assignment writes the referent. Null/one-past bindings, function/nonstatic-member references, TLS retain restrictions. Dynamic bindings follow the first-use and startup contracts. Paired source/protocol, IR, relocation and O0/O2 fixtures require implementing CI. See the [alias contract](cpp-core-v2.md#static-reference-bindings). |
| Static temporary lifetime extension in `cpp-core-v2` | Constant temporaries and synchronized first-use construction of local static temporaries, registering required destruction at completion. Exact extending reference or aggregate owners retain complete permanent storage, subobject aliases, self pointers, constness, selected conditional arms and template identity. An independent dynamic binding or aggregate owns its zero-initialized IR children through checked `initialization_owner` metadata and one publication guard. Exceptions, TLS and full STL remain unfinished. Source/protocol, IR, relocation, O0/O2 and concurrent-reader fixtures require implementing CI. See the [lifetime contract](cpp-core-v2.md#dynamic-local-static-temporaries). |
| Static record objects in `cpp-core-v2` | Constant-initialized mutable/const namespace, local and class static records, including admitted template instances. Actual object evaluation preserves constexpr construction, default fields, self pointers and copy semantics; trivial static default construction receives zero initialization. Layout, canonical identity, mutation, receiver effects and relocation have paired source/protocol, IR and O0/O2 fixtures requiring implementing CI. TLS and full STL remain unfinished. See the [record contract](cpp-core-v2.md#static-record-objects). |
| Built-in frontend/SDK delivery | Implemented in normal NeverC builds; upstream Clang is a pinned build-time source input. Runtime and installation evidence is tracked separately below. |
| Deduced reference conversions in `cpp-core-v2` | `decltype(auto)` and collapsing `auto&&` preserve supported reference results, receiver constness, selected calls and actual aliases. Ineligible value-returning template bodies stay lazy during reference-only candidate filtering. Fresh reference arguments deduce necessary results before compatibility checks; the direct-binding filter does not exempt a candidate from result deduction needed for later indirect initialization. Source checks and C++ diagnostics remain active. Full C++17/STL is unfinished; native evidence requires implementing CI. [Contract](cpp-core-v2.md#deduced-reference-conversions). |

## `cpp-core-v1` acceptance contract — P2

One C++17 source unit, native hosted user space, no includes. The profile is
immutable after release; broader behavior requires an explicitly selected new
profile. All unlisted constructs remain unsupported until entered into this
matrix with lowering and rejection tests.

| Construct | Contract |
| --- | --- |
| Types | `int`, `unsigned int` (both exactly 32 bits in this initial profile), `bool`, `void`; source target widths/conversions must agree with the generated target. |
| Aggregates | Nonempty simple records composed of supported fields, explicit aggregate initialization, copy, and field access. Reject recursive by-value records, user-defined constructors/destructors/member functions, inheritance, unions, bitfields, access/lifetime features outside this subset. |
| Functions | Supported by-value arguments/results and locals; ordinary functions and scalar recursion; namespace and overload resolution performed by Clang. No external function dependencies. |
| Initialization | Preserve explicit initialization and permitted source initialization semantics. Do not silently zero-initialize an uninitialized C++ local. No dynamic global initialization or static locals. |
| Arithmetic/conversions | Source integer promotions, usual signed/unsigned conversions, unsigned wrap, comparisons, and bool conversion. The guarantee covers defined source executions; it does not promise complete static detection of arbitrary undefined behavior. |
| Control flow | Branches/loops, early return, short-circuit `&&`/`||`, and supported scalar expressions. Lower effects explicitly; do not inherit C evaluation order accidentally. Additional statement forms require explicit support/tests. |
| Names | Namespaces and resolved overloads; deterministic emitted identities, with file identity for internal linkage. Avoid NeverC/runtime reserved names. |
| Globals | V1 profiles retain compile-time constants. Core v2 also supports mutable integer/bool/enum/float/double namespace globals with zero or fully checked constant initialization and one owned definition; reads, references and pointers share that storage. Fixed arrays follow their static-storage contract; records follow their static-object contract; dynamic initialization follows the first-use and startup contracts. Native results require the implementing revision. |
| Entry points | No-argument `int main()` when present; modules may omit `main`. Never invent an entry point. |
| C ABI exports | Explicit supported scalar parameter/result signatures; no aggregate ABI, class ABI, or foreign-object identity guarantee. |
| Preprocessing | All includes rejected, including unused includes. Time-dependent and path-observing macros (`__FILE__`, `__BASE_FILE__`, `__FILE_NAME__`) are rejected. Admitted macro inputs are recorded. Unsupported directives/pragma behavior must be diagnosed. |

The following are rejected: pointers/references, arrays, floating-point types,
enums, unions/bitfields, user-defined special members/member functions,
templates, inheritance/virtual dispatch, exceptions, RTTI, lambdas, variadics,
volatile/atomic operations, static locals, dynamic global initialization,
foreign-function dependencies, and unsupported preprocessing/options. Rejection
applies to all selected owned declarations/definitions, even unused code.

This is a source migration contract. It does not preserve arbitrary C++ binary
ABI, support the whole C++ standard, or automatically translate an ecosystem.
Naïve source-to-source printing is not an accepted implementation.

## Explicit extension profiles

| Profile | Extension | Status |
| --- | --- | --- |
| `cpp-project-v1` | `cpp-core-v1` plus explicitly selected compilation-database inputs, owned project headers, dependency closure, shared types/linkage, scalar C ABI boundaries, and explicit target validation. | Implemented; host and delivery evidence below. |
| `cpp-math-v1` | `cpp-project-v1` plus the bounded binary64 operations below and approved `std::fabs(double)`/`std::floor(double)` mappings. | Implemented; SDK/runtime gates and host evidence below. |
| String library follow-up | Exact byte operations with encoding/NUL/aliasing/ownership/lifetime/allocation-error contract. NeverC UTF-8 `string` is not automatically `std::string`. | Core v2 literals and constant-array storage are implemented above; `std::string` library operations and allocation remain unfinished. |
| Trivial-vector follow-up | Exact element/operation list with bounds, invalidation, storage, lifetime, growth/overflow, and allocation-error behavior. | Unversioned and unimplemented. |

Project and math profiles require `--compdb`, `--project-root`, explicitly selected
source files, and `--out-dir` or `--check`. They emit one `translated.nc` and
`translated.h`, after analyzing each original translation unit independently.
The [context contract](p3-compilation-context.md) defines supported options,
response files, selection, and raw versus normalized input hashes; the
[project contract](p3-project-emission.md) defines linkage and ODR restrictions.

Math accepts binary64 literals, value copies, parameters/results and aggregate
fields; conversions between `double` and supported bool/int32/uint32 values;
unary `+`/`-`; and comparisons. General double arithmetic, compound arithmetic,
increment/decrement, `float`, `long double`, and source floating-environment APIs
remain rejected. Source builtin NaN/infinity constructors, bit casts, and
`numeric_limits` are rejected; runtime NaNs and infinities may enter through
module parameters. Constant folding cannot hide an unsupported source construct.
The accepted source options fix no fast math and disabled FP contraction. The
FP contract `cpp.math.binary64.masked.v1` requires masked traps and the documented
binary64 environment; it does not authorize arbitrary FP options or operations.

Math requires an explicit x86_64 or arm64 macOS 15.0 target and the approved
`neverc-embedded-clang20.1.8-libcxx200100-macos15.5-r11` distribution. Equivalent Darwin
target spellings are accepted only when their effective deployment is macOS
15.0. The immutable built-in catalog approves 381 original headers and
NeverC-authored version metadata; there is no external SDK selection or host
discovery. Standard-library overloads or distributions outside this boundary fail.

P3B uses P3A include/context handling. Full Clang may parse standard-library
internals to resolve approved operations, but those bodies are not recursively
translated. An ignored header must not hide owned code, custom specializations,
or unapproved calls. Broader profiles are never inferred from an include.

## Host/target evidence

The original process-separated implementation passed 126 integrated tests on
both native arm64 and Rosetta x86_64, including SDK/runtime gates, environment
isolation, O0/O2 execution and full-value differentials. Its installed generated
output also ran independently of the old frontend and SDK descriptor. Those
runs are a semantic regression baseline, not proof of the new built-in delivery.

| Host / source target | Built-in implementation evidence | Support boundary |
| --- | --- | --- |
| macOS arm64 / `arm64-apple-macosx15.0.0` | Earlier recorded built-in core protocol suite: 73 passed. Earlier built-in project and math suites passed 33 and 49 cases respectively. Isolated fresh-prefix installation, translation and O0/O2 execution passed with default mimalloc, Python plugins and bundled Python enabled. Full integrated-suite status is recorded below. | Native macOS runner; bounded profiles only. |
| macOS under Rosetta x86_64 / `x86_64-apple-darwin24.6.0` | Prior 126-test process-separated baseline above. Local built-in validation was not completed. | Math target is explicitly macOS 15.0; built-in and native Intel execution are not established. |
| Linux x64 / arm64 | At `b02468ad`, the [x64](https://github.com/NeverSight/NeverC/actions/runs/34596323748) and [arm64](https://github.com/NeverSight/NeverC/actions/runs/34596323441) formal CI workflows completed successfully. | Workflow completion evidence only; per-test execution and installed-prefix translation were not separately reviewed here. The math profile remains restricted to its approved macOS targets. |
| Windows x64 / arm64 | At `b02468ad`, the [x64 Clang](https://github.com/NeverSight/NeverC/actions/runs/34596323650) and [arm64 Clang](https://github.com/NeverSight/NeverC/actions/runs/34596323464) logs verify the built-in build, embedded-runtime relink, installation and full CTest run. Counts and skips are recorded below. | Bounded profiles and explicit test skips; installation of the compiler is distinct from an installed-prefix C++ translation smoke. The math profile remains restricted to its approved macOS targets. |
| Android, kernel, freestanding, dyncode | Outside initial hosted translation profile. Existing NeverC support for such modes is not translator validation. | Not advertised. |

The arm64 SDK probes explicitly select their target. A NeverC build under Rosetta
can default to x86_64 on the same arm64 machine; its source and generated targets
must be aligned explicitly and its execution evidence recorded separately.

The passing arm64 installation smoke used the real CMake installation components
for NeverC, resource headers, standard resources and the macOS arm64 runtime.
Its sandbox denied both development source/build trees, Homebrew and the system
developer directory. Inside that sandbox, the installed compiler translated
core, project and math fixtures, built and executed their output at `-O0` and
`-O2`, and linked separate C clients for the generated modules. Recursive dynamic
dependency inspection found no external Clang/LLVM libraries. Installed license
notices and all 267 catalog source entries were verified by hash: 266 original
headers and NeverC's version metadata. The installed compiler retained its
bundled Python and ordinary system dependencies. This smoke does not substitute
for a complete regression run on every advertised host.

Before built-in integration, an additional arm64-compiler → x86_64-target probe with default mimalloc enabled
could not complete its runtime link on this host. This combination is not in the
accepted matrix. After the process-cleanup fix, the driver returned `TR0404` at
the link stage in about 60 seconds, published a failure report, and removed staging
without publishing generated artifacts. The OS kept the killed compiler child
unreapable; the driver reported that condition instead of waiting indefinitely.
The old x86_64 baseline used the matching Rosetta compiler with mimalloc off.

NeverC's existing compiler target matrix must not be copied into the translator's
support claims. Each advertised combination requires matching source/generated
data models, SDK provenance, runtime capabilities when used, and execution on a
matching runner. Cross-compilation alone is insufficient.

Other language adapters remain planned in issue #16's order: E Language after
the C++ P3 baseline, then Python, Go, Rust, TS, and JS. C compilation continues
through the existing direct NeverC path.

## Repository regression validation

The early local built-in arm64 integrated run passed 131 of 132 tests. Its
remaining shadow-header assertion was corrected; the second local run stopped
when full validation moved to GitHub CI. These are historical results. The
earlier local protocol and installation evidence above predates the numeric byte
initializer for MSVC, Windows path normalization and full-host symbol audit.

At code revision [`b02468ad1ccda72b16393e4fab9147c201f2ee1f`](https://github.com/NeverSight/NeverC/commit/b02468ad1ccda72b16393e4fab9147c201f2ee1f),
all ten formal CI workflows completed successfully: seven platform builds,
the C++ frontend tools, code quality, and documentation/test-dependency checks.
This includes the [macOS arm64 workflow](https://github.com/NeverSight/NeverC/actions/runs/34596323438).
Linux and macOS evidence for this revision is workflow completion only; no
additional per-test, numerical-observation or installed-prefix smoke results
are inferred here from those workflow statuses.

The Windows Clang build, installation and test logs were reviewed separately.
Both architectures completed the initial and embedded-runtime executable links,
passed the private/host ABI audit, and installed `neverc.exe`. Each CTest
inventory contained 2,899 tests:

| Windows Clang runner | Passed | Skipped | Failed |
| --- | ---: | ---: | ---: |
| x64 | 2,829 | 70 | 0 |
| arm64 | 2,780 | 119 | 0 |

Each runner's `neverc-translate.*` group contained 130 passed and 21 skipped
tests. The skips cover three independent reference-compiler comparisons,
15 macOS-specific math tests, two POSIX process fixtures and one controlled
long-path test. The logs do not identify which long-path environment condition
caused that skip. Core/project protocol tests and the executable core translation
fixtures passed. Windows does not register the math protocol suite.

The separate `cpp-frontend-tools` workflow exercises the archive-index parser
and ABI collision checks on Linux, Windows and macOS before the full compiler
builds finish. Compiler builds also run those checks against their real archives,
including Clang LTO and MSVC LTCG inputs.
The macOS workflow includes the fresh-prefix installation smoke with reads of
the checkout, selected Xcode developer directory and installed Homebrew/developer
roots denied to NeverC and its generated programs. That gate checks installed
core, project and math translation at O0/O2. Its current detailed smoke log was
not separately reviewed for the CI summary above; earlier measured installation
evidence remains recorded in its own section.

The repository gate requires default mimalloc and Python-plugin options enabled,
with bundled Python for the installed configuration. `PluginGlobalState`
validates symbols from the enabled Python plugin implementation; a development build with that option disabled does not
satisfy that artifact check. Python plugins are an existing compiler extension
mechanism and do not provide Python-to-NeverC source translation.

The external Clang reference compiler and its own headers/SDK are independent
test oracles only. Production translation and built-in SDK admission tests do
not select an external frontend or require an SDK environment variable.
Arm64 CI builds must use a native execution environment so CMake's recorded host
architecture matches the compiler and managed Python runtime. The DynCode test
loader also explicitly selects arm64 when its test harness is arm64.

The focused translation suite covers source/IR equivalence, SDK/runtime
admission, project closure, bounded file and JSON input, process cancellation,
artifact publication and environment isolation. The frontend protocol suites
exercise all owned declarations, including unreachable unsupported source, and
ensure pruned mapped calls leave no unused mapping metadata. Installed-prefix
checks use normal CMake installation components and verify translation plus
generated project/math execution at O0/O2 with development Clang/SDK/source paths
unavailable. They also inspect symbols and dependencies outside that sandbox.
No repository test exclusion is added by this change.
